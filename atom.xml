<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>子恒的博客</title>
  
  <link href="/atom.xml" rel="self"/>
  
  <link href="chestnutheng.cn/"/>
  <updated>2021-02-02T12:31:13.000Z</updated>
  <id>chestnutheng.cn/</id>
  
  <author>
    <name>吕子恒</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>[Go]Go语言的设计和坑</title>
    <link href="chestnutheng.cn/2021/02/02/go/"/>
    <id>chestnutheng.cn/2021/02/02/go/</id>
    <published>2021-02-02T12:31:09.000Z</published>
    <updated>2021-02-02T12:31:13.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文介绍了Go的语言设计和一些容易踩坑的细节：</p>
<ul>
<li>理解Go为什么X，摆脱原语言的思维</li>
<li>解决写代码时比较困惑和不满的点，对容易出错的语法有个印象</li>
</ul>
<a id="more"></a>
<p>Go学起来非常简单，但是这是语言设计者刻意为之，很多复杂的细节都藏在语言实现里，导致我们迅速学会Go之后不断踩坑</p>
<h1 id="Why-Go"><a href="#Why-Go" class="headerlink" title="Why Go"></a>Why Go</h1><p>2007年，Google设计Go，目的在于提高在并行编程（多核CPU越来越多）、分布式部署、大型代码库（以及维护他们的非常多的开发人员）的情况下的开发效率。设计时，在吸收C++优点的基础上，收集于很多工程师之间流传的的“不要像C++”<br>Go like C++：</p>
<ul>
<li>内存消耗少</li>
<li>执行速度快</li>
<li>启动快</li>
</ul>
<p>Go not like C++：</p>
<ul>
<li>程序编译时间短（按照我过去的经验，一个C++大型项目即使make -j8也需要编译一个小时以上）</li>
<li>像动态语言一样灵活（runtime、interface、闭包、反射）</li>
<li>内置并发支持（C++的协程至少得等到std23才有，非常落后）</li>
<li>丰富的原生库（C++解析json，建立http服务器，使用redis这种都很难找到靠谱的库）</li>
<li>多语义（取消了指针运算、取消隐式类型转换、取消类型别名，取消重载，++和赋值作为表达式…）</li>
</ul>
<p>Go的优点：</p>
<ul>
<li>面向工程：简单。只有25个关键字，代码风格统一，可读性高，go mod包丰富</li>
<li>自动垃圾回收：语言运行时内置垃圾回收</li>
<li>语言级并发：非常好用的routine和channel，更高层次的并发抽象</li>
<li>静态语言动态特性</li>
</ul>
<p>Go的缺点：</p>
<ul>
<li>runtime的性能还需要提高</li>
<li>没有泛型</li>
<li>冗余的错误处理</li>
<li>Go mod不够完善</li>
</ul>
<p><a href="https://chai2010.cn/static-public/talks/giac2018-go-talk.pdf" target="_blank" rel="noopener">Go语⾔将⾛向何⽅?</a><br><a href="https://blog.csdn.net/liigo/article/details/23699459" target="_blank" rel="noopener">我为什么放弃Go语言</a></p>
<h1 id="Go的设计哲学"><a href="#Go的设计哲学" class="headerlink" title="Go的设计哲学"></a>Go的设计哲学</h1><p><a href="https://talks.golang.org/2012/splash.article" target="_blank" rel="noopener">创始人Rob Pike在SPLASH上的演讲，阐述了设计Go的初衷</a><br><a href="https://www.infoq.cn/article/go-based-on-connection-combination-language-1" target="_blank" rel="noopener">许式伟，Go和Java在继承观念上的对比</a><br><a href="https://studygolang.com/articles/2944" target="_blank" rel="noopener">对面向对象的批评</a><br><a href="https://www.yinwang.org/blog-cn/2013/03/07/design-patterns" target="_blank" rel="noopener">王垠：解密“设计模式”，对设计模式的批评</a></p>
<ul>
<li>少即是多（less is more）：如果一个特性并不对解决任何问题有显著价值，那么go就不提供它；如果需要一个特性，那么只有一种方法去实现</li>
<li>面向接口编程：非侵入式接口，反对继承、反对虚函数和虚函数重载（多态）、删除构造和析构函数</li>
<li>正交+组合的语言特性：语言的特性之间相互独立，不相互影响。比如类型和方法是互相独立的，类型之间也是相互独立的，没有子类，包也没有子包。不同特性用组合的方式来松耦合</li>
<li>并发在语言层面支持：并发更好利用多核，有更强的表现力来模拟真实世界</li>
</ul>
<p>在设计上，Go秉承了C的简单粗暴。</p>
<h2 id="为什么没有继承？"><a href="#为什么没有继承？" class="headerlink" title="为什么没有继承？"></a>为什么没有继承？</h2><p>Go没有子类型的概念，只能把类型嵌入到另一个类型中，所以没有类型系统。Go的作者认为类型系统被过度使用了，应该在这个方向上退一步。</p>
<ul>
<li>使用伸缩性良好的组合，而不是继承</li>
<li>数据和方法不再绑定在一起，数据的集合用struct，方法的集合用interface，保持正交</li>
</ul>
<blockquote>
<p> 类似子类父类的系统造成非常脆弱的代码。类型的层次必须在早期进行设计，通常会是程序设计的第一步，但是一旦写出程序后，早期的决策就很难进行改变了。所以，类型层次结构会促成早期的过度设计，因为程序员要尽力对软件可能需要的各种可能的用法进行预测，不断地为了避免挂一漏万，不断的增加类型和抽象的层次。这种做法有点颠倒了，系统各个部分之间交互的方式本应该随着系统的发展而做出相应的改变，而不应该在一开始就固定下来。</p>
</blockquote>
<p>作者附了一个例子，是一些以接口为参数并且其返回结果也是一个接口的函数：</p>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 入参是接口的函数，而不是成员方法</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ReadAll</span><span class="params">(r io.Reader)</span> <span class="params">([]<span class="keyword">byte</span>, error)</span></span></span><br><span class="line"><span class="function">// 封装器 - 出入参都是接口</span></span><br><span class="line"><span class="function"><span class="title">func</span> <span class="title">LoggingReader</span><span class="params">(r io.Reader)</span> <span class="title">io</span>.<span class="title">Reader</span>    //读到的内容录入日志</span></span><br><span class="line"><span class="function"><span class="title">func</span> <span class="title">LimitingReader</span><span class="params">(r io.Reader, n <span class="keyword">int64</span>)</span> <span class="title">io</span>.<span class="title">Reader</span>    //读<span class="title">n</span>个字节停下来</span></span><br><span class="line"><span class="function"><span class="title">func</span> <span class="title">ErrorInjector</span><span class="params">(r io.Reader)</span> <span class="title">io</span>.<span class="title">Reader</span></span></span><br></pre></td></tr></table></figure>
<p>这种组合+函数的模式是相当灵活的。如果用继承，我们可能会多三个io.Reader的定义；然后用多态去获得对应的功能</p>
<h2 id="为什么没有异常？"><a href="#为什么没有异常？" class="headerlink" title="为什么没有异常？"></a>为什么没有异常？</h2><p>panic和recover这些函数是故意弄的不好用的，因为我们应该减少使用他们。不像Java库中使用异常那样，在go的库中这两个关键字几乎没有使用。</p>
<ol>
<li>业务中的错误并不是真正的异常情况，if和return完全可以胜任，无需控制流</li>
<li>如果错误要使用特殊的控制结构，错误处理就会扭曲程序的控制流，非常复杂</li>
<li>显式的错误检查会迫使程序员在错误出现的时候对错误进行思考，并进行相应的处理，而不是推给前面的调用堆栈</li>
</ol>
<p>毫无疑问这会使代码更长一些，但如此编码带来的清晰度和简单性可以弥补其冗长的缺点</p>
<h2 id="为什么没有X？"><a href="#为什么没有X？" class="headerlink" title="为什么没有X？"></a>为什么没有X？</h2><p>总结：Go的设计着眼于编程的便利性、编译的速度、概念的正交性以及支持并发和垃圾回收等功能。如果你在Go中找不到其他语言的X特性，那么只能说明这个特性不适合Go，比如它会影响编译速度或设计的清晰度，或者使得基础系统变得特别复杂。</p>
<h1 id="容易出错的细节"><a href="#容易出错的细节" class="headerlink" title="容易出错的细节"></a>容易出错的细节</h1><h2 id="创建对象"><a href="#创建对象" class="headerlink" title="创建对象"></a>创建对象</h2><p>新建一个对象在go里面有好几种方法，让人迷惑，而且似乎和简洁这一设计原则违背。我们按照对象类型讨论一下：</p>
<ol>
<li>对于结构体，<code>new(T)</code>和<code>&amp;T{}</code>是等价的，都会给对象赋零值（一般人很少用new）。<br>Note：直接<code>var obj T;&amp;T</code>也是等价的，只不过变量有可能在堆上，有可能在栈上</li>
<li>对于slice、map、chan，<code>make(map[string]int)</code>和<code>map[string]int{}</code>等价，会对对象进行初始化。</li>
</ol>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> a []<span class="keyword">int</span>                      <span class="comment">// nil</span></span><br><span class="line">a := []<span class="keyword">int</span>&#123;&#125;                     <span class="comment">// not nil</span></span><br><span class="line">a := *<span class="built_in">new</span>([]<span class="keyword">int</span>)                 <span class="comment">// nil</span></span><br><span class="line">a := <span class="built_in">make</span>([]<span class="keyword">int</span>,<span class="number">0</span>)               <span class="comment">// not nil</span></span><br></pre></td></tr></table></figure>
<h2 id="零值"><a href="#零值" class="headerlink" title="零值"></a>零值</h2><p>零值和未初始化的值并不相同。不同类型的零值是什么？</p>
<ol>
<li>布尔类型是false，整型是0，字符串是””</li>
<li>指针、函数、interface、slice、channel和map的零值都是nil</li>
<li>结构体的零值是递归生成的，每个成员都是对应的零值</li>
</ol>
<p>我们来看一个例子。一个为nil的slice和map能做什么操作：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 一个为nil的slice，除了不能索引外，其他的操作都是可以的</span></span><br><span class="line"><span class="comment">// Note: 如果这个slice是个指针，不适用这里的规则</span></span><br><span class="line"><span class="keyword">var</span> a []<span class="keyword">int</span>        </span><br><span class="line">fmt.Printf(<span class="string">"len(a):%d, cap(a):%d, a==nil:%v\n"</span>, <span class="built_in">len</span>(a),<span class="built_in">cap</span>(a), a == <span class="literal">nil</span>) <span class="comment">//0 0 true</span></span><br><span class="line"><span class="keyword">for</span> _, v := <span class="keyword">range</span> a&#123;<span class="comment">// 不会panic</span></span><br><span class="line">        fmt.Println(v) </span><br><span class="line">&#125;</span><br><span class="line">aa := a[<span class="number">0</span>:<span class="number">0</span>]     <span class="comment">// 也不会panic，只要索引都是0</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// nil的map，我们可以简单把它看成是一个只读的map</span></span><br><span class="line"><span class="keyword">var</span> b <span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">string</span></span><br><span class="line"><span class="keyword">if</span> val, ok := b[<span class="string">"notexist"</span>];ok&#123;<span class="comment">// 不会panic</span></span><br><span class="line">        fmt.Println(val)</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> k, v := <span class="keyword">range</span> b&#123;<span class="comment">// 不会panic</span></span><br><span class="line">        fmt.Println(k,v)</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">delete</span>(b, <span class="string">"foo"</span>) <span class="comment">// 也不会panic</span></span><br><span class="line">fmt.Printf(<span class="string">"len(b):%d, b==nil:%v\n"</span>, <span class="built_in">len</span>(b), b == <span class="literal">nil</span>) <span class="comment">// 0 true</span></span><br></pre></td></tr></table></figure></p>
<h2 id="值传递"><a href="#值传递" class="headerlink" title="值传递"></a>值传递</h2><p>Go语言中所有的传参都是值传递，都是原值的一个副本，或者说一个拷贝。传入的数据能不能在函数内被修改，取决于是不是指针或者含有指针的类型（指针被值传递复制后依然指向同一块地址）。这就让人很疑惑，什么时候传入的参数修改会生效，什么时候不会生效？<br>slice类型在 值传递的时候len和cap不会变，所以函数内append没有用：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> slice <span class="keyword">struct</span> &#123;</span><br><span class="line">    array unsafe.Pointer</span><br><span class="line">    <span class="built_in">len</span>   <span class="keyword">int</span></span><br><span class="line">    <span class="built_in">cap</span>   <span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// badcase</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">appendMe</span><span class="params">(s []<span class="keyword">int</span>)</span></span>&#123;</span><br><span class="line">    s = <span class="built_in">append</span>(s, <span class="number">-1</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>map 和 chan类型，本来就是个指针，所以函数内修改一定会生效：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// map实际上是一个 *hmap</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">makemap</span><span class="params">(t *maptype, hint <span class="keyword">int64</span>, h *hmap, bucket unsafe.Pointer)</span> *<span class="title">hmap</span></span> &#123;</span><br><span class="line">    <span class="comment">//省略无关代码</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// chan实际上是个 *hchan</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">makechan</span><span class="params">(t *chantype, size <span class="keyword">int64</span>)</span> *<span class="title">hchan</span></span> &#123;</span><br><span class="line">    <span class="comment">//省略无关代码</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>再比如一个结构体作为参数：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 这是一个典型的指针包裹类型</span></span><br><span class="line"><span class="keyword">type</span> Person <span class="keyword">struct</span> &#123;</span><br><span class="line">    name <span class="keyword">string</span></span><br><span class="line">    age  *<span class="keyword">int</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">modify</span><span class="params">(x Person)</span></span>&#123;</span><br><span class="line">    x.name = <span class="string">"modified"</span></span><br><span class="line">    *x.age = <span class="number">66</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这个结构体里的age是个指针类型，所以在函数内会被修改。<br>这种含有指针的结构体类型，里面的指针指向了其他的内存。在发生拷贝的时候，只有结构体本身的内存会被拷贝，指向的内存是和原值共享的。<br><a href="https://gfw.go101.org/article/value-part.html" target="_blank" rel="noopener">更多细节参考 ：值部</a><br>但是我们一般希望的是，要么结构体的成员一起改变（这个简单，参数传person的指针），要么一起不改变（深拷贝）。那么另一个让人头疼的问题来了，那我如何深拷贝这个对象？</p>
<h2 id="深拷贝"><a href="#深拷贝" class="headerlink" title="深拷贝"></a>深拷贝</h2><p>对于slice，go提供了似乎还不错的方式：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 自己复制</span></span><br><span class="line">s1 := []<span class="keyword">int</span>&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;</span><br><span class="line">s2 := <span class="built_in">append</span>([]<span class="keyword">int</span>&#123;&#125;, s1...)</span><br><span class="line"><span class="comment">// 效率更高的复制</span></span><br><span class="line">s1 := []<span class="keyword">int</span>&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;</span><br><span class="line">s2 := <span class="built_in">make</span>([]<span class="keyword">int</span>, <span class="built_in">len</span>(s1))</span><br><span class="line"><span class="built_in">copy</span>(s2, s1)</span><br></pre></td></tr></table></figure></p>
<p>如果你要拷贝一个map，只能用for循环依次把键值对赋值到新map里。<br>切记：需要拷贝map一定要深拷贝，不然如果后续在不同的协程里操作map会panic<br>如果有其他更复杂的结构体需要深拷贝呢？目前还没有很好的办法：</p>
<ol>
<li>自己写一个复制值的函数</li>
<li>用序列化/反序列化的方法来做，json，bson</li>
<li>用反射来做</li>
</ol>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">age := <span class="number">22</span></span><br><span class="line">p := &amp;Person&#123;<span class="string">"Bob"</span>, &amp;age&#125;</span><br><span class="line"></span><br><span class="line">v := reflect.ValueOf(p).Elem()</span><br><span class="line">vp2 := reflect.New(v.Type())</span><br><span class="line">vp2.Elem().Set(v)</span><br></pre></td></tr></table></figure>
<h2 id="小心interface判等"><a href="#小心interface判等" class="headerlink" title="小心interface判等"></a>小心interface判等</h2><p>go实现接口的时候有两个属性，type T和value V，判等的时候两个属性都要比较。比如一个interface存了3，那么T=int，v=3。只有当两个值都没有设置才等于nil。<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> pi *<span class="keyword">int</span> = <span class="literal">nil</span></span><br><span class="line"><span class="keyword">var</span> pb *<span class="keyword">bool</span> = <span class="literal">nil</span></span><br><span class="line"><span class="keyword">var</span> x <span class="keyword">interface</span>&#123;&#125; = pi</span><br><span class="line"><span class="keyword">var</span> y <span class="keyword">interface</span>&#123;&#125; = pb</span><br><span class="line"><span class="keyword">var</span> z <span class="keyword">interface</span>&#123;&#125; = <span class="literal">nil</span></span><br><span class="line"></span><br><span class="line">fmt.Println(x == y)   <span class="comment">// false</span></span><br><span class="line">fmt.Println(x == <span class="literal">nil</span>) <span class="comment">// false</span></span><br><span class="line">fmt.Println(x == z)   <span class="comment">// false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// badcase</span></span><br><span class="line"><span class="keyword">type</span> error <span class="keyword">interface</span> &#123;</span><br><span class="line">    Error() <span class="keyword">string</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">returnsError</span><span class="params">()</span> <span class="title">error</span></span> &#123;</span><br><span class="line">    <span class="keyword">var</span> p *MyError = <span class="literal">nil</span></span><br><span class="line">    <span class="keyword">if</span> bad() &#123;</span><br><span class="line">        p = ErrBad</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> p <span class="comment">// Will always return a non-nil error.</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>还有一种常见的场景是我们容易漏掉的。int64和int的interface也不相等：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> int1,int2 <span class="keyword">interface</span>&#123;&#125;</span><br><span class="line">int1 = <span class="keyword">int64</span>(<span class="number">0</span>)</span><br><span class="line">int2 = <span class="keyword">int</span>(<span class="number">0</span>)</span><br><span class="line">fmt.Printf(<span class="string">"%v %v = %v"</span>, int1, int2, int1 == int2) <span class="comment">// 0 0 false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果函数参数用了interface，如果我们很容易犯错</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(m *Map)</span> <span class="title">Load</span><span class="params">(key, value <span class="keyword">interface</span>&#123;&#125;)</span></span> &#123;</span><br><span class="line">    <span class="keyword">if</span> e, ok := read.m[key]; ok &#123;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// badcase 1: key的类型不一致导致缓存无法取出</span></span><br><span class="line">m := sync.Map&#123;&#125;</span><br><span class="line">m.Store(<span class="number">0</span>, <span class="string">"ManualCache"</span>)</span><br><span class="line">val, ok := m.Load(<span class="keyword">int64</span>(<span class="number">0</span>)) <span class="comment">// nil false </span></span><br><span class="line"><span class="comment">// badcase 2: value的类型不一致导致断言失败</span></span><br><span class="line">m.Store(<span class="string">"key"</span>, <span class="number">0</span>)</span><br><span class="line"><span class="keyword">if</span> val, ok := m.Load(<span class="string">"key"</span>); ok &#123;</span><br><span class="line">        _ = val.(<span class="keyword">int64</span>)    <span class="comment">// panic</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="点点点"><a href="#点点点" class="headerlink" title="点点点"></a>点点点</h2><p><code>...</code>是个很常用的语法糖，能帮我们节省很多代码。<br>用作展开：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">x := []<span class="keyword">int</span>&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;</span><br><span class="line">y := []<span class="keyword">int</span>&#123;<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>&#125;</span><br><span class="line">x = <span class="built_in">append</span>(x, y...) <span class="comment">//而不是for循环</span></span><br><span class="line">x = <span class="built_in">append</span>(x, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>) <span class="comment">//等价于上面的</span></span><br></pre></td></tr></table></figure></p>
<p>用作可变参数列表：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Println prints to the standard logger in the manner of fmt.Println.</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Println</span><span class="params">(v ...<span class="keyword">interface</span>&#123;&#125;)</span></span> &#123;</span><br><span class="line">    std.Output(<span class="number">2</span>, fmt.Sprintln(v...))  <span class="comment">// Output takes parameters (int, string)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>用作简化数组声明：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> _ = [...]language&#123;</span><br><span class="line">        &#123;<span class="string">"C"</span>, <span class="number">1972</span>&#125;,</span><br><span class="line">        &#123;<span class="string">"Python"</span>, <span class="number">1991</span>&#125;,</span><br><span class="line">        &#123;<span class="string">"Go"</span>, <span class="number">2009</span>&#125;,</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">var</span> b = [...]<span class="keyword">string</span>&#123;<span class="number">0</span>: <span class="string">"foo"</span>, <span class="number">2</span>: <span class="string">"foo"</span>&#125;        <span class="comment">// [3]string&#123;"foo", "", "foo"&#125;</span></span><br></pre></td></tr></table></figure></p>
<h2 id="闭包里的局部变量是引用"><a href="#闭包里的局部变量是引用" class="headerlink" title="闭包里的局部变量是引用"></a>闭包里的局部变量是引用</h2><p>闭包里起的go协程里面引用的是变量i的地址。所有的go协程启动后等待调用，在上面的协程中，部分协程很可能在for循环完成之后才被调用，所以输出结果很多都是最后一个i的值<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// bad case</span></span><br><span class="line">done := <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">bool</span>)</span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">5</span>; i++ &#123;</span><br><span class="line">    <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="built_in">println</span>(i)</span><br><span class="line">        done &lt;- <span class="literal">true</span></span><br><span class="line">    &#125;()</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> _ = <span class="keyword">range</span> values &#123;</span><br><span class="line">    &lt;-done</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 5 5 5 5 5</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// good sample 1</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">5</span>; i++ &#123;</span><br><span class="line">    <span class="keyword">defer</span> <span class="function"><span class="keyword">func</span><span class="params">(i <span class="keyword">int</span>)</span></span> &#123;</span><br><span class="line">        <span class="built_in">println</span>(i)</span><br><span class="line">        done &lt;- <span class="literal">true</span></span><br><span class="line">    &#125;(i)</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// good sample 2</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">5</span>; i++ &#123;</span><br><span class="line">    i := i    <span class="comment">// 新建变量</span></span><br><span class="line">    <span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="built_in">println</span>(i)</span><br><span class="line">        done &lt;- <span class="literal">true</span></span><br><span class="line">    &#125;()</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//1 3 5 4 2</span></span><br></pre></td></tr></table></figure></p>
<h2 id="不要引用大数组"><a href="#不要引用大数组" class="headerlink" title="不要引用大数组"></a>不要引用大数组</h2><p>被切片引用的数据不会被释放（即使你仅仅引用了很小一部分），会大幅降低代码性能<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">headerMap := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="keyword">string</span>][]<span class="keyword">byte</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="number">5</span>; i++ &#123;</span><br><span class="line">    name := <span class="string">"/path/to/file"</span></span><br><span class="line">    data, err := ioutil.ReadFile(name)</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        log.Fatal(err)</span><br><span class="line">    &#125;</span><br><span class="line">    headerMap[name] = data[:<span class="number">1</span>]</span><br><span class="line">    <span class="comment">// better:  headerMap[name] = append([]byte&#123;&#125;, data[:1]...)</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="赋值不是原子操作"><a href="#赋值不是原子操作" class="headerlink" title="赋值不是原子操作"></a>赋值不是原子操作</h2><p>在64位的机器上，赋值很可能被拆成mov两次的汇编代码，因此不是原子的。我们可以用atomic里的方法帮助我们做原子操作。<br>考虑一个内存cache定时刷新的协程：因为随时有请求在读cache，所以刷新cache的时候需要保证cache的指针存取是原子操作。<br>举例：<code>mycache *map[string]*Cache</code><br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 加载（读取）</span></span><br><span class="line"><span class="keyword">var</span> _ = (*T)(atomic.LoadPointer((*unsafe.Pointer)(unsafe.Pointer(mycache))))</span><br><span class="line"></span><br><span class="line"><span class="comment">// 存储（修改）</span></span><br><span class="line">atomic.StorePointer(</span><br><span class="line">    (*unsafe.Pointer)(unsafe.Pointer(mycache)), unsafe.Pointer(&amp;newMycache))</span><br></pre></td></tr></table></figure></p>
<p>所有的操作，只要存在同时存在多个goroutine同时操作一个资源（临界区），除了带有sync，atomic，或者channel关键字的，都不安全。包括但不限于：</p>
<ol>
<li>并发读写map</li>
<li>并发append切片</li>
<li>自增变量</li>
<li>赋值</li>
</ol>
<h2 id="接收器用指针还是值"><a href="#接收器用指针还是值" class="headerlink" title="接收器用指针还是值"></a>接收器用指针还是值</h2><p>Go的接收器可以传指针进来，也可以传值。注意传值的时候接收器不会被改变。官方推荐下面两种情况该用指针：</p>
<ol>
<li>MyStruct很大，需要拷贝的成本太高</li>
<li>方法需要修改MyStruct</li>
</ol>
<p>否则Go推荐使用值接收器<br>Note：如果对象有可能并发执行方法，指针接收器中可能产生数据竞争，记得加锁<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span>（<span class="title">s</span> * <span class="title">MyStruct</span>）<span class="title">pointerMethod</span>（）</span>&#123;    <span class="comment">// 指针方法</span></span><br><span class="line">    s.Age = <span class="number">-1</span>  <span class="comment">// useful</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span>（<span class="title">s</span> <span class="title">MyStruct</span>）<span class="title">valueMethod</span>（）</span>&#123;        <span class="comment">// 值方法</span></span><br><span class="line">    s.Age = <span class="number">-1</span> <span class="comment">// no use</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="for循环里的变量都是副本"><a href="#for循环里的变量都是副本" class="headerlink" title="for循环里的变量都是副本"></a>for循环里的变量都是副本</h2><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> key, element = <span class="keyword">range</span> aContainer &#123;...&#125;</span><br></pre></td></tr></table></figure>
<p>关于上面for循环有几个点：</p>
<ol>
<li>实际遍历的aContainer是原始值的一个副本</li>
<li>element是遍历到的元素的原始值的一个副本</li>
<li>key和element整个循环都是同一个变量，而不是每次迭代都生成新变量</li>
</ol>
<p>这里涉及到几个问题。一个是aContainer和element的拷贝成本。aContainer是数组的时候的拷贝成本比较大，而切片和map的拷贝成本比较小。如果想要缩小拷贝成本，我们有几个建议：</p>
<ol>
<li>遍历大数组时，可以先创建大数组的切片再放在range后面</li>
<li>element结构比较大的时候，直接用下标key遍历，舍弃element</li>
</ol>
<p>还有一个问题是遍历的时候修改，能不能生效？</p>
<ol>
<li>当aContainer是数组时，因为数组是整个复制，所以直接修改aContainer不会生效</li>
<li>直接修改key或者element，？</li>
<li>因为切片和map是浅复制，在循环中操作aContainer或者aContainer[key]可以生效</li>
</ol>
<p>因为循环里的副本和函数参数的副本非常类似，所以我们可以参考上面的“值传递”中的内容来判断修改副本是否会使得修改达到想要的效果。</p>
<h2 id="map的值不可取址"><a href="#map的值不可取址" class="headerlink" title="map的值不可取址"></a>map的值不可取址</h2><p>map是哈希表实现的，所以值的地址在哈希表动态调整的时候可能会产生变化。因此。存着map值的地址是没有意义的，go中直接禁止了map的值的取地址。这些类型都不能取址：</p>
<ul>
<li>map元素</li>
<li>string的字节元素</li>
<li>常量（有名常量和字面量都不可以）</li>
<li>中间结果值（函数调用、显式值转换、各种操作）</li>
</ul>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 下面这几行编译不通过。</span></span><br><span class="line">_ = &amp;[<span class="number">3</span>]<span class="keyword">int</span>&#123;<span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>&#125;[<span class="number">0</span>]        <span class="comment">//字面量</span></span><br><span class="line">_ = &amp;<span class="keyword">map</span>[<span class="keyword">int</span>]<span class="keyword">bool</span>&#123;<span class="number">1</span>: <span class="literal">true</span>&#125;[<span class="number">1</span>]  <span class="comment">//字面量</span></span><br><span class="line"><span class="keyword">const</span> pi = <span class="number">3.14</span></span><br><span class="line">_ = &amp;pi                        <span class="comment">//有名常量</span></span><br><span class="line">m := <span class="keyword">map</span>[<span class="keyword">int</span>]<span class="keyword">bool</span>&#123;<span class="number">1</span>: <span class="literal">true</span>&#125;</span><br><span class="line">_ = &amp;m[<span class="number">1</span>]                      <span class="comment">//map的value</span></span><br><span class="line">lt := [<span class="number">3</span>]<span class="keyword">int</span>&#123;<span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>&#125;</span><br><span class="line">_ = &amp;lt[<span class="number">1</span>:<span class="number">1</span>]                   <span class="comment">//切片操作</span></span><br></pre></td></tr></table></figure>
<p>一般来说，一个不可寻址的值的直接部分是不可修改的。但是map的元素是个例外。 map的元素虽然不可寻址，但是每个映射元素可以被整个修改（但不可以被部分修改）：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> T <span class="keyword">struct</span>&#123;age <span class="keyword">int</span>&#125;</span><br><span class="line">mt := <span class="keyword">map</span>[<span class="keyword">string</span>]T&#123;&#125;</span><br><span class="line">mt[<span class="string">"John"</span>] = T&#123;age: <span class="number">29</span>&#125; <span class="comment">// 整体修改是允许的</span></span><br><span class="line">ma := <span class="keyword">map</span>[<span class="keyword">int</span>][<span class="number">5</span>]<span class="keyword">int</span>&#123;&#125;</span><br><span class="line">ma[<span class="number">1</span>] = [<span class="number">5</span>]<span class="keyword">int</span>&#123;<span class="number">1</span>: <span class="number">789</span>&#125; <span class="comment">// 整体修改是允许的</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 这两个赋值编译不通过，因为部分修改一个映射元素是非法的。这看上去确实有些反直觉。</span></span><br><span class="line">ma[<span class="number">1</span>][<span class="number">1</span>] = <span class="number">123</span>      <span class="comment">// error</span></span><br><span class="line">mt[<span class="string">"John"</span>].age = <span class="number">30</span> <span class="comment">// error</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 读取映射元素的元素或者字段是没问题的。</span></span><br><span class="line">fmt.Println(ma[<span class="number">1</span>][<span class="number">1</span>])       <span class="comment">// 789</span></span><br><span class="line">fmt.Println(mt[<span class="string">"John"</span>].age) <span class="comment">// 29</span></span><br></pre></td></tr></table></figure></p>
<h1 id="逃逸分析"><a href="#逃逸分析" class="headerlink" title="逃逸分析"></a>逃逸分析</h1><p>关心变量在栈或者堆上有助于我们对变量的生命周期有所了解，写出更好性能的代码。比如一些短周期的变量的指针如果和长生命周期的变量绑定，就会使得这个变量迟迟不能回收，影响性能。<br>Go在栈上的变量不会产生GC成本，因为变量会随着函数的退出一起销毁（当然这样性能也是最高的）。但是，变量是否在栈上，不能简单的通过是否局部变量或者是否使用new构建的引用类型来判断。有一个基本的判断原则：<br>情况1：如果变量的引用被声明它的函数返回了，那么这个变量就会逃逸到堆上</p>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">ref</span><span class="params">(z S)</span> *<span class="title">S</span></span> &#123;</span><br><span class="line">  <span class="keyword">return</span> &amp;z</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// go run -gcflags '-m -l' main.go</span></span><br><span class="line">./escape.<span class="keyword">go</span>:<span class="number">10</span>: moved to heap: z</span><br><span class="line">./escape.<span class="keyword">go</span>:<span class="number">11</span>: &amp;z escapes to heap</span><br></pre></td></tr></table></figure>
<p>情况2：返回的结构体引用的对象会逃逸<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">refStruct</span><span class="params">(y <span class="keyword">int</span>)</span> <span class="params">(z S)</span></span> &#123;</span><br><span class="line">  z.M = &amp;y</span><br><span class="line">  <span class="keyword">return</span> z</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// go run -gcflags '-m -l' main.go</span></span><br><span class="line">./escape.<span class="keyword">go</span>:<span class="number">12</span>: moved to heap: y</span><br><span class="line">./escape.<span class="keyword">go</span>:<span class="number">13</span>: &amp;y escapes to heap</span><br></pre></td></tr></table></figure></p>
<p>情况3：map、slice、chan引用的对象会逃逸<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    a := <span class="built_in">make</span>([]*<span class="keyword">int</span>,<span class="number">1</span>)</span><br><span class="line">    b := <span class="number">12</span></span><br><span class="line">    a[<span class="number">0</span>] = &amp;b</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// go run -gcflags '-m -l' maint.go</span></span><br><span class="line">./maint.<span class="keyword">go</span>:<span class="number">5</span>:<span class="number">2</span>: moved to heap: b</span><br><span class="line">./maint.<span class="keyword">go</span>:<span class="number">4</span>:<span class="number">11</span>: <span class="built_in">make</span>([]*<span class="keyword">int</span>, <span class="number">1</span>) does not escape</span><br></pre></td></tr></table></figure></p>
<p>我们看一个例子，逃逸使得性能下降了不少：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">BenchmarkHeap</span><span class="params">(b *testing.B)</span></span> &#123;</span><br><span class="line">    b.ResetTimer()</span><br><span class="line">    c := <span class="built_in">make</span>(<span class="keyword">chan</span> *T, b.N)</span><br><span class="line">    <span class="comment">// c := make(chan T, b.N)</span></span><br><span class="line">    <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; b.N; i++ &#123;</span><br><span class="line">        b := T&#123;a: <span class="number">3</span>, b: <span class="number">5</span>&#125;</span><br><span class="line">        c &lt;- &amp;b</span><br><span class="line">        <span class="comment">// c &lt;- b</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//  go test -bench=. -run=none</span></span><br><span class="line">BenchmarkStack<span class="number">-12</span>       <span class="number">32297865</span>                <span class="number">32.1</span> ns/op</span><br><span class="line">BenchmarkHeap<span class="number">-12</span>        <span class="number">28062832</span>                <span class="number">40.2</span> ns/op</span><br></pre></td></tr></table></figure></p>
<h1 id="routine"><a href="#routine" class="headerlink" title="routine"></a>routine</h1><h2 id="Golang并发注意点"><a href="#Golang并发注意点" class="headerlink" title="Golang并发注意点"></a>Golang并发注意点</h2><ol>
<li>最好确认routine任务的开销大于上下文切换的开销时，才使用routine。</li>
<li>要尽量控制routine的数量，不然会起到反效果</li>
<li>channel要注意缓冲区的大小和每次写入的数量，尽量打包写入</li>
</ol>
<h2 id="防止泄漏"><a href="#防止泄漏" class="headerlink" title="防止泄漏"></a>防止泄漏</h2><p>如果routine在运行中被阻塞，或者速度很慢，就会发生泄漏（routine的数量会迅速线性增长）</p>
<ol>
<li>routinue卡死在读取chan却没数据<br>理想情况下，我们设计的读取chan的routine会把所有的内容读取完毕后才会关闭。但是，一旦读取者在读取完成之前退出，写入方写满chan之后就会卡死。</li>
<li>routinue处理的速度过慢<br>这个情况有点类似消息队列消费者的堆积，如果新起的routine处理速度比主协程还慢的话，堆积起来的routine会越来越多，最终打爆内存</li>
</ol>
<h2 id="复用timer来替代timer-After"><a href="#复用timer来替代timer-After" class="headerlink" title="复用timer来替代timer.After"></a>复用timer来替代timer.After</h2><p>timer.After会创建很多的timer，引发很大的GC消耗。<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 如果有100w个msg推进来，就会有100w个timer被销毁</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">longRunning</span><span class="params">(messages &lt;-<span class="keyword">chan</span> <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line">        <span class="keyword">for</span> &#123;</span><br><span class="line">                <span class="keyword">select</span> &#123;</span><br><span class="line">                <span class="comment">// 消息间隔超过1min会return</span></span><br><span class="line">                <span class="keyword">case</span> &lt;-time.After(time.Minute):</span><br><span class="line">                        <span class="keyword">return</span></span><br><span class="line">                <span class="keyword">case</span> msg := &lt;-messages:</span><br><span class="line">                        fmt.Println(msg)</span><br><span class="line">                &#125;</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">longRunning</span><span class="params">(messages &lt;-<span class="keyword">chan</span> <span class="keyword">string</span>)</span></span> &#123;</span><br><span class="line">        timer := time.NewTimer(time.Minute)</span><br><span class="line">        <span class="keyword">defer</span> timer.Stop()</span><br><span class="line">        <span class="keyword">for</span> &#123;</span><br><span class="line">                <span class="keyword">select</span> &#123;</span><br><span class="line">                <span class="keyword">case</span> &lt;-timer.C: <span class="comment">// 过期了</span></span><br><span class="line">                        <span class="keyword">return</span></span><br><span class="line">                <span class="keyword">case</span> msg := &lt;-messages:</span><br><span class="line">                        fmt.Println(msg)</span><br><span class="line">                        <span class="comment">// 此if代码块很重要。</span></span><br><span class="line">                        <span class="keyword">if</span> !timer.Stop() &#123;</span><br><span class="line">                                &lt;-timer.C</span><br><span class="line">                        &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 必须重置以复用。</span></span><br><span class="line">                timer.Reset(time.Minute)</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<ul>
<li>我们在每次处理完消息后调用timer.Stop()以便于复用。如果timer已经过期，stop会返回false，C里面还有一条过期消息，我们需要把它取出来；如果timer没有过期，stop会返回true，继续执行循环</li>
<li>在一个Timer终止（stopped）之后并且在重置和重用此Timer值之前，我们应该确保此Timer的通道C中肯定不存在过期的通知</li>
</ul>
<h1 id="常用的仓库"><a href="#常用的仓库" class="headerlink" title="常用的仓库"></a>常用的仓库</h1><h2 id="sync和atomic"><a href="#sync和atomic" class="headerlink" title="sync和atomic"></a>sync和atomic</h2><h2 id="strings"><a href="#strings" class="headerlink" title="strings"></a>strings</h2><p>Strings库是重复造轮子的重灾区，很多人试图自己再写一遍</p>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> s = <span class="string">"abaay森z众xbbab"</span></span><br><span class="line">o := fmt.Println</span><br><span class="line">o(strings.TrimPrefix(s, <span class="string">"ab"</span>)) <span class="comment">// aay森z众xbbab</span></span><br><span class="line">o(strings.TrimSuffix(s, <span class="string">"ab"</span>)) <span class="comment">// abaay森z众xbb</span></span><br><span class="line">o(strings.TrimLeft(s, <span class="string">"ab"</span>))   <span class="comment">// y森z众xbbab</span></span><br><span class="line">o(strings.TrimRight(s, <span class="string">"ab"</span>))  <span class="comment">// abaay森z众x</span></span><br><span class="line">o(strings.Trim(s, <span class="string">"ab"</span>))       <span class="comment">// y森z众x</span></span><br><span class="line">o(strings.TrimFunc(s, <span class="function"><span class="keyword">func</span><span class="params">(r <span class="keyword">rune</span>)</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">        <span class="keyword">return</span> r &lt; <span class="number">128</span> <span class="comment">// trim all ascii chars</span></span><br><span class="line">&#125;)) <span class="comment">// 森z众</span></span><br><span class="line"></span><br><span class="line">分割与合并</span><br><span class="line"><span class="comment">// "1 2 3" -&gt; ["1","2","3"]</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">Fields</span><span class="params">(s <span class="keyword">string</span>)</span> []<span class="title">string</span>     // 用空白字符分割字符串</span></span><br><span class="line"><span class="function">// "1|2|3" -&gt; ["1","2","3"]</span></span><br><span class="line"><span class="function"><span class="title">func</span> <span class="title">Split</span><span class="params">(s, sep <span class="keyword">string</span>)</span> []<span class="title">string</span> // 用<span class="title">sep</span>分割字符串，<span class="title">sep</span>会被去掉</span></span><br><span class="line"><span class="function">// ["1","2","3"] -&gt; "1,2,3"</span></span><br><span class="line"><span class="function"><span class="title">func</span> <span class="title">Join</span><span class="params">(a []<span class="keyword">string</span>, sep <span class="keyword">string</span>)</span> <span class="title">string</span> // 将一系列字符串连接为一个字符串，之间用<span class="title">sep</span>来分隔</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">// <span class="title">Note</span>:</span></span><br><span class="line"><span class="function">// "1||3" -&gt; ["1","","3"]</span></span><br></pre></td></tr></table></figure>
<h2 id="演化中的错误处理"><a href="#演化中的错误处理" class="headerlink" title="演化中的错误处理"></a>演化中的错误处理</h2><p>满足下面的诉求：</p>
<ol>
<li>可以把异常传递下去，并不丢失自己的类型</li>
<li>可以保存堆栈信息</li>
</ol>
<p>Go的错误处理一直在讨论和演进，目前官方已经有几种不同的方案。对于反复写错误处理代码的问题，有几种解决的设想，可以看看上面的（Go语⾔将⾛向何⽅?）<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> (</span><br><span class="line">   <span class="string">"golang.org/x/xerrors"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">bar</span><span class="params">()</span> <span class="title">error</span></span> &#123;</span><br><span class="line">   <span class="keyword">if</span> err := foo(); err != <span class="literal">nil</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> xerrors.Errorf(<span class="string">"bar failed: %w"</span>, foo())</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">foo</span><span class="params">()</span> <span class="title">error</span></span> &#123;</span><br><span class="line">   <span class="keyword">return</span> xerrors.Errorf(<span class="string">"foo failed: %w"</span>, sql.ErrNoRows)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">   err := bar()</span><br><span class="line">   <span class="keyword">if</span> xerrors.Is(err, sql.ErrNoRows) &#123;</span><br><span class="line">      fmt.Printf(<span class="string">"data not found, %v\n"</span>, err)</span><br><span class="line">      fmt.Printf(<span class="string">"%+v\n"</span>, err)</span><br><span class="line">      <span class="keyword">return</span></span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* Outputs:data not found, bar failed: foo failed: sql: no rows in result set</span></span><br><span class="line"><span class="comment">bar failed:</span></span><br><span class="line"><span class="comment">    main.bar</span></span><br><span class="line"><span class="comment">        /usr/four/main.go:12</span></span><br><span class="line"><span class="comment">  - foo failed:</span></span><br><span class="line"><span class="comment">    main.foo</span></span><br><span class="line"><span class="comment">        /usr/four/main.go:18</span></span><br><span class="line"><span class="comment">  - sql: no rows in result set</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure></p>
<h1 id="参考资料"><a href="#参考资料" class="headerlink" title="参考资料"></a>参考资料</h1><p>《Go Tour》（一个小时学会Go）<a href="https://tour.go-zh.org/welcome/1" target="_blank" rel="noopener">https://tour.go-zh.org/welcome/1</a><br>《The Go Programming Language Specification》（语法细节）<a href="https://golang.org/ref/spec#Introduction（中文版《Go语言编码规范》）" target="_blank" rel="noopener">https://golang.org/ref/spec#Introduction（中文版《Go语言编码规范》）</a><br>《Go语言圣经》（语法细节）<a href="https://docs.hacknode.org/gopl-zh/" target="_blank" rel="noopener">https://docs.hacknode.org/gopl-zh/</a><br>《Effective Go》（适合刚学完Go的基础语法时候读）<a href="https://www.kancloud.cn/kancloud/effective/72199" target="_blank" rel="noopener">https://www.kancloud.cn/kancloud/effective/72199</a><br>《Go语言设计和实现》（适合想了解Go某个特性实现原理的时候参考）<a href="https://draveness.me/golang/docs/part1-prerequisite/ch02-compile/golang-compile-intro/" target="_blank" rel="noopener">https://draveness.me/golang/docs/part1-prerequisite/ch02-compile/golang-compile-intro/</a><br>《Go Q&amp;A 101》（可以和官方QA结合看）<a href="https://gfw.go101.org/article/unofficial-faq.html#time-sleep-after" target="_blank" rel="noopener">https://gfw.go101.org/article/unofficial-faq.html#time-sleep-after</a><br>《Go 语言高级编程》<a href="https://chai2010.cn/advanced-go-programming-book/" target="_blank" rel="noopener">https://chai2010.cn/advanced-go-programming-book/</a><br>《Go语言原本》<a href="https://golang.design/under-the-hood/" target="_blank" rel="noopener">https://golang.design/under-the-hood/</a><br>《uber go规范》<a href="https://github.com/xxjwxc/uber_go_guide_cn" target="_blank" rel="noopener">https://github.com/xxjwxc/uber_go_guide_cn</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本文介绍了Go的语言设计和一些容易踩坑的细节：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;理解Go为什么X，摆脱原语言的思维&lt;/li&gt;
&lt;li&gt;解决写代码时比较困惑和不满的点，对容易出错的语法有个印象&lt;/li&gt;
&lt;/ul&gt;
    
    </summary>
    
      <category term="Go" scheme="chestnutheng.cn/categories/Go/"/>
    
    
      <category term="Go" scheme="chestnutheng.cn/tags/Go/"/>
    
  </entry>
  
  <entry>
    <title>[Redis]Redis 应用篇</title>
    <link href="chestnutheng.cn/2020/09/24/redis-1/"/>
    <id>chestnutheng.cn/2020/09/24/redis-1/</id>
    <published>2020-09-24T07:57:27.000Z</published>
    <updated>2020-09-24T07:57:32.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="分布式锁"><a href="#分布式锁" class="headerlink" title="分布式锁"></a>分布式锁</h1><h2 id="简单加锁"><a href="#简单加锁" class="headerlink" title="简单加锁"></a>简单加锁</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">// 思想：利用setnx检测有没有set过，如果set过就表示没有抢到锁</span><br><span class="line">&gt; setnx locker true</span><br><span class="line">OK</span><br><span class="line">// ... do somthing ...</span><br><span class="line">&gt; del locker</span><br></pre></td></tr></table></figure>
<h2 id="处理set之后进程崩溃的死锁问题"><a href="#处理set之后进程崩溃的死锁问题" class="headerlink" title="处理set之后进程崩溃的死锁问题"></a>处理set之后进程崩溃的死锁问题</h2><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">// 思想：给锁加上过期时间，即使set之后进程挂掉，也不会死锁</span><br><span class="line">&gt; setnx locker true</span><br><span class="line">OK</span><br><span class="line">&gt; expire locker 5</span><br><span class="line">// ... do somthing ...</span><br><span class="line">&gt; del locker</span><br></pre></td></tr></table></figure>
<h2 id="处理非原子性问题"><a href="#处理非原子性问题" class="headerlink" title="处理非原子性问题"></a>处理非原子性问题</h2><p>setnx之后，expire之前，进程挂了，也会死锁。怎么处理这种情况？</p>
<ol>
<li>使用redis事务吗？事务里没有if else，要么全部执行，要么全部不执行。需求是setnx成功才执行expire，有依赖关系，没法用事务</li>
<li>使用新的原子命令，如下<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&gt; set locker true ex5 nx</span><br><span class="line">OK</span><br><span class="line">// ... do somthing ...</span><br><span class="line">&gt; del locker</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="处理超时问题"><a href="#处理超时问题" class="headerlink" title="处理超时问题"></a>处理超时问题</h2><p>上面的方案设定了超时时间。但是如果少数操作的时间超过了超时时间怎么办？有两个问题：</p>
<ol>
<li>这个时候其他线程就会抢占到锁，导致资源发生竞争。</li>
<li>如果锁是超时释放的，当前进程处理完操作之后又会释放锁，会把别人的锁释放<br>我们先来解决问题2。我们可以把locker的value设置为随机数，保证锁只会被当前线程释放。这个操作也要保证原子性，我们用lua脚本实现：<figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">// del lua</span><br><span class="line"><span class="keyword">if</span> redis.call(<span class="string">"get"</span>, key) == tag <span class="keyword">then</span></span><br><span class="line">	<span class="keyword">return</span> redis.call(<span class="string">"del"</span>, key)</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span></span><br><span class="line"><span class="keyword">end</span></span><br><span class="line">// set java</span><br><span class="line">tag = random.nextint()</span><br><span class="line"><span class="keyword">if</span> redis.set(key, tag, nx=True, ex=<span class="number">5</span>):</span><br><span class="line">	do_somthing()</span><br><span class="line">	// eval del lua</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="获得不了锁的处理"><a href="#获得不了锁的处理" class="headerlink" title="获得不了锁的处理"></a>获得不了锁的处理</h2><p>上面介绍了锁的实现，现在我们需要关注获取锁失败的分支。这个时候资源正在被其他过程占用，我们有几个处理办法：</p>
<ol>
<li>直接抛出异常，比如管理端场景</li>
<li>sleep一会重试，但是sleep会阻塞处理线程，并不好</li>
<li>把请求放到一个延时队列，延后处理</li>
</ol>
<h1 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h1><p>使用list和lpush/rpop，生产者使用lpush推入队列，消费者通过rpop推出队列。因为redis的命令是原子的，所以这里不会有竞争的问题。</p>
<p>我们该什么时候调用pop？<br>一种常见的方法是pull，定时去消费（pop），比如每隔1s，但是这样消息会有一定的延时<br>另一种方法是调用b开头的阻塞函数，比如brpop，会阻塞到队列里有消息才会返回。需要注意的是，如果阻塞过久，redis会认为这个网络连接是闲置连接，会断开这些连接，所以客户端里要做好重试</p>
<p>还会有什么问题？</p>
<ol>
<li>做消费确认ACK比较麻烦，就是不能保证消费者在读取之后，未处理后的宕机问题。导致消息意外丢失。通常需要自己维护一个Pending列表，保证消息的处理确认。</li>
<li>不能广播、分组消费、重复消费</li>
</ol>
<h2 id="多次消费"><a href="#多次消费" class="headerlink" title="多次消费"></a>多次消费</h2><p>如果要有多个消费者需要消费同一条消息，list就无法满足。Redis提供了pub/sub模式来解决这一问题。有下面几个操作：</p>
<ol>
<li>SUBSCRIBE，用于订阅信道</li>
<li>PUBLISH，向信道发送消息</li>
<li>UNSUBSCRIBE，取消订阅</li>
</ol>
<p>生产者和消费者通过一个队列进行交互。通常会有多个消费者，多个消费者订阅同一个信道，当生产者向信道发布消息时，该信道会立即将消息逐一发布给每个消费者。</p>
<p>优点：</p>
<ol>
<li>典型的广播模式，一个消息可以发布到多个消费者</li>
<li>多信道订阅，消费者可以同时订阅多个信道，从而接收多类消息</li>
<li>消息即时发送，消息不用等待消费者读取，消费者会自动接收到信道发布的消息</li>
</ol>
<p>缺点：</p>
<ol>
<li>消息一旦发布，不能接收。换句话就是发布时若客户端不在线，则消息丢失，不能寻回</li>
<li>不能保证每个消费者接收的时间是一致的</li>
<li>若消费者客户端出现消息积压，到一定程度，会被强制断开，导致消息意外丢失。通常发生在消息的生产远大于消费速度时</li>
</ol>
<p>可见，Pub/Sub 模式不适合做消息存储，消息积压类的业务，而是擅长处理广播，即时通讯，即时反馈的业务。</p>
<h2 id="延时队列"><a href="#延时队列" class="headerlink" title="延时队列"></a>延时队列</h2><p>我们实现一个五秒后再处理消息的延时队列。</p>
<ol>
<li>使用一个zset，score存放到期的时间戳。</li>
<li>然后用一个循环，zrangebyscore查出到期的消息，进行处理，如果没有消息就sleep一小会</li>
<li>如果有消息，用zrem先删除消息，然后再处理，确保只有一个线程处理</li>
</ol>
<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">delay</span><span class="params">(msg)</span>:</span></span><br><span class="line">	msg.id = str(uuid.uuid4()) <span class="comment"># 保证 value 值唯一</span></span><br><span class="line">	value = json.dumps(msg)</span><br><span class="line">	retry_ts = time.time() + <span class="number">5</span> <span class="comment"># 5 秒后重试</span></span><br><span class="line">	redis.zadd(<span class="string">"delay-queue"</span>, retry_ts, value)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">loop</span><span class="params">()</span>:</span></span><br><span class="line">	<span class="keyword">while</span> <span class="keyword">True</span>:</span><br><span class="line">		<span class="comment"># 最多取 1 条</span></span><br><span class="line">		values = redis.zrangebyscore(<span class="string">"delay-queue"</span>, <span class="number">0</span>, time.time(), start=<span class="number">0</span>, num=<span class="number">1</span>)</span><br><span class="line">		<span class="keyword">if</span> <span class="keyword">not</span> values: </span><br><span class="line">			time.sleep(<span class="number">1</span>) <span class="comment"># 延时队列空的，休息 1s</span></span><br><span class="line">			<span class="keyword">continue</span></span><br><span class="line">		value = values[<span class="number">0</span>] <span class="comment"># 拿第一条，也只有一条</span></span><br><span class="line">		success = redis.zrem(<span class="string">"delay-queue"</span>, value) <span class="comment"># 从消息队列中移除该消息</span></span><br><span class="line">		<span class="keyword">if</span> success: <span class="comment"># 因为有多进程并发的可能，最终只会有一个进程可以抢到消息</span></span><br><span class="line">			msg = json.loads(value)</span><br><span class="line">			handle_msg(msg)</span><br></pre></td></tr></table></figure>
<h2 id="Redis-Stream"><a href="#Redis-Stream" class="headerlink" title="Redis Stream"></a>Redis Stream</h2><p><a href="http://www.hellokang.net/redis/stream.html" target="_blank" rel="noopener">Redis对消息队列（MQ，Message Queue）的完善实现</a></p>
<h1 id="hyperloglog-做基数-UV-统计"><a href="#hyperloglog-做基数-UV-统计" class="headerlink" title="hyperloglog 做基数(UV)统计"></a>hyperloglog 做基数(UV)统计</h1><p>对于一些网站统计uv的场景，常见的做法用set是记录下访问过站点的uid，然后返回set的大小。<br>hyperloglog不存储具体的uid，只是做一个数学估计。</p>
<p>每次进来一个数字，我们看他从低位开始全是0的位有多少个。然后取<br>maxbit = max(maxbit, now0bit)<br>我们看看一次添加后发生了什么。比如这个命令：</p>
<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">PFADD runoobkey <span class="string">"r"</span></span><br><span class="line"><span class="comment">// 第一步，r的二进制为 01110010，得到最后只有1个0</span></span><br><span class="line"><span class="comment">// now0bit=1，maxbit=max(maxbit, now0bit)=1</span></span><br></pre></td></tr></table></figure>
<p>我们会发现maxbit和总共的基数N有一定的关系。多次测试后，发现N和maxbit的关系大概是：<br>$$N=2^{maxbit}$$<br>但是这里可能会有很大的误差。只要有一个数字的now0bit非常大，就会把这个估计拉的很远。所以这里要用分组平均一下。<br>假设我们分为1024个桶，然后对每个桶分别估计的N取调和平均值，这样误差就会比较小。当取1024个桶时，误差大概在10%以内。此时N和maxbit的关系大概是（n=1024）：<br>$$ N={\frac{n}{\sum_{i=1}^{n}{\frac{1}{maxbit_{i}}}}}$$<br>Note: redis把数字分为16384(2^14)个桶，每个桶的maxbit占用6bit，所以占用的空间大概为2^14*6/8=12KB，误差在1%以内。</p>
<h1 id="位图和布隆过滤器"><a href="#位图和布隆过滤器" class="headerlink" title="位图和布隆过滤器"></a>位图和布隆过滤器</h1><h2 id="位图"><a href="#位图" class="headerlink" title="位图"></a>位图</h2><p>redis提供了位图操作</p>
<ol>
<li><code>setbit key offset value</code> 设置某个位</li>
<li><code>getbit key offset</code> 获取某个位</li>
<li><code>bitcount key start end</code> 获取某个区间之间的1的数量</li>
<li><code>bitop and/or/not/xor key1 key2</code></li>
</ol>
<p>数据结构：这里面的存储结构实际上是一个SDS。在Redis Object里有一个char数组buf，里面存放着位信息，每个char能存8个位。在buf数组的末尾有一个<code>&#39;\0&#39;</code>表示位图的结尾。</p>
<h3 id="setbit-getbit"><a href="#setbit-getbit" class="headerlink" title="setbit/getbit"></a>setbit/getbit</h3><p>如何实现：setbit和getbit，都只要通过模余运算得到对应的buf下标和位，就可以拿到对应的值。<br>我们怎么拿到第n位的值？需要一个第n位为1的掩码 <code>1&lt;&lt;(7 - n)</code></p>
<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// getbit</span></span><br><span class="line"><span class="comment">// 计算出 offset 所指定的位所在的字节 = offset/8</span></span><br><span class="line">byte = bitoffset &gt;&gt; <span class="number">3</span>;</span><br><span class="line"><span class="comment">// 计算出位所在的位置 = offset%8, bitoffset &amp; 0x7得到后三位（offset%8）</span></span><br><span class="line">bit = <span class="number">7</span> - (bitoffset &amp; <span class="number">0x7</span>);</span><br><span class="line">bitval = ((<span class="keyword">uint8_t</span>*)buf)[byte] &amp; (<span class="number">1</span> &lt;&lt; bit);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 第17位为例子 getbit [key] 17</span></span><br><span class="line"><span class="comment">// 除以8得到第几个byte，byte = 17 &gt;&gt; 3 = 2</span></span><br><span class="line"><span class="comment">// 模8取得字节数，取后三位 17 &amp; 0111 = 01 0001 &amp; 0111 = 0001</span></span><br><span class="line"><span class="comment">// 得到掩码 7 - 0001 = 6，1 &lt;&lt; 6 = 0100 0000</span></span><br><span class="line"><span class="comment">// 取出第3个字节的第二位 buf[2] &amp; 0100 0000</span></span><br></pre></td></tr></table></figure>
<p>setbit稍微麻烦一些。在设置位的基础上还要考虑到SDS扩容的情况，如果setbit的offset超过了最大值，那么需要进行扩容并把扩好的空间初始化位0。</p>
<h3 id="bitcount"><a href="#bitcount" class="headerlink" title="bitcount"></a>bitcount</h3><p>计算二进制中1的个数，有几种方法：</p>
<ol>
<li>查表。预处理好0000 0001 ~ 1111 1111 和其中1的个数的映射，比如0000 0001(1)，后面来了直接查表。我们最高可以建16位的表，仅需百KB左右的内存</li>
<li>variable-precision swar算法。<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">i = (i &amp; <span class="number">0x55555555</span>) + ((i &gt;&gt; <span class="number">1</span>) &amp; <span class="number">0x55555555</span>);</span><br><span class="line">i = (i &amp; <span class="number">0x33333333</span>) + ((i &gt;&gt; <span class="number">2</span>) &amp; <span class="number">0x33333333</span>);</span><br><span class="line">i = (i &amp; <span class="number">0x0F0F0F0F</span>) + ((i &gt;&gt; <span class="number">4</span>) &amp; <span class="number">0x0F0F0F0F</span>);</span><br><span class="line">i = (i * (<span class="number">0x01010101</span>) &gt;&gt; <span class="number">24</span>);</span><br><span class="line"><span class="keyword">return</span> i</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>Redis中，不足128位的bitmap会查表，有一个8位的表查16次；超过8位的，每次载入128位，然后调用四次SWAR算法计算汉明重量。</p>
<h2 id="布隆过滤器"><a href="#布隆过滤器" class="headerlink" title="布隆过滤器"></a>布隆过滤器</h2><p>布隆过滤器，是用来判断元素在不在set里的一种方法。上面的hyperloglog只能支持count操作，但是不支持contains操作。要大概地判断元素是否出现过就可以用布隆过滤器，他会用很多个哈希函数对元素进行哈希，把对应的位都设置；查找时也同样哈希，并查看对应的位是否被设置。布隆过滤器有一定的概率会把不存在的元素判定为存在（取决于要求的占用空间，），但是存在的元素一定不会判断错。</p>
<ol>
<li>bf.add</li>
<li>bf.exists</li>
</ol>
<h1 id="限流"><a href="#限流" class="headerlink" title="限流"></a>限流</h1><h2 id="简单限流"><a href="#简单限流" class="headerlink" title="简单限流"></a>简单限流</h2><p>每一个行为到来时，都维护一次时间窗口。将时间窗口外的记录全部清理掉，只保留窗口内的记录。zset 集合中只有 score 值非常重要，value 值没有特别的意义，只需要保证它是唯一的就可以了（也可以用uuid，这里用了毫秒）。</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 业务代码 调用这个接口 , 一分钟内只允许最多回复 5 个帖子</span></span><br><span class="line">can_reply = is_action_allowed(<span class="string">"laoqian"</span>, <span class="string">"reply"</span>, <span class="number">60</span>, <span class="number">5</span>)</span><br><span class="line"><span class="keyword">if</span> can_reply:</span><br><span class="line">	do_reply()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">is_action_allowed</span><span class="params">(user_id, action_key, period, max_count)</span>:</span></span><br><span class="line">	key = <span class="string">'hist:%s:%s'</span> % (user_id, action_key)</span><br><span class="line">	now_ts = int(time.time() * <span class="number">1000</span>) <span class="comment"># 毫秒时间戳</span></span><br><span class="line">	<span class="keyword">with</span> client.pipeline() <span class="keyword">as</span> pipe: <span class="comment"># client 是 StrictRedis 实例</span></span><br><span class="line">		<span class="comment"># 记录行为</span></span><br><span class="line">		pipe.zadd(key, now_ts, now_ts) <span class="comment"># value 和 score 都使用毫秒时间戳</span></span><br><span class="line">		<span class="comment"># 移除时间窗口之前的行为记录，剩下的都是时间窗口内的</span></span><br><span class="line">		pipe.zremrangebyscore(key, <span class="number">0</span>, now_ts - period * <span class="number">1000</span>)</span><br><span class="line">		<span class="comment"># 获取窗口内的行为数量</span></span><br><span class="line">		pipe.zcard(key)</span><br><span class="line">		<span class="comment"># 设置 zset 过期时间，避免冷用户持续占用内存</span></span><br><span class="line">		<span class="comment"># 过期时间应该等于时间窗口的长度，再多宽限 1s</span></span><br><span class="line">		pipe.expire(key, period + <span class="number">1</span>)</span><br><span class="line">	<span class="comment"># 批量执行</span></span><br><span class="line">	_, _, current_count, _ = pipe.execute()</span><br><span class="line">	<span class="comment"># 比较数量是否超标</span></span><br><span class="line">	<span class="keyword">return</span> current_count &lt;= max_count</span><br></pre></td></tr></table></figure>
<h2 id="漏桶限流-Redis-Cell"><a href="#漏桶限流-Redis-Cell" class="headerlink" title="漏桶限流 Redis-Cell"></a>漏桶限流 Redis-Cell</h2><p>Redis4.0之后提供了更好用的限流组件。<br><code>CL.THROTTLE reply 15 30 60 1</code><br><code>CL.THROTTLE key 初始容量 次数 秒数 每次加几个资格</code><br>这条命令限定了回复行为的频率为每 60s 最多 30 次(漏水速率)，漏斗的初始容量为 15，也就是说一开始可以连续回复 15 个帖子，然后才开始受漏水速率的影响。返回值也很有用：<br>参数1： 0 表示允许，1 表示拒绝<br>参数2： 漏斗容量 capacity<br>参数3： 漏斗剩余空间 left_quota<br>参数4： 如果拒绝了，需要多长时间后再试(漏斗有空间了，单位秒)<br>参数5： 多长时间后，漏斗完全空出来(left_quota==capacity，单位秒)</p>
<p>我们来看漏桶的一个python实现。每个漏桶</p>
<ol>
<li>会存一个上一次漏水时间；</li>
<li>每次有请求过来就得到<code>新增资格数=(计算当前时间-上次漏水时间)*每秒加入资格数</code>，加到剩余空间上，但要保证剩余空间不大于capacity</li>
<li>剩余空间减掉请求的空间，并更新上次漏水时间</li>
</ol>
<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> time</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Funnel</span><span class="params">(object)</span>:</span></span><br><span class="line">	<span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, capacity, leaking_rate)</span>:</span></span><br><span class="line">		self.capacity = capacity <span class="comment"># 漏斗容量</span></span><br><span class="line">		self.leaking_rate = leaking_rate <span class="comment"># 漏嘴流水速率</span></span><br><span class="line">		self.left_quota = capacity <span class="comment"># 漏斗剩余空间</span></span><br><span class="line">		self.leaking_ts = time.time() <span class="comment"># 上一次漏水时间</span></span><br><span class="line">	<span class="function"><span class="keyword">def</span> <span class="title">make_space</span><span class="params">(self)</span>:</span></span><br><span class="line">		now_ts = time.time()</span><br><span class="line">		delta_ts = now_ts - self.leaking_ts <span class="comment"># 距离上一次漏水过去了多久</span></span><br><span class="line">		delta_quota = delta_ts * self.leaking_rate <span class="comment"># 又可以腾出不少空间了</span></span><br><span class="line">		<span class="keyword">if</span> delta_quota &lt; <span class="number">1</span>: <span class="comment"># 腾的空间太少，那就等下次吧</span></span><br><span class="line">			<span class="keyword">return</span></span><br><span class="line">		self.left_quota += delta_quota <span class="comment"># 增加剩余空间</span></span><br><span class="line">		self.leaking_ts = now_ts <span class="comment"># 记录漏水时间</span></span><br><span class="line">		<span class="keyword">if</span> self.left_quota &gt; self.capacity: <span class="comment"># 剩余空间不得高于容量</span></span><br><span class="line">			self.left_quota = self.capacity</span><br><span class="line">	<span class="function"><span class="keyword">def</span> <span class="title">watering</span><span class="params">(self, quota)</span>:</span></span><br><span class="line">		self.make_space()</span><br><span class="line">		<span class="keyword">if</span> self.left_quota &gt;= quota: <span class="comment"># 判断剩余空间是否足够</span></span><br><span class="line">			self.left_quota -= quota</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">True</span></span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">False</span></span><br><span class="line"></span><br><span class="line">funnels = &#123;&#125; <span class="comment"># 所有的漏斗</span></span><br><span class="line"><span class="comment"># capacity 漏斗容量</span></span><br><span class="line"><span class="comment"># leaking_rate 漏嘴流水速率 quota/s</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">is_action_allowed</span><span class="params">(user_id, action_key, capacity, leaking_rate)</span>:</span></span><br><span class="line">	key = <span class="string">'%s:%s'</span> % (user_id, action_key)</span><br><span class="line">	funnel = funnels.get(key)</span><br><span class="line">	<span class="keyword">if</span> <span class="keyword">not</span> funnel:</span><br><span class="line">		funnel = Funnel(capacity, leaking_rate)</span><br><span class="line">		funnels[key] = funnel</span><br><span class="line">	<span class="keyword">return</span> funnel.watering(<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">20</span>):</span><br><span class="line">	<span class="keyword">print</span> is_action_allowed(<span class="string">'laoqian'</span>, <span class="string">'reply'</span>, <span class="number">15</span>, <span class="number">0.5</span>)</span><br></pre></td></tr></table></figure>
<h1 id="GeoHash-附近的人"><a href="#GeoHash-附近的人" class="headerlink" title="GeoHash 附近的人"></a>GeoHash 附近的人</h1><p>业务中经常有判断两个地址是否靠近，寻找某个地址周边的餐厅或超市，类似这样的需求。请求量不大时，我们可以用mysql解决，用一个半径圈出范围：<br><code>select id from positions where x0-r &lt; x &lt; x0+r and y0-r &lt; y &lt; y0+r</code><br>请求量比较大时，我们就要借助redis 3.2以后的GeoHash的指令：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">// 添加地址</span><br><span class="line">geoadd company 116.48105 39.996794 juejin</span><br><span class="line">geoadd company 116.489033 40.007669 meituan</span><br><span class="line">// 列出地址</span><br><span class="line">geodist company juejin meituan km</span><br><span class="line">// 列出某个地址周围20km的最近三个地址</span><br><span class="line">georadiusbymember company juejin 20 km count 3 asc</span><br><span class="line">// 列出某个经纬度附近的三个地址</span><br><span class="line">georadius company 116.514202 39.905409 20 km withdist count 3 asc</span><br></pre></td></tr></table></figure></p>
<p>实际上，GeoHash内部是一个zset，score是经纬度映射到一维后的一个数字。平面上靠的越近，zset中score值也会越接近。那么要怎么做这种映射呢？数学上有很多二位空间到一维空间的映射，参见<br><a href="https://halfrost.com/go_spatial_search/" target="_blank" rel="noopener">https://halfrost.com/go_spatial_search/</a><br><a href="https://www.jianshu.com/p/2fd0cf12e5ba" target="_blank" rel="noopener">https://www.jianshu.com/p/2fd0cf12e5ba</a></p>
]]></content>
    
    <summary type="html">
    
      本文介绍了Redis的一些应用，包括分布式锁、消息/延迟队列、hyperloglog、位图、布隆过滤器、限流、地理位置等。
    
    </summary>
    
      <category term="中间件" scheme="chestnutheng.cn/categories/%E4%B8%AD%E9%97%B4%E4%BB%B6/"/>
    
    
      <category term="Redis" scheme="chestnutheng.cn/tags/Redis/"/>
    
      <category term="中间件" scheme="chestnutheng.cn/tags/%E4%B8%AD%E9%97%B4%E4%BB%B6/"/>
    
  </entry>
  
  <entry>
    <title>[C++]C++ 11/14 笔记（四）</title>
    <link href="chestnutheng.cn/2020/08/18/cpp-11-4/"/>
    <id>chestnutheng.cn/2020/08/18/cpp-11-4/</id>
    <published>2020-08-18T11:34:11.000Z</published>
    <updated>2021-02-02T12:24:08.000Z</updated>
    
    <content type="html"><![CDATA[<p>本部分介绍了c++11的并发编程。这些笔记是未完成的。<br><a id="more"></a></p>
<p>语法参考这里：<br><a href="https://changkun.de/modern-cpp/zh-cn/07-thread/index.html" target="_blank" rel="noopener">现代C++教程</a><br>实现参考这里：<br><a href="http://hengyunabc.github.io/cpp11-mutex-lock-condition/" target="_blank" rel="noopener">C++11中的mutex, lock，condition variable实现分析</a></p>
<h1 id="std-thread"><a href="#std-thread" class="headerlink" title="std::thread"></a>std::thread</h1><p>C++ 11为我们带来了语言级的线程支持，包括线程的创建和等待：<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//g++ -o t main.cpp -lpthread --std=c++11</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;chrono&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span> sec)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// sleep_for() 休眠sec秒后唤醒</span></span><br><span class="line">    <span class="built_in">std</span>::this_thread::sleep_for(<span class="built_in">std</span>::chrono::seconds(sec));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// join() 阻塞等待线程</span></span><br><span class="line">    <span class="comment">// joinable() 是否可以join（没设置任务函数不可以join）</span></span><br><span class="line">    <span class="built_in">std</span>::thread t;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"before starting, joinable: "</span> &lt;&lt; t.joinable() &lt;&lt; <span class="string">'\n'</span>;</span><br><span class="line">    t = <span class="built_in">std</span>::thread(foo, <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"after starting, joinable: "</span> &lt;&lt; t.joinable() &lt;&lt; <span class="string">'\n'</span>;</span><br><span class="line">    t.join();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// detach() 将当前线程对象所代表的执行实例与该线程对象分离，使得线程的执行可以单独进行。一旦线程执行完毕，它所分配的资源将会被释放。</span></span><br><span class="line">    <span class="comment">// 永远不要用detach()。失去控制的线程会有很多问题</span></span><br><span class="line">    <span class="comment">// get_id() 打印线程id</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="function">thread <span class="title">t1</span><span class="params">(foo, <span class="number">2</span>)</span></span>;</span><br><span class="line">    <span class="built_in">std</span>::thread::id t1_id = t1.get_id();</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"t1_id: "</span> &lt;&lt; t1_id &lt;&lt; <span class="string">'\n'</span>;</span><br><span class="line">    t1.detach();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="std-mutex和std-lock-guard同步"><a href="#std-mutex和std-lock-guard同步" class="headerlink" title="std::mutex和std::lock_guard同步"></a>std::mutex和std::lock_guard同步</h1><p>有了线程就必然需要锁。我们看一个例子，用RAII 的方式管理线程和锁：<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// g++ -o t th.cpp --std=c++11 -lpthread</span></span><br><span class="line"><span class="keyword">int</span> v = <span class="number">1</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">critical_section</span><span class="params">(<span class="keyword">int</span> change_v)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">std</span>::mutex mtx;</span><br><span class="line">    <span class="built_in">std</span>::lock_guard&lt;<span class="built_in">std</span>::mutex&gt; lock(mtx);</span><br><span class="line">    <span class="comment">// 执行竞争操作</span></span><br><span class="line">    v = change_v;</span><br><span class="line">    <span class="comment">// 离开此作用域后 mtx 会被释放</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::thread t1(critical_section, 2), t2(critical_section, 3);</span><br><span class="line">    t1.join();</span><br><span class="line">    t2.join();</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; v &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>看看源码，很简单地用构造和析构包了一下锁：</p>
<ol>
<li>发生异常时也可以析构，所以锁是异常安全的</li>
<li>禁止拷贝构造、赋值</li>
<li>锁状态是<code>adopt_lock_t</code>（已获得锁），会调用不加锁的构造函数<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="number">_</span>Mutex&gt;</span><br><span class="line"><span class="keyword">class</span> lock_guard</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">typedef</span> <span class="number">_</span>Mutex mutex_type;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    mutex_type&amp; <span class="number">__</span>m_;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">lock_guard</span><span class="params">(mutex_type&amp; <span class="number">__</span>m)</span></span></span><br><span class="line">        : __m_(__m) &#123;__m_.lock();&#125;</span><br><span class="line">    lock_guard(mutex_type&amp; <span class="number">__</span>m, <span class="keyword">adopt_lock_t</span>)</span><br><span class="line">        : <span class="number">__</span>m_(<span class="number">__</span>m) &#123;&#125;</span><br><span class="line">    ~lock_guard() &#123;<span class="number">__</span>m_.unlock();&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    lock_guard(lock_guard <span class="keyword">const</span>&amp;);<span class="comment">// = delete;</span></span><br><span class="line">    lock_guard&amp; <span class="keyword">operator</span>=(lock_guard <span class="keyword">const</span>&amp;);<span class="comment">// = delete;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h1 id="std-unique-lock缩小锁的粒度"><a href="#std-unique-lock缩小锁的粒度" class="headerlink" title="std::unique_lock缩小锁的粒度"></a>std::unique_lock缩小锁的粒度</h1><p>lock_guard 是一种RAII的思想管理锁，但是有时候我们需要自己加更小粒度的锁。</p>
<ol>
<li>同样，构造时加锁，析构时释放锁</li>
<li>提供了lock、unlock、trylock、release等额外的功能，当然，开销也更大</li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">critical_section</span><span class="params">(<span class="keyword">int</span> change_v)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="built_in">std</span>::mutex mtx;</span><br><span class="line">    <span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; lock(mtx);</span><br><span class="line">    <span class="comment">// 执行竞争操作</span></span><br><span class="line">    v = change_v;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; v &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="comment">// 将锁进行释放</span></span><br><span class="line">    lock.unlock();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在此期间，任何人都可以抢夺 v 的持有权</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 开始另一组竞争操作，再次加锁</span></span><br><span class="line">    lock.lock();</span><br><span class="line">    v += <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 析构也会自动释放</span></span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>
<h1 id="std-future-期物"><a href="#std-future-期物" class="headerlink" title="std::future 期物"></a>std::future 期物</h1><p>在 C++11 的 <code>std::future</code> 被引入之前，并行获取数据的通常的做法是： 创建一个线程 A，在线程 A 里启动任务 B，当准备完毕后发送一个事件，并将结果保存在全局变量中。 而主函数线程 A 里正在做其他的事情，当需要结果的时候，调用一个线程等待函数来获得执行的结果。</p>
<p>而 C++11 提供的 <code>std::future</code> 简化了这个流程，可以用来获取异步任务的结果：</p>
<ol>
<li>使用<code>packaged_task</code>把我们的任务函数包起来得到<code>task</code></li>
<li>获得期物<code>result=task.get_future()</code>（此时他还没有结果）</li>
<li>在另一个线程中执行<code>task</code></li>
<li>阻塞等待<code>result.wait()</code></li>
<li>获取结果<code>result.get()</code></li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;future&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 将一个返回值为7的 lambda 表达式封装到 task 中</span></span><br><span class="line">    <span class="comment">// std::packaged_task 的模板参数为要封装函数的类型</span></span><br><span class="line">    <span class="built_in">std</span>::packaged_task&lt;<span class="keyword">int</span>()&gt; task([]()&#123;<span class="keyword">return</span> <span class="number">7</span>;&#125;);</span><br><span class="line">    <span class="comment">// 获得 task 的期物</span></span><br><span class="line">    <span class="built_in">std</span>::future&lt;<span class="keyword">int</span>&gt; result = task.get_future(); </span><br><span class="line">    <span class="comment">// 在一个线程中执行 task</span></span><br><span class="line">    <span class="built_in">std</span>::thread(<span class="built_in">std</span>::move(task)).detach();</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"waiting..."</span>;</span><br><span class="line">    result.wait(); <span class="comment">// 在此设置屏障，阻塞到期物的完成</span></span><br><span class="line">    <span class="comment">// 输出执行结果</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"done!"</span> &lt;&lt; <span class="built_in">std</span>:: <span class="built_in">endl</span> &lt;&lt; <span class="string">"future result is "</span> &lt;&lt; result.get() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="原子操作-std-atomic"><a href="#原子操作-std-atomic" class="headerlink" title="原子操作 std::atomic"></a>原子操作 std::atomic</h1><p>C++11 中多线程下共享变量的读写这一问题上，还引入了 std::atomic 模板，使得我们实例化一个原子类型，将一个 原子类型读写操作从一组指令，最小化到单个 CPU 指令。</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;atomic&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::atomic&lt;<span class="keyword">int</span>&gt; count = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function">thread <span class="title">t1</span><span class="params">([]()</span></span>&#123;</span><br><span class="line">        count.fetch_add(<span class="number">1</span>);</span><br><span class="line">    &#125;);</span><br><span class="line">    <span class="built_in">std</span>::<span class="function">thread <span class="title">t2</span><span class="params">([]()</span></span>&#123;</span><br><span class="line">        count++;        <span class="comment">// 等价于 fetch_add</span></span><br><span class="line">        count += <span class="number">1</span>;     <span class="comment">// 等价于 fetch_add</span></span><br><span class="line">    &#125;);</span><br><span class="line">    t1.join();</span><br><span class="line">    t2.join();</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; count &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="条件变量-std-condition-variable"><a href="#条件变量-std-condition-variable" class="headerlink" title="条件变量 std::condition_variable"></a>条件变量 std::condition_variable</h1><p>条件变量 std::condition_variable 是为了解决死锁而生，当互斥操作不够用而引入的。 比如，线程可能需要等待某个条件为真才能继续执行， 而一个忙等待循环中可能会导致所有其他线程都无法进入临界区使得条件为真时，就会发生死锁。 所以，condition_variable 实例被创建出现主要就是用于唤醒等待线程从而避免死锁。 std::condition_variable的 notify_one() 用于唤醒一个线程； notify_all() 则是通知所有线程。下面是一个生产者和消费者模型的例子：</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;chrono&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;condition_variable&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">queue</span>&lt;<span class="keyword">int</span>&gt; produced_nums;</span><br><span class="line">    <span class="built_in">std</span>::mutex mtx;</span><br><span class="line">    <span class="built_in">std</span>::condition_variable cv;</span><br><span class="line">    <span class="keyword">bool</span> notified = <span class="literal">false</span>;  <span class="comment">// 通知信号</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 生产者</span></span><br><span class="line">    <span class="keyword">auto</span> producer = [&amp;]() &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; ; i++) &#123;</span><br><span class="line">            <span class="built_in">std</span>::this_thread::sleep_for(<span class="built_in">std</span>::chrono::milliseconds(<span class="number">900</span>));</span><br><span class="line">            <span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; lock(mtx);</span><br><span class="line">            <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"producing "</span> &lt;&lt; i &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">            produced_nums.push(i);</span><br><span class="line">            notified = <span class="literal">true</span>;</span><br><span class="line">            cv.notify_all(); <span class="comment">// 此处也可以使用 notify_one</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="comment">// 消费者</span></span><br><span class="line">    <span class="keyword">auto</span> consumer = [&amp;]() &#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">            <span class="built_in">std</span>::unique_lock&lt;<span class="built_in">std</span>::mutex&gt; lock(mtx);</span><br><span class="line">            <span class="keyword">while</span> (!notified) &#123;  <span class="comment">// 避免虚假唤醒</span></span><br><span class="line">                cv.wait(lock);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 短暂取消锁，使得生产者有机会在消费者消费空前继续生产</span></span><br><span class="line">            lock.unlock();</span><br><span class="line">            <span class="built_in">std</span>::this_thread::sleep_for(<span class="built_in">std</span>::chrono::milliseconds(<span class="number">1000</span>)); <span class="comment">// 消费者慢于生产者</span></span><br><span class="line">            lock.lock();</span><br><span class="line">            <span class="keyword">while</span> (!produced_nums.empty()) &#123;</span><br><span class="line">                <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"consuming "</span> &lt;&lt; produced_nums.front() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">                produced_nums.pop();</span><br><span class="line">            &#125;</span><br><span class="line">            notified = <span class="literal">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 分别在不同的线程中运行</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="function">thread <span class="title">p</span><span class="params">(producer)</span></span>;</span><br><span class="line">    <span class="built_in">std</span>::thread cs[<span class="number">2</span>];</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; ++i) &#123;</span><br><span class="line">        cs[i] = <span class="built_in">std</span>::thread(consumer);</span><br><span class="line">    &#125;</span><br><span class="line">    p.join();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; ++i) &#123;</span><br><span class="line">        cs[i].join();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="内存顺序"><a href="#内存顺序" class="headerlink" title="内存顺序"></a>内存顺序</h1><p>两个线程读写临界区变量的时候，虽然加锁可以保证原子操作，但是不能保证原子操作的顺序。一般来讲，我们也不需要关注原子操作的顺序，如果需要关注，那就请用最强的强一致内存模型，而不是像语言那样采用六种顺序模型（工程里会带来不必要的复杂度和错误使用的风险）。<br>C++11 为了追求极致的性能，实现各种强度要求的一致性，为原子操作定义了六种不同的内存顺序 std::memory_order 的选项，表达了四种多线程间的同步模型<br><a href="https://www.zhihu.com/question/24301047" target="_blank" rel="noopener">https://www.zhihu.com/question/24301047</a></p>
<h2 id="宽松模型-relaxed"><a href="#宽松模型-relaxed" class="headerlink" title="宽松模型 relaxed"></a>宽松模型 relaxed</h2><p>在此模型下，<strong>单个线程内的原子操作都是顺序执行</strong>的，不允许指令重排，但<strong>不同线程间原子操作的顺序是任意</strong>的。类型通过 <code>std::memory_order_relaxed</code> 指定。<br>下面的例子中，每个线程内的<code>fetch_add</code>都是顺序执行，但不同线程间的<code>fetch_add</code>顺序是乱序的。<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">std</span>::atomic&lt;<span class="keyword">int</span>&gt; counter = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::thread&gt; vt;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; ++i) &#123;</span><br><span class="line">    vt.emplace_back([&amp;]()&#123;</span><br><span class="line">        counter.fetch_add(<span class="number">1</span>, <span class="built_in">std</span>::memory_order_relaxed);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">auto</span>&amp; t : vt) &#123;</span><br><span class="line">    t.join();</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"current counter:"</span> &lt;&lt; counter &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br></pre></td></tr></table></figure></p>
<h2 id="释放-消费模型-release-consume"><a href="#释放-消费模型-release-consume" class="headerlink" title="释放/消费模型 release/consume"></a>释放/消费模型 release/consume</h2><p>在此模型中，加了一个限制，<strong>任何一次读操作都能读到数据最近一次写入</strong>的数据。<br>比如线程A写了三次，B线程来读，必须保证B线程看到的是最后一次。A线程写的顺序则不作要求。</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 初始化为 nullptr 防止 consumer 线程从野指针进行读取</span></span><br><span class="line"><span class="built_in">std</span>::atomic&lt;<span class="keyword">int</span>*&gt; ptr(<span class="literal">nullptr</span>);</span><br><span class="line"><span class="keyword">int</span> v;</span><br><span class="line"><span class="built_in">std</span>::<span class="function">thread <span class="title">producer</span><span class="params">([&amp;]()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span>* p = <span class="keyword">new</span> <span class="keyword">int</span>(<span class="number">42</span>);</span><br><span class="line">    v = <span class="number">1024</span>;</span><br><span class="line">    ptr.store(p, <span class="built_in">std</span>::memory_order_release);</span><br><span class="line">&#125;);</span><br><span class="line"><span class="built_in">std</span>::<span class="function">thread <span class="title">consumer</span><span class="params">([&amp;]()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span>* p;</span><br><span class="line">    <span class="keyword">while</span>(!(p = ptr.load(<span class="built_in">std</span>::memory_order_consume)));</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"p: "</span> &lt;&lt; *p &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"v: "</span> &lt;&lt; v &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;);</span><br><span class="line">producer.join();</span><br><span class="line">consumer.join();</span><br></pre></td></tr></table></figure>
<h2 id="释放-获取模型-release-acquire"><a href="#释放-获取模型-release-acquire" class="headerlink" title="释放/获取模型 release/acquire"></a>释放/获取模型 release/acquire</h2><p>在此模型下，我们可以进一步加紧对不同线程间原子操作的顺序的限制。release/acquire模型保证了线程 A 中所有发生在 release x 之前的写操作，对在线程 B acquire x 之后的任何读操作都可见。<br><code>std::memory_order_release</code>确保了它之后的写行为不会发生在释放操作之前，是一个向前的屏障<br><code>std::memory_order_acquire</code>确保了它之前的写行为，不会发生在该获取操作之后，是一个向后的屏障<br><code>std::memory_order_acq_rel</code>则结合了这两者的特点，唯一确定了一个内存屏障，使得当前线程对内存的读写不会被重排到此操作的前后。</p>
<h2 id="顺序一致模型-sequential-consistency"><a href="#顺序一致模型-sequential-consistency" class="headerlink" title="顺序一致模型 sequential consistency"></a>顺序一致模型 sequential consistency</h2><p>在此模型下，原子操作满足顺序一致性，进而可能对性能产生损耗。可显式的通过 <code>std::memory_order_seq_cst</code> 进行指定。<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">std</span>::atomic&lt;<span class="keyword">int</span>&gt; counter = &#123;<span class="number">0</span>&#125;;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::thread&gt; vt;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; ++i) &#123;</span><br><span class="line">    vt.emplace_back([&amp;]()&#123;</span><br><span class="line">        counter.fetch_add(<span class="number">1</span>, <span class="built_in">std</span>::memory_order_seq_cst);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">auto</span>&amp; t : vt) &#123;</span><br><span class="line">    t.join();</span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"current counter:"</span> &lt;&lt; counter &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br></pre></td></tr></table></figure></p>
<h1 id="实现一个线程池"><a href="#实现一个线程池" class="headerlink" title="实现一个线程池"></a>实现一个线程池</h1><p>我们用一个例子<a href="https://github.com/progschj/ThreadPool/blob/master/ThreadPool.h" target="_blank" rel="noopener">C++11线程池</a>学习一下c11提供的这些新特性。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;本部分介绍了c++11的并发编程。这些笔记是未完成的。&lt;br&gt;
    
    </summary>
    
      <category term="C++" scheme="chestnutheng.cn/categories/C/"/>
    
    
      <category term="c++" scheme="chestnutheng.cn/tags/c/"/>
    
      <category term="c++11" scheme="chestnutheng.cn/tags/c-11/"/>
    
  </entry>
  
  <entry>
    <title>[后台]负载均衡 （三）限流篇</title>
    <link href="chestnutheng.cn/2020/07/27/load-balance3/"/>
    <id>chestnutheng.cn/2020/07/27/load-balance3/</id>
    <published>2020-07-27T09:28:37.000Z</published>
    <updated>2020-07-27T09:28:37.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="限流"><a href="#限流" class="headerlink" title="限流"></a>限流</h1><p>限流能力是高并发系统中，对于服务提供方的一种保护手段。通过限流功能，我们可以通过控制QPS的方式，以避免被瞬时的流量高峰冲垮，从而保障系统的高可用性。</p>
<h1 id="考虑的问题"><a href="#考虑的问题" class="headerlink" title="考虑的问题"></a>考虑的问题</h1><p>完成一个限流系统， 我们可以结合场景的需要做下面的考虑</p>
<ol>
<li>多规则匹配：是否会存在有多重规则的限流？比如有的规则限制每天1000次，有的规则限制每分钟1次？是同时生效还是优先生效某个？</li>
<li>资源类型：能限流什么？QPS，连接数，并发数</li>
<li>全局限流/单机限流：多个服务的实例共享一个全局的流量限额，比如所有机器共享1000QPS。或者单个实例的限流，比如被调限定每台机器不超过1000QPS</li>
<li>限流阈值：单位时间内的最大配额数。是按照每秒种一次，还是按照每分钟60次？</li>
<li>限流处理：客户端如何处理超出限额的请求？超额后直接拒绝，还是超额后进行排队？</li>
</ol>
<p>抽象出一个方案<br>接口级别限流：每个接口分配一个appid和key，各自计算各自的配额<br>多维度限流：支持每秒N次、每分钟N次、每天N次等维度<br>匀速防刷：假设配置了每分钟60次，依然可能出现第一秒访问了60次用光了配额。匀速防刷可以匀速消耗配额，解决这个问题<br>多级限流：支持不同的限流规则，并有采用的优先级，采用优先级最高的方案进行限流</p>
<h1 id="限流算法"><a href="#限流算法" class="headerlink" title="限流算法"></a>限流算法</h1><h2 id="固定窗口"><a href="#固定窗口" class="headerlink" title="固定窗口"></a>固定窗口</h2><p>固定窗口是在一段时间内可以限制访问次数的方法。</p>
<ul>
<li>将时间划分为多个窗口</li>
<li>在每个窗口内每有一次请求就将计数器加一</li>
<li>如果计数器超过了限制数量,则本窗口内所有的请求都被丢弃。当时间到达下一个窗口时,计数器重置</li>
</ul>
<p>这样有一定的限流效果，但是限制住的流量可能是有毛刺的。比如1000次/分钟，可能<code>00:59</code>的时候有1000流量，<code>01:00</code>的时候也有1000流量，这样这两秒内就有2000流量！<br>具体实现：用一个变量C标记访问次数，一个事件定时过期，并在过期时把变量C清零：<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> FixedWindowCounter <span class="keyword">struct</span> &#123;</span><br><span class="line">	TimeSlice  time.Duration</span><br><span class="line">	NowCount   <span class="keyword">int32</span></span><br><span class="line">	AllowCount <span class="keyword">int32</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(p *FixedWindowCounter)</span> <span class="title">Take</span><span class="params">()</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">	once.Do(<span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">		<span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">			<span class="keyword">for</span> &#123;</span><br><span class="line">				<span class="keyword">select</span> &#123;</span><br><span class="line">				<span class="keyword">case</span> &lt;-time.After(p.TimeSlice):</span><br><span class="line">					atomic.StoreInt32(&amp;p.NowCount, <span class="number">0</span>)</span><br><span class="line">				&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;()</span><br><span class="line">	&#125;)</span><br><span class="line"></span><br><span class="line">	nowCount := atomic.LoadInt32(&amp;p.NowCount)</span><br><span class="line">	<span class="keyword">if</span> nowCount &gt;= p.AllowCount &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> !atomic.CompareAndSwapInt32(&amp;p.NowCount, nowCount, nowCount+<span class="number">1</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="滑动窗口"><a href="#滑动窗口" class="headerlink" title="滑动窗口"></a>滑动窗口</h2><p>滑动窗口把一个固定窗口划分为多个小区间，以固定的窗口大小在区间上滑动，在一定程度上缓解了固定窗口的毛刺问题（我们假设某一区间qps非常高）。当然性能损耗也比较大。</p>
<ul>
<li>将时间划分为多个区间</li>
<li>在每个区间内每有一次请求就将当前区间的计数器加一</li>
<li>每经过一个区间的时间,则抛弃窗口中最老的一个区间,并纳入最新的一个区间；</li>
<li>如果当前窗口内区间的请求计数总和超过了限制数量,则本窗口内所有的请求都被丢弃</li>
</ul>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> SlideWindowCounter <span class="keyword">struct</span> &#123;</span><br><span class="line">	lastWindowCount <span class="keyword">int32</span>         <span class="comment">// 当前区间请求计数</span></span><br><span class="line">	window          <span class="keyword">chan</span> <span class="keyword">int32</span>    <span class="comment">// 滑动窗口</span></span><br><span class="line">	windowSlice     time.Duration <span class="comment">// 单区间大小</span></span><br><span class="line">	nowCount        <span class="keyword">int32</span>         <span class="comment">// 滑动窗口请求计数</span></span><br><span class="line">	AllowCount      <span class="keyword">int32</span>         <span class="comment">// 滑动窗口内允许的请求数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewSlideWindowCounter</span><span class="params">(timeSlice time.Duration,</span></span></span><br><span class="line"><span class="function"><span class="params">	windowLen <span class="keyword">int32</span>, AllowCount <span class="keyword">int32</span>)</span> *<span class="title">SlideWindowCounter</span></span> &#123;</span><br><span class="line">	obj := &amp;SlideWindowCounter&#123;</span><br><span class="line">		window:      <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">int32</span>, windowLen),</span><br><span class="line">		windowSlice: timeSlice / time.Duration(windowLen),</span><br><span class="line">		AllowCount:  AllowCount,</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">go</span> obj.sliding()</span><br><span class="line">	<span class="keyword">return</span> obj</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(l *SlideWindowCounter)</span> <span class="title">Take</span><span class="params">()</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">	nowCount := atomic.LoadInt32(&amp;l.nowCount)</span><br><span class="line">	<span class="keyword">if</span> nowCount &gt;= l.AllowCount &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> !atomic.CompareAndSwapInt32(&amp;l.nowCount, nowCount, nowCount+<span class="number">1</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 当前区间的请求数</span></span><br><span class="line">	atomic.AddInt32(&amp;l.lastWindowCount, <span class="number">1</span>)</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(l *SlideWindowCounter)</span> <span class="title">sliding</span><span class="params">()</span></span> &#123;</span><br><span class="line">	<span class="comment">// 窗口没满的时候</span></span><br><span class="line">	notFull := <span class="literal">true</span></span><br><span class="line">	<span class="keyword">for</span> notFull &#123;</span><br><span class="line">		<span class="keyword">select</span> &#123;</span><br><span class="line">		<span class="keyword">case</span> &lt;-time.After(l.windowSlice):</span><br><span class="line">			<span class="comment">// 经过了一个区间的时间，把这个区间的请求数放入窗口，并开始新的区间</span></span><br><span class="line">			<span class="comment">// 等于 l.window &lt;- l.lastWindowCount; l.lastWindowCount = 0</span></span><br><span class="line">			t := atomic.SwapInt32(&amp;l.lastWindowCount, <span class="number">0</span>)</span><br><span class="line">			l.window &lt;- t</span><br><span class="line">			<span class="keyword">if</span> <span class="built_in">len</span>(l.window) == <span class="built_in">cap</span>(l.window) &#123;</span><br><span class="line">				notFull = <span class="literal">false</span></span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 窗口满了，开始滑动，每经过一个区间的时间,则抛弃最老的一个区间,并纳入最新的一个区间</span></span><br><span class="line">	<span class="keyword">for</span> &#123;</span><br><span class="line">		<span class="keyword">select</span> &#123;</span><br><span class="line">		<span class="keyword">case</span> &lt;-time.After(l.windowSlice):</span><br><span class="line">			t := &lt;-l.window</span><br><span class="line">			<span class="keyword">if</span> t != <span class="number">0</span> &#123;</span><br><span class="line">				atomic.AddInt32(&amp;l.nowCount, -t)</span><br><span class="line">			&#125;</span><br><span class="line">			newt := atomic.SwapInt32(&amp;l.lastWindowCount, <span class="number">0</span>)</span><br><span class="line">			l.window &lt;- newt</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="漏桶"><a href="#漏桶" class="headerlink" title="漏桶"></a>漏桶</h2><p>漏桶的思想其实大家都非常熟悉，就是一个用来缓冲的队列。不过，出队的速度是均匀的，比如每秒只出队一百个，就会有一百个请求发出去。入队的速度没有限制，但是如果队列满了，请求就会被抛弃。<br>漏洞算法其实是一种流量整形算法，</p>
<ol>
<li>优点：简单、高效，能恰当拦截容量外的暴力流量。</li>
<li>缺点：无法对流量做频率处理。比如:桶设置的过大（比如每秒一百个），桶容量又不可以设置的过小，否则容易卡死正常用户</li>
</ol>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> LeakyBucket <span class="keyword">struct</span> &#123;</span><br><span class="line">	TimeSlice  time.Duration</span><br><span class="line">	AllowCount <span class="keyword">int32</span>       <span class="comment">// 上面的时间段内匀速漏出多少请求</span></span><br><span class="line">	Bucket     <span class="keyword">chan</span> <span class="function"><span class="keyword">func</span><span class="params">()</span> // 桶</span></span><br><span class="line"><span class="function">&#125;</span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="title">func</span> <span class="title">NewLeakyBucket</span><span class="params">(timeSlice time.Duration, bucketSize <span class="keyword">int</span>, allowCount <span class="keyword">int32</span>)</span> *<span class="title">LeakyBucket</span></span> &#123;</span><br><span class="line">	obj := &amp;LeakyBucket&#123;</span><br><span class="line">		TimeSlice:  timeSlice,</span><br><span class="line">		Bucket:     <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="function"><span class="keyword">func</span><span class="params">()</span>, <span class="title">bucketSize</span>),</span></span><br><span class="line"><span class="function">		<span class="title">AllowCount</span>: <span class="title">allowCount</span>,</span></span><br><span class="line"><span class="function">	&#125;</span></span><br><span class="line"><span class="function">	<span class="title">go</span> <span class="title">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">		<span class="comment">// 定时漏出去一个请求</span></span><br><span class="line">		<span class="keyword">for</span> &#123;</span><br><span class="line">			<span class="keyword">select</span> &#123;</span><br><span class="line">			<span class="keyword">case</span> &lt;-time.After(time.Duration(obj.TimeSlice.Nanoseconds() / <span class="keyword">int64</span>(obj.AllowCount))):</span><br><span class="line">				task := &lt;-obj.Bucket</span><br><span class="line">				task()</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;()</span><br><span class="line">	<span class="keyword">return</span> obj</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(t *LeakyBucket)</span> <span class="title">Take</span><span class="params">(task <span class="keyword">func</span>()</span>) <span class="title">bool</span></span> &#123;</span><br><span class="line">	<span class="keyword">select</span> &#123;</span><br><span class="line">	<span class="keyword">case</span> t.Bucket &lt;- task:</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">	<span class="keyword">default</span>:</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="令牌桶"><a href="#令牌桶" class="headerlink" title="令牌桶"></a>令牌桶</h2><p>令牌桶的思想是在一个桶中按一定速度加入资格，接受流量的时候消耗资格，没有资格则拒绝请求</p>
<ol>
<li>每秒会有 r 个令牌放入桶中，或者说，每过 1/r 秒桶中增加一个令牌；</li>
<li>桶中最多存放 b 个令牌，如果桶满了，新放入的令牌会被丢弃；</li>
<li>当一个 n 字节的数据包到达时，消耗 n 个令牌，然后发送该数据包；</li>
<li>如果桶中可用令牌小于 n，则该数据包将被缓存或丢弃。<br>优缺点:</li>
<li>令牌桶的一个好处是可以方便的改变速度。 一旦需要提高速率，则按需提高放入桶中的令牌的速率。</li>
<li>可以限制总请求大小，还限制平均频率大小；能允许某种程度的突发传输</li>
<li>还是容易导致误判等问题</li>
</ol>
<figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">type</span> TokenBucket <span class="keyword">struct</span> &#123;</span><br><span class="line">	TimeSlice  time.Duration</span><br><span class="line">	AllowCount <span class="keyword">int32</span>         <span class="comment">// 上面的时间段内生成多少个令牌</span></span><br><span class="line">	Bucket     <span class="keyword">chan</span> <span class="keyword">struct</span>&#123;&#125; <span class="comment">// 桶</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewTokenBucket</span><span class="params">(timeSlice time.Duration, bucketSize <span class="keyword">int</span>, allowCount <span class="keyword">int32</span>)</span> *<span class="title">TokenBucket</span></span> &#123;</span><br><span class="line">	obj := &amp;TokenBucket&#123;</span><br><span class="line">		TimeSlice:  timeSlice,</span><br><span class="line">		Bucket:     <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">struct</span>&#123;&#125;, bucketSize),</span><br><span class="line">		AllowCount: allowCount,</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">go</span> <span class="function"><span class="keyword">func</span><span class="params">()</span></span> &#123;</span><br><span class="line">		<span class="comment">// 定时漏出去一个令牌</span></span><br><span class="line">		<span class="keyword">for</span> &#123;</span><br><span class="line">			<span class="keyword">select</span> &#123;</span><br><span class="line">			<span class="keyword">case</span> &lt;-time.After(time.Duration(obj.TimeSlice.Nanoseconds() / <span class="keyword">int64</span>(obj.AllowCount))):</span><br><span class="line">				obj.Bucket &lt;- <span class="keyword">struct</span>&#123;&#125;&#123;&#125;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;()</span><br><span class="line">	<span class="keyword">return</span> obj</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(t *TokenBucket)</span> <span class="title">Take</span><span class="params">()</span> <span class="title">bool</span></span> &#123;</span><br><span class="line">	<span class="keyword">select</span> &#123;</span><br><span class="line">	<span class="keyword">case</span> &lt;-t.Bucket:</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">	<span class="keyword">default</span>:</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="预热桶"><a href="#预热桶" class="headerlink" title="预热桶"></a>预热桶</h2><p>在机器刚刚启动时，可能缓存尚未建立，或者正在初始化，直接打入大量请求可能会导致系统崩溃。所以我们需要一个预热的过程，在请求较少时，缓慢放开请求的量级；在请求正常后恢复正常量级。这里就要用到预热桶。<br>预热桶其实就是令牌桶的升级版，主要区别在于：我们假设系统的阈值QPS为count，在令牌桶中获取单个令牌的时间是固定的1/count 。而从预热桶中获取单个令牌的时间是随着存量令牌的数量而变化的。<br>我们假设系统刚启动或者长时间没有收到请求处于冷却状态，这个时候令牌达到令牌数上限（饱和状态）。此时从预热桶中获取令牌的时间要比稳定状态要长。随着令牌的减少，获取单个令牌的时间会慢慢变短，最终到达一个稳定值。<br>所以我们可以设一个函数，获取单个令牌的时间 = k * 1 / QPS<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/lb3_1.jpg" alt="预热算法"><br>假设我们用一条垂直于X轴的竖线表示当前的请求状态，竖线从右向左移动时，表示系统接收到请求（预热中），令牌正在被消耗，假设系统连续接收到 k 个请求，获取对应令牌所需要的时间为[令牌数上限-k, 令牌数上限]的定积分，就是包围的面积。<br>反过来，竖线从左向右移动时，表示系统正在冷却。</p>
<h2 id="redis实现的简单限流模型"><a href="#redis实现的简单限流模型" class="headerlink" title="redis实现的简单限流模型"></a>redis实现的简单限流模型</h2><p><a href="http://doc.redisfans.com/string/incr.html" target="_blank" rel="noopener">http://doc.redisfans.com/string/incr.html</a><br>多机限流的方案中，使用redis的自增incr和过期expire是一种最简单的方案。假设我们要在1秒内限制100次，那么只需要保留一个一秒过期的key，每次incr并判断incr之后的值是否会超过100。<br>要注意：</p>
<ol>
<li>incr这个操作本身是原子的，并且每次递增后会返回incr的值</li>
<li>incr和expire这两个操作必须当一个事务，否则expire失败了，或者incr和expire之间插入的别的incr都会有问题。我们这里用一个lua脚本在redis中EVAL解决这个问题</li>
</ol>
<figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">local</span> current</span><br><span class="line">current = redis.call(<span class="string">"incr"</span>,KEYS[<span class="number">1</span>])</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">tonumber</span>(current) == <span class="number">1</span> <span class="keyword">then</span></span><br><span class="line">    redis.call(<span class="string">"expire"</span>,KEYS[<span class="number">1</span>],<span class="number">1</span>)</span><br><span class="line"><span class="keyword">end</span></span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      负载均衡是集群中一个重要的组成部分。这个模块一般集成了名字服务、负载均衡、过载保护、限流等功能。第二部分是针对限流算法和实现的介绍。
    
    </summary>
    
      <category term="后台" scheme="chestnutheng.cn/categories/%E5%90%8E%E5%8F%B0/"/>
    
    
      <category term="负载均衡" scheme="chestnutheng.cn/tags/%E8%B4%9F%E8%BD%BD%E5%9D%87%E8%A1%A1/"/>
    
      <category term="后台" scheme="chestnutheng.cn/tags/%E5%90%8E%E5%8F%B0/"/>
    
  </entry>
  
  <entry>
    <title>[后台]负载均衡 （二）能力篇</title>
    <link href="chestnutheng.cn/2020/05/21/load-balance2/"/>
    <id>chestnutheng.cn/2020/05/21/load-balance2/</id>
    <published>2020-05-21T09:18:55.000Z</published>
    <updated>2020-07-16T12:10:14.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="名字服务"><a href="#名字服务" class="headerlink" title="名字服务"></a>名字服务</h1><h2 id="基础设计"><a href="#基础设计" class="headerlink" title="基础设计"></a>基础设计</h2><p><a href="http://jm.taobao.org/2018/06/26/%E8%81%8A%E8%81%8A%E5%BE%AE%E6%9C%8D%E5%8A%A1%E7%9A%84%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0/" target="_blank" rel="noopener">名字服务考虑的基本设计</a></p>
<p>客户端发现：<br>服务提供者的实例在启动时或者位置信息发生变化时会向服务注册表注册自身，在停止时会向服务注册表注销自身，如果服务提供者的实例发生故障，在一段时间内不发送心跳之后，也会被服务注册表注销。<br>服务消费者的实例会向服务注册表查询服务提供者的位置信息，然后通过这些位置信息直接向服务提供者发起请求。</p>
<p>服务端发现：<br>第一步与客户端发现相同。<br>服务消费者不直接向服务注册表查询，也不直接向服务提供者发起请求，而是将对服务提供者的请求发往一个中央路由器或者负载均衡器，中央路由器或者负载均衡器查询服务注册表获取服务提供者的位置信息，并将请求转发给服务提供者。</p>
<p>这两种模式各有利弊，客户端发现模式的优势是，服务消费者向服务提供者发起请求时比服务端发现模式少了一次网络跳转，劣势是服务消费者需要内置特定的服务发现客户端和服务发现逻辑；<br>服务端发现模式的优势是服务消费者无需内置特定的服务发现客户端和服务发现逻辑，劣势是多了一次网络跳转，并且需要基础设施环境提供中央路由机制或者负载均衡机制。目前客户端发现模式应用的多一些，因为这种模式的对基础设施环境没有特殊的要求，和基础设施环境也没有过多的耦合性。<br>主调调用被调时，根据被调的名字从服务注册中心获取服务实例列表，包括节点ip、端口、权重、地理位置等；一般采取分钟级别的定时任务去拉取，本地做缓存，异步更新。</p>
<p>实现方式</p>
<ol>
<li>DNS，传播速度太慢，没法发现端口。SkyDNS解决了这个问题，在k8s里大量使用</li>
<li>zookeeper或者etcd，如SmartStack，能保证强一致，但是要做很多开发</li>
<li>Eureka。Netflix的java生态里的优秀方案</li>
<li>Consul，提供服务配置、服务的内存和磁盘监测等</li>
</ol>
<h2 id="服务注册信息"><a href="#服务注册信息" class="headerlink" title="服务注册信息"></a>服务注册信息</h2><h3 id="IP和端口"><a href="#IP和端口" class="headerlink" title="IP和端口"></a>IP和端口</h3><p>一个服务端要接入名字服务，必须要先提供自己的IP和端口信息。<br>IP的获取方法：</p>
<ol>
<li>通过遍历网卡的方式去获取，找到第一个不为本地环回地址的 IP 地址。dubbo就是这种方法</li>
<li>指定网卡名interfaceName，来获取IP</li>
<li>直接与服务注册中心建立 socket 连接，然后通过socket.getLocalAddress() 这种方式来获取本机的 IP<br>端口的获取方法：<br>一般的RPC服务或者Web服务监听的端口都在配置中写好，可以直接获取上报。</li>
</ol>
<h3 id="扩展设计"><a href="#扩展设计" class="headerlink" title="扩展设计"></a>扩展设计</h3><p>除了IP和端口，可能还有一些常用的服务信息需要注册上来，提供更高级的功能：<br>1.支持TLS：想知道某个 HTTP 服务是否开启了 TLS。<br>2.权重：对相同服务下的不同节点设置不同的权重，进行流量调度。<br>3.环境分配：将服务分成预发环境和生产环境，方便进行AB Test功能。<br>4.机房：不同机房的服务注册时加上机房的标签，以实现同机房优先的路由规则。</p>
<h2 id="无损注册-下线"><a href="#无损注册-下线" class="headerlink" title="无损注册/下线"></a>无损注册/下线</h2><p>虽然服务注册一般发生在服务的启动阶段，但是细分的话，服务注册应该在服务已经完全启动成功，并准备对外提供服务之后才能进行注册。<br>1.有些 RPC 框架自身提供了方法来判断服务是否已经启动完成，如 Thrift ，我们可以通过 Server.isServing() 来判断。<br>2.有一些 RPC 框架本身没有提供服务是否启动完成的方式，这时我们可以通过检测端口是否已经处于监听状态来判断。<br>3.而对于 HTTP 服务，服务是否启动完毕也可以通过端口是否处于监听状态来判断。<br>下线也是一样的，可以注册服务下线的回调，或者监听服务下线的信号，或者做健康检查</p>
<h2 id="健康检查"><a href="#健康检查" class="headerlink" title="健康检查"></a>健康检查</h2><p>客户端主动心跳上报健康：</p>
<ol>
<li>客户端每隔一定时间主动发送“心跳”的方式来向服务端表明自己的服务状态正常，心跳可以是 TCP 的形式，也可以是 HTTP 的形式。</li>
<li>也可以通过维持客户端和服务端的一个 socket 长连接自己实现一个客户端心跳的方式。<br>客户端的健康检查只能表明网络可达，不能代表服务可用。服务端的健康检查可以准确获得服务的健康状态：</li>
<li>服务端调用服务发布者某个 HTTP 接口来完成健康检查。</li>
<li>对于没有提供 HTTP 服务的 RPC 应用，服务端调用服务发布者的接口来完成健康检查。</li>
<li>可以通过执行某个脚本的形式来进行综合检查，覆盖多个场景。<br>服务端检查也有问题，一个是调用服务的方式不通用，需要额外实现，还有就是服务注册中心可能和服务的网络不互通。</li>
</ol>
<h2 id="节点变化通知"><a href="#节点变化通知" class="headerlink" title="节点变化通知"></a>节点变化通知</h2><p>当服务有节点退出或新的节点加入时，订阅者如何及时收到通知？经典的push和pull的问题</p>
<ol>
<li>Push 的经典实现有两种，基于 socket 长连接的 notify，典型的实现如 zookeeper；另一种为 HTTP 连接所使用 Long Polling。<br>但是基于 socket 长连接的 notify 和基于 HTTP 协议的 Long Polling 都会存在notify消息丢失的问题。</li>
<li>Pull 的定时轮询也需要支持，要选好查询的间隔时间，在服务性能和业务规模之间权衡</li>
<li>还有一种真push，客户端开启一个 UDP server，服务注册中心通过 UDP 的方式进行数据推送，当然这个也受限于网络的连通性</li>
</ol>
<h2 id="容灾"><a href="#容灾" class="headerlink" title="容灾"></a>容灾</h2><p>客户端容灾：<br>首先，本地内存缓存，当运行时与服务注册中心的连接丢失或服务注册中心完全宕机，仍能正常地调用服务。<br>然后，本地缓存文件，当应用与服务注册中心发生网络分区或服务注册中心完全宕机后，应用进行了重启操作，内存里没有数据，此时应用可以通过读取本地缓存文件的数据来获取到最后一次订阅到的内容。<br>最后，本地容灾文件夹。正常的情况下，容灾文件夹内是没有内容的。当服务端完全宕机且长时间不能恢复，同时服务提供者又发生了很大的变更时，运维可以通过在容灾文件夹内手动添加文件的方式来开启本地容灾。此时客户端会忽略原有的本地缓存文件，只从本地容灾文件中读取配置。<br>服务端容灾：<br>由于地址服务是无状态的，服务端容灾可以做的很薄，主要有两点：新的服务端加入，地址服务器会更新并和其他地址服务器保持同步；服务端删除，地址服务器能保证快速删除。</p>
<h1 id="熔断"><a href="#熔断" class="headerlink" title="熔断"></a>熔断</h1><h2 id="熔断器模式"><a href="#熔断器模式" class="headerlink" title="熔断器模式"></a>熔断器模式</h2><p><a href="https://martinfowler.com/bliki/CircuitBreaker.html" target="_blank" rel="noopener">熔断器模式</a>是一种用于故障恢复的设计模式，也常用在负载均衡中。它可以防止一个应用不断地去尝试一个很可能失败的操作，避免服务持续过载。他由几个状态组成：</p>
<ol>
<li>关闭(Closed)：默认情况下Circuit Breaker是关闭的，此时允许操作执行。Circuit Breaker内部记录着最近失败的次数，如果对应的操作执行失败，次数就会续一次。如果在某个时间段内，失败次数（或者失败比率）达到阈值，Circuit Breaker会转换到开启(Open)状态。</li>
<li>开启(Open)：在此状态下，执行对应的操作将会立即失败并且立即抛出异常。在开启状态中，Circuit Breaker会启用一个超时计时器，设这个计时器的目的是给集群相应的时间来恢复故障。当计时器时间到的时候，Circuit Breaker会转换到半开启(Half-Open)状态。</li>
<li>半开启(Half-Open)：在此状态下，Circuit Breaker 会允许执行一定数量的操作。如果所有操作全部成功，Circuit Breaker就会假定故障已经恢复，它就会转换到关闭状态，并且重置失败次数。如果其中 任意一次 操作失败了，Circuit Breaker就会认为故障仍然存在，所以它会转换到开启状态并再次开启计时器（再给系统一些时间使其从失败中恢复）。</li>
</ol>
<p>我们可以借鉴此设计模式设计一种负载均衡的熔断策略：<br>请求失败比例过高熔断：当服务端在上一个时间窗（比如10秒）内，成功的请求量小于最低请求量要求requestVolumeThreshold（默认100个），且成功率小于最低成功率errorThresholdPercentage（比如50%），服务端就会进入隔离状态，熔断器开启。<br>请求连续失败熔断：当服务端在上一个时间窗（比如10秒）内，连续失败的请求超过连续失败上限bound2（默认10个），服务器进入隔离状态，熔断器开启<br>熔断隔离时间sleepWindowInMilliseconds：默认隔离30s，支持可配置。</p>
<h2 id="熔断器发现-时间窗上报"><a href="#熔断器发现-时间窗上报" class="headerlink" title="熔断器发现-时间窗上报"></a>熔断器发现-时间窗上报</h2><p>要想再问题发生的时候熔断，首先要发现问题。我们看看Hystrix的熔断器模型怎么收集上报：<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/lb2_1.png" alt="Hystrix的时间窗"><br>Hystrix滑动窗口策略，以秒为单位来统计请求处理情况，上面每个格子代表1秒，格子中的数据就是1秒内各请求的处理结果，称为一个桶。<br>我们假设每次决策都以最近的十个桶来决定是否熔断，比如失败率超过50%就熔断。这10秒就是一个滑动窗口，每经过一秒，最老的一个桶就会被丢弃，同时增加一个新的桶。<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/lb2_2.png" alt="Hystrix的结构"></p>
<h2 id="熔断器恢复-探针"><a href="#熔断器恢复-探针" class="headerlink" title="熔断器恢复-探针"></a>熔断器恢复-探针</h2><p>熔断器在半开状态时，如果用请求来检测服务可用，那么还是会有一些请求会丢失的风险。一种比较好的办法是使用探针来检测服务的可用性。探针一般有TCP、UDP、HTTP几种，还要支持用户自定义探针。<br>主调需要在配置中指定要用的探针类型和探针的使用顺序，在需要探测时会以这个顺序调用探测插件进行探测；只要有一个探测插件成功，就认为服务已经恢复，停止探测。</p>
<p>比如一个udp探针：</p>
<ul>
<li>探测插件名：udp</li>
<li>探测请求包：0x0001</li>
<li>期望回包：0x000F</li>
<li>超时时间：100ms</li>
<li>探测次数：5</li>
</ul>
<h1 id="过载保护"><a href="#过载保护" class="headerlink" title="过载保护"></a>过载保护</h1><p><a href="https://mp.weixin.qq.com/s/uv4WkTIPvDCFlvKAEXrT2g" target="_blank" rel="noopener">微信的过载保护</a><br>操作系统里有CPU、内存、网卡、磁盘等各种资源，当程序处理海量请求时很容易使得某一种资源到达处理性能瓶颈，从而使得服务的处理能力迅速下降。一般的后台服务可能会有几个下面的瓶颈：</p>
<ol>
<li>CPU，计算密集型服务</li>
<li>内存，内存消耗型服务</li>
<li>CPU LOAD，多线程型服务（进程调度频繁导致CPU空跑）</li>
<li>IO，包括磁盘IO（比如随机IO导致cache性能下降，机械臂移动频繁，swap频繁），网络IO、网络连接数多的服务（网络拥塞）<br>这些瓶颈中的某一个因素打破平衡后，会传播和放大（比如用户不断失败重试），形成滚雪球效应，导致整个系统崩溃。此时就需要我们的负载均衡理论来保障系统的可用性。</li>
</ol>
<h2 id="四大方法"><a href="#四大方法" class="headerlink" title="四大方法"></a>四大方法</h2><h3 id="轻重分离"><a href="#轻重分离" class="headerlink" title="轻重分离"></a>轻重分离</h3><p>轻重分离是一种类似高内聚，低耦合的方法论，用来限制服务崩溃的影响扩大。<br>按服务的重要性对服务分别隔离部署，避免一些不重要的服务影响要重要的服务。服务的隔离最好能做到物理隔离，包括服务器、带宽、IDC级别的隔离。</p>
<ol>
<li>按重要性分离：比如微博热点出现时大家都去访问某明星的主页，我们尽量只让个人主页服务挂掉，不要影响其他的资讯、推送服务</li>
<li>按部署分离：电信、联通、教育网、海外用户之间的分离</li>
<li>按快慢分离：下载资源服务和资讯页面服务的分离</li>
<li>按set分离用户：按游戏区服、用户uid的哈希值分离，从逻辑分离到物理隔离</li>
</ol>
<h3 id="及早拒绝"><a href="#及早拒绝" class="headerlink" title="及早拒绝"></a>及早拒绝</h3><p>问题解决的阶段越早，成本越低，影响越小的一种思想。<br>原则：前端保护后端，后端不信任前端。</p>
<ol>
<li>前后端要交流：后端把自己的负载情况也报告给前端</li>
<li>前端收到后端的负载情况后，要用正确的策略调度后端，后端负载满时不再发起请求</li>
<li>后端接到前端的请求后，如果自己负载很高，要拒绝发来的新请求<br>这里面有两个关键点：</li>
</ol>
<ul>
<li>高负载识别 负载能力需要一些具体的指标来识别，比如包量、并发连接数</li>
<li>拒绝方法 拒绝的时候要有降级策略，用有损服务和柔性可用来保障体验</li>
</ul>
<h3 id="量力而为"><a href="#量力而为" class="headerlink" title="量力而为"></a>量力而为</h3><p>每个服务要先做好自我保护，再考虑对关联系统的保护。<br>对自己的保护需要做好过载监控要做好告警机制，比如在系统负载达到70%的时候发出预警，在后台负载90%的时候启动过载保护</p>
<h3 id="动态调节"><a href="#动态调节" class="headerlink" title="动态调节"></a>动态调节</h3><p>结合上面的三个方法，持续监控服务过载状态，形成一个良性的正反馈循环：<br>业务正常状态-&gt; 过载保护状态 -&gt; 业务灰度恢复 -&gt; 业务正常状态</p>
<h2 id="LB和过载保护"><a href="#LB和过载保护" class="headerlink" title="LB和过载保护"></a>LB和过载保护</h2><p>LB和容灾解决的是大容量业务的平行扩展及可用性问题，在做好容量管理的前提下具备一定的应对突发流量的能力（因为具备一定的资源冗余），但是LB只做到流量的均衡分布处理，却没有实现流量超出系统总体容量时的保护、控制。<br>WRR仅仅实现了根据运营配置的静态负载均衡策略，当集群中某个节点过载时，不能及时调整负载均衡策略以保护该节点，使得过载节点雪上加霜最终假死，更可怕的是节点假死（或者某节点故障）后，LB会马上摘除该节点，并把该节点的负载分担到其它正常节点上，从而可能造成正常节点的过载，如此循环往复，过载不断扩散，最终使得系统整体雪崩。<br>我们可以优化一下LB的机制，把过载时多出来的请求直接抛弃掉。但是该机制还未能很好的解决过载的源头问题，即用户失去耐心后的无效请求倍增的滚雪球效应，同时也未能给用户较好的有损体验。<br>还有一种方法是一损俱损，当整个后台集群不可用时，LB把所有的机器都返回，这样在极端情况下也能保障系统的部分可用性。</p>
<h2 id="请求队列和过载保护"><a href="#请求队列和过载保护" class="headerlink" title="请求队列和过载保护"></a>请求队列和过载保护</h2><p>实现请求队列的目的是通过非阻塞方式实现异步系统，优化系统架构，从而提升系统性能。<br>异步系统的问题是在分层系统中，各层次所维护的请求/响应队列与原始请求发起方失去了直接的联系，这就造成队列数据有效性（通常是采用轮询超时的方式处理）无法保障。<br>当过载发生时，用户不断的刷新请求，这也就意味着此时系统请求队列中大部分访问请求已经无效（用户用新的刷新访问代替了上一次的访问请求），但此时队列中的“无效访问请求“尚未超出设定的超时阀值，后端系统（往往是系统的瓶颈所在）还在按FIFO的原则继续处理，这样的结果是：<br>1) 系统的宝贵资源都用来处理无效的访问请求，极大的浪费了资源；<br>2) 用户得不到及时的反馈，不断的刷新访问请求，导致滚雪球效应；<br>3) 请求的涌入使得瓶颈资源无法处理，LB作出调度使得过载扩散，导致多米诺骨牌效应；<br>从以上分析我们可知，请求队列机制无法及时剔除无效数据，从而控制雪球的增长，也无法控制过载的扩散，故 有了请求队列，仍然需要丰富的过载保护机制</p>
]]></content>
    
    <summary type="html">
    
      负载均衡是集群中一个重要的组成部分。这个模块一般集成了名字服务、负载均衡、过载保护、限流等功能。第二部分是针对名字服务、负载均衡、过载保护手段的介绍。
    
    </summary>
    
      <category term="后台" scheme="chestnutheng.cn/categories/%E5%90%8E%E5%8F%B0/"/>
    
    
      <category term="负载均衡" scheme="chestnutheng.cn/tags/%E8%B4%9F%E8%BD%BD%E5%9D%87%E8%A1%A1/"/>
    
      <category term="后台" scheme="chestnutheng.cn/tags/%E5%90%8E%E5%8F%B0/"/>
    
  </entry>
  
  <entry>
    <title>[后台]负载均衡 （一）算法篇</title>
    <link href="chestnutheng.cn/2020/05/06/load-balance/"/>
    <id>chestnutheng.cn/2020/05/06/load-balance/</id>
    <published>2020-05-06T12:24:30.000Z</published>
    <updated>2020-05-21T09:13:47.000Z</updated>
    
    <content type="html"><![CDATA[<p>当单机的访问压力很大时，就需要引入集群。集群一个很重要的事情就是把请求均匀地分配在各个机器上，这就是负载均衡的雏形。<br>有基于MAC地址的二层负载均衡和基于IP地址的三层负载均衡。 二层负载均衡会通过一个虚拟MAC地址接收请求，然后再分配到真实的MAC地址；三层负载均衡会通过一个虚拟IP地址接收请求，然后再分配到真实的IP地址；<br>四层通过虚拟IP+端口接收请求，然后再分配到真实的服务器（比如LVS，F5）；七层通过虚拟的URL或主机名接收请求，然后再分配到真实的服务器（Haproxy和Nginx）。<br>四层和七层是最常见的负载均衡模型。<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/load_ban1.jpg" alt="七层和四层模型"><br><strong>四层：</strong>以常见的TCP为例，负载均衡设备在接收到第一个来自客户端的SYN请求时，通过负载均衡算法选择服务器，并对报文中目标IP地址进行修改（改为后端服务器IP），直接转发给该服务器。TCP的连接建立，即三次握手是客户端和服务器直接建立的，负载均衡设备只是起到一个类似路由器的转发动作。在某些部署情况下，为保证服务器回包可以正确返回给负载均衡设备，在转发报文的同时可能还会对报文原来的源地址进行修改。<br><strong>七层：</strong>以常见的TCP为例，负载均衡设备如果要根据真正的应用层内容再选择服务器，只能先代理最终的服务器和客户端建立连接(三次握手)后，才可能接受到客户端发送的真正应用层内容的报文，然后再根据该报文中的特定字段，再加上设置的负载均衡算法，选择内部某台服务器。负载均衡设备在这种情况下，更类似于一个代理服务器。负载均衡和前端的客户端以及后端的服务器会分别建立TCP连接。所以从这个技术原理上来看，七层负载均衡明显的对负载均衡设备的要求更高，处理七层的能力也必然会低于四层模式的部署方式。<br><a href="https://kb.cnblogs.com/page/188170/" target="_blank" rel="noopener">参考资料：四层和七层负载均衡的区别</a></p>
<h1 id="nginx用的负载均衡算法"><a href="#nginx用的负载均衡算法" class="headerlink" title="nginx用的负载均衡算法"></a>nginx用的负载均衡算法</h1><p>Nginx可以作为HTTP反向代理，把访问本机的HTTP请求，均分到后端集群的若干台服务器上。负载均衡的核心就是负载均衡所使用的平衡算法，适用于各种场景。<br><a href="https://www.kancloud.cn/digest/sknginx/130029" target="_blank" rel="noopener">Nginx的负载均衡算法</a><br>Nginx目前提供的负载均衡模块：<br>ngx_http_upstream_round_robin，加权轮询，可均分请求，是默认的HTTP负载均衡算法，集成在框架中。<br>ngx_http_upstream_ip_hash_module，IP哈希，可保持会话。<br>ngx_http_upstream_least_conn_module，最少连接数，可均分连接。适用于链接数体现资源的服务，比如FTP。<br>ngx_http_upstream_hash_module，一致性哈希，可减少缓存数据的失效。</p>
<h2 id="随机访问"><a href="#随机访问" class="headerlink" title="随机访问"></a>随机访问</h2><p>在介绍nginx的模式前，先介绍下普通的负载均衡方法。假设有7个请求，我们给A、B、C三个节点分别4、2、1的权重。最朴素的负载均衡方式有下面几种：</p>
<ol>
<li>完全轮询：访问完A去访问B，访问完B去访问C，再去访问A。缺点是没有权重，不能根据负载调节。</li>
<li>列表轮询：构造一个数组[A, A, A, A, B, B, C]，每次pop出去一个访问。缺点是pop出去的元素太随机，可能一次集中访问A ，而且占用内存太大，对于几万的权重范围不合适。</li>
<li>随机数：我们按照A、B、C的权重划分好区间，A（0、1、2、3），B（4、5），C（6），然后取一个随机数，模余7，看看最后的结果在哪个区间内，就取哪个节点。缺点是完全随机，无法避免集中访问。</li>
</ol>
<h2 id="加权轮询"><a href="#加权轮询" class="headerlink" title="加权轮询"></a>加权轮询</h2><p>假设有7个请求，我们给A、B、C三个节点分别4、2、1的权重。如果随机按照概率来选，那么很可能出现连续四个请求都在A上面的情况，这样只能保证结果看起来均衡，但是时间段内不均衡。Nginx采用了一种平滑的加权平均算法来选取节点（Weighted Round Robin）。<br>先引入三个概念，都用来描述服务器节点的权重：</p>
<ol>
<li>$W$ : weight 我们指定的权重，就是上面例子中的4、2、1。</li>
<li>$W_{ew}$: effective_weight 有效权重，初始值为$W$。用来对故障节点降权。<br>如果通信中有错误产生，就减小effective_weight。（故障降权）<br>此后有新的请求过来时，再逐步增加effective_weight，最终又恢复到weight。（自动恢复）</li>
<li>$W_{cw}$ : current_weight 当前真实权重，每次都会选到最大的真实权重的节点去请求</li>
</ol>
<p>真实权重$W_{cw}$计算方式：</p>
<ol>
<li>初始化：$W_{cw}$ 起始值为0</li>
<li>获得实时权重：请求到来后，给每个节点的真实权重加上有效权重，即$每个节点 W_{cw} = W_{cw} + W_{ew}$ </li>
<li>选出最大权重：选择真实权重最大的节点最为本次请求的目标</li>
<li>回避刚选的节点：最选择的节点的实时权重减去所有节点（包括自己）的有效权重和。即$选中节点 W_{cw} = W_{cw} - (W_{ew1} + W_{ew2} + … + W_{ewn})$ </li>
</ol>
<p>来看一个具体的例子：<br>假设A、B、C三个节点的权重分别为4、2、1。</p>
<table>
<thead>
<tr>
<th style="text-align:center">请求序号</th>
<th style="text-align:center">请求后的current_weight</th>
<th style="text-align:center">选择的节点</th>
<th style="text-align:center">选择后的current_weight</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">未请求</td>
<td style="text-align:center">{0,0,0}</td>
<td style="text-align:center">/</td>
<td style="text-align:center">/</td>
</tr>
<tr>
<td style="text-align:center">1</td>
<td style="text-align:center">{4,2,1}</td>
<td style="text-align:center">A</td>
<td style="text-align:center">{-3,2,1}</td>
</tr>
<tr>
<td style="text-align:center">2</td>
<td style="text-align:center">{1,4,2}</td>
<td style="text-align:center">B</td>
<td style="text-align:center">{1,-3,2}</td>
</tr>
<tr>
<td style="text-align:center">3</td>
<td style="text-align:center">{5,-1,3}</td>
<td style="text-align:center">A</td>
<td style="text-align:center">{-2,-1,3}</td>
</tr>
<tr>
<td style="text-align:center">4</td>
<td style="text-align:center">{2,1,4}</td>
<td style="text-align:center">C</td>
<td style="text-align:center">{2,1,-3}</td>
</tr>
<tr>
<td style="text-align:center">5</td>
<td style="text-align:center">{6,3,-2}</td>
<td style="text-align:center">A</td>
<td style="text-align:center">{-1,3,-2}</td>
</tr>
<tr>
<td style="text-align:center">6</td>
<td style="text-align:center">{3,5,-1}</td>
<td style="text-align:center">B</td>
<td style="text-align:center">{3,-2,-1}</td>
</tr>
<tr>
<td style="text-align:center">7</td>
<td style="text-align:center">{7,0,0}</td>
<td style="text-align:center">A</td>
<td style="text-align:center">{0,0,0}</td>
</tr>
</tbody>
</table>
<p>三个结论：每个节点被选中的次数是符合权重的；A没有被连续选取；七次之后权重会归零，是一个循环。</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> ngx_http_upstream_rr_peer_t *<span class="title">ngx_http_upstream_get_peer</span><span class="params">(<span class="keyword">ngx_http_upstream_rr_peer_data_t</span> *rrp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">time_t</span>                        now;</span><br><span class="line">    <span class="keyword">uintptr_t</span>                     m;</span><br><span class="line">    <span class="keyword">ngx_int_t</span>                     total;</span><br><span class="line">    <span class="keyword">ngx_uint_t</span>                    i, n, p;</span><br><span class="line">    <span class="keyword">ngx_http_upstream_rr_peer_t</span>  *peer, *best;</span><br><span class="line"></span><br><span class="line">    now = ngx_time();</span><br><span class="line">    best = <span class="literal">NULL</span>;</span><br><span class="line">    total = <span class="number">0</span>;</span><br><span class="line">    ...</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 遍历集群中的所有后端 */</span></span><br><span class="line">    <span class="keyword">for</span> (peer = rrp-&gt;peers-&gt;peer, i = <span class="number">0</span>;</span><br><span class="line">         peer;</span><br><span class="line">         peer = peer-&gt;next, i++)</span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        n = i / (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line">        m = (<span class="keyword">uintptr_t</span>) <span class="number">1</span> &lt;&lt; i % (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 检查该后端服务器在位图中对应的位，为1时表示不可用 */</span></span><br><span class="line">        <span class="keyword">if</span> (rrp-&gt;tried[n] &amp; m)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">       </span><br><span class="line">        <span class="comment">/* 永久不可用的标志 */</span></span><br><span class="line">        <span class="keyword">if</span> (peer-&gt;down) </span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        </span><br><span class="line">       <span class="comment">/* 在一段时间内，如果此后端服务器的失败次数，超过了允许的最大值，那么不允许使用此后端了 */</span></span><br><span class="line">       <span class="keyword">if</span> (peer-&gt;max_fails</span><br><span class="line">            &amp;&amp; peer-&gt;fails &gt;= peer-&gt;max_fails</span><br><span class="line">            &amp;&amp; now - peer-&gt;checked &lt;= peer-&gt;fail_timeout)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        </span><br><span class="line">        peer-&gt;current_weight += peer-&gt;effective_weight; <span class="comment">/* 对每个后端，增加其当前权重 */</span></span><br><span class="line">        total += peer-&gt;effective_weight; <span class="comment">/* 累加所有后端的有效权重 */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 如果之前此后端发生了失败，会减小其effective_weight来降低它的权重。          </span></span><br><span class="line"><span class="comment">         * 此后在选取后端的过程中，又通过增加其effective_weight来恢复它的权重。          </span></span><br><span class="line"><span class="comment">         */</span>        </span><br><span class="line">        <span class="keyword">if</span> (peer-&gt;effective_weight &lt; peer-&gt;weight) </span><br><span class="line">            peer-&gt;effective_weight++;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">/* 选取当前权重最大者，作为本次选定的后端 */</span></span><br><span class="line">        <span class="keyword">if</span> (best == <span class="literal">NULL</span> || peer-&gt;current_weight &gt; best-&gt;current_weight) &#123;</span><br><span class="line">            best = peer;</span><br><span class="line">            p = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (best == <span class="literal">NULL</span>) <span class="comment">/* 没有可用的后端 */</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    </span><br><span class="line">    rrp-&gt;current = best; <span class="comment">/* 保存本次选定的后端 */</span></span><br><span class="line"></span><br><span class="line">    n = p / (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line">    m = (<span class="keyword">uintptr_t</span>) <span class="number">1</span> &lt;&lt; p % (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 对于本次请求，如果之后需要再次选取后端，不能再选取这个后端了 */</span>    </span><br><span class="line">    rrp-&gt;tried[n] |= m;</span><br><span class="line"></span><br><span class="line">    best-&gt;current_weight -= total; <span class="comment">/* 选定后端后，需要降低其当前权重 */</span>  </span><br><span class="line">    <span class="comment">/* 更新checked时间 */</span></span><br><span class="line">    <span class="keyword">if</span> (now - best-&gt;checked &gt; best-&gt;fail_timeout)</span><br><span class="line">        best-&gt;checked = now;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> best;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="ip-hash"><a href="#ip-hash" class="headerlink" title="ip_hash"></a>ip_hash</h2><p>ip_hash是基于客户端IP的哈希值来选择服务器。同一个客户端的请求，都会发往同一台后端，除非该后端不可用了。ip_hash能够达到保持会话的效果。<br>和随机加权哈希一样，ip哈希借鉴了权重分段的思想，先算出哈希值, 然后模余total_weight，得到初始权重W [0, total_weight)，开始遍历节点。如果哈希值小于当前节点的权重，就选择当前节点；如果哈希值大于等于当前节点的权重，就减去当前节点的权重，再去尝试下个节点。我们还是讨论A，B，C三个节点的情况，假设权重分别为2、4、1 。<br>权重的和是7，那么哈希值应该是0~6 （因为模了哈希值）<br>6的情况：6 &gt;= 2 , 所以不选A，减去2。 6-2 &gt;= 4，所以不选B，减去4。2-4 &lt; 1，所以选C<br>5的情况：5 &gt;= 2 , 所以不选A，减去2。 5-2 &lt; 4，所以选B<br>4的情况：4 &gt;= 2 , 所以不选A，减去2。 4-2 &lt; 4，所以选B 。哈希值为3、2的情况也一样。<br>1、0的情况：1 &lt; 2，所以选A<br>可以看到最后四个选B，两个选A，一个选C，还是均衡的。</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">    <span class="comment">/* 1.根据客户端IP、本次选取的初始hash值，计算得到本次最终的hash值 */</span></span><br><span class="line">    <span class="comment">/* hash1 = (hash0 * 113 + addr[0]) % 6271; hash2 = (hash1 * 113 + addr[1]) % 6271;...; */</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; (<span class="keyword">ngx_uint_t</span>) iphp-&gt;addrlen; i++)</span><br><span class="line">        hash = (hash * <span class="number">113</span> + iphp-&gt;addr[i]) % <span class="number">6271</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 2. 先给w赋值为所有节点的权重和。total_weight和weight都是固定值 */</span></span><br><span class="line">    w = hash % iphp-&gt;rrp.peers-&gt;total_weight;</span><br><span class="line">    peer = iphp-&gt;rrp.peers-&gt;peer; <span class="comment">/* 第一台后端 */</span></span><br><span class="line">    p = <span class="number">0</span>;</span><br><span class="line">	</span><br><span class="line">	<span class="comment">/* 3.遍历后端链表时，依次减去每个后端的权重，直到w小于某个后端的权重 */</span></span><br><span class="line">    <span class="keyword">while</span> (w &gt;= peer-&gt;weight) &#123;</span><br><span class="line">        w -= peer-&gt;weight;</span><br><span class="line">        peer = peer-&gt;next;</span><br><span class="line">        p++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br><span class="line">choose(p);</span><br></pre></td></tr></table></figure>
<h2 id="least-conn"><a href="#least-conn" class="headerlink" title="least_conn"></a>least_conn</h2><p>有的场景下，把请求转发给连接数较少的后端，能够达到更好的负载均衡效果。<br>least_conn算法很简单，首选遍历后端集群，比较每个后端的conns/weight，选取该值最小的后端。如果有多个后端的conns/weight值同为最小的，那么对它们采用加权轮询算法。</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (peer = peers-&gt;peer, i = <span class="number">0</span>; peer; peer = peer-&gt;next, i++)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">/* 检查此后端在状态位图中对应的位，为1时表示不可用 */</span> </span><br><span class="line">    n = i / (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line">    m = (<span class="keyword">uintptr_t</span>) <span class="number">1</span> &lt;&lt; i % (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (rrp-&gt;tried[n] &amp; m)</span><br><span class="line">       <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* server指令中携带了down属性，表示后端永久不可用 */</span></span><br><span class="line">    <span class="keyword">if</span> (peer-&gt;down)</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 在一段时间内，如果此后端服务器的失败次数，超过了允许的最大值，那么不允许使用此后端了 */</span></span><br><span class="line">     <span class="keyword">if</span> (peer-&gt;max_fails &amp;&amp; peer-&gt;fails &gt;= peer-&gt;max_fails</span><br><span class="line">         &amp;&amp; now - peer-&gt;checked &lt;= peer-&gt;fail_timeout)</span><br><span class="line">        <span class="keyword">continue</span>;</span><br><span class="line">        </span><br><span class="line">    <span class="comment">/* 比较各个后端的conns/weight，选取最小者；</span></span><br><span class="line"><span class="comment">     * 如果有多个最小者，记录第一个的序号p，且设置many标志。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">if</span> (best == <span class="literal">NULL</span> || peer-&gt;conns * best-&gt;weight &lt; best-&gt;conns * peer-&gt;weight)</span><br><span class="line">    &#123;</span><br><span class="line">        best = peer;</span><br><span class="line">        many = <span class="number">0</span>;</span><br><span class="line">        p = i;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (peer-&gt;conns * best-&gt;weight == best-&gt;conns * peer-&gt;weight)</span><br><span class="line">        many = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 如果有多个后端的conns/weight同为最小者，则对它们使用轮询算法 */</span></span><br><span class="line"><span class="keyword">if</span> (many) &#123;</span><br><span class="line">    <span class="keyword">for</span> (peer = best, i = p; peer; peer-&gt;peer-&gt;next, i++)</span><br><span class="line">    &#123;</span><br><span class="line">       <span class="comment">/* conns/weight必须为最小的 */</span></span><br><span class="line">        <span class="keyword">if</span> (peer-&gt;conns * best-&gt;weight != best-&gt;conns * peer-&gt;weight)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        peer-&gt;current_weight += peer-&gt;effective_weight; <span class="comment">/* 对每个后端，增加其当前权重 */</span></span><br><span class="line">        total += peer-&gt;effective_weight; <span class="comment">/* 累加所有后端的有效权重 */</span></span><br><span class="line"></span><br><span class="line">       <span class="comment">/* 如果之前此后端发生了失败，会减小其effective_weight来降低它的权重。          </span></span><br><span class="line"><span class="comment">          * 此后在选取后端的过程中，又通过增加其effective_weight来恢复它的权重。          </span></span><br><span class="line"><span class="comment">          */</span>        </span><br><span class="line">        <span class="keyword">if</span> (peer-&gt;effective_weight &lt; peer-&gt;weight) </span><br><span class="line">            peer-&gt;effective_weight++;</span><br><span class="line">    </span><br><span class="line">        <span class="comment">/* 选取当前权重最大者，作为本次选定的后端 */</span></span><br><span class="line">        <span class="keyword">if</span> (best == <span class="literal">NULL</span> || peer-&gt;current_weight &gt; best-&gt;current_weight) &#123;</span><br><span class="line">            best = peer;</span><br><span class="line">            p = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="一致哈希"><a href="#一致哈希" class="headerlink" title="一致哈希"></a>一致哈希</h2><p>当后端是缓存服务器时，经常使用一致性哈希算法来进行负载均衡。使用一致性哈希的好处在于，增减集群的缓存服务器时，只有少量的缓存会失效，回源量较小。常见的CDN架构都是使用一致性哈希。<br>我们知道的<a href="https://www.codeproject.com/Articles/56138/Consistent-hashing" target="_blank" rel="noopener">一致性哈希</a>是一个环，每个哈希值对应的请求属于哈希值在环上遇到的下一个节点。为了使得请求分布更加均衡，我们建立了很多虚拟节点，请求会对应到虚拟节点的真实节点上。</p>
<ol>
<li>创造虚拟节点<br>在nginx中，为了保证节点的权重，一般一个真实节点对应weight * 160个虚拟节点。<br>每个虚拟节点的hash值hash = crc32(base_hash PREV_HASH)，其中，PREV_HASH表示上个虚拟节点的哈希值，这样就可以不断产出虚拟节点。base_hash 表示对应真实节点的哈希值（crc32(HOST 0 PORT)）。<br>创造完所有数量后，我们对虚拟节点按照哈希值排序。</li>
<li>请求分配<br>先对请求做哈希，得出hash值，然后使用二分查找，寻找第一个hash值大于等于请求的哈希值的虚拟节点，即顺时针方向最近的一个虚拟节点。</li>
<li>找到真实节点<br>遍历真实节点数组，寻找可用的、该虚拟节点归属的真实节点(server成员相同)，如果有多个真实节点同时符合条件，那么使用轮询来从中选取一个真实节点。</li>
</ol>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> ( ; ; ) &#123;</span><br><span class="line">    <span class="comment">/* 在peer.init中，已根据请求的哈希值，找到顺时针方向最近的一个虚拟节点，</span></span><br><span class="line"><span class="comment">     * hash为该虚拟节点在数组中的索引。</span></span><br><span class="line"><span class="comment">     * 一开始hash值肯定小于number，之后每尝试一个虚拟节点后，hash++。取模是为了防止越界访问。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    server = point[hp-&gt;hash % points-&gt;number].server;</span><br><span class="line">    best = <span class="literal">NULL</span>;</span><br><span class="line">    best_i = <span class="number">0</span>;</span><br><span class="line">    total = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 遍历真实节点数组，寻找可用的、该虚拟节点归属的真实节点(server成员相同)，</span></span><br><span class="line"><span class="comment">      * 如果有多个真实节点同时符合条件，那么使用轮询来从中选取一个真实节点。</span></span><br><span class="line"><span class="comment">      */</span></span><br><span class="line">    <span class="keyword">for</span> (peer = hp-&gt;rrp.peers-&gt;peer, i = <span class="number">0</span>; peer; peer = peer-&gt;next, i++) &#123;</span><br><span class="line">        <span class="comment">/* 检查此真实节点在状态位图中对应的位，为1时表示不可用 */</span></span><br><span class="line">        n = i / (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line">        m = (<span class="keyword">uintptr_t</span>) <span class="number">1</span> &lt;&lt; i % (<span class="number">8</span> * <span class="keyword">sizeof</span>(<span class="keyword">uintptr_t</span>));</span><br><span class="line">        <span class="keyword">if</span> (hp-&gt;rrp.tried[n] &amp; m)</span><br><span class="line">            <span class="keyword">continue</span>;            </span><br><span class="line"></span><br><span class="line">        <span class="comment">/* server指令中携带了down属性，表示后端永久不可用 */</span></span><br><span class="line">        <span class="keyword">if</span> (peer-&gt;down)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 如果真实节点的server成员和虚拟节点的不同，表示虚拟节点不属于此真实节点 */</span></span><br><span class="line">        <span class="keyword">if</span> (peer-&gt;server.len != server-&gt;len || </span><br><span class="line">            ngx_strncmp(peer-&gt;server.data, server-&gt;data, server-&gt;len) != <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">       <span class="comment">/* 在一段时间内，如果此真实节点的失败次数，超过了允许的最大值，那么不允许使用了 */</span></span><br><span class="line">       <span class="keyword">if</span> (peer-&gt;max_fails</span><br><span class="line">            &amp;&amp; peer-&gt;fails &gt;= peer-&gt;max_fails</span><br><span class="line">            &amp;&amp; now - peer-&gt;checked &lt;= peer-&gt;fail_timeout)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">    </span><br><span class="line">        peer-&gt;current_weight += peer-&gt;effective_weight; <span class="comment">/* 对每个真实节点，增加其当前权重 */</span></span><br><span class="line">        total += peer-&gt;effective_weight; <span class="comment">/* 累加所有真实节点的有效权重 */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 如果之前此真实节点发生了失败，会减小其effective_weight来降低它的权重。          </span></span><br><span class="line"><span class="comment">         * 此后又通过增加其effective_weight来恢复它的权重。          </span></span><br><span class="line"><span class="comment">         */</span>        </span><br><span class="line">        <span class="keyword">if</span> (peer-&gt;effective_weight &lt; peer-&gt;weight) </span><br><span class="line">            peer-&gt;effective_weight++;</span><br><span class="line">    </span><br><span class="line">        <span class="comment">/* 选取当前权重最大者，作为本次选定的真实节点 */</span></span><br><span class="line">        <span class="keyword">if</span> (best == <span class="literal">NULL</span> || peer-&gt;current_weight &gt; best-&gt;current_weight) &#123;</span><br><span class="line">            best = peer;</span><br><span class="line">            best_i = i;</span><br><span class="line">        &#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">/* 如果选定了一个真实节点 */</span></span><br><span class="line">	<span class="keyword">if</span> (best) &#123;</span><br><span class="line">	    best-&gt;current_weight -= total; <span class="comment">/* 如果使用了轮询，需要降低选定节点的当前权重 */</span></span><br><span class="line">	    <span class="keyword">goto</span> found;</span><br><span class="line">	&#125;</span><br><span class="line">	</span><br><span class="line">	hp-&gt;hash++; <span class="comment">/* 增加虚拟节点的索引，即“沿着顺时针方向” */</span></span><br><span class="line">	hp-&gt;tries++; <span class="comment">/* 已经尝试的虚拟节点数 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="恐慌阈值-Envoy"><a href="#恐慌阈值-Envoy" class="headerlink" title="恐慌阈值 Envoy"></a>恐慌阈值 Envoy</h2><p>负载均衡一般是根据集群中主机的健康情况灵活变动的。当某台主机跪了，LB算法将会把它从候选列表中踢出去，这也是很合理的。</p>
<p>但是我们假设这么一种情况，某一时间，所有服务主机的负载情况是最大负载的80%，（负载800；最大处理能力1000）<br>因为某种原因，导致20.0%的机器彻底崩溃。（负载800；最大处理能力800）<br>LB策略忽略20%的机器，导致剩下的80%的机器都在最大处理负载上运行；<br>又来了一个网络波动，造成所有的服务器一个接一个崩溃，整个集群雪崩。<br>每拉起一台新的机器，LB策略立刻把所有的流量打到这么一台机器上，导致它再次崩溃。</p>
<p>如果有一个恐慌阈值，譬如50%，那么LB会在50%机器崩溃的时候，禁用淘汰策略，把所有机器都当做健康的，在整体集群上执行普通的Round-Robin策略。<br>多数机器恢复，整个集群的处理能力恢复80%的正确率。这使得整个集群能够在遇到极特殊情况的时候能够从困境中恢复</p>
<h1 id="LB用的负载均衡算法"><a href="#LB用的负载均衡算法" class="headerlink" title="LB用的负载均衡算法"></a>LB用的负载均衡算法</h1><h2 id="EDF调度算法"><a href="#EDF调度算法" class="headerlink" title="EDF调度算法"></a>EDF调度算法</h2><p><a href="http://zablog.me/2019/08/02/2019-08-02/" target="_blank" rel="noopener">EDF调度算法</a><br>最早截止时间优先调度法 Earliest Deadline First (EDF) scheduler，是加权轮转调度算法（WRR，Weighted Round-Robin）的一种实现方式。<br>其核心思想是为每个条目截止时间赋值为当前时间加权重的倒数，然后采用最早截止时间优先的方式进行调度。<br>调度算法最主要的应用是操作系统调度进程，重要的调度理论基本上都是在此时涌现的。而后续反向代理对下游条目进行负载均衡，也可以参考一样的调度理论，只是进程的运行和切换转变为请求的接受与投递。</p>
<p>假设有三个条目可供调度，分别是A、B、C，他们的权重分别是3：2：1。<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/lb2.png" alt="调度队列"><br>图上有一个数轴，从0到3，分别表示三个周期。<br>A条目的权重是3，我们以1/3为分隔不断重绘A，使得数轴的1/3，2/3，1，4/3等位置印上A；<br>B条目的权重是2，我们以1/2为分隔不断重绘B，使得数轴的1/2，1，3/2，2等位置印上B；<br>C条目的权重是1，我们以1为分隔不断重绘C，使得数轴的1，2，3等位置印上C；<br>最后，我们使用一个游标从左向右扫，扫描到的顺序就是调度的顺序，因此我们调度的顺序为A-B-A-C-B-A-A-B-A…<br>显而易见，调用的顺序含有一个循环节A-B-A-C-B-A，所以当调度足够多次数后，A、B、C的调度比值将会趋近于3：2：1</p>
<h3 id="实现1"><a href="#实现1" class="headerlink" title="实现1"></a>实现1</h3><p>最简单的实现，是使用上述办法模拟一个周期，然后把周期存到数组中，用游标扫描即可。<br>以上述情形为例，首先我们定制一个数组 A-B-A-C-B-A，然后不断回环扫描这个数组，就可以完成加权轮转调度。<br>这种方法的每次调度的时间复杂度为O(1)，空间复杂度为O(M*N)，其中M是条目的平均权重，N是条目的数量。<br>优势：<br>实现简单，容易理解<br>单次调度的很快<br>多线程共享游标index即可，协作方便<br>劣势：<br>空间复杂度高<br>对条目修改很不友好（修改其中一个条目的权重，那么整个表需要重新构建，耗CPU）</p>
<h3 id="实现2"><a href="#实现2" class="headerlink" title="实现2"></a>实现2</h3><p>我们可以维护一个大小为节点数量的堆，按照最快到期时间来排序，每次取最快到期的节点（就是线段上最靠前的节点），返回后更新它的节点到期时间。</p>
<ol>
<li>初始时 entry.deadline = 1.0/entry.Weight</li>
<li>调度的时候，从中选择 deadline 最小的使用，并把 deadline 设置为 ttl（当前时间）+ 1.0/entry.Weight，然后重新把这个条目放入优先队列中。重新排布优先队列，如此往复。</li>
</ol>
<p>举个例子：<br>三个节点A、B、C权重分别为5、2、1</p>
<table>
<thead>
<tr>
<th style="text-align:center">请求序号</th>
<th style="text-align:center">选择的节点</th>
<th style="text-align:center">选择节点后的堆</th>
<th style="text-align:center">选择时的TTL</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">未请求</td>
<td style="text-align:center">/</td>
<td style="text-align:center">[A(1/5), B(1/2), C(1)]</td>
<td style="text-align:center">0</td>
</tr>
<tr>
<td style="text-align:center">1</td>
<td style="text-align:center">A</td>
<td style="text-align:center">[A(2/5), B(1/2), C(1)]</td>
<td style="text-align:center">1/5</td>
</tr>
<tr>
<td style="text-align:center">2</td>
<td style="text-align:center">A</td>
<td style="text-align:center">[B(1/2), A(3/5), C(1)]</td>
<td style="text-align:center">2/5</td>
</tr>
<tr>
<td style="text-align:center">3</td>
<td style="text-align:center">B</td>
<td style="text-align:center">[A(3/5), B(1), C(1)]</td>
<td style="text-align:center">1/2</td>
</tr>
<tr>
<td style="text-align:center">4</td>
<td style="text-align:center">A</td>
<td style="text-align:center">[A(4/5), B(1), C(1)]</td>
<td style="text-align:center">3/5</td>
</tr>
<tr>
<td style="text-align:center">5</td>
<td style="text-align:center">A</td>
<td style="text-align:center">[A(1), B(1), C(1)]</td>
<td style="text-align:center">4/5</td>
</tr>
<tr>
<td style="text-align:center">6</td>
<td style="text-align:center">A</td>
<td style="text-align:center">[B(1), C(1), A(6/5)]</td>
<td style="text-align:center">1</td>
</tr>
<tr>
<td style="text-align:center">7</td>
<td style="text-align:center">B</td>
<td style="text-align:center">[C(1), A(6/5), B(3/2)]</td>
<td style="text-align:center">3/2</td>
</tr>
<tr>
<td style="text-align:center">8</td>
<td style="text-align:center">C</td>
<td style="text-align:center">[A(6/5), B(3/2), C(2)]</td>
<td style="text-align:center">1</td>
</tr>
</tbody>
</table>
<p>复杂度分析：<br>空间复杂度降低为 O(N) ，每次Pick的时间复杂度为 O(logN)<br>初始化的时间复杂度为 O(N)，也就是堆排序的复杂度<br><figure class="highlight go"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 节点</span></span><br><span class="line"><span class="keyword">type</span> Entry <span class="keyword">struct</span> &#123;</span><br><span class="line">	deadline <span class="keyword">float64</span></span><br><span class="line">	index    <span class="keyword">int64</span></span><br><span class="line">	Value    <span class="keyword">string</span></span><br><span class="line">	Weight   <span class="keyword">float64</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// EDF implements the Earliest Deadline First scheduling algorithm</span></span><br><span class="line"><span class="keyword">type</span> EDF <span class="keyword">struct</span> &#123;</span><br><span class="line">	pq       *priorityQueue    <span class="comment">//这个优先级队列用deadline排序</span></span><br><span class="line">	curIndex <span class="keyword">int64</span></span><br><span class="line">	curDDL   <span class="keyword">float64</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Add a new entry for load balance</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(e *EDF)</span> <span class="title">Add</span><span class="params">(entry *Entry)</span></span> &#123;</span><br><span class="line">	entry.deadline = e.curDDL + <span class="number">1</span>/entry.Weight</span><br><span class="line">	e.curIndex++</span><br><span class="line">	entry.index = e.curIndex</span><br><span class="line">	heap.Push(e.pq, entry)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// AddRaw add a new entry for load balance without sort</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(e *EDF)</span> <span class="title">AddRaw</span><span class="params">(entry *Entry)</span></span> &#123;</span><br><span class="line">	entry.deadline = e.curDDL + <span class="number">1</span>/entry.Weight</span><br><span class="line">	e.curIndex++</span><br><span class="line">	entry.index = e.curIndex</span><br><span class="line">	*e.pq = <span class="built_in">append</span>(*e.pq, entry)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Delete an entry</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(e *EDF)</span> <span class="title">Delete</span><span class="params">(entry *Entry)</span></span> &#123;</span><br><span class="line">	entry.Weight = <span class="number">-1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Pick an available entry</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="params">(e *EDF)</span> <span class="title">Pick</span><span class="params">()</span> *<span class="title">Entry</span></span> &#123;</span><br><span class="line">	<span class="comment">// if no available entry, return nil</span></span><br><span class="line">	<span class="keyword">if</span> <span class="built_in">len</span>(*e.pq) &lt;= <span class="number">0</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">	&#125;</span><br><span class="line">	entry := heap.Pop(e.pq).(*Entry)</span><br><span class="line">	<span class="keyword">if</span> entry.Weight &lt;= <span class="number">0</span> &#123;</span><br><span class="line">		<span class="comment">// if Weight isn't positive, try another entry</span></span><br><span class="line">		<span class="keyword">return</span> e.Pick()</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// curDDL should be entry's deadline so that new added entry would have a fair</span></span><br><span class="line">	<span class="comment">// competition environment with the old ones</span></span><br><span class="line">	e.curDDL = entry.deadline</span><br><span class="line">	entry.deadline = entry.deadline + <span class="number">1</span>/entry.Weight</span><br><span class="line">	e.curIndex++</span><br><span class="line">	entry.index = e.curIndex</span><br><span class="line">	heap.Push(e.pq, entry)</span><br><span class="line">	<span class="keyword">return</span> entry</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// NewEDF create a new edf scheduler</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">NewEDF</span><span class="params">(entries []*Entry)</span> *<span class="title">EDF</span></span> &#123;</span><br><span class="line">	<span class="comment">// make a new edf</span></span><br><span class="line">	priorityQueue := <span class="built_in">make</span>(priorityQueue, <span class="number">0</span>)</span><br><span class="line">	edf := &amp;EDF&#123;</span><br><span class="line">		pq:       &amp;priorityQueue,</span><br><span class="line">		curIndex: <span class="number">0</span>,</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// put entries into priority queue</span></span><br><span class="line">	<span class="comment">// TODO(maziang): use O(N) heap.Init instead of O(NlogN) Add.</span></span><br><span class="line">	<span class="keyword">for</span> _, entry := <span class="keyword">range</span> entries &#123;</span><br><span class="line">		edf.AddRaw(entry)</span><br><span class="line">	&#125;</span><br><span class="line">	heap.Init(edf.pq)</span><br><span class="line"></span><br><span class="line">	<span class="comment">// avoid instance flood pressure for the first entry</span></span><br><span class="line">	<span class="comment">// start from a random one via pick random times</span></span><br><span class="line">	rand.Seed(time.Now().UnixNano())</span><br><span class="line">	randomPick := rand.Intn(<span class="built_in">len</span>(entries))</span><br><span class="line">	<span class="keyword">for</span> i := <span class="number">0</span>; i &lt; randomPick; i++ &#123;</span><br><span class="line">		edf.Pick()</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> edf</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>Note1 同样权重的情况: index是节点进入堆的次序。当deadline一样时，会选取index小的那一个。这是为了避免当所有节点权重一样时，变成完全随机调度。如果有了index，那么每次选取之后该节点的index会加一（相当于排到后面去），这样就能保证下一个节点能被调度到。<br>Note2 存储空间优化：<br>Note3 起始随机化：如果有个节点权重很大，那么在调度器全部重启的时候可能都会调度到这个节点，导致这个节点压力过大。所以NewEDF的初始化阶段会有随机化操作。</p>
]]></content>
    
    <summary type="html">
    
      负载均衡是集群中一个重要的组成部分。这个模块一般集成了名字服务、负载均衡、过载保护、限流等功能。第一部分是针对均衡本身算法的介绍。
    
    </summary>
    
      <category term="后台" scheme="chestnutheng.cn/categories/%E5%90%8E%E5%8F%B0/"/>
    
    
      <category term="负载均衡" scheme="chestnutheng.cn/tags/%E8%B4%9F%E8%BD%BD%E5%9D%87%E8%A1%A1/"/>
    
      <category term="后台" scheme="chestnutheng.cn/tags/%E5%90%8E%E5%8F%B0/"/>
    
  </entry>
  
  <entry>
    <title>[Linux]文件和零拷贝</title>
    <link href="chestnutheng.cn/2019/10/31/linux-io/"/>
    <id>chestnutheng.cn/2019/10/31/linux-io/</id>
    <published>2019-10-31T09:25:36.000Z</published>
    <updated>2019-10-31T09:25:41.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h2><h3 id="文件描述符"><a href="#文件描述符" class="headerlink" title="文件描述符"></a>文件描述符</h3><ol>
<li>文件描述符：在Linux中，所有的文件都是通过文件描述符引用。fd是一个非负整数。按照惯例，标准输入的fd是0，标准输出的fd是1，标准错误的fd是2。分别作为<code>STDIN_FILENO</code>、<code>STDOUT_FILENO</code>、<code>STDERR_FILENO</code>定义在unistd中。</li>
<li>文件描述符的上限：fd的范围是 <code>0 ~ OEPN_MAX-1</code> 。OPEN_MAX一般是20或者64。这代表一个进程最多打开19或63个文件。</li>
</ol>
<h3 id="文件内核API"><a href="#文件内核API" class="headerlink" title="文件内核API"></a>文件内核API</h3><ol>
<li><p>文件的打开：<code>int open(const char *pathname, int flags)</code>参数填上要打开的文件的名字（甚至可以不存在），会返回打开的fd。下面是一些常用的选项：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">O_APPEND 文件将以追加模式打开，每次写操作之前，文件偏移量都会置于文件末尾。</span><br><span class="line">O_CREAT 创建文件。如果文件已经存在，则会直接打开。</span><br><span class="line">O_EXCL 和上面的O_CREAT联用时，表示如果文件已经存在，就会失败。可以保证多进程同时创建文件的原子操作。</span><br><span class="line">O_SYNC 打开文件用于同步I/O。在数据写到磁盘之前写操作不会完成；一般的读操作已是同步的，所以这个标志对读操作没有影响。</span><br><span class="line">O_NONBLOCK 如果可以，文件将在非阻塞模式下打开。任何其它操作都不会使该进程在I/O中阻塞。这种情况可能只用于FIFO。</span><br><span class="line">O_DIRECT 打开文件用于直接I/O。将会绕过缓冲区操作。</span><br></pre></td></tr></table></figure>
</li>
<li><p>文件的关闭：<code>int close(int fd)</code> 关闭一个文件会释放上面所有的记录锁。一个进程终止后，内核会自动关闭它打开的所有文件。</p>
</li>
<li>文件定位： <code>off_t lseek(int fd, off_t offset, int whence)</code> 参数whence指定了偏移地址（开始点<code>SEEK_SET</code> 当前点<code>SEEK_CUR</code> 结束点<code>SEET_END</code>），另一个参数offset是从参考点开始的偏移量（可正可负）。返回新的偏移地址。</li>
<li>空洞文件：如果写入一部分之后lseek到后面去写入，中间就会产生一个空洞，实际不占用磁盘大小。</li>
<li>文件读取：<code>ssize_t read(int fd, void *buf, size_t nbytes)</code> 返回读取到的字节数。下面的情况可能使得读取到的字节数少于需要的字节数： 1)再读这么多就到了文件尾 2)读网络缓冲区读完 3)读FIFO管道包含的字节少于需要的长度 4)读终端设备，一次一行</li>
<li>文件写入：<code>ssize_t write(int fd, const void* buf, size_t nbytes)</code> 返回写入的字节数。一般和nbytes相同。</li>
<li>文件属性编辑：<code>int fcntl(int fd, int cmd, ... /* arg */ )</code> 提供了编辑fd属性标志的方法。</li>
</ol>
<h3 id="read-buf该设置多大"><a href="#read-buf该设置多大" class="headerlink" title="read buf该设置多大"></a>read buf该设置多大</h3><p>在Linux ext4系统上，磁盘块长度<code>st_blksize</code>为4096字节。测试表明，在4096的整数倍上，读磁盘有更快的速率，可以根据需要选择4096或8192等字节的buf。<br>系统为了优化频繁写磁盘的情况，会使用预读取（read ahead）技术，在检测到顺序读时，会比本次读取需要的读出更多的数据，读入缓冲区。</p>
<h3 id="文件结构"><a href="#文件结构" class="headerlink" title="文件结构"></a>文件结构</h3><p>要设计一个多线程读取文件的程序，必须先知道文件和线程的关系。内核用3张表表示打开的文件，他们分别是：</p>
<ol>
<li>进程中的文件表，记录着进程打开的所有文件。每个文件用了一个<strong>fd标志</strong>和一个<strong>文件指针</strong>（指向2）表示。</li>
<li>内核为每个进程打开的每个文件创建了一个文件表，包含了<strong>文件状态标志</strong>、<strong>文件偏移量</strong>和<strong>i-node指针</strong>。需要注意不同进程打开了不同文件的时候，会有两个文件表。所以并发读取是安全的。</li>
<li>每个文件都对应一个<code>i-node</code>。这个node里包含了文件的磁盘块位置、<strong>文件的长度</strong>、拥有者、权限等。这些信息会被读入内存。</li>
</ol>
<p>Note1：磁盘划分为数据区和<code>i-node</code>区，一般每4个块（一个块为4K，8个扇区）就会有一个<code>i-node</code>，占地256字节。每创建一个文件，系统就分配一个<code>i-node</code>给文件，并把文件名和<code>i-node</code>编号关联起来。查找的时候，根据文件名找到编号，再找到文件的磁盘位置。<br>Note2：每次write完毕，内核文件表2中的文件偏移量都会增加写入的字节数。如果此时文件的偏移量大于<code>i-node</code>记录的文件长度，<code>i-node</code>记录会被更新为这个偏移量（文件变长）。<br>Note3：O_APPEND会被写内核文件表2中的文件状态标志中。这样，每次write发现这个标志，都会先把文件偏移量设置为i-node的文件长度，这样就可以写到尾端。<br>Note4：lseek只改变内核文件表2的文件偏移量，不进行任何I/O操作。<br>Note5：如果子进程是父进程fork出来的，那么他们会共享2中的内核文件表。</p>
<p>这三个表的关系如下图所示。下面有两个进程打开了同一文件：</p>
<p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/aupe/file_1.png" alt="文件表"></p>
<h3 id="原子操作"><a href="#原子操作" class="headerlink" title="原子操作"></a>原子操作</h3><p>假设有两个进程要不断往一个日志文件的结尾添加日志。你可能写出下面的代码：<br><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (lseek(fd, OL, <span class="number">2</span>) &lt; <span class="number">0</span>)error(<span class="string">"lseek error"</span>);</span><br><span class="line"><span class="keyword">if</span> (write(fd, buf, <span class="number">100</span>) &lt; <span class="number">0</span>)error(<span class="string">"write error"</span>);</span><br></pre></td></tr></table></figure></p>
<p>即使linux保证了他的系统调用是原子的，但是还是肯能发生如下的操作序列：</p>
<ol>
<li>A进程seek到文件尾，文件长度1500，偏移量1500。</li>
<li>B进程seek到文件尾，文件长度1500，偏移量1500。</li>
<li>B进程write 100字节，从1500开始写，文件长度1500-&gt;1600。</li>
<li>A进程write 100字节，从1500开始写，覆盖了B的写入。<br>Linux提供了下面的原子函数，结合了lseek和读写，保证了这种操作的原子性。<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">ssize_t pread(int fd, void *buf, size_t count, off_t offset);</span><br><span class="line">ssize_t pwrite(int fd, const void *buf, size_t count, off_t offset);</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>另一种方式是采用<code>O_APPEND</code>。这种模式保证了每次写的时候偏移量都在文件尾，所以不同竞争进程间不会为了偏移量相互影响。一些开源软件的日志就是这么实现的。</p>
<p>除了这个例子外，<code>O_EXCL</code>也用于打开文件时保证原子性。如果没有这个标志，一个进程创建文件可能会分为<strong>检查文件是否存在</strong>、<strong>创建文件</strong>两步，有可能发生重复创建导致数据丢失的情况。</p>
<h2 id="内核的I-O-Cache"><a href="#内核的I-O-Cache" class="headerlink" title="内核的I/O Cache"></a>内核的I/O Cache</h2><p>read和write操作如果相当频繁，一方面会带来频繁的系统调用，另一方面，读写磁盘的效率远远跟不上。所以unix系统在I/O方面做了内核的优化，一方面用缓冲区削峰，把write调用再内存中整合后再写入磁盘（一般是list结构），另一方面把热点数据也缓存在内存中，使得read调用大部分都命中内存而不是磁盘（一般是hashmap）。</p>
<h3 id="缓冲区缓存-Buffer-Cache"><a href="#缓冲区缓存-Buffer-Cache" class="headerlink" title="缓冲区缓存 Buffer Cache"></a>缓冲区缓存 Buffer Cache</h3><p>unix系统中最朴素的I/O加速技术就是缓冲区。在调用write时，内核将数据拷贝到缓冲区中，然后排入队列，晚些时候一起写入磁盘。这种方法称为延迟写（delayed write）。延迟写可能有一些问题需要担心：</p>
<ol>
<li>此时read会不一致吗？如果一个read调用在写入磁盘前读取还未刷盘的这部分数据，内核将从缓冲区中读取，而不是读取磁盘上陈旧的数据。</li>
<li>会改变写顺序吗？内核将会把缓冲区队列的数据重新安排，所以写顺序会被改变。但是，写的位置并不会改变，很少有对写顺序有要求的程序。</li>
<li>刷盘时错误怎么办？刷盘错误程序不会感知，所以比如掉电等故障中没刷盘成功的数据会丢失。内核一般会用10ms的间隔来刷盘保证丢失的数据不会太多。</li>
</ol>
<p>以下两个条件会触发刷盘：</p>
<ol>
<li>当空闲内存小于设定的阈值时，脏的缓冲区就会回写到磁盘上，被清理的缓冲区可能会被移除，来释放内存空间。</li>
<li>当一个脏的缓冲区寿命超过设定的阀值时（一般为10ms），缓冲区被回写至磁盘。以此来避免数据的不确定性。</li>
</ol>
<h3 id="页缓存-Page-Cache"><a href="#页缓存-Page-Cache" class="headerlink" title="页缓存 Page Cache"></a>页缓存 Page Cache</h3><p>相比起CPU而言，现在的磁盘速度远远落后。所以主存中会存有一份磁盘中常用数据的拷贝，以便于以后操作只操作主存，减少磁盘访问。那放哪些数据进去呢？这是一些思考：</p>
<ol>
<li>时间局部性：该方法的思考是，刚被访问的资源很可能会在不久后再次被访问。页缓存是内核寻找文件系统数据的第一目的地。只有缓存中找不到时内核，才会调用存储子系统从磁盘中读取数据。当数据第一次读取后，就会从磁盘读<br>入页缓存中，并从缓存中返回给应用。如果那项数据被再次读取，就直接从缓</li>
<li>空间局部性：是关于数据的连续使用的性质。基于这个原理，内核实现了页缓存预读技术。预读是在每次读请求时从磁盘数据中读取更多的数据到页缓存中的动作一—多读一点点会很有效。当内核从磁盘读取一块数据时，也会读取接下来一两块数据。一次读取较大的连续数据块时磁盘不需要经常寻道，所以会比较有效。</li>
</ol>
<h3 id="Cache的同步"><a href="#Cache的同步" class="headerlink" title="Cache的同步"></a>Cache的同步</h3><h4 id="主动刷盘-fsync"><a href="#主动刷盘-fsync" class="headerlink" title="主动刷盘 fsync"></a>主动刷盘 fsync</h4><p>同步I/O函数提供了针对单文件的主动刷盘：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">// 把缓冲区刷盘</span><br><span class="line">void sync(viod);</span><br><span class="line">// 把某个fd的文件属性和文件刷盘</span><br><span class="line">int fsync(int fd);</span><br><span class="line">// 把某个fd的文件刷盘</span><br><span class="line">int fdatasync(int fd);</span><br></pre></td></tr></table></figure></p>
<h4 id="主动刷盘-O-SYNC"><a href="#主动刷盘-O-SYNC" class="headerlink" title="主动刷盘 O_SYNC"></a>主动刷盘 O_SYNC</h4><p><code>O_SYNC</code>看起来就像是在每个write操作后都隐式地执行fsync。尽管这在语法上是毫无问题的，但Linux内核实现的O_SYNC效率会更高。<br><code>O_SYNC</code> 根据写入文件的大小，可能会使大量的时间消耗在进程的I/O等待时间上，此时的O_SYNC会使总耗时增加一到两个数量级。这种时间开销增长是非常可观的，所以同步I/O一般是在无计可施情况下的最后选择。一般情况下在关键操作之后使用fsync会更加合理。</p>
<h4 id="直接IO"><a href="#直接IO" class="headerlink" title="直接IO"></a>直接IO</h4><p>内核的I/O Cache给我们带来了很多便利，但是我们无法预知I/O系统的复杂行为。尤其在一些数据库的应用中，他们倾向于自己做缓存。使用<code>O_DIRECT</code>标志会使内核最小化I/O管理的影响，直接写到磁盘。使用这个标志时，注意下面几点：</p>
<ol>
<li>I/O操作将忽略页缓存机制，直接对用户空间缓冲区和设备进行初始化。</li>
<li>所有的I/O将是同步的，操作在完成之前不会返回。</li>
<li>当使用直接I/O时，请求长度，缓冲区对齐，和文件偏移必须是设备扇区大小（通常是512字节）的整数倍。</li>
</ol>
<h3 id="I-O-Cache的一些思考"><a href="#I-O-Cache的一些思考" class="headerlink" title="I/O Cache的一些思考"></a>I/O Cache的一些思考</h3><p><a href="https://www.zhihu.com/question/26190832/answer/32387918" target="_blank" rel="noopener">Buffer和Cache的区别是什么？</a><br>1、<code>Buffer</code>（缓冲区）是系统两端处理速度平衡（从长时间尺度上看）时使用的。它的引入是为了减小短期内突发I/O的影响，起到流量整形的作用。比如生产者——消费者问题，他们产生和消耗资源的速度大体接近，加一个buffer可以抵消掉资源刚产生/消耗时的突然变化。<br>2、<code>Cache</code>（缓存）则是系统两端处理速度不匹配时的一种折衷策略。因为CPU和memory之间的速度差异越来越大，所以人们充分利用数据的局部性（locality）特征，通过使用存储系统分级（memory hierarchy）的策略来减小这种差异带来的影响。</p>
<p>Buffer和Cache需要同步吗？<br>buffer和cache在Linux 2.4之前是两种缓存，也就是说同一份数据有两份内容在内核中。这两份数据的同步和维护其实带来了一些麻烦。在Linux 2.4之后，人们想到了统一这两种缓存，就把buffer指向了cache，使得数据只剩下一份实体。对于为什么是cache作为了主要的语义，参见<a href="https://www.quora.com/What-is-the-major-difference-between-the-buffer-cache-and-the-page-cache-Why-were-they-separate-entities-in-older-kernels-Why-were-they-merged-later-on" target="_blank" rel="noopener">这里</a>。</p>
<h3 id="IO栈"><a href="#IO栈" class="headerlink" title="IO栈"></a>IO栈</h3><p>我们上面只说明了内核中的文件系统做的操作，在写入磁盘之前还有几个步骤。<a href="https://www.0xffffff.org/2017/05/01/41-linux-io/" target="_blank" rel="noopener">Linux下的IO栈</a>致大致有三个层次：</p>
<ol>
<li>文件系统层，以<code>write(2)</code>为例，内核拷贝了<code>write(2)</code>参数指定的用户态数据到Page Cache中，并适时向下层同步</li>
<li>块层，管理块设备的IO队列，对IO请求进行合并、排序（IO调度算法和blk-mq都在这一层）</li>
<li>设备层，通过DMA（一种磁盘、网卡等直接写内存的快速通道，不需要CPU参与）与内存直接交互，完成数据和具体设备之间的交互</li>
</ol>
<p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/linux/linux-io.png" alt="IO分层"></p>
<p>传统的Buffered IO使用<code>read(2)</code>读取文件的过程什么样的？假设要去读一个冷文件（Cache中不存在）</p>
<ol>
<li><code>open(2)</code>打开文件内核后建立了一系列的数据结构</li>
<li>接下来调用<code>read(2)</code>，到达文件系统这一层，发现Page Cache中不存在该位置的磁盘映射，然后创建相应的Page Cache并和相关的扇区关联。</li>
<li>然后请求继续到达块设备层，在IO队列里排队，接受一系列的调度后到达设备驱动层，此时一般使用DMA方式读取相应的磁盘扇区拷贝到Page Cache中</li>
<li>然后<code>read(2)</code>拷贝数据到用户提供的用户态buffer中去（<code>read(2)</code>的参数指出的）。<br>可以看到，一次read总共进行了三次拷贝。相对地，我们以socket收发一次数据为例，看看需要什么成本：<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/linux/linux_copy.png" alt="IO拷贝"><br>图中总共进行了4次拷贝（其中两次是DMA拷贝，CPU没参与），4次上下文切换。大量数据的拷贝，用户态和内核态的频繁切换，会消耗大量的 CPU 资源，严重影响数据传输的性能，有数据表明，在Linux内核协议栈中，这个拷贝的耗时甚至占到了数据包整个处理流程的57.1%。这就是I/O加速带给我们的代价。</li>
</ol>
<h2 id="stdio"><a href="#stdio" class="headerlink" title="stdio"></a>stdio</h2><p>上面可以看到，对系统进行和磁盘快大小整数倍的读写时，I/O的效率最高。仅仅是内核的I/O优化还不够，需要对普通文件执行许多轻量级I/O请求的程序通常使用用户缓冲I/O。用户缓冲I/O是在用户空间而不是在内核中完成的，它可以在程序中设定，也可以调用标准库透明地执行。一般程序可选的I/O优化有几种：</p>
<ol>
<li>直接使用内核提供的<code>write</code>和<code>read</code>系统调用。</li>
<li>使用stdio提供的<code>fgetc fgets fread fputc fputs fwrite</code>等带用户态缓冲区的标准库函数。</li>
<li>自己实现用户态缓冲区，用自己的用户态缓冲区+<code>write</code>和<code>read</code>系统调用。</li>
<li>自己实现用户态缓冲区和内核缓冲区，使用<code>Direct I/O</code>读写磁盘。<br>用户态缓冲区的基本思想是，如果是写请求，先在缓冲区存一份数据，然后在一个缓冲区满的时候再调用write写进去。最合适的缓冲区大小是和磁盘块对齐的，一般4096或者8192的大小速度就会达到最快。如果是读请求，缓冲区就会成块读取数据，当一个块被读完后再预先读一块。无论读取的时候有没有对齐，缓冲区总会对齐了读。</li>
</ol>
<h3 id="设置缓冲区"><a href="#设置缓冲区" class="headerlink" title="设置缓冲区"></a>设置缓冲区</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">int setvbuf (FILE *stream, char *buf, int mode, size_t size)</span><br><span class="line">// 1. 该函数设置流的缓冲类型模式，模式必须是以下的一种：</span><br><span class="line">IONBF //无缓冲 每输入一个字符，就刷入内核缓冲区</span><br><span class="line">IOLBF //行缓冲 遇到换行符，就刷入内核缓冲区</span><br><span class="line">IOFBF //块缓冲 默认的文件缓冲。每写满一个磁盘块就刷入一次</span><br><span class="line">// 2. buf可以设置为自定义的缓冲区。如果不需要就设置为空，系统会自动分配。</span><br><span class="line">// 3. 要特别注意buf的生命周期。buf必须在流刚打开时就设置。另外，buf必须在流关闭时依然没被回收。</span><br></pre></td></tr></table></figure>
<h3 id="清洗缓冲区"><a href="#清洗缓冲区" class="headerlink" title="清洗缓冲区"></a>清洗缓冲区</h3><p><code>int fflush(FILE *stream)</code> 会把stream指向的流中的所有未写入的数据会被清洗到内核中。如果stream是NULL，所有进程打开的流会被清洗掉。</p>
<ol>
<li><code>fflush</code>只是把用户缓冲的数据直接调用<code>write</code>写入到内核缓冲区。这并不保证数据能够写入物理介质，如果需要的话，请使用<code>fsync</code>这一类函数。</li>
<li>一般可以在调用<code>fflush</code>后，立即调用<code>fsync</code>，这样可以直接刷到磁盘上。主要注意用户态的<code>fwrite</code>一类的函数一定不要和write一类的系统调用同时使用。</li>
</ol>
<h2 id="零拷贝"><a href="#零拷贝" class="headerlink" title="零拷贝"></a>零拷贝</h2><p>之前的章节可以看到，内核中的I/O缓存虽然给我们带来了磁盘读写速度的便利，但是确牺牲了很多的CPU时间。其实有很多拷贝不是必须的，我们也有一些手段去避免无用的反复拷贝和上下文切换。</p>
<h3 id="mmap"><a href="#mmap" class="headerlink" title="mmap"></a>mmap</h3><p>我们可以用mmap把数据直接映射到内核的Page Cache，这样就可以少一次用户态到内核的拷贝：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">tmp_buf = mmap(file, len);</span><br><span class="line">write(socket, tmp_buf, len);</span><br></pre></td></tr></table></figure></p>
<p><a href="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/linux/copy1.png" target="_blank" rel="noopener">mmap copy</a><br>图中可以看到，DMA引擎将文件内容复制到内核缓冲区中，然后与用户进程共享缓冲区。这不会在内核和用户存储器空间之间执行任何复制。<br>mmap是有代价的。例如，当你的程序map了一个文件，但是当这个文件被另一个进程截断(truncate)时, write系统调用会因为访问非法地址而被SIGBUS信号终止。SIGBUS信号默认会杀死你的进程并产生一个coredump,如果你的服务器这样被中止了，那会产生一笔损失。<br>通常我们使用以下解决方案避免这种问题：</p>
<ol>
<li>处理SIGBUS信号 当遇到SIGBUS信号时，信号处理程序简单地返回，write系统调用在被中断之前会返回已经写入的字节数，并且errno会被设置成success。但是这是一种糟糕的处理办法，因为你并没有解决问题的实质核心。</li>
<li>使用文件租借锁 我们为文件向内核申请一个租借锁，当其它进程想要截断这个文件时，内核会向我们发送一个实时的<code>RT_SIGNAL_LEASE</code>信号，告诉我们内核正在破坏你加持在文件上的读写锁。这样在程序访问非法内存并且被SIGBUS杀死之前，你的write系统调用会被中断。write会返回已经写入的字节数，并且置errno为success。</li>
</ol>
<h3 id="sendfile"><a href="#sendfile" class="headerlink" title="sendfile"></a>sendfile</h3><p>如果是给套接字发数据，sendfile提供了一种从一个fd中读取内容写入到另一个fd的方式。<br><code>ssize_t sendfile(int out_fd, int in_fd, off_t *offset, size_t count)</code> </p>
<ol>
<li>描述符<code>out_fd</code>必须指向一个套接字，而<code>in_fd</code>指向的文件必须是可以mmap的(sendfile只能将数据从文件传递到套接字上) 。</li>
<li><code>offset</code>标识了从<code>in_fd</code>中读取的位置，<code>count</code>则表明读取的长度。<br>如果是管道，可以用下面的splice（两个fd至少有一个是管道）：<br><code>ssize_t splice(int fd_in, loff_t *off_in, int fd_out, loff_t *off_out, size_t len, unsigned int flags)</code></li>
</ol>
<p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/linux/copy1.png" alt="sendfile copy"><br>图中可以看到，<code>sendfile</code>可以直接把文件从内核缓冲区拷贝到socket缓冲区。虽然也是三次复制，但是少了一次陷入用户态的上下文切换。在我们调用<code>sendfile</code>时，如果有其它进程截断了文件会发生什么呢？假设我们没有设置任何信号处理程序，<code>sendfile</code>调用仅仅返回它在被中断之前已经传输的字节数，errno会被置为success。如果我们在调用<code>sendfile</code>之前给文件加了锁，<code>sendfile</code>的行为仍然和之前相同，我们还会收到<code>RT_SIGNAL_LEASE</code>的信号</p>
<h3 id="sendfile-with-DMA"><a href="#sendfile-with-DMA" class="headerlink" title="sendfile with DMA"></a>sendfile with DMA</h3><p>常规 <code>sendfile</code> 还有一次内核态的拷贝操作，能不能也把这次拷贝给去掉呢？<br>答案就是这种 <code>DMA</code> 辅助的 <code>sendfile</code>。<br>这种方法借助硬件的帮助，在数据从内核缓冲区到 socket 缓冲区这一步操作上，并不是拷贝数据，而是拷贝缓冲区描述符，待完成后，<code>DMA</code> 引擎直接将数据从内核缓冲区拷贝到协议引擎中去，避免了最后一次拷贝。<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/linux/copy3.png" alt="enter image description here"></p>
<h3 id="写时复制（COW），fbuf，netmap"><a href="#写时复制（COW），fbuf，netmap" class="headerlink" title="写时复制（COW），fbuf，netmap"></a>写时复制（COW），fbuf，netmap</h3><p>一些其他的零拷贝技术，参见<br><a href="https://zhuanlan.zhihu.com/p/76640160" target="_blank" rel="noopener">知乎上的零拷贝</a><br><a href="https://www.linuxjournal.com/article/6345" target="_blank" rel="noopener">Zero Copy I: User-Mode Perspective</a><br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/linux/zerocopy.png" alt="零拷贝"></p>
]]></content>
    
    <summary type="html">
    
      Linux中一切皆文件。但是内核中设计了复杂的I/O缓存和文件结构，在带来方便的同时也带来了拷贝的成本。本文介绍了文件的基本API和数据结构，还有内核和标准库在I/O加速上所做的努力，以及如何灵活避免这些加速，做到零拷贝。
    
    </summary>
    
      <category term="Linux" scheme="chestnutheng.cn/categories/Linux/"/>
    
    
      <category term="Linux" scheme="chestnutheng.cn/tags/Linux/"/>
    
      <category term="文件" scheme="chestnutheng.cn/tags/%E6%96%87%E4%BB%B6/"/>
    
  </entry>
  
  <entry>
    <title>[Linux]Linux socket API</title>
    <link href="chestnutheng.cn/2019/10/21/socket/"/>
    <id>chestnutheng.cn/2019/10/21/socket/</id>
    <published>2019-10-21T09:45:09.000Z</published>
    <updated>2019-10-21T09:45:14.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="创建和关闭"><a href="#创建和关闭" class="headerlink" title="创建和关闭"></a>创建和关闭</h2><p><code>int socket(int domain, int type, int protocol)</code> 创建一个socket。</p>
<ol>
<li><p><code>domain</code> 指定了通信的特性。<code>AF_UNIX</code> Unix域，<code>AF_INET</code> IPv4域。 <code>AF_INET6</code> IPv6域。</p>
</li>
<li><p><code>type</code> 指定了连接的类型。<br><code>SOCK_STREAM</code>     有序、可靠、双向、面向连接的字节流 TCP<br><code>SOCK_DGRAM</code>      不可靠、无连接、固定长度的报文 UDP<br><code>SOCK_SEQPACKET</code>  固定长度、有序、可靠、双向、面向连接的字节流</p>
</li>
<li><p><code>protocol</code> 为0时，选择指定域的默认协议。<code>AF_INET</code>域的<code>SOCK_STREAM</code>默认的协议为TCP，<code>SOCK_DGRAM</code>则为UDP。还有<code>IPPROTO_ICMP</code>、<code>IPPROTO_IP</code>、<code>IPPROTO_RAW</code>等。<code>SOCK_STREAM</code>提供字节流服务，所以程序分不出报文的界限。</p>
</li>
</ol>
<p><code>int shutdown(int socketfd, int how)</code> 关闭一个socket。</p>
<ol>
<li>套接字函数是双向的。可以用这个函数关闭套接字的某个方向的IO。</li>
<li><code>how</code>是<code>SHUT_RD</code>表示关闭读端，无法从套接字读取数据。<code>SHUT_WR</code>是关闭写端，无法向套接字写入数据。<code>SHUT_RDWR</code>无法读也无法写。</li>
<li><code>close()</code>是一个fd的通用释放函数。他和<code>shutdown</code>有何不同？1) <code>close</code>要等所有的活动引用关闭后才释放套接字。这使得<code>dup</code>之后的套接字必须等待所有的引用释放。但是<code>shutdown</code>和引用fd数量无关。2)<code>shutdown</code>可以方便地关闭读写的任何一端。</li>
</ol>
<h2 id="网络地址"><a href="#网络地址" class="headerlink" title="网络地址"></a>网络地址</h2><h3 id="地址的结构体"><a href="#地址的结构体" class="headerlink" title="地址的结构体"></a>地址的结构体</h3><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">// Linux 套接字</span><br><span class="line">// 这是Linux定义的一个通用结构。所有地址都可以强转为这个结构体，比如IPV4、IPV6等，便于使用。</span><br><span class="line">struct sockaddr &#123;</span><br><span class="line">        u_short sa_family;              /* address family */</span><br><span class="line">        char    sa_data[14];            /* up to 14 bytes of direct address */</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">// IPv4 地址</span><br><span class="line">// 1. in_port_t 是16位的网络序整数，需要htons转换到网络序再赋值</span><br><span class="line">// 2. sin_addr 有几种常见的常量，INADDR_ANY表示所有网卡</span><br><span class="line">sockaddr_in &#123;</span><br><span class="line">    sa_family_t sin_family;</span><br><span class="line">    in_port_t sin_port;			    /* Port number.  (typedef uint16_t in_port_t)*/</span><br><span class="line">    struct in_addr sin_addr;		/* Internet address.  （typedef uint32_t in_addr_t)*/</span><br><span class="line">    unsigned char sin_zero[8];		/* sockaddr 里sa_data定义的14(sa_data)-2(sin_port)-4(sin_addr)=8字节*/</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="网络序"><a href="#网络序" class="headerlink" title="网络序"></a>网络序</h3><p>下面的函数提供了网络序端口和主机序端口（int类型）转换的方法。<br><code>uint32_t htonl(uint32_t hostint32)</code>返回以网络序（大端序）表示的整数。<br><code>uint16_t htons(uint16_t hostint32)</code><br><code>uint32_t ntohl(uint32_t netint32)</code> 返回以主机序（小端序）表示的整数。<br><code>uint16_t ntohs(uint16_t netint32)</code></p>
<h3 id="地址和字符串的转换"><a href="#地址和字符串的转换" class="headerlink" title="地址和字符串的转换"></a>地址和字符串的转换</h3><p><code>const char *inet_ntop(int af, const void *src, char *dst, socklen_t size)</code>地址到字符串。</p>
<p><code>int inet_pton(int af, const char *src, void *dst)</code> 字符串到地址。</p>
<ol>
<li>其中，<code>af</code>有<code>AF_INET</code>和<code>AF_INET6</code>两个值。<code>src</code>是地址，<code>dst</code>是输出的字符串。<code>size</code>是字符串大小。</li>
<li>这两个函数的工作是把二进制和字符串互转，例如将串<code>192.168.33.123</code> 转为 <code>1100 0000  1010 1000  0010 0001  0111 1011</code>。每八位代表IP地址中的一段，比如末尾的<code>0111 1011</code>就是<code>123</code>。<br>一个例子：<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> in_addr addr;</span><br><span class="line"><span class="keyword">if</span>(inet_pton(AF_INET, <span class="string">"127.0.0.1"</span>, &amp;addr.s_addr) == <span class="number">1</span>)</span><br><span class="line">       <span class="built_in">printf</span>(<span class="string">"NetIP: %x\n"</span>, addr.s_addr);    <span class="comment">//NetIP: 100007f</span></span><br><span class="line"><span class="keyword">char</span> str[<span class="number">20</span>];</span><br><span class="line"><span class="keyword">if</span>(inet_ntop(AF_INET, &amp;addr.s_addr, str, <span class="keyword">sizeof</span> str))</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"StrIP: %s\n"</span>, str);    <span class="comment">//StrIP: 127.0.0.1</span></span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>一些老的函数，只能用于IPV4：<br><code>in_addr_t inet_addr(const char *cp)</code> 字符串直接初始化为地址。（仅用于IPV4）</p>
<p><code>int inet_aton(const char *cp, struct in_addr *inp)</code> 字符串到地址。（仅用于IPV4）</p>
<p><code>int inet_ntoa(const char *cp, struct in_addr *inp)</code> 地址到字符串。（仅用于IPV4）</p>
<h3 id="地址查询"><a href="#地址查询" class="headerlink" title="地址查询"></a>地址查询</h3><p><code>int getaddrinfo(const char *node, const char *service,
    const struct addrinfo *hints,
    struct addrinfo **res);</code></p>
<ol>
<li><code>node</code>可以是域名、IP，如 <code>www.qq.com</code></li>
<li><code>service</code>可以是<code>http</code>或者端口号，其定义在<code>/etc/services</code>文件中。这个文件的作用是使程序可以在其代码中进行<code>getportbyname</code>接字调用，以了解应使用的端口。例如，POP3电子邮件守护程序将执行<code>getportbyname(POP3)</code>，以检索运行POP3的数字110。 </li>
<li><code>hints</code> 是一系列选项。<code>res</code>是返回的地址结果。我们可以用下面的例子使用它：<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> _POSIX_SOURCE</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netdb.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> status;</span><br><span class="line">	<span class="keyword">struct</span> addrinfo hints, *res, *<span class="keyword">this</span>;</span><br><span class="line">	<span class="keyword">char</span> ipaddr[INET6_ADDRSTRLEN];</span><br><span class="line">	<span class="keyword">if</span> (argc != <span class="number">2</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"usage: showip hostname\n"</span>);</span><br><span class="line">		<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">memset</span>(&amp;hints, <span class="number">0</span>, <span class="keyword">sizeof</span> hints);</span><br><span class="line">	hints.ai_family = AF_UNSPEC;     <span class="comment">/* AF_INET(IPv4) AF_INET6(IPv6) */</span></span><br><span class="line">	hints.ai_socktype = SOCK_STREAM; <span class="comment">/* TCP stream sockets */</span></span><br><span class="line">	<span class="keyword">if</span> ((status = getaddrinfo(argv[<span class="number">1</span>], <span class="literal">NULL</span>, &amp;hints, &amp;res))) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"getaddrinfo: %s\n"</span>, gai_strerror(status));</span><br><span class="line">		<span class="keyword">return</span> <span class="number">2</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"IP addresses for %s:\n\n"</span>, argv[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span>(<span class="keyword">this</span> = res; <span class="keyword">this</span> != <span class="literal">NULL</span>; <span class="keyword">this</span> = <span class="keyword">this</span>-&gt;ai_next) &#123;</span><br><span class="line">		<span class="keyword">void</span> *addr;</span><br><span class="line">		<span class="keyword">char</span> *ipver;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>-&gt;ai_family == AF_INET) &#123; <span class="comment">/* IPv4 */</span></span><br><span class="line">			<span class="keyword">struct</span> sockaddr_in *ipv4;</span><br><span class="line">			ipv4 = (<span class="keyword">struct</span> sockaddr_in *)<span class="keyword">this</span>-&gt;ai_addr;</span><br><span class="line">			addr = &amp;(ipv4-&gt;sin_addr);</span><br><span class="line">			ipver = <span class="string">"IPv4"</span>;</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123; <span class="comment">/* IPv6 */</span></span><br><span class="line">			<span class="keyword">struct</span> sockaddr_in6 *ipv6;</span><br><span class="line">			ipv6 = (<span class="keyword">struct</span> sockaddr_in6 *)<span class="keyword">this</span>-&gt;ai_addr;</span><br><span class="line">			addr = &amp;(ipv6-&gt;sin6_addr);</span><br><span class="line">			ipver = <span class="string">"IPv6"</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="comment">/* convert the IP to a string and print it */</span></span><br><span class="line">		inet_ntop(<span class="keyword">this</span>-&gt;ai_family, addr, ipaddr, <span class="keyword">sizeof</span>(ipaddr));</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"%s:\t%s\n"</span>, ipver, ipaddr);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="绑定端口"><a href="#绑定端口" class="headerlink" title="绑定端口"></a>绑定端口</h2><p><code>int bind(int sockfd, const struct sockaddr *addr,  socklen_t addrlen)</code> 服务端给自己绑定一个众所周知的地址。</p>
<ol>
<li>一个端口一般只能绑定一个socket。</li>
<li>端口号必须大于1024，除非是root用户。</li>
</ol>
<p>如果没有bind，在connect或者listen时系统会自动bind。这时候可以用其他函数来查询socket的地址。下面两个函数，一个查本地的地址，一个查远端的地址。<br><code>int getsockname(int sockfd, struct sockaddr *addr, socklen_t *addrlen)</code> 查找一个本地的fd绑定的socket。<br><code>int getpeername(int sockfd, struct sockaddr *addr, socklen_t *addrlen)</code> 查找一个本地的fd连接的远程socket。<br>下面这段代码给了一个获取远端socket地址的简短例子。<br><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// client</span></span><br><span class="line"><span class="keyword">int</span> rc;</span><br><span class="line"><span class="keyword">int</span> sockfd;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> sockaddr_in addr;</span><br><span class="line">addr.sin_addr.s_addr = inet_addr(<span class="string">"127.0.0.1"</span>);</span><br><span class="line">addr.sin_family = AF_INET;</span><br><span class="line">addr.sin_port = htons(<span class="number">8888</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"Server ip=%s port=%d\n"</span>, inet_ntoa(addr.sin_addr), ntohs(addr.sin_port));</span><br><span class="line"></span><br><span class="line">sockfd = socket(AF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">rc = connect(sockfd, (<span class="keyword">const</span> <span class="keyword">struct</span> sockaddr *)&amp;addr, <span class="keyword">sizeof</span>(<span class="keyword">struct</span> sockaddr_in));</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> sockaddr_in svraddr;</span><br><span class="line"><span class="keyword">socklen_t</span> len = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> sockaddr_in);</span><br><span class="line">getsockname(sockfd,(<span class="keyword">struct</span> sockaddr *)&amp;svraddr, &amp;len);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"Local #%d ip=%s port=%d\n"</span>, sockfd,</span><br><span class="line">      inet_ntoa(svraddr.sin_addr), ntohs(svraddr.sin_port));</span><br><span class="line"></span><br><span class="line">close(sockfd);</span><br></pre></td></tr></table></figure></p>
<h2 id="有连接的socket的准备工作"><a href="#有连接的socket的准备工作" class="headerlink" title="有连接的socket的准备工作"></a>有连接的socket的准备工作</h2><h3 id="客户端-socket-connect-write-read-close"><a href="#客户端-socket-connect-write-read-close" class="headerlink" title="客户端 (socket-connect-write-read-close)"></a>客户端 (socket-connect-write-read-close)</h3><p>客户端的<code>write-read</code>这三步都是默认阻塞的。<code>socket-connect-close</code>会立即返回。<br><code>int connect(int sockfd, const struct sockaddr *addr, socklen_t addrlen)</code> 连接一个指定的服务器。</p>
<ol>
<li>如果socket是<code>STREAM</code>或者<code>SEQPACKET</code>，那么通信前就要建立连接。当然，也可用于<code>DGRAM</code>，虽然没连接，但是这样不用每次都指定一个服务端地址。</li>
<li>connect会为<code>sockfd</code>指定的socket自动绑定一个端口。</li>
<li>connect遇到对方服务器忙时，会立即返回-1。如果此时socket是非阻塞模式，会把errno设为EINPROGRESS。</li>
<li>如果connect失败，该socket可能变成<strong>未定义</strong>的。所以每次失败都必须关闭socket。</li>
<li><code>addrlen</code>是前面地址的长度。比如普通的IPv4地址可能是一个<code>sockaddr_in</code>的大小。</li>
</ol>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc , <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// socket</span></span><br><span class="line">    <span class="keyword">int</span> sockfd = <span class="number">0</span>;</span><br><span class="line">    sockfd = socket(AF_INET , SOCK_STREAM , <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sockfd == <span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Fail to create a socket."</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 服务端的地址</span></span><br><span class="line">    <span class="keyword">struct</span> sockaddr_in info;</span><br><span class="line">    bzero(&amp;info,<span class="keyword">sizeof</span>(info));</span><br><span class="line">    info.sin_family = PF_INET;</span><br><span class="line">    info.sin_addr.s_addr = inet_addr(<span class="string">"127.0.0.1"</span>);</span><br><span class="line">    info.sin_port = htons(<span class="number">8700</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// connect</span></span><br><span class="line">    <span class="keyword">int</span> err = connect(sockfd,(<span class="keyword">struct</span> sockaddr *)&amp;info,<span class="keyword">sizeof</span>(info));</span><br><span class="line">    <span class="keyword">if</span>(err==<span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Connection error"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 发送消息</span></span><br><span class="line">    <span class="keyword">char</span> message[] = &#123;<span class="string">"Hi there"</span>&#125;;</span><br><span class="line">    <span class="keyword">char</span> receiveMessage[<span class="number">100</span>] = &#123;&#125;;</span><br><span class="line">    send(sockfd,message,<span class="keyword">sizeof</span>(message),<span class="number">0</span>);</span><br><span class="line">    recv(sockfd,receiveMessage,<span class="keyword">sizeof</span>(receiveMessage),<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"%s"</span>,receiveMessage);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"close Socket\n"</span>);</span><br><span class="line">    close(sockfd);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="服务端（socket-bind-listen-accept-read-write-close）"><a href="#服务端（socket-bind-listen-accept-read-write-close）" class="headerlink" title="服务端（socket-bind-listen-accept-read-write-close）"></a>服务端（socket-bind-listen-accept-read-write-close）</h3><p>服务端的<code>accept-read-write</code>这三步都是默认阻塞的。<code>socket-bind-listen-close</code>会立即返回。<br><code>int listen(int sockfd, int backlog)</code> 使得socket处于可以接收连接的状态。</p>
<ol>
<li><code>sockfd</code> 指明谁在listen。</li>
<li><code>backlog</code>提示系统本线程还有多少连接需要建立。系统指定了等待连接的最大数量。如果队列满了，其他连接都会被丢弃。系统默认的net.core.somaxconn为128。</li>
</ol>
<p><code>int accept(int sockfd, struct sockaddr *addr, socklen_t *addrlen)</code> 接受一个连接。</p>
<ol>
<li><code>sockfd</code> 指明谁在accept。</li>
<li><code>sockaddr</code>和<code>addrlen</code>会填充为接受到的连接地址和长度。（对端的）</li>
<li>如果没有请求过来，会一直阻塞到有请求为止。如果此时socket是非阻塞模式，会立即返回-1，并把errno设为EWOULDBLOCK。</li>
</ol>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc , <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//socket</span></span><br><span class="line">    <span class="keyword">char</span> inputBuffer[<span class="number">256</span>] = &#123;&#125;;</span><br><span class="line">    <span class="keyword">char</span> message[] = &#123;<span class="string">"Hi,this is server.\n"</span>&#125;;</span><br><span class="line">    <span class="keyword">int</span> sockfd = <span class="number">0</span>,forClientSockfd = <span class="number">0</span>;</span><br><span class="line">    sockfd = socket(AF_INET , SOCK_STREAM , <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sockfd == <span class="number">-1</span>)&#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Fail to create a socket."</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// bind &amp; listen</span></span><br><span class="line">    <span class="keyword">struct</span> sockaddr_in serverInfo,clientInfo;</span><br><span class="line">    <span class="keyword">int</span> addrlen = <span class="keyword">sizeof</span>(clientInfo);</span><br><span class="line">    bzero(&amp;serverInfo,<span class="keyword">sizeof</span>(serverInfo));</span><br><span class="line">    serverInfo.sin_family = PF_INET;</span><br><span class="line">    serverInfo.sin_addr.s_addr = INADDR_ANY;</span><br><span class="line">    serverInfo.sin_port = htons(<span class="number">8700</span>);</span><br><span class="line">    </span><br><span class="line">    bind(sockfd,(<span class="keyword">struct</span> sockaddr *)&amp;serverInfo,<span class="keyword">sizeof</span>(serverInfo));</span><br><span class="line">    listen(sockfd,<span class="number">5</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 收发消息</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">        forClientSockfd = accept(sockfd,(<span class="keyword">struct</span> sockaddr*) &amp;clientInfo, &amp;addrlen);</span><br><span class="line">        recv(forClientSockfd,inputBuffer,<span class="keyword">sizeof</span>(inputBuffer),<span class="number">0</span>);</span><br><span class="line">        send(forClientSockfd,message,<span class="keyword">sizeof</span>(message),<span class="number">0</span>);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"sended: %s\n"</span>, message);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="收发数据"><a href="#收发数据" class="headerlink" title="收发数据"></a>收发数据</h2><p><code>ssize_t send(int sockfd, const void *buf, size_t len, int flags)</code> 发送数据</p>
<ol>
<li>尽管可以使用write和read操作socket，但是send提供了更多的选项。</li>
<li><code>buf</code>和<code>len</code>提供了发送的数据包。</li>
<li><code>flags</code>提供了发送的选项。比如<code>MSG_DONTROUTE</code>不路由出本地，<code>MSG_DONTWAIT</code>允许非阻塞操作, <code>MSG_EOF</code> 写完后关闭服务端。</li>
<li><code>send</code>会返回发送成功的字节数。如果send成功返回，则表示已经成功发送到了网络驱动程序上。如果失败，返回-1。</li>
<li>对于字节流（TCP）协议，send会阻塞到报文传输完成。对于有报文最大长度限制的协议，send到达限制后会返回-1，设置错误码为EMSGSIZE。</li>
</ol>
<p><code>ssize_t sendto(int sockfd, const void *buf, size_t len, int flags, const struct sockaddr *dest_addr, socklen_t addrlen)</code> 和<code>send</code>功能一样，但是可以指定地址。</p>
<p><code>ssize_t recv(int sockfd, void *buf, size_t len, int flags)</code>  接受数据</p>
<ol>
<li><code>buf</code>和<code>len</code>提供了发送的数据包。</li>
<li><code>flags</code>提供了发送的选项。比如<code>MSG_PEEK</code>只会读取数据，但是不会改动，<code>MSG_DONTWAIT</code>允许非阻塞操作, <code>MSG_EOF</code> 写完后关闭服务端。</li>
<li><code>recv</code>会返回接受到的字节数。如果对端已经调用shutdown或者协议已经自动关闭，recv会返回0。</li>
</ol>
<p><code>ssize_t recvfrom(int sockfd, void *buf, size_t len, int flags, struct sockaddr *src_addr, socklen_t *addrlen)</code> 可以额外获取发送者的地址。地址会被写入<code>src_addr</code>对应的内存中。</p>
]]></content>
    
    <summary type="html">
    
      socket支持传输层的各种协议，是网络通信的基础组件。本文介绍了socket的创建和关闭、通信、socket地址的结构和获取等内容，并提供了有连接和无连接socket的示例。
    
    </summary>
    
      <category term="Linux" scheme="chestnutheng.cn/categories/Linux/"/>
    
    
      <category term="Linux" scheme="chestnutheng.cn/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>[中间件]RocketMQ的架构和设计</title>
    <link href="chestnutheng.cn/2019/09/04/rocketmq/"/>
    <id>chestnutheng.cn/2019/09/04/rocketmq/</id>
    <published>2019-09-04T11:00:56.000Z</published>
    <updated>2019-11-19T07:48:10.000Z</updated>
    
    <content type="html"><![CDATA[<p>主要整理文献：<br><a href="https://github.com/apache/rocketmq/blob/master/docs/cn/design.md" target="_blank" rel="noopener">RocketMQ部署架构和技术架构 - Github</a><br><a href="https://github.com/apache/rocketmq/blob/master/docs/cn/design.md" target="_blank" rel="noopener">RocketMQ关键机制的设计原理 - Github</a><br><a href="http://gd-rus-public.cn-hangzhou.oss-pub.aliyun-inc.com/attachment/201604/08/20160408165024/RocketMQ_design.pdf" target="_blank" rel="noopener">RocketMQ 原理简介 - 淘宝消息中间件项目组</a></p>
<h1 id="设计理念和部署"><a href="#设计理念和部署" class="headerlink" title="设计理念和部署"></a>设计理念和部署</h1><h2 id="消息队列需要解决的问题"><a href="#消息队列需要解决的问题" class="headerlink" title="消息队列需要解决的问题"></a>消息队列需要解决的问题</h2><ol>
<li><strong>发布/订阅</strong> 最基础的需求，可以做解耦&amp;聚合，如果用Redis做，不够可靠</li>
<li>支持<strong>优先级队列、延时队列</strong></li>
<li><strong>顺序消费</strong>，rockmq严格有序</li>
<li>支持<strong>消息过滤</strong>，Producer和consumer共同过滤</li>
<li><strong>持久化</strong> 内存缓存+文件</li>
<li><strong>异常恢复</strong><br>broker crash，os crash，掉电 —保证消息不丢，或者丢失少量数据（依赖刷盘方式是同步还是异步）<br>磁盘损坏，机器永久损坏  —通过异步复制，可保证99%的消息不丢</li>
<li><strong>实时性</strong> RocketMQ使用长轮询Pull方式，可保证消息非常实时，消息实时性不低于Push。</li>
<li><strong>At least Once</strong> 和 Exactly Only Once， 至少消费一次且只消费一次</li>
<li>broker的<strong>buffer容量问题</strong>。RocketMQ 的内存Buffer持久化在硬盘，抽象成一个无限长度的队列，不管有多少数据进来都能装得下，当然也会定时清理。</li>
<li><strong>回溯消费</strong> 一般是按照时间维度，例如由于 Consumer 系统故障，恢复后需要重新消费 1 小时前的数据，那么 Broker 要提供一种机制，可以按照时间维度来回退消费进度。<br>RocketMQ 支持按照时间回溯消费，时间维度精确到毫秒，可以向前回溯，也可以向后回溯。</li>
<li><strong>消息堆积</strong> 消息堆积在内存Buffer，一旦超过内存Buffer，可以根据一定的丢弃策略来丢弃消息，对性能影响不大，但是不能堆积太多<br>消息堆积到持久化存储系统中，例如DB，KV存储，文件记录形式。 当消息不能在内存Cache命中时，要不可避免的访问磁盘，会产生大量读IO，读IO的吞吐量直接决定了消息堆积后的访问能力。</li>
<li><strong>消息重试</strong> 消息重试有两种原因，一种是消息本身处理失败，如编码有问题等，重试永远不会成功。另一部分是处理消息依赖的下游服务暂时不可用，一段时间重试后可以成功。所以可以消极重试，逐步重试增大等待重试间隔。</li>
</ol>
<h2 id="RockMQ-模块"><a href="#RockMQ-模块" class="headerlink" title="RockMQ 模块"></a>RockMQ 模块</h2><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq/rocketmq_architecture_1.png"> </p>
<ol>
<li><p><strong>Name Server</strong> ：NameServer是一个非常简单的Topic路由注册中心，其角色类似Dubbo中的zookeeper，支持Broker的动态注册与发现。<br>(1) 路由管理<br>Broker管理：NameServer接受Broker集群的注册信息并且保存下来作为路由信息的基本数据。然后提供心跳检测机制，检查Broker是否还存活；<br>路由信息管理：每个NameServer将保存关于Broker集群的整个路由信息和用于客户端查询的队列信息。然后Producer和Conumser通过NameServer就可以知道整个Broker集群的路由信息，找到对应topic的路由信息，从而进行消息的投递和消费。<br>(2) 无状态：NameServer通常也是集群的方式部署，各实例间相互不进行信息通讯。它是一个几乎无状态的结点，他们之间互不通信。Broker是向每一台NameServer注册自己的路由信息，所以每一个NameServer实例上面都保存一份完整的路由信息。当某个NameServer因某种原因下线了，Broker仍然可以向其它NameServer同步其路由信息，Producer,Consumer仍然可以动态感知Broker的路由的信息。<br>(3) 随机选择：客户端连接时，会随机选择。<br>(4) 长连接：Broker向所有的NameServer结点建立长连接，注册Topic信息。Producer和Consumer也是长连接。</p>
</li>
<li><p><strong>Broker</strong>：处理消息存储，转发等处理的服务器。<br>(0) 分Group：Broker以group分开，每个group只允许一个master，若干个slave。<br>(1) 读写分离：只有master才能进行写入操作，slave不允许。<br>(2) 主从同步：slave从master中同步数据。同步策略取决于master的配置，可以采用同步双写，异步复制两种。<br>(3) 默认消费：在默认情况下，消费者都从master消费，只有master挂掉或者产生消息堆积了才从slave消费。<br>Broker有下面几个重要的子模块：<br>(1) Remoting Module：整个Broker的实体，负责处理来自clients端的请求。<br>(2) Client Manager：负责管理客户端(Producer/Consumer)和维护Consumer的Topic订阅信息<br>(3) Store Service：提供方便简单的API接口处理消息存储到物理硬盘和查询功能。<br>(4) HA Service：高可用服务，提供Master Broker 和 Slave Broker之间的数据同步功能。<br>(5) Index Service：根据特定的Message key对投递到Broker的消息进行索引服务，以提供消息的快速查询。</p>
</li>
<li><strong>Producer</strong>：消息发布的角色，支持分布式集群方式部署。Producer通过MQ的负载均衡模块选择相应的Broker集群队列进行消息投递，投递的过程支持快速失败并且低延迟。和NameServer、master都建立长连接，从NameServer拉取topic信息，给master发送心跳。完全无状态</li>
<li><strong>Consumer</strong>：消息消费的角色，支持分布式集群方式部署。支持以push推，pull拉两种模式对消息进行消费。同时也支持集群方式和广播方式的消费，它提供实时消息订阅机制，可以满足大多数用户的需求。和NameServer、master、slave都建立长连接，从NameServer拉取topic信息，给master、slave发送心跳。主备都可以订阅消息，订阅的对象由broker决定。</li>
</ol>
<h2 id="网络部署特点"><a href="#网络部署特点" class="headerlink" title="网络部署特点"></a>网络部署特点</h2><ol>
<li><strong>NameServer</strong>是一个几乎无状态节点，可集群部署，节点之间无任何信息同步。</li>
<li><strong>Broker</strong>部署相对复杂，Broker分为Master与Slave，一个Master可以对应多个Slave，但是一个Slave只能对应一个Master，Master与Slave 的对应关系通过指定相同的BrokerName，不同的BrokerId 来定义，BrokerId为0表示Master，非0表示Slave。Master也可以部署多个。每个Broker与NameServer集群中的所有节点建立长连接，定时注册Topic信息到所有NameServer。 注意：当前RocketMQ版本在部署架构上支持一Master多Slave，但只有BrokerId=1的从服务器才会参与消息的读负载。</li>
<li><strong>Producer</strong>与NameServer集群中的其中一个节点（随机选择）建立长连接，定期从NameServer获取Topic路由信息，并向提供Topic 服务的Master建立长连接，且定时向Master发送心跳。Producer完全无状态，可集群部署。</li>
<li><strong>Consumer</strong>与NameServer集群中的其中一个节点（随机选择）建立长连接，定期从NameServer获取Topic路由信息，并向提供Topic服务的Master、Slave建立长连接，且定时向Master、Slave发送心跳。Consumer既可以从Master订阅消息，也可以从Slave订阅消息，消费者在向Master拉取消息时，Master服务器会根据拉取偏移量与最大偏移量的距离（判断是否读老消息，产生读I/O），以及从服务器是否可读等因素建议下一次是从Master还是Slave拉取。</li>
</ol>
<h2 id="网络模块的工作流程"><a href="#网络模块的工作流程" class="headerlink" title="网络模块的工作流程"></a>网络模块的工作流程</h2><ol>
<li>启动NameServer，NameServer起来后监听端口，等待Broker、Producer、Consumer连上来，相当于一个路由控制中心。</li>
<li>Broker启动，跟所有的NameServer保持长连接，定时发送心跳包。心跳包中包含当前Broker信息(IP+端口等)以及存储所有Topic信息。注册成功后，NameServer集群中就有Topic跟Broker的映射关系。</li>
<li>收发消息前，先创建Topic，创建Topic时需要指定该Topic要存储在哪些Broker上，也可以在发送消息时自动创建Topic。</li>
<li>Producer发送消息，启动时先跟NameServer集群中的其中一台建立长连接，并从NameServer中获取当前发送的Topic存在哪些Broker上，轮询从队列列表中选择一个队列，然后与队列所在的Broker建立长连接从而向Broker发消息。</li>
<li>Consumer跟Producer类似，跟其中一台NameServer建立长连接，获取当前订阅Topic存在哪些Broker上，然后直接跟Broker建立连接通道，开始消费消息。</li>
</ol>
<h2 id="模块的通信机制"><a href="#模块的通信机制" class="headerlink" title="模块的通信机制"></a>模块的通信机制</h2><p>RocketMQ消息队列集群主要包括NameServe、Broker(Master/Slave)、Producer、Consumer4个角色，基本通讯流程如下：<br>(1) Broker启动后需要完成一次将自己注册至NameServer的操作；随后每隔30s时间定时向NameServer上报Topic路由信息。<br>(2) 消息生产者Producer作为客户端发送消息时候，需要根据消息的Topic从本地缓存的TopicPublishInfoTable获取路由信息。如果没有则更新路由信息会从NameServer上重新拉取，同时Producer会默认每隔30s向NameServer拉取一次路由信息。<br>(3) 消息生产者Producer根据2）中获取的路由信息选择一个队列（MessageQueue）进行消息发送；Broker作为消息的接收者接收消息并落盘存储。<br>(4) 消息消费者Consumer根据2）中获取的路由信息，并再完成客户端的负载均衡后，选择其中的某一个或者某几个消息队列来拉取消息并进行消费。<br>从上面1）~3）中可以看出在消息生产者, Broker和NameServer之间都会发生通信（这里只说了MQ的部分通信），因此如何设计一个良好的网络通信模块在MQ中至关重要，它将决定RocketMQ集群整体的消息传输能力与最终的性能。</p>
<h1 id="消息存储"><a href="#消息存储" class="headerlink" title="消息存储"></a>消息存储</h1><h2 id="消息存储结构-磁盘"><a href="#消息存储结构-磁盘" class="headerlink" title="消息存储结构(磁盘)"></a>消息存储结构(磁盘)</h2><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq/mq_file.png" alt="File"><br>(1) <strong>CommitLog</strong>：消息主体以及元数据的存储主体，存储Producer端写入的消息主体内容,消息内容不是定长的。单个文件大小默认1G ，文件名长度为20位，左边补零，剩余为起始偏移量，比如00000000000000000000代表了第一个文件，起始偏移量为0，文件大小为1G=1073741824；当第一个文件写满了，第二个文件为00000000001073741824，起始偏移量为1073741824，以此类推。消息主要是顺序写入日志文件，当文件满了，写入下一个文件；<br>(2) <strong>ConsumeQueue</strong>：消息消费队列，引入的目的主要是提高消息消费的性能，由于RocketMQ是基于主题topic的订阅模式，消息消费是针对主题进行的，如果要遍历commitlog文件中根据topic检索消息是非常低效的。Consumer即可根据ConsumeQueue来查找待消费的消息。其中，ConsumeQueue（逻辑消费队列）作为消费消息的索引，保存了指定Topic下的队列消息在CommitLog中的起始物理偏移量offset，消息大小size和消息Tag的HashCode值。consumequeue文件可以看成是基于topic的commitlog索引文件，故consumequeue文件夹的组织方式如下：topic/queue/file三层组织结构，具体存储路径为：`$HOME/store/consumequeue/{topic}/{queueId}/{fileName}。同样consumequeue文件采取定长设计，每一个条目共20个字节，分别为8字节的commitlog物理偏移量、4字节的消息长度、8字节tag hashcode，单个文件由30W个条目组成，可以像数组一样随机访问每一个条目，每个ConsumeQueue文件大小约5.72M；<br>(3) <strong>IndexFile</strong>：索引文件提供了一种可以通过key或时间区间来查询消息的方法。Index文件的存储位置是：$HOME/store/index/{fileName}，文件名fileName是以创建时的时间戳命名的，固定的单个IndexFile文件大小约为400M，一个IndexFile可以保存 2000W个索引，IndexFile的底层存储设计为在文件系统中实现HashMap结构，故rocketmq的索引文件其底层实现为hash索引。（具体的文件设计见下面的<strong>消息查询</strong>）</p>
<p>在上面的RocketMQ的消息存储整体架构图中可以看出，RocketMQ采用的是混合型的存储结构，即为Broker单个实例下所有的队列共用一个日志数据文件（即为CommitLog）来存储。RocketMQ的混合型存储结构(多个Topic的消息实体内容都存储于一个CommitLog中)针对Producer和Consumer分别采用了数据和索引部分相分离的存储结构，Producer发送消息至Broker端，然后Broker端使用同步或者异步的方式对消息刷盘持久化，保存至CommitLog中。只要消息被刷盘持久化至磁盘文件CommitLog中，那么Producer发送的消息就不会丢失。正因为如此，Consumer也就肯定有机会去消费这条消息。当无法拉取到消息后，可以等下一次消息拉取，同时服务端也支持长轮询模式，如果一个消息拉取请求未拉取到消息，Broker允许等待30s的时间，只要这段时间内有新消息到达，将直接返回给消费端。这里，RocketMQ的具体做法是，使用Broker端的后台服务线程—ReputMessageService不停地分发请求并异步构建ConsumeQueue（逻辑消费队列）和IndexFile（索引文件）数据。</p>
<h2 id="内存缓存-PageCache"><a href="#内存缓存-PageCache" class="headerlink" title="内存缓存 PageCache"></a>内存缓存 PageCache</h2><p>页缓存（PageCache)是OS对文件的缓存，用于加速对文件的读写。一般来说，程序对文件进行顺序读写的速度几乎接近于内存的读写速度，主要原因就是由于OS使用PageCache机制对读写访问操作进行了性能优化，将一部分的内存用作PageCache。对于数据的写入，OS会先写入至Cache内，随后通过异步的方式由pdflush内核线程将Cache内的数据刷盘至物理磁盘上。对于数据的读取，如果一次读取文件时出现未命中PageCache的情况，OS从物理磁盘上访问读取文件的同时，会顺序对其他相邻块的数据文件进行预读取。<br>在RocketMQ中，ConsumeQueue逻辑消费队列存储的数据较少，并且是顺序读取，在page cache机制的预读取作用下，Consume Queue文件的读性能几乎接近读内存，即使在有消息堆积情况下也不会影响性能。而对于CommitLog消息存储的日志数据文件来说，读取消息内容时候会产生较多的随机访问读取，严重影响性能。如果选择合适的系统IO调度算法，比如设置调度算法为“Deadline”（此时块存储采用SSD的话），随机读的性能也会有所提升。<br>另外，RocketMQ主要通过MappedByteBuffer对文件进行读写操作。其中，利用了NIO中的FileChannel模型将磁盘上的物理文件直接映射到用户态的内存地址中（这种Mmap的方式减少了传统IO将磁盘文件数据在操作系统内核地址空间的缓冲区和用户应用程序地址空间的缓冲区之间来回进行拷贝的性能开销），将对文件的操作转化为直接对内存地址进行操作，从而极大地提高了文件的读写效率（正因为需要使用内存映射机制，故RocketMQ的文件存储都使用定长结构来存储，方便一次将整个文件映射至内存）。</p>
<h2 id="刷盘策略"><a href="#刷盘策略" class="headerlink" title="刷盘策略"></a>刷盘策略</h2><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq/rocketmq_design_2.png"> <br><strong>异步刷盘</strong>：能够充分利用OS的PageCache的优势，只要消息写入PageCache即可将成功的ACK返回给Producer端。消息刷盘采用后台异步线程提交的方式进行，降低了读写延迟，提高了MQ的性能和吞吐量。<br><strong>同步刷盘</strong>：与异步刷盘的唯一区别是异步刷盘写完 PAGECACHE 直接返回，而同步刷盘需要等待刷盘完成才返回，同步刷盘流程如下：<br>(1) 写入PAGECACHE后，线程等待，通知刷盘线程刷盘。<br>(2) 刷盘线程刷盘后，唤醒前端等待线程，可能是一批线程。<br>(3) 前端等待线程向用户返回成功。<br>同步刷盘对MQ消息可靠性来说是一种不错的保障，但是性能上会有较大影响，一般适用于金融业务应用该模式较多。</p>
<p><strong>异步刷盘的思考</strong>：<br>在有 RAID 卡，SAS 15000 转磁盘测试顺序写文件，速度可以达到 300M 每秒左右，而线上的网卡一般都为千兆网卡，写磁盘速度明显快于数据网络入口速度，那么是否可以做到写完内存就向用户返回，由后台线程刷盘呢？<br>(1) 由于磁盘速度大于网卡速度，那么刷盘的进度肯定可以跟上消息的写入速度。<br>(2) 万一由于此时系统压力过大，可能堆积消息，除了写入 IO，还有读取 IO，万一出现磁盘读取落后情况，会不会导致系统内存溢出，答案是否定的，原因如下：<br>a) 写入消息到 PAGECACHE 时，如果内存不足，则尝试丢弃干净的 PAGE，腾出内存供新消息使用，策略是 LRU 方式。<br>b) 如果干净页不足，此时写入 PAGECACHE 会被阻塞，系统尝试刷盘部分数据，大约每次尝试 32 个 PAGE。</p>
<h2 id="高并发的队列"><a href="#高并发的队列" class="headerlink" title="高并发的队列"></a>高并发的队列</h2><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq_1.png"> <br>基本的<strong>刷盘流程</strong>：<br>(1) 所有数据单独存储到一个 Commit Log，完全顺序写，随机读。<br>(2) 对最终用户展现的队列实际只存储消息在 Commit Log 的位置信息，并且串行方式刷盘。<br>这样做的<strong>好处</strong>如下：<br>(1) 队列轻量化，单个队列数据量非常少。<br>(2) 对磁盘的访问串行化，避免磁盘竟争，不会因为队列增加导致 IOWAIT 增高。<br>每个方案都有<strong>缺点</strong>，它的缺点如下：<br>(1) 乱序。写虽然完全是顺序写，但是读却变成了完全的随机读。<br>(2) 增大开销。读一条消息，会先读 Consume Queue，再读 Commit Log，增加了开销。<br>(3) 编码复杂。要保证Commit Log 与 Consume Queue 完全的一致，增加了编程的复杂度。<br>以上<strong>缺点如何克服</strong>：<br>(1) 随机读，尽可能让读命中 PAGECACHE，减少 IO 读操作，所以内存越大越好。如果系统中堆积的消息过多，<br>读数据要访问磁盘会不会由于随机读导致系统性能急剧下降，答案是否定的。<br>a) 访问 PAGECACHE 时，即使只访问 1k 的消息，系统也会提前预读出更多数据，在下次读时，就可能命中内存。<br>b) 随机访问 Commit Log 磁盘数据，系统 IO 调度算法设置为 NOOP 方式，会在一定程度上将完全的随机读变成顺序跳跃方式，而顺序跳跃方式读较完全的随机读性能会高 5 倍以上。（Noop调度算法也叫作电梯调度算法，它将IO请求放入到一个FIFO队列中，然后逐个执行这些IO请求，当然对于一些在磁盘上连续的IO请求，Noop算法会适当做一些合并。这个调度算法特别适合那些不希望调度器重新组织IO请求顺序的应用。）<br>另外 4k 的消息在完全随机访问情况下，仍然可以达到 8K 次每秒以上的读性能。<br>(2) 由于 Consume Queue 存储数据量极少，而且是顺序读，在 PAGECACHE 预读作用下，Consume Queue 的读性能几乎与内存一致，即使堆积情况下。所以可认为 Consume Queue 完全不会阻碍读性能。<br>(3) Commit Log 中存储了所有的元信息，包含消息体，类似于 Mysql、Oracle 的 redolog，所以只要有 Commit Log 在，Consume Queue 即使数据丢失，仍然可以恢复出来。</p>
<h2 id="关于随机读写"><a href="#关于随机读写" class="headerlink" title="关于随机读写"></a>关于随机读写</h2><p>全随机写无疑是最慢的写入方式，在logic dump测试中很惊讶的发现，将200M的内存数据随机的写入到100G的磁盘数据里面，竟然要2个小时之多。原因就是虽然只有200M的数据，但实际上却是200万次随机写，根据测试，在2850机器上，这样完全的随机写，r/s 大约在150～350之间，在180机器上，r/s难以达到250，这样计算，难怪需要2～3个小时之久。<br>如何改进这种单线程随机写慢的问题呢？<br>一种方法就是尽量将完全随机写变成有序的跳跃随机写。实现方式，可以是简单的在内存中缓存一段时间，然后排序，使得在写盘的时候，不是完全随机的，而是使得磁盘磁头的移动只向一个方向。根据测试，简单的先在内存中排序，竟然直接使得写盘时间缩短到1645秒，磁盘的r/s也因此提升到1000以上。写盘的速度，一下子提高了5倍</p>
<h2 id="消息周转的过程"><a href="#消息周转的过程" class="headerlink" title="消息周转的过程"></a>消息周转的过程</h2><p>(1)Producer 发送消息，消息从 socket 进入 java 堆。<br>(2)Producer 发送消息，消息从 java 堆转入 PAGACACHE，物理内存。<br>(3)Producer 发送消息，由异步线程刷盘，消息从 PAGECACHE 刷入磁盘。<br>(4)Consumer 拉消息（多数情况），消息直接从 PAGECACHE（数据在物理内存）转入 socket，到达 consumer，不经过 java 堆。这种消费场景最多，线上 96G 物理内存，按照 1K 消息算，可以在物理内存缓存 1 亿条消息。<br>(5)Consumer 拉消息（少数情况），消息直接从 PAGECACHE（数据在虚拟内存）转入 socket。<br>(6)Consumer 拉消息（少数情况），由于 Socket 访问了虚拟内存，产生缺页中断，此时会产生磁盘 IO，从磁盘 Load 消息到 PAGECACHE，然后直接从 socket 发出去。</p>
<h1 id="RockMQ高级特性"><a href="#RockMQ高级特性" class="headerlink" title="RockMQ高级特性"></a>RockMQ高级特性</h1><h2 id="At-least-Once-和-Exactly-Only-Once"><a href="#At-least-Once-和-Exactly-Only-Once" class="headerlink" title="At least Once 和 Exactly Only Once"></a>At least Once 和 Exactly Only Once</h2><p>At least Once 是指每个消息必须投递一次<br>RocketMQ Consumer 先 pull 消息到本地，消费完成后，才向服务器返回 ack，如果没有消费一定不会 ack 消息，所以 RocketMQ 可以很好的支持此特性。<br>Exactly Only Once 是指只消费一次，即生产和消费都只能进行一次<br>在分布式系统环境下，不可避免要产生巨大的开销。所以 RocketMQ 为了追求高性能，并不保证此特性，要求在业务上进行去重，也就是说消费消息要做到幂等性。RocketMQ 虽然不能严格保证不重复，但是正常情况下很少会出现重复发送、消费情况，只有网络异常，Consumer 启停等异常情况下会出现消息重复。</p>
<h2 id="顺序消息"><a href="#顺序消息" class="headerlink" title="顺序消息"></a>顺序消息</h2><p>一个订单产生了 3 条消息，分别是订单创建，订单付款，订单完成。消费时，要按照这个顺序消费才能有意义。但是同时订单之间是可以并行消费的。所以我们只要保证同一个订单的消息在同一个队列里处理，就可以保证顺序性。</p>
<ol>
<li>顺序消息<br>消费消息的顺序要同发送消息的顺序一致，在 RocketMQ 中，主要指的是局部顺序，即一类消息为满足顺序性，必须 Producer 单线程顺序发送，且发送到同一个队列，这样 Consumer 就可以按照 Producer 发送的顺序去消费消息。</li>
<li>普通顺序消息<br>顺序消息的一种，正常情况下可以保证完全的顺序消息。这种消息需要保证三点：<ul>
<li>消息被发送时保持顺序</li>
<li>消息被存储时保持和发送的顺序一致</li>
<li>消息被消费时保持和存储的顺序一致<br>第一，发送的时候要保持有序，这里rockmq把需要保持顺序的消息哈希到同一个队列（不一定同分区，如图）<br>第二，落盘的时候有序，msg queue本来就是顺序写<br>第三，消费的时候有序，如果queue被多个consumer协程消费就会乱序。这里有两种消费模式，一种是consumer msg orderly，在消费队列时会加锁，确保一对一消费。还有一种是consumer msg concurrently，多协程广播消费，就会有问题，所以只能指定单协程。<br>但是一旦发生通信异常，Broker 重启，由于队列总数发生变化，哈希取模后定位的队列会变化，产生短暂的消息顺序不一致。如果业务能容忍在集群异常情况（如某个 Broker 宕机或者重启）下，消息短暂的乱序，使用普通顺序方式比较合适。</li>
</ul>
</li>
<li>严格顺序消息<br>顺序消息的一种，无论正常异常情况都能保证顺序，但是牺牲了分布式 Failover 特性，即 Broker 集群中只要有一台机器不可用，则整个集群都不可用，服务可用性大大降低。如果服务器部署为同步双写模式，此缺陷可通过备机自动切换为主避免，不过仍然会存在几分钟的服务不可用。（依赖同步双写，主备自动切换，自动切换功能目前还未实现）<br>目前已知的应用只有数据库 binlog 同步强依赖严格顺序消息，其他应用绝大部分都可以容忍短暂乱序，推荐使用普通的顺序消息。</li>
</ol>
<h2 id="优先级消息"><a href="#优先级消息" class="headerlink" title="优先级消息"></a>优先级消息</h2><p>优先级是指在一个消息队列中，每条消息都有不同的优先级，一般用整数来描述，优先级高的消息先投递。如果要用严格的优先级，则需要按照优先级排序确认消费次序，代价很大。<br>rocketmq实现的不是严格意义上的优先级，通常将优先级划分为高、中、低，或者再多几个级别。每个优先级可以用不同的 topic 表示，发消息时，指定不同的 topic 来表示优先级，随后优先消费某些topic。这种方式可以解决绝大部分的优先级问题，但是对业务的优先级精确性做了妥协。</p>
<h2 id="延迟消息"><a href="#延迟消息" class="headerlink" title="延迟消息"></a>延迟消息</h2><p><a href="https://www.cnblogs.com/sunshine-2015/p/9017426.html" target="_blank" rel="noopener">RocketMQ源码-RocketMQ延时消息</a><br>因为按照时间排序的复杂度太高，所以采用了折中的办法，降低延迟消息准确性，分为18个延迟队列（1s, 2s, …, 30min, 1h, 2h）写入</p>
<ol>
<li>延迟消息正常提交给CommitLog保存</li>
<li>因为是延迟消息，单独写到延时队列专用的topic，这样就不会被马上消费</li>
<li>每一个延时等级对应一个queue，queueId = delayLevel - 1</li>
<li>延时队列调度器轮询查看相应的队列中消息，是否到了要执行的时间</li>
<li>到了执行时间的消息，恢复原来消息的topic和queueId，发给写入普通的消费broker。这样就能正常消费了</li>
</ol>
<h2 id="负载均衡"><a href="#负载均衡" class="headerlink" title="负载均衡"></a>负载均衡</h2><h3 id="发送消息负载均衡"><a href="#发送消息负载均衡" class="headerlink" title="发送消息负载均衡"></a>发送消息负载均衡</h3><ol>
<li>发送策略：采取轮询的方式，给每个队列依次发送消息。比如有5个队列，可以部署在一台机器或者分别部署在5台机器上，发送消息通过轮询队列的方式发送，每个队列接收平均的消息量。通过增加机器，可以水平扩展队列容量。</li>
<li>退避策略（latencyFaultTolerance）：是指对之前失败的，按一定的时间做退避。例如，如果上次请求的latency超过550Lms，就退避3000Lms；超过1000L，就退避60000L；</li>
</ol>
<h3 id="订阅消息负载均衡"><a href="#订阅消息负载均衡" class="headerlink" title="订阅消息负载均衡"></a>订阅消息负载均衡</h3><p>如果有 5 个队列，2 个 consumer，那么第一个 Consumer 消费 3 个队列，第二 consumer 消费 2 个队列。<br>这样即可达到平均消费的目的，可以水平扩展 Consumer 来提高消费能力。但是 Consumer 数量要小于等于队列数<br>量，如果 Consumer 超过队列数量，那么多余的 Consumer 将不能消费消息。<br>如果有 10 个队列，20 个 consumer, 11-20号消费者则不能订阅到消息。<br>核心设计理念是在一个消息消费队列在同一时间只允许被同一消费组内的一个消费者消费，一个消息消费者能同时消费多个消息队列。一个负载均衡的流程如下：</p>
<ol>
<li>上报自己：在Consumer启动后，它就会通过定时任务不断地向RocketMQ集群中的所有Broker实例发送心跳包（其中包含了消息消费分组名称、订阅关系集合等信息）。Broker端在收到Consumer的心跳消息后，会将它们都维护在本地缓存变量consumerTable备用。</li>
<li>定时均衡：Consumer中有一个RebalanceService线程，每隔20s执行一次策略。<br> (A) 拉取所有queue：获取这个topic的consumer queue集合mqset<br> (B) 拉取所有消费者：Consumer使用topic和consumerGroup为参数对broker发起RPC请求，获取broker的consumerTable<br> (C) 平均分配：拿到Topic下所有的consumer queue、Consumer Id排序，把queue平均分配给所有的Consumer 。几乎每个consumer都会分到相同数量的queue。<br> (D) 改变消费连接：根据新建立的映射关系调整消费者和queue的连接。把分配到的consumer queue集合和正在处理的consumer queue做比对。对于正在处理的但是没有分配到的，移除这些连接；对于分配到没有处理的，连接到这些queue开始消费。其余的不处理。</li>
</ol>
<h2 id="并行消费"><a href="#并行消费" class="headerlink" title="并行消费"></a>并行消费</h2><p>单队列并行消费：<br>单队列并行消费采用滑动窗口方式并行消费，如图所示，3~7 的消息在一个滑动窗口区间，可以有多个线程并行消<br>费，但是每次提交的 Offset 都是最小 Offset，例如 3 。</p>
<h2 id="消息过滤"><a href="#消息过滤" class="headerlink" title="消息过滤"></a>消息过滤</h2><ol>
<li>在 Broker 端进行 Message Tag 比对，先遍历 Consume Queue，如果存储的 Message Tag 与订阅的 Message<br>Tag 不符合，则跳过，继续比对下一个，符合则传输给 Consumer。注意：Message Tag 是字符串形式，Consume<br>Queue 中存储的是其对应的 hashcode，比对时也是比对 hashcode。</li>
<li>Consumer 收到过滤后的消息后，同样也要执行在 Broker 端的操作，但是比对的是真实的 Message Tag 字<br>符串，而不是 Hashcode。<br>为什么过滤要这样做？</li>
<li>Hashcode更短。Message Tag 存储 Hashcode，是为了在 Consume Queue 定长方式存储，节约空间。</li>
<li>和Commit Log解耦。过滤过程中不会访问Commit Log数据，可以保证堆积情况下也能高效过滤。</li>
<li>双重保证。即使存在 Hash 冲突，也可以在 Consumer 端进行修正，保证万无一失。</li>
</ol>
<h2 id="消息查询"><a href="#消息查询" class="headerlink" title="消息查询"></a>消息查询</h2><h3 id="A-按照MessageId查询消息"><a href="#A-按照MessageId查询消息" class="headerlink" title="A. 按照MessageId查询消息"></a>A. 按照MessageId查询消息</h3><p>MessageId的长度总共有16字节，其中包含了消息存储主机地址（IP地址和端口），消息Commit Log offset。<br>Client端从MessageId中解析出Broker的地址（IP地址和端口）和Commit Log的偏移地址后封装成一个RPC请求后通过Remoting通信层发送（业务请求码：VIEW_MESSAGE_BY_ID）。Broker端走的是QueryMessageProcessor，读取消息的过程用其中的 commitLog offset 和 size 去 commitLog 中找到真正的记录并解析成一个完整的消息返回。</p>
<h3 id="B-按照Message-Key查询消息"><a href="#B-按照Message-Key查询消息" class="headerlink" title="B. 按照Message Key查询消息"></a>B. 按照Message Key查询消息</h3><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq/rocketmq_design_13.png"> <br>Index File由下面几个部分组成：</p>
<ol>
<li><strong>索引文件头</strong> 存了已用slot个数、已用索引个数、第一个和最后一个消息的落盘时间和在CommitLog的offset</li>
<li><strong>Slot Table</strong> 一个存放指针的哈希表，里面存着指向indexs的地址</li>
<li><strong>Indexs</strong> 索引主体，存放着下面的内容：<ul>
<li>key hash value: message key的hash值</li>
<li>phyOffset: message在CommitLog的物理文件地址, 可以直接查询到该消息(索引的核心机制)</li>
<li>timeDiff: message的落盘时间与header里的beginTimestamp的差值(为了节省存储空间，如果直接存message的落盘时间就得8bytes)</li>
<li>prevIndex: hash冲突处理的关键之处, 相同hash值上一个消息索引的index<br>Note: 这个prevIndex是用来解决hash冲突的。如果没有冲突，prevIndex就是0。如果有冲突，slot table的指针会指向比较新的那个indexs的地址，然后把新的indexs的prevIndex写上旧的indexs地址。这样，在遍历的时候，从slot table开始查找，经过一个<code>key hash slot -&gt; slot value -&gt; curIndex -&gt; prevIndex -&gt; ... -&gt; prevIndex -&gt; 相同的hash value</code>的链路，最后总会找到相同hash值的key。<br>Note: 如果在插入Indexs的时候采用append的形式，插入的偏移量：<br><code>文件偏移量=索引文件头长度+Slot Table长度+Indexs个数*单个Indexs大小</code></li>
</ul>
</li>
</ol>
<p>我们看一个通常的插入key的流程：</p>
<ol>
<li>根据查询的 key 的 hashcode % slotNum 得到具体的槽的位置（slotNum 索引文件slots上限的数目，一般像图中 slotNum=5000000）。</li>
<li>根据 slotValue（slot 位置对应的值）查找到索引项列表的最后一项（slotValue总是指向最新的一个）。</li>
<li>顺着<code>prevIndex</code>遍历所有索引项列表，匹配<code>key hash value</code>相同的索引项，返回查询时间范围内的结果集（默认一次最大返回的 32 条记录）。</li>
</ol>
<p>Note: 如果值的<code>key hash value</code>值相等但 key 不等，其实这里是检查不出来的。出于性能的考虑冲突的检测放到客户端处理（key 的原始值是存储在消息文件中的，避免对数据文件的解析），客户端比较一次消息体的 key 是否相同。</p>
<ol start="5">
<li>存储：为了节省空间索引项中存储的时间是时间差值（存储时间-开始时间，开始时间存储在索引文件头中），整个索引文件是定长的，结构也是固定的。索引文件存储结构如上图。</li>
</ol>
<h2 id="Pull-和-Push"><a href="#Pull-和-Push" class="headerlink" title="Pull 和 Push"></a>Pull 和 Push</h2><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq/longpolling.png" alt="long polling"><br>RocketMQ消息订阅有两种模式，一种是Push模式（MQPushConsumer），即MQServer主动向消费端推送；另外一种是Pull模式（MQPullConsumer），即消费端在需要时，主动到MQServer拉取。但在具体实现时，Push和Pull模式都是采用消费端主动拉取的方式，即consumer轮询从broker拉取消息。区别是：<br>Push方式里，consumer把轮询过程封装了，并注册MessageListener监听器，取到消息后，唤醒MessageListener的consumeMessage()来消费，对用户而言，感觉消息是被推送过来的。<br>Pull方式里，取消息的过程需要用户自己写，首先通过打算消费的Topic拿到MessageQueue的集合，遍历MessageQueue集合，然后针对每个MessageQueue批量取消息，一次取完后，记录该队列下一次要取的开始offset，直到取完了，再换另一个MessageQueue。<br>Push的问题：慢消费。如果消费者的速度比发送者的速度慢很多，势必造成消息在broker的堆积。对于消息量有限且到来的速度不均匀的情况，pull模式比较合适。<br>Pull的问题：消息延迟和忙等。pull需要轮询，就需要设置一个间隔时间，这个间隔太短就会引起无效的忙等，间隔太长会导致消息延迟。<br>在RocketMQ里，有一种优化的做法——长轮询 Pull ，来平衡推拉模型各自的缺点。基本思路是：</p>
<ol>
<li>consumer尝试拉取，发现broker上没有消息（有消息就直接返回了）</li>
<li>broker不直接返回, 而是把连接挂在那里wait</li>
<li>producer如果有新的消息到来，把连接notify起来，返回给consumer</li>
<li>如果没有消息到来，超时后释放链接（比如30s）<br>缺点：但海量的长连接block对系统的开销还是不容小觑</li>
</ol>
<h2 id="事务消息"><a href="#事务消息" class="headerlink" title="事务消息"></a>事务消息</h2><h3 id="事务的流程"><a href="#事务的流程" class="headerlink" title="事务的流程"></a>事务的流程</h3><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq/rocketmq_design_10.png" alt="flow"><br>MQ也提供了对事务的支持，比如操作A可以放在生产者的本地事务里，操作B可以放在消费者里</p>
<ol>
<li>发送方向 MQ 服务端发送消息</li>
<li>broker将消息持久化成功之后，向发送方 ACK 确认消息已经发送成功，此时消息为prepared状态。</li>
<li>发送方开始执行本地事务逻辑。</li>
<li>发送方根据本地事务执行结果向 broker 提交二次确认（Commit 或是 Rollback）</li>
<li>broker 收到 Commit 状态则将半消息标记为可投递，订阅方最终将收到该消息；broker 收到 Rollback 状态则删除prepared的消息，订阅方将不会接受该消息。</li>
</ol>
<p>补充逻辑</p>
<ol start="5">
<li>在断网或者是应用重启的特殊情况下，上述步骤4提交的二次确认最终未到达 broker，经过固定时间后 broker 将对该消息发起消息回查。 </li>
<li>发送方收到消息回查后，需要检查对应消息的本地事务执行的最终结果。 发送方根据检查得到的本地事务的最终状态再次提交二次确认，broker 仍按照步骤4对prepare的消息进行操作。</li>
</ol>
<h3 id="事务的实现"><a href="#事务的实现" class="headerlink" title="事务的实现"></a>事务的实现</h3><p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/rockmq/rocketmq_design_12.png" alt="design"><br>看一下mq具体处理事务消息的办法，如果一个事务消息被写入：</p>
<ol>
<li>写入的如果是事务消息，对消息的Topic和Queue等属性进行替换写入half topic，同时将原来的Topic和Queue信息存储到消息的属性中<br>（正因为消息主题被替换，故消息并不会转发到该原主题的消息消费队列，消费者无法感知消息的存在，不会消费，和延时消息一样的套路）</li>
<li>消息commit或者rollback时，会在op topic中存储一份，表示消息的状态，op topic的消息体是到half topic的索引，便于后面回查<br>如果是rollback，消息直接设置为回滚，就不会再处理了</li>
<li>Commit之后，读取出Half消息，并将Topic和Queue替换成真正的目标的Topic和Queue，然后走普通消息的写入流程</li>
</ol>
<p>Note：如果commit因为网络等原因失败，Broker端对未确定状态的消息（在half topic不在op topic里的）<br>发起定时回查，将消息发送到对应的Producer，由Producer根据消息来检查本地事务的状态，进而执行Commit或者Rollback。</p>
]]></content>
    
    <summary type="html">
    
      描述RocketMQ的多个关键特性的实现原理，并对消息中间件实现遇到的各种问题进行总结，阐述RocketMQ 如何解决这些问题。
    
    </summary>
    
      <category term="中间件" scheme="chestnutheng.cn/categories/%E4%B8%AD%E9%97%B4%E4%BB%B6/"/>
    
    
      <category term="中间件" scheme="chestnutheng.cn/tags/%E4%B8%AD%E9%97%B4%E4%BB%B6/"/>
    
      <category term="消息队列" scheme="chestnutheng.cn/tags/%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97/"/>
    
  </entry>
  
  <entry>
    <title>[Linux]进程间通信</title>
    <link href="chestnutheng.cn/2019/08/01/ipc/"/>
    <id>chestnutheng.cn/2019/08/01/ipc/</id>
    <published>2019-08-01T12:26:09.000Z</published>
    <updated>2019-08-06T12:51:39.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="管道"><a href="#管道" class="headerlink" title="管道"></a>管道</h1><p>管道是进程间通信的最古老方式。它通过共享文件来完成进程间的通信。它有两个局限性：</p>
<ol>
<li>它是半双工的。数据只能从一个进程流向另一个进程。</li>
<li>通信的进程之间必须另一个进程是fork出来的。通常，一个进程会创建一个管道，然后执行fork，这样管道就会在两个进程之间共享。<br>FIFO解决了第一种局限性，Unix域套接字解决了第二种。我们先来看管道。</li>
</ol>
<p><code>int pipe(int pipefd[2]);</code>管道由pipe函数创建。</p>
<ol>
<li>参数<code>pipefd</code>是一个两个元素的数组。<code>pipefd[0]</code>用来读，<code>pipefd[1]</code>用来写。</li>
<li>成功返回0 。失败返回-1并设置errno。</li>
<li>单进程的管道没有任何用处。在这个函数之后一般会fork，然后一个进程来写<code>pipefd[1]</code>，一个进程来读<code>pipefd[0]</code>。他们的另一个fd元素将会被关闭。</li>
</ol>
<p>下面是一段实例, 父进程通过管道向子进程传递了信息，子进程接收并把他们输出：<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"apue.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span>		n;</span><br><span class="line">	<span class="keyword">int</span>		fd[<span class="number">2</span>];</span><br><span class="line">	<span class="keyword">pid_t</span>	pid;</span><br><span class="line">	<span class="keyword">char</span>	line[MAXLINE];</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (pipe(fd) &lt; <span class="number">0</span>)</span><br><span class="line">		err_sys(<span class="string">"pipe error"</span>);</span><br><span class="line">	<span class="keyword">if</span> ((pid = fork()) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">		err_sys(<span class="string">"fork error"</span>);</span><br><span class="line">	&#125; <span class="keyword">else</span> <span class="keyword">if</span> (pid &gt; <span class="number">0</span>) &#123;		<span class="comment">/* parent */</span></span><br><span class="line">		close(fd[<span class="number">0</span>]);</span><br><span class="line">		write(fd[<span class="number">1</span>], <span class="string">"hello world\n"</span>, <span class="number">12</span>);</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;					<span class="comment">/* child */</span></span><br><span class="line">		close(fd[<span class="number">1</span>]);</span><br><span class="line">		n = read(fd[<span class="number">0</span>], line, MAXLINE);</span><br><span class="line">		write(STDOUT_FILENO, line, n);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p><code>FILE *popen(const char *command, const char *type)</code> 函数popen执行一个命令，然后返回这个命令的文件指针。</p>
<ol>
<li><code>type</code>参数为’r’，表示这个文件可读，如果是’w’，表示可写。</li>
<li><code>command</code>参数是要执行的命令</li>
<li>使用完用<code>int pclose(FILE *stream)</code>关闭管道。</li>
<li>这个函数是管道实现的一个例子。它先创建一个管道，然后fork一个子进程，关闭未使用的管道端（读或写），然后在子进程执行命令，最后等待执行完毕。</li>
</ol>
<h1 id="FIFO"><a href="#FIFO" class="headerlink" title="FIFO"></a>FIFO</h1><p><code>int mkfifo(const char *pathname, mode_t mode)</code>提供了任意进程间通过文件通信的方式。</p>
<ol>
<li>参数<code>path</code>表示管道的路径。</li>
<li>参数<code>mode</code>和<code>open</code>函数的模式一样。</li>
</ol>
<p><code>int mkfifoat(int dirfd, const char *pathname, mode_t mode)</code>提供了更多选择的路径控制。</p>
<h2 id="FIFO用于复制输出流"><a href="#FIFO用于复制输出流" class="headerlink" title="FIFO用于复制输出流"></a>FIFO用于复制输出流</h2><p>下面的命令展示了复制输出流的方法。输入文件先到达prog1，然后通过tee命令复制给fifo和prog2。fifo中的数据会流到prog3。<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">mkfifo fifo1</span><br><span class="line">prog3 &lt; fifo1 &amp;</span><br><span class="line">prog1 &lt; infile | tee fifo1 |prog2</span><br></pre></td></tr></table></figure></p>
<h2 id="FIFO用于服务端和客户端通信"><a href="#FIFO用于服务端和客户端通信" class="headerlink" title="FIFO用于服务端和客户端通信"></a>FIFO用于服务端和客户端通信</h2><p>使用FIFO可以用来做服务端-客户端通信。服务端提供一个FIFO让客户端写入，这样就可以接收到客户端的请求。那么如何响应客户端？可以让客户端在请求中携带进程ID，然后服务端为每个客户端都创建一个FIFO（和客户端约定好路径），然后让客户端从中读取。<br>这种方法有些问题：</p>
<ol>
<li>客户端可能随时进程消失，FIFO没有被回收</li>
<li>客户端发起单向调用就终止时，FIFO没有被回收（监听SIGPIPE可以解决）</li>
<li>客户进程从1变成0时，服务端需要处理EOF（请求FIFO可以设为读-写模式解决）</li>
</ol>
<h1 id="XSI-IPC"><a href="#XSI-IPC" class="headerlink" title="XSI IPC"></a>XSI IPC</h1><h2 id="IPC标识符"><a href="#IPC标识符" class="headerlink" title="IPC标识符"></a>IPC标识符</h2><p>进行进程间通信，必须要有一个编号来表明所用的资源（IPC对象）是哪个。在Linux中，IPC标识符是一个key_t类型（一般实现为长整型，从0开始分配，每有一个IPC对象就加一，直到最大值后又从0开始）。使用IPC操作必须提供这个IPC标识符，那么不同进程如何共享这个标识符呢？</p>
<h3 id="ftok"><a href="#ftok" class="headerlink" title="ftok"></a>ftok</h3><p><code>key_t ftok(const char *pathname, int proj_id)</code> 通过ftok函数，不同进程可以产生相同的key。</p>
<ol>
<li>参数 <code>pathname</code> 是进程间约定的已经存在的文件路径。</li>
<li>参数 <code>proj_id</code> 是约定的0-255的值，也是一个约定的项目id。</li>
<li>事实上，ftok取了<code>pathname</code>中文件的设备编号<code>st_dev</code>的低8bit、文件iNode<code>st_ino</code>的低16bit，和<code>proj_id</code>的低8bit合并成了返回的<code>key_t</code>值。</li>
<li>这意味着如果文件在使用中被删除并重建，key会发生变化。如果<code>proj_id</code>一致，也有可能造成冲突。</li>
</ol>
<h3 id="IPC-PRIVATE"><a href="#IPC-PRIVATE" class="headerlink" title="IPC_PRIVATE"></a>IPC_PRIVATE</h3><p><code>IPC_PRIVATE</code> 实际上是定义的值是0 。用这个做key时，系统将会产生一个新的IPC对象。一般用于不同进程间不需要约定key的时候，比如父进程在获得对象后传承给子进程。</p>
<h3 id="IPC-权限"><a href="#IPC-权限" class="headerlink" title="IPC 权限"></a>IPC 权限</h3><p>所有的IPC对象都有一个权限字段来标识所属的线程。一般IPC对象只能由创建的进程或者超级权限进程来删除。这个权限信息里记录了一些创建者的信息。uid、gid、mode三个字段可以由用户通过msgctl、shmctl、semctl等函数来修改。修改者必须是创建进程或者超级权限进程。<br><code>mode</code> 字段有一些特定的值表示权限，类似文件的<code>chmod</code>。下面给出了该字段每一位的含义。<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> ipc_perm &#123;</span><br><span class="line">    <span class="keyword">key_t</span>          <span class="number">__</span>key;    <span class="comment">/* Key supplied to shmget(2) */</span></span><br><span class="line">    <span class="keyword">uid_t</span>          uid;      <span class="comment">/* Effective UID of owner */</span></span><br><span class="line">    <span class="keyword">gid_t</span>          gid;      <span class="comment">/* Effective GID of owner */</span></span><br><span class="line">    <span class="keyword">uid_t</span>          cuid;     <span class="comment">/* Effective UID of creator */</span></span><br><span class="line">    <span class="keyword">gid_t</span>          cgid;     <span class="comment">/* Effective GID of creator */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">short</span> mode;     <span class="comment">/* Permissions + SHM_DEST and SHM_LOCKED flags */</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">short</span> <span class="number">__</span>seq;    <span class="comment">/* Sequence number */</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// mode字段</span></span><br><span class="line"><span class="number">0400</span> 用户读</span><br><span class="line"><span class="number">0200</span> 用户写</span><br><span class="line"><span class="number">0040</span> 组读</span><br><span class="line"><span class="number">0020</span> 组写</span><br><span class="line"><span class="number">0004</span> 其他读</span><br><span class="line"><span class="number">0002</span> 其他写</span><br></pre></td></tr></table></figure></p>
<h3 id="IPC的缺点"><a href="#IPC的缺点" class="headerlink" title="IPC的缺点"></a>IPC的缺点</h3><ol>
<li>IPC不会主动回收资源。IPC是系统来维护的，没有引用计数。如果一个进程在一个消息队列中添加了消息然后终止，系统不会删除这些消息，直到下个进程显示地使用或删除他们。<br>PIPE会在最后一个使用的进程终止后自动销毁。FIFO虽然在最后一个使用的进程结束后会保留名字，但是其中的数据会删除。</li>
<li>IPC在文件系统中没有名字。Linux是一切皆文件的，没有文件描述符有几个麻烦：a)无法ls，rm，chmod，无法像操作文件一样操作IPC对象，只能用其特定的函数。b)没办法使用select，poll这样的多路复用技术，这将导致进程无法同时监听两个消息队列。</li>
</ol>
<h2 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h2><p><code>int msgget(key_t key, int msgflg)</code>创建一个msg对象。</p>
<ol>
<li><code>key</code>可以是<code>IPC_PRIVATE</code>或者一个存在的IPC的key。</li>
<li><code>msgflg</code>提供了创建消息队列的选项。a)为0时返回<code>key</code>的消息队列的标识符，不存在会报错 b) <code>PC_CREAT</code>：当msgflg&amp;IPC_CREAT为真时，则新建一个消息队列；如果存在这样的消息队列，返回此消息队列的标识符 c) <code>IPC_CREAT|IPC_EXCL</code> 不存在则创建一个key的消息队列，存在则返回错误。</li>
<li>成功返回消息队列的标识符，失败返回-1，并写errno。</li>
</ol>
<p><code>int msgctl(int msqid, int cmd, struct msqid_ds *buf)</code> 读写msg对象的属性</p>
<ol>
<li><code>msqid</code>是一个消息队列的标识符。</li>
<li><code>cmd</code> 提供了读写的选项。a) <code>IPC_STAT</code> 读取属性到buf中。 b) <code>IPC_SET</code> 写入属性到buf中 c) <code>IPC_RMID</code> 删除消息队列和队列中的所有消息。删除前会用<code>msg_perm</code>校验归属。</li>
<li><code>buf</code> 是设置的详情，见下表。</li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> msqid_ds &#123;</span><br><span class="line">	<span class="keyword">struct</span> ipc_perm msg_perm;     <span class="comment">// Ownership and permissions 权限，初始化时填0 </span></span><br><span class="line">	<span class="keyword">time_t</span>          msg_stime;    <span class="comment">// Time of last msgsnd(2) 初始化时填0 </span></span><br><span class="line">	<span class="keyword">time_t</span>          msg_rtime;    <span class="comment">// Time of last msgrcv(2) 初始化时填0 </span></span><br><span class="line">	<span class="keyword">time_t</span>          msg_ctime;    <span class="comment">// Time of last change 初始化时设置为当前时间 </span></span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span>   <span class="number">__</span>msg_cbytes; <span class="comment">// Current number of bytes in queue (non-standard)  </span></span><br><span class="line">	<span class="keyword">msgqnum_t</span>       msg_qnum;     <span class="comment">// Current number of messages in queue 初始化时填0 </span></span><br><span class="line">	<span class="keyword">msglen_t</span>        msg_qbytes;   <span class="comment">// Maximum number of bytes allowed in queue 初始化时填系统值MSGMNB </span></span><br><span class="line">	<span class="keyword">pid_t</span>           msg_lspid;    <span class="comment">// PID of last msgsnd(2) 初始化时填0  </span></span><br><span class="line">	<span class="keyword">pid_t</span>           msg_lrpid;    <span class="comment">// PID of last msgrcv(2) 初始化时填0  </span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>int msgsnd(int msqid, const void *msgp, size_t msgsz, int msgflg)</code> 发送消息。如果消息队列已满，会阻塞等待。</p>
<ol>
<li>参数<code>msqid</code> 是发送消息的消息队列</li>
<li><p>参数<code>msgp</code> 发送给队列的消息。msgp可以是任何类型的结构体，但第一个字段必须为long类型，即表明此发送消息的类型。msgp定义的参照格式如下：</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> s_msg&#123; <span class="comment">/*msgp定义的参照格式*/</span></span><br><span class="line">	<span class="keyword">long</span> type; <span class="comment">/* 必须大于0,消息类型 */</span></span><br><span class="line">	<span class="keyword">char</span> mtext[<span class="number">256</span>]; <span class="comment">/*消息正文，可以是其他任何类型*/</span></span><br><span class="line">&#125; msgp;</span><br></pre></td></tr></table></figure>
</li>
<li><p>参数<code>msgsz</code> 要发送消息的大小, 其中不含消息类型的4个字节。</p>
</li>
<li>参数<code>msgflg</code> 设置发送的模式。<code>IPC_NOWAIT</code>类似一个非阻塞IO，写不进消息队列也会立即返回<code>IPC_NOERROR</code> 如果消息超过长度限制<code>msgsz</code>，会截断消息。</li>
<li>成功返回0，失败返回-1，设置errno。EAGAIN 消息队列已满，EIDRM 消息队列已删除，EACCESS 无权限写入， EINVAL 参数错误。</li>
<li>msgsnd会一直阻塞等待，直到 a) 消息队列有空余 b) 消息队列被删除 c) msgsnd函数被信号中断。</li>
</ol>
<p><code>size_t msgrcv(int msqid, void *msgp, size_t msgsz, long msgtyp, int msgflg)</code> 会读取消息存入<code>msgp</code>指针，然后把消息从队列中删除。在此之前，此函数会一直阻塞。</p>
<ol>
<li>参数<code>msqid</code> 是接收消息的消息队列</li>
<li>参数<code>msgp</code> 存放读出来的消息。结构体必须和上面的发送结构体相同。</li>
<li>参数<code>msgsz</code> 读出来消息的大小, 其中不含消息类型的4个字节。</li>
<li>参数<code>msgtyp</code>  a) 为0表示读第一个消息 b) 大于0表示读消息类型等于本参数的第一个消息 c) 小于0表示读消息类型小于等于本参数绝对值的第一个消息</li>
<li>参数<code>msgflg</code> 设置读的模式。<code>IPC_NOWAIT</code>类似一个非阻塞IO，没有消息可读也会立即返回，此时错误为 ENOMSG。<code>IPC_NOERROR</code> 如果消息超过长度限制<code>msgsz</code>，会截断消息。</li>
<li>成功返回消息长度，失败返回-1，设置errno。E2BIG 消息数据长度大于msgsz而msgflag没有设置IPC_NOERROR 。</li>
</ol>
<p>下面给出了一个两个进程间通过消息队列通信的样例。由于消息队列创建后不会自动删除，需要运行<code>ipcrm -q msqid</code>来手动删除队列。<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/ipc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span>  <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TEXT_SIZE  512</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> msgbuf &#123;</span><br><span class="line">    <span class="keyword">long</span> mtype;</span><br><span class="line">    <span class="keyword">int</span>  status;</span><br><span class="line">    <span class="keyword">char</span> time[<span class="number">20</span>];</span><br><span class="line">    <span class="keyword">char</span> mtext[TEXT_SIZE];</span><br><span class="line">&#125;</span><br><span class="line">;</span><br><span class="line"><span class="function"><span class="keyword">char</span>  *<span class="title">getxtsj</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">time_t</span>  tv;</span><br><span class="line">    <span class="keyword">struct</span>  tm   *tmp;</span><br><span class="line">    <span class="keyword">static</span>  <span class="keyword">char</span>  buf[<span class="number">20</span>];</span><br><span class="line">    tv = time( <span class="number">0</span> );</span><br><span class="line">    tmp = localtime( &amp;tv );</span><br><span class="line">    <span class="built_in">sprintf</span>( buf, <span class="string">"%02d:%02d:%02d"</span>, tmp-&gt;tm_hour, tmp-&gt;tm_min, tmp-&gt;tm_sec );</span><br><span class="line">    <span class="keyword">return</span>   buf;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( <span class="keyword">int</span> argc, <span class="keyword">char</span> **argv )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> msqid;</span><br><span class="line">    <span class="keyword">struct</span> msqid_ds info;</span><br><span class="line">    <span class="keyword">struct</span> msgbuf buf;</span><br><span class="line">    <span class="keyword">struct</span> msgbuf buf1;</span><br><span class="line">    <span class="keyword">int</span> flag;</span><br><span class="line">    <span class="keyword">int</span> sendlength, recvlength;</span><br><span class="line">    <span class="keyword">int</span> key;</span><br><span class="line">    key = ftok( <span class="string">"msg.tmp"</span>, <span class="number">0x01</span> );</span><br><span class="line">    <span class="keyword">if</span> ( key &lt; <span class="number">0</span> ) &#123;</span><br><span class="line">        perror( <span class="string">"ftok key error"</span> );</span><br><span class="line">        <span class="keyword">return</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    msqid = msgget( key, <span class="number">0600</span> | IPC_CREAT );</span><br><span class="line">    <span class="keyword">if</span> ( msqid &lt; <span class="number">0</span> ) &#123;</span><br><span class="line">        perror( <span class="string">"create message queue error"</span> );</span><br><span class="line">        <span class="keyword">return</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    buf.mtype = <span class="number">1</span>;</span><br><span class="line">    buf.status = <span class="number">9</span>;</span><br><span class="line">    <span class="built_in">strcpy</span>( buf.time, getxtsj() );</span><br><span class="line">    <span class="built_in">strcpy</span>( buf.mtext, <span class="string">"happy new year!"</span> );</span><br><span class="line">    sendlength = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> msgbuf) - <span class="keyword">sizeof</span>(<span class="keyword">long</span>);</span><br><span class="line">    flag = msgsnd( msqid, &amp;buf, sendlength, <span class="number">0</span> );</span><br><span class="line">    <span class="keyword">if</span> ( flag &lt; <span class="number">0</span> ) &#123;</span><br><span class="line">        perror( <span class="string">"send message error"</span> );</span><br><span class="line">        <span class="keyword">return</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    buf.mtype = <span class="number">3</span>;</span><br><span class="line">    buf.status = <span class="number">9</span>;</span><br><span class="line">    <span class="built_in">strcpy</span>( buf.time, getxtsj() );</span><br><span class="line">    <span class="built_in">strcpy</span>( buf.mtext, <span class="string">"good bye!"</span> );</span><br><span class="line">    sendlength = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> msgbuf) - <span class="keyword">sizeof</span>(<span class="keyword">long</span>);</span><br><span class="line">    flag = msgsnd( msqid, &amp;buf, sendlength, <span class="number">0</span> );</span><br><span class="line">    <span class="keyword">if</span> ( flag &lt; <span class="number">0</span> ) &#123;</span><br><span class="line">        perror( <span class="string">"send message error"</span> );</span><br><span class="line">        <span class="keyword">return</span> - <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    system( <span class="string">"ipcs -q"</span> );</span><br><span class="line">    <span class="keyword">return</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>接受消息：<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/ipc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/msg.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TEXT_SIZE  512</span></span><br><span class="line"><span class="keyword">struct</span> msgbuf &#123;</span><br><span class="line">    <span class="keyword">long</span> mtype ;</span><br><span class="line">    <span class="keyword">int</span>  status ;</span><br><span class="line">    <span class="keyword">char</span> time[<span class="number">20</span>] ;</span><br><span class="line">    <span class="keyword">char</span> mtext[TEXT_SIZE] ;</span><br><span class="line">&#125;</span><br><span class="line">;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> msqid ;</span><br><span class="line">    <span class="keyword">struct</span> msqid_ds info ;</span><br><span class="line">    <span class="keyword">struct</span> msgbuf buf1 ;</span><br><span class="line">    <span class="keyword">int</span> flag ;</span><br><span class="line">    <span class="keyword">int</span>  recvlength ;</span><br><span class="line">    <span class="keyword">int</span> key ;</span><br><span class="line">    <span class="keyword">int</span> mtype ;</span><br><span class="line">    key = ftok(<span class="string">"msg.tmp"</span>, <span class="number">0x01</span> ) ;</span><br><span class="line">    <span class="keyword">if</span> ( key &lt; <span class="number">0</span> ) &#123;</span><br><span class="line">        perror(<span class="string">"ftok key error"</span>) ;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    msqid = msgget( key, <span class="number">0</span> ) ;</span><br><span class="line">    <span class="keyword">if</span> ( msqid &lt; <span class="number">0</span> ) &#123;</span><br><span class="line">        perror(<span class="string">"get ipc_id error"</span>) ;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    recvlength = <span class="keyword">sizeof</span>(<span class="keyword">struct</span> msgbuf) - <span class="keyword">sizeof</span>(<span class="keyword">long</span>) ;</span><br><span class="line">    <span class="built_in">memset</span>(&amp;buf1, <span class="number">0x00</span>, <span class="keyword">sizeof</span>(<span class="keyword">struct</span> msgbuf)) ;</span><br><span class="line">    mtype = <span class="number">1</span> ;</span><br><span class="line">    flag = msgrcv( msqid, &amp;buf1, recvlength ,mtype,<span class="number">0</span> ) ;</span><br><span class="line">    <span class="keyword">if</span> ( flag &lt; <span class="number">0</span> ) &#123;</span><br><span class="line">        perror(<span class="string">"recv message error\n"</span>) ;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span> ;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"type=%d,time=%s, message=%s\n"</span>, buf1.mtype, buf1.time,  buf1.mtext) ;</span><br><span class="line">    system(<span class="string">"ipcs -q"</span>) ;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span> ;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="信号量"><a href="#信号量" class="headerlink" title="信号量"></a>信号量</h2><p>信号量是一个计数器，用于控制多个进程对资源的访问。当一个进程访问信号量控制的资源时，以以下步骤执行：</p>
<ol>
<li>取信号量的值。如果值是正，则进程可以继续执行。进程访问资源，并把信号量的值减一，表示他占用了一个资源。</li>
<li>如果值是0，进程会休眠，直到信号量的值大于0，进程被唤醒，再执行1。</li>
<li>当进程释放资源时，信号量值加一，并唤醒正在等待的进程。</li>
</ol>
<p>实际上，XSI中的信号量要复杂一些，他有几个缺点：</p>
<ol>
<li>信号量并非是一个数值，而是一个多个值的集合。创建时要指定值的数量。</li>
<li>信号量的创建和初始化是分开的，并非一个原子操作。这样就没法原子地创建一个信号量。</li>
<li>即使已经没有进程再使用信号量，他们依然不会销毁。</li>
</ol>
<p>关于信号量不再展开描述，参见 <a href="https://blog.csdn.net/ljianhui/article/details/10243617" target="_blank" rel="noopener">Linux进程间通信——使用信号量</a></p>
<h2 id="共享内存"><a href="#共享内存" class="headerlink" title="共享内存"></a>共享内存</h2><p>进程间通信最方便的方法，就是一起读写内存。由于进程不需要把这块内存复制到自己的进程空间内，所以共享内存的方法非常快。需要注意的是，在一个进程写内存的时候，其他进程不能操作这块内存。这里可以用记录锁、信号量或者互斥量来同步。</p>
<p>通过<code>mmap</code>把文件映射到内存中也是一种共享内存的实现。于这种方式不同的是，XSI中的共享内存没有文件映射，是一个内存的匿名段。</p>
<p><code>int shmget(key_t key, size_t size, int shmflg)</code> 用于创建一个共享内存。</p>
<ol>
<li><code>key</code>可以是<code>IPC_PRIVATE</code>或者一个存在的共享内存的key（可能是ftok生成）。</li>
<li><code>size</code> 表示要创建的共享内存的大小，必须为内存页大小的整数倍。如果是读取线程获取已经存在的共享内存，填0。</li>
<li><code>shmflg</code>提供了创建消息队列的选项。a)为0时返回<code>key</code>的共享内存的标识符，不存在会报错 b) <code>IPC_CREAT</code>：当<code>shmflg&amp;IPC_CREAT</code>为真时，则新建一个共享内存；如果存在这样的共享内存，返回此共享内存的标识符 c) <code>IPC_CREAT|IPC_EXCL</code> 不存在则创建一个key的共享内存，存在则返回错误。</li>
<li>成功返回0，失败返回-1。<code>EINVAL</code> 参数size小于SHMMIN或大于SHMMAX。<code>EEXIST</code> 预建立key所指的共享内存，但已经存在。 <code>EIDRM</code>参数key所指的共享内存已经删除。 <code>ENOSPC</code>超过了系统允许建立的共享内存的最大值(SHMALL)。 <code>ENOENT</code>参数key所指的共享内存不存在，而参数shmflg未设IPC_CREAT位。<code>EACCES</code>没有权限 <code>ENOMEM</code> 核心内存不足。</li>
</ol>
<p><code>int shmctl(int shmid, int cmd, struct shmid_ds *buf)</code> 设置共享内存。</p>
<ol>
<li>参数 <code>shmid</code>是共享内存的id 。</li>
<li>参数 <code>cmd</code> 指定了本次操作的行为。<code>IPC_STAT</code> 得到共享内存的状态，把共享内存的<code>shmid_ds</code>结构复制到buf中。<code>IPC_SET</code>改变共享内存的状态，把buf所指的<code>shmid_ds</code>结构中的uid、gid、mode复制到共享内存的<code>shmid_ds</code>结构内<code>IPC_RMID</code>删除这片共享内存。</li>
<li>参数 <code>shmid_ds</code>是一些共享内存的设置。创建新字段时，应按照下面的初始化设置进行初始化。<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> shmid_ds &#123;</span><br><span class="line">	<span class="keyword">struct</span> ipc_perm shm_perm;    <span class="comment">// Ownership and permissions 权限初始化</span></span><br><span class="line">	<span class="keyword">size_t</span>          shm_segsz;   <span class="comment">// Size of segment (bytes) 初始化设置为请求的size</span></span><br><span class="line">	<span class="keyword">time_t</span>          shm_atime;   <span class="comment">// Last attach time 初始化设置为0</span></span><br><span class="line">	<span class="keyword">time_t</span>          shm_dtime;   <span class="comment">// Last detach time 初始化设置为0</span></span><br><span class="line">	<span class="keyword">time_t</span>          shm_ctime;   <span class="comment">// Last change time 初始化设置为当前时间</span></span><br><span class="line">	<span class="keyword">pid_t</span>           shm_cpid;    <span class="comment">// PID of creator </span></span><br><span class="line">	<span class="keyword">pid_t</span>           shm_lpid;    <span class="comment">// PID of last shmat(2)/shmdt(2) 初始化设置为0</span></span><br><span class="line">	<span class="keyword">shmatt_t</span>        shm_nattch;  <span class="comment">// No. of current attaches 初始化设置为0</span></span><br><span class="line">	...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p><code>void *shmat(int shmid, const void *shmaddr, int shmflg)</code> 连接指定id的共享内存。连接后，这片共享内存就可以在进程空间内访问。</p>
<ol>
<li>参数<code>shmid</code>是共享内存的id。</li>
<li>参数<code>shmaddr</code>指定本进程内内存的地址，之后这片地址会映射到共享内存。如果设为NULL，由系统决定地址（没有特殊需要就设为NULL）。</li>
<li>参数<code>shmflg</code> 提供了一些选项。<code>SHM_RDONLY</code> 为只读模式，其他为读写模式。</li>
<li>成功返回连接的本进程内存地址，失败返回-1。</li>
<li>函数fork后，子进程会继承已连接的这片地址。exec后，子进程会和这片共享内存结束连接。</li>
</ol>
<p><code>int shmdt(const void *shmaddr)</code> 结束这片共享内存的映射。</p>
<ol>
<li>参数<code>shmaddr</code>指定本进程内内存的地址，是上面的<code>shmat</code>返回的。</li>
<li>成功返回0，失败返回-1。</li>
<li><code>shmdt</code>执行后，<code>shm_nattch</code>会减一。</li>
</ol>
]]></content>
    
    <summary type="html">
    
      使用进程间通信来完成信息互换是很常见的场景。本文介绍了指令形式的通信（信号、信号量），文本形式的通信（共享内存，管道，FIFO，Unix域套接字）来完成这一基本的任务。
    
    </summary>
    
      <category term="Linux" scheme="chestnutheng.cn/categories/Linux/"/>
    
    
      <category term="Linux" scheme="chestnutheng.cn/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>[Linux]高级IO</title>
    <link href="chestnutheng.cn/2019/07/31/linux-epoll/"/>
    <id>chestnutheng.cn/2019/07/31/linux-epoll/</id>
    <published>2019-07-31T12:20:43.000Z</published>
    <updated>2019-08-01T12:07:18.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="如果有多个IO需要处理"><a href="#如果有多个IO需要处理" class="headerlink" title="如果有多个IO需要处理"></a>如果有多个IO需要处理</h1><p>当一个描述符读，然后又写到另一个描述符时，可以用循环的方式访问阻塞io：<br><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BUF_SIZE 128</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">	<span class="keyword">char</span> buf[BUF_SIZE];</span><br><span class="line">	<span class="keyword">int</span> n;</span><br><span class="line">	<span class="keyword">while</span> ( n = read(STDIN_FILENO, buf, BUF_SIZE) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">	        <span class="keyword">if</span> (write(STDOUT_FILENO, buf, n) != n)&#123;</span><br><span class="line">	                <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"%s\n"</span>, <span class="string">"write error!"</span>);</span><br><span class="line">	                <span class="keyword">break</span>;</span><br><span class="line">	        &#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>但是如果要从两个fd读的时候，就不能用阻塞io去读了。因为进程阻塞在某个fd的时候，另一个fd即使输入了数据也无法处理。<br>比如一个telnet程序的输出有两个来源，用户输入回显和远端回包。如果阻塞在等待远端回包，用户输入就不会有回显了。解决这个问题就几种思路：</p>
<ol>
<li>再fork一个进程，每个进程处理一个fd。这看起来很好，但是处理EOF却成了问题。如果子进程先读到EOF，那么子进程终止，返回SIGCHLD给父进程，然后父进程终止。如果父进程读到EOF，父进程就需要通知子进程结束，此时需要额外的信号（如SIGUSER1）.</li>
<li>用两个线程来处理，每个线程一个fd。同样的，处理线程之间的同步也会变的比较复杂。</li>
<li>用<code>非阻塞的io</code>来轮询。先read一个fd，如果没数据立即返回，然后等待若干时间，然后再read下一个fd，直到某个fd有数据读为止。这个方法有两个缺点，一是频繁的read调用浪费了cpu时间，但是大部分时间是没数据读的。二是每次read返回后等待的时间不好确定，太久会读取不及时，太短会使得cpu更加繁忙。</li>
<li>使用<code>异步IO</code>。当fd归属的设备准备好的时候，用信号通知处理进程。这个方法有两个缺点，一是信号在不同的系统上实现不同，移植性较差。二是进程收到的信号只有一种（SIGPOLL或者SIGIO），进程无法分辨是哪个fd。</li>
</ol>
<p>有没有比较完善的方案？io多路复用来了。这种方案会记录一个我们需要的fd的列表，然后我们去查询，当这个列表中有fd有数据时，查询就会返回这个fd。</p>
<h1 id="IO多路复用：select"><a href="#IO多路复用：select" class="headerlink" title="IO多路复用：select"></a>IO多路复用：select</h1><p><code>int select(int nfds, fd_set *readfds, fd_set *writefds, fd_set *exceptfds, struct timeval *timeout);</code> select 函数提供了查询fd状态的能力。我们传入希望监听的fd，内核告诉我们哪些fd已经有事件发生并返回。</p>
<ol>
<li>参数 <code>timeout</code> 控制愿意等待的时间。timeval为NULL时，select会一直阻塞等待某个fd准备好。（如果接收到信号，select也会提前返回-1，并把errno设为INTR）</li>
<li>参数 <code>readfds</code>, <code>writefds</code>, <code>exceptfds</code> 是我们告诉内核希望监听的fd的指针。如果希望监听某fd的读事件，就加入到<code>readfds</code>中。后面两个用于监听写事件和异常事件。</li>
<li>返回时，<code>readfds</code>, <code>writefds</code>, <code>exceptfds</code> 中会留下对应事件已经就绪的fd。此时这些fd是可读的、可写的或发生异常的。</li>
<li>参数 <code>nfds</code> 是三个fd集合中的最大值加一，它制定了fd的遍历范围。也可以把它设为<code>FD_SETSIZE</code>，一般为1024，但是这样会导致select遍历系统中所有的fd。</li>
<li>返回值：a)如果没有就绪的fd，函数返回0。b)如果有就绪的fd，函数返回就绪的fd数量。c)如果收到信号，返回-1，并把errno设为INTR d)特别地，如果fd到达EOF，函数调用read，然后返回0</li>
<li>函数<code>pselect</code>采用了timespec类型的超时设置。此外，还可以设置sigmask用来屏蔽一些信号，防止自己被终止。</li>
</ol>
<p><code>fd_set</code> 是一个fd的集合。在实现上是一个大bit数组，它只支持赋值操作和下面宏定义的操作：<br><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 如果fd在fd_set中返回0</span></span><br><span class="line"><span class="function"><span class="keyword">int</span>  <span class="title">FD_ISSET</span><span class="params">(<span class="keyword">int</span> fd, fd_set *<span class="built_in">set</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化fd_set</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">FD_ZERO</span><span class="params">(fd_set *<span class="built_in">set</span>)</span></span>;</span><br><span class="line"><span class="comment">// 设置某个fd</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">FD_SET</span><span class="params">(<span class="keyword">int</span> fd, fd_set *<span class="built_in">set</span>)</span></span>;</span><br><span class="line"><span class="comment">// 清除某个fd</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">FD_CLR</span><span class="params">(<span class="keyword">int</span> fd, fd_set *<span class="built_in">set</span>)</span></span>;</span><br></pre></td></tr></table></figure></p>
<p>下面给了一个select的例子。他会等待输入五秒钟，如果有输入会立马回显。如果超时，则会直接退出。</p>
<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TIMEOUT 5       <span class="comment">/* select timeout in seconds */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BUF_LEN 1024    <span class="comment">/* read buffer in bytes */</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( <span class="keyword">void</span> )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">struct</span> timeval	tv;</span><br><span class="line">	fd_set		readfds;</span><br><span class="line">	<span class="keyword">int</span>		ret;</span><br><span class="line"><span class="comment">/* Wait on stdin for input. */</span></span><br><span class="line">	FD_ZERO( &amp;readfds );</span><br><span class="line">	FD_SET( STDIN_FILENO, &amp;readfds );</span><br><span class="line"><span class="comment">/* Wait up to five seconds. */</span></span><br><span class="line">	tv.tv_sec	= TIMEOUT;</span><br><span class="line">	tv.tv_usec	= <span class="number">0</span>;</span><br><span class="line"><span class="comment">/* All right, now block! */</span></span><br><span class="line">	ret = select( STDIN_FILENO + <span class="number">1</span>,</span><br><span class="line">		      &amp;readfds,</span><br><span class="line">		      <span class="literal">NULL</span>,</span><br><span class="line">		      <span class="literal">NULL</span>,</span><br><span class="line">		      &amp;tv );</span><br><span class="line">	<span class="keyword">if</span> ( ret == <span class="number">-1</span> )</span><br><span class="line">	&#123;</span><br><span class="line">		perror(<span class="string">"select"</span>);</span><br><span class="line">		<span class="keyword">return</span>(<span class="number">1</span>);</span><br><span class="line">	&#125; <span class="keyword">else</span> <span class="keyword">if</span> ( !ret )</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"%d seconds elapsed.\n"</span>, TIMEOUT );</span><br><span class="line">		<span class="keyword">return</span>(<span class="number">0</span>);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> *  * Is our file descriptor ready to read?</span></span><br><span class="line"><span class="comment"> *   * (It must be, as it was the only fd that</span></span><br><span class="line"><span class="comment"> *    * we provided and the call returned</span></span><br><span class="line"><span class="comment"> *     * nonzero, but we will humor ourselves.)</span></span><br><span class="line"><span class="comment"> *      */</span></span><br><span class="line">	<span class="keyword">if</span> ( FD_ISSET( STDIN_FILENO, &amp;readfds ) )</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">char</span>	buf[BUF_LEN + <span class="number">1</span>];</span><br><span class="line">		<span class="keyword">int</span>	len;</span><br><span class="line"><span class="comment">/* guaranteed to not block */</span></span><br><span class="line">		len = read( STDIN_FILENO, buf, BUF_LEN );</span><br><span class="line">		<span class="keyword">if</span> ( len == <span class="number">-1</span> )</span><br><span class="line">		&#123;</span><br><span class="line">			perror(<span class="string">"read"</span>);</span><br><span class="line">			<span class="keyword">return</span>(<span class="number">1</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> ( len )</span><br><span class="line">		&#123;</span><br><span class="line">			buf[len] =<span class="string">'\0'</span>;</span><br><span class="line">			<span class="built_in">printf</span>(<span class="string">"read: %s \n"</span>, buf );</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span>(<span class="number">0</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">fprintf</span>( <span class="built_in">stderr</span>,<span class="string">"This should not happen !\n"</span>);</span><br><span class="line">	<span class="keyword">return</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="IO多路复用：poll"><a href="#IO多路复用：poll" class="headerlink" title="IO多路复用：poll"></a>IO多路复用：poll</h1><p>select有一些缺点。由于传入的fd的集合是一个bit数组，所以select必须得从0开始一直扫描到nfds指定的最大值，效率很低。poll优化了select的一些易用性的问题。</p>
<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">poll</span><span class="params">(<span class="keyword">struct</span> pollfd *fds, <span class="keyword">nfds_t</span> nfds, <span class="keyword">int</span> timeout)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> pollfd &#123;</span><br><span class="line">    <span class="keyword">int</span>   fd;         <span class="comment">/* file descriptor */</span></span><br><span class="line">    <span class="keyword">short</span> events;     <span class="comment">/* requested events */</span></span><br><span class="line">    <span class="keyword">short</span> revents;    <span class="comment">/* returned events */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// short events 定义</span></span><br><span class="line">POLLIN 有数据可读。（和POLLRDNORM | POLLRDBAND 等价）</span><br><span class="line">POLLRDNORM 有正常数据可读。</span><br><span class="line">POLLRDBAND 有优先数据可读。</span><br><span class="line">POLLPRI 有高优先数据可读。</span><br><span class="line">POLLOUT 写操作不会阻塞。（和POLLWRNORM | POLLBAND 等价）</span><br><span class="line">POLLWRNORM 写正常数据不会阻塞。</span><br><span class="line">POLLBAND 写优先数据不会阻塞。</span><br><span class="line">POLLMSG 有一个SIGPOLL消息可用。</span><br><span class="line"></span><br><span class="line">POLLER 输入的fd有错误。</span><br><span class="line">POLLHUP fd被挂起。（请注意，文件到达EOF，会返回POLLIN，然后read返回<span class="number">0</span>）</span><br><span class="line">POLLNVAL 输入的fd无效。</span><br><span class="line"></span><br><span class="line">POLLIN | POLLPRI 对应select的读事件</span><br><span class="line">POLLOUT | POLLWRBAND 对应select的写事件</span><br></pre></td></tr></table></figure>
<p>poll 函数提供了一个select的改进版本。</p>
<ol>
<li>参数 <code>fds</code> 是一个<code>poolfd</code>的指针，或者一个数组（同时监听多个fd）。<code>poolfd</code>这个结构体包含三个参数，监视的文件描述符<code>fd</code>，关注的event事件类型<code>events</code>，和内核返回的fd的事件类型<code>revents</code> 。</li>
<li>参数 <code>nfds</code> 告诉我们需要监听的fd数量，即参数 <code>fds</code> 的长度。</li>
<li>参数 <code>timeout</code> 告诉我们等待多久超时，单位为ms。为-1时，poll永远不超时。为0时，poll立即返回。为正数时，poll等待对应的毫秒数。</li>
</ol>
<p>那么到底poll有哪些改进？</p>
<ol>
<li>poll优化了参数。a)无需调用者知道fd最大值加一是什么。b)而且不用每次调用前都初始化select的fdset（select会改变自己的fd集合参数）。</li>
<li>poll效率更高。它只会在参数里的几个fd中查询fd状态（这些fd放在也给链表中）。而select必须从0开始查询到fd最大值。因此poll不必局限于文件描述符的数量。<br>不过，select也提供了更为精确的超时时间，以及更好的可移植性。</li>
</ol>
<p>下面给出了一个poll的例子。他会检测读写标准输入输出的可用状态并打印。<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;poll.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TIMEOUT 5 <span class="comment">/* poll timeout, in seconds */</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">( <span class="keyword">void</span> )</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">struct</span> pollfd	fds[<span class="number">2</span>];</span><br><span class="line">	<span class="keyword">int</span>		ret;</span><br><span class="line">	<span class="comment">/* watch stdin for input */</span></span><br><span class="line">	fds[<span class="number">0</span>].fd	= STDIN_FILENO;</span><br><span class="line">	fds[<span class="number">0</span>].events	= POLLIN;</span><br><span class="line">	<span class="comment">/* watch stdout for ability to write (almost always true) */</span></span><br><span class="line">	fds[<span class="number">1</span>].fd	= STDOUT_FILENO;</span><br><span class="line">	fds[<span class="number">1</span>].events	= POLLOUT;</span><br><span class="line">	<span class="comment">/* All set, block! */</span></span><br><span class="line">	ret = poll( fds, <span class="number">2</span>, TIMEOUT * <span class="number">1000</span> );</span><br><span class="line">	<span class="keyword">if</span> ( ret == <span class="number">-1</span> )</span><br><span class="line">	&#123;</span><br><span class="line">		perror( <span class="string">"poll"</span> );</span><br><span class="line">		<span class="keyword">return</span>(<span class="number">1</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> ( !ret )</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>( <span class="string">"%d seconds elapsed.\n"</span>, TIMEOUT );</span><br><span class="line">		<span class="keyword">return</span>(<span class="number">0</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> ( fds[<span class="number">0</span>].revents &amp; POLLIN )</span><br><span class="line">		<span class="built_in">printf</span>( <span class="string">"stdin is readable\n"</span> );</span><br><span class="line">	<span class="keyword">if</span> ( fds[<span class="number">1</span>].revents &amp; POLLOUT )</span><br><span class="line">		<span class="built_in">printf</span>( <span class="string">"stdout is writable\n"</span> );</span><br><span class="line">	<span class="keyword">return</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// ./poll 时，poll会发现写此时可用。输出stdout is writable</span></span><br><span class="line"><span class="comment">// ./poll &lt; a.txt poll会发现写此时读写都可用。输出stdout is writable stdin is readable</span></span><br></pre></td></tr></table></figure></p>
<h1 id="IO多路复用：epoll"><a href="#IO多路复用：epoll" class="headerlink" title="IO多路复用：epoll"></a>IO多路复用：epoll</h1><p>select主要缺陷是，对单个进程打开的文件描述是有一定限制的，它由FD_SETSIZE设置，默认值是1024，虽然可以通过编译内核改变，但相对麻烦，另外在检查数组中是否有文件描述需要读写时，采用的是线性扫描的方法，即不管这些socket是不是活跃的，我都轮询一遍，所以效率比较低。<br>poll本质和select没有区别，但其采用链表存储，解决了select最大连接数存在限制的问题，但其也是采用遍历的方式来判断是否有设备就绪，所以效率比较低，另外一个问题是大量的fd数组在用户空间和内核空间之间来回复制传递（每次调用poll都会拷贝），也浪费了不少性能。<br>我们用一种新的事件驱动技术epoll来解决上述问题。</p>
<h2 id="初始化和注册事件：epoll-create-epoll-ctl"><a href="#初始化和注册事件：epoll-create-epoll-ctl" class="headerlink" title="初始化和注册事件：epoll_create / epoll_ctl"></a>初始化和注册事件：epoll_create / epoll_ctl</h2><p><code>int epoll_create(int size)</code> 创建一个epoll实例，返回该实例的fd。</p>
<ol>
<li>参数<code>size</code>为估计的处理fd数量，越精确性能越高。Linux 2.6后不在使用该参数</li>
<li>成功时，返回fd。失败时，返回-1，并设置errno。</li>
<li>返回的fd和要处理的fd没有关联，只是用来调用epoll。</li>
</ol>
<p><code>int epoll_ctl(int epfd, int op, int fd, struct epoll_event *event)</code> 在一个epoll中添加监听的fd和对应的时间类型</p>
<ol>
<li>参数<code>epfd</code>为上面create返回的epollfd</li>
<li>参数<code>op</code>有几个值：<code>EPOLL_CTL_ADD</code> 添加fd，<code>EPOLL_CTL_DEL</code> 删除fd，<code>EPOLL_CTL_MOD</code> 改变fd监听类型。</li>
<li>参数<code>fd</code>是监听的fd对象。</li>
<li>参数<code>event</code>见下面的代码块。</li>
<li>成功后，epoll_ctl返回0 。失败时，返回-1，并设置errno。</li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> epoll_data &#123;</span><br><span class="line">    <span class="keyword">void</span>        *ptr;</span><br><span class="line">    <span class="keyword">int</span>          fd;</span><br><span class="line">    <span class="keyword">__uint32_t</span>   u32;</span><br><span class="line">    <span class="keyword">__uint64_t</span>   u64;</span><br><span class="line">&#125; <span class="keyword">epoll_data_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> epoll_event &#123;</span><br><span class="line">    <span class="keyword">__uint32_t</span>   events;      <span class="comment">/* Epoll 事件类型，见下面 */</span></span><br><span class="line">    <span class="keyword">epoll_data_t</span> data;        <span class="comment">/* 用户存储数据，稍后会返回 */</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Epoll 事件类型       </span></span><br><span class="line">EPOLLIN 文件可读</span><br><span class="line">EPOLLOUT 文件可写</span><br><span class="line">EPOLLPRI 高优先级文件可读</span><br><span class="line">EPOLLET 设置为边沿触发，默认为水平触发。</span><br><span class="line">EPOLLHUP 文件挂起。默认会监听</span><br><span class="line">EPOLLERR 文件出错。默认会监听</span><br><span class="line">EPOLLONESHOT 一次性监听，有事件发生并处理后不再监听该文件。</span><br><span class="line"></span><br><span class="line"><span class="comment">// 下面是一个关联事件类型的样例</span></span><br><span class="line"><span class="keyword">struct</span> epoll_event event;</span><br><span class="line">event.data.fd = fd;</span><br><span class="line">event.events = EPOLLIN | EPOLLOUT;</span><br><span class="line"><span class="keyword">int</span> ret = epoll_ctl(epfd, EPOLL_CTL_ADD, fd, &amp;event);</span><br><span class="line"><span class="keyword">if</span>(ret != <span class="number">0</span>) perror(<span class="string">"epoll_ctl"</span>)</span><br></pre></td></tr></table></figure>
<h2 id="监听事件：epoll-wait"><a href="#监听事件：epoll-wait" class="headerlink" title="监听事件：epoll_wait"></a>监听事件：epoll_wait</h2><p><code>int epoll_wait(int epfd, struct epoll_event *events, int maxevents, int timeout)</code> 等待某个epoll上的fd发生事件，时限为timeout毫秒。</p>
<ol>
<li>参数<code>epfd</code>为需要等待的epollfd</li>
<li>参数<code>events</code>为返回的事件的地址。也可以是一个数组。</li>
<li>参数 <code>maxevents</code> 表明返回的事件数量不能超过该值，一般时数组的长度。</li>
<li>参数 <code>timeout</code> 告诉我们等待多久超时，单位为ms。为-1时，poll永远不超时。为0时，poll立即返回。为正数时，poll等待对应的毫秒数。</li>
</ol>
<p>下面时一个完整的wait样例。<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX_EVENTS 64</span></span><br><span class="line"><span class="keyword">struct</span> epoll_event	*events;</span><br><span class="line"><span class="keyword">int</span>	nr_events, i, epfd;</span><br><span class="line">events = <span class="built_in">malloc</span>( <span class="keyword">sizeof</span>(<span class="keyword">struct</span> epoll_event) * MAX_EVENTS );</span><br><span class="line"><span class="keyword">if</span> ( !events )</span><br><span class="line">&#123;</span><br><span class="line">	perror( <span class="string">"malloc"</span> );</span><br><span class="line">	<span class="keyword">return</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line">nr_events = epoll_wait( epfd, events, MAX_EVENTS, −<span class="number">1</span> );</span><br><span class="line"><span class="keyword">if</span> ( nr_events &lt; <span class="number">0</span> )</span><br><span class="line">&#123;</span><br><span class="line">	perror( <span class="string">"epoll_wait"</span> );</span><br><span class="line">	<span class="built_in">free</span>( events );</span><br><span class="line">	<span class="keyword">return</span>(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">for</span> ( i = <span class="number">0</span>; i &lt; nr_events; i++ )</span><br><span class="line">&#123;</span><br><span class="line">	<span class="built_in">printf</span>( <span class="string">"event=%ld on fd=%d\n"</span>,</span><br><span class="line">		events[i].events,</span><br><span class="line">		events[i].data.fd );</span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * We now can, per events[i].events, operate on</span></span><br><span class="line"><span class="comment">	 * events[i].data.fd without blocking.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">free</span>( events )</span><br></pre></td></tr></table></figure></p>
<h2 id="边沿触发和水平触发"><a href="#边沿触发和水平触发" class="headerlink" title="边沿触发和水平触发"></a>边沿触发和水平触发</h2><h3 id="两种触发的区别"><a href="#两种触发的区别" class="headerlink" title="两种触发的区别"></a>两种触发的区别</h3><p>epoll默认的工作方式是水平触发。在水平触发模式下，只要满足fd可读/写，每次调用epoll_wait都会返回这个fd。在边沿触发模式下，只有fd从不可读(写)变为可读(写)时，才会返回这个fd。（读缓冲区有数据就是可读，写缓冲区不满就是可写）</p>
<p>考虑下面的情况，两种触发模式的epoll都监听了读缓冲区：</p>
<ol>
<li>读缓冲区刚开始是空的</li>
<li>读缓冲区写入2KB数据</li>
<li>水平触发和边缘触发模式此时都会发出可读信号</li>
<li>收到信号通知后，读取了1kb的数据，读缓冲区还剩余1KB数据</li>
<li>水平触发会再次进行通知，而边缘触发不会再进行通知<br>所以，边缘触发需要一次性的把缓冲区的数据读完为止，也就是一直读，直到读到EGAIN为止，EGAIN说明缓冲区已经空了，因为这一点，边缘触发需要设置文件句柄为非阻塞。</li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//水平触发LT</span></span><br><span class="line">ret = read(fd, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line"></span><br><span class="line"><span class="comment">//边缘触发ET</span></span><br><span class="line"><span class="keyword">while</span>(<span class="literal">true</span>) &#123;</span><br><span class="line">    ret = read(fd, buf, <span class="keyword">sizeof</span>(buf);</span><br><span class="line">    <span class="keyword">if</span> (ret == EAGAIN) <span class="keyword">break</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="两种触发的使用方法"><a href="#两种触发的使用方法" class="headerlink" title="两种触发的使用方法"></a>两种触发的使用方法</h3><p>参考 <a href="https://blog.csdn.net/dongfuye/article/details/50880251" target="_blank" rel="noopener"> dongyue-epoll LT/ET 深入剖析</a></p>
<p>LT的处理过程：<br>. accept一个连接，添加到epoll中监听EPOLLIN事件<br>. 当EPOLLIN事件到达时，read fd中的数据并处理<br>. 当需要写出数据时，把数据write到fd中；如果数据较大，无法一次性写出，那么在epoll中监听EPOLLOUT事件<br>. 当EPOLLOUT事件到达时，继续把数据write到fd中；如果数据写出完毕，那么在epoll中关闭EPOLLOUT事件</p>
<p>ET的处理过程：<br>. accept一个一个连接，添加到epoll中监听EPOLLIN|EPOLLOUT事件<br>. 当EPOLLIN事件到达时，read fd中的数据并处理，read需要一直读，直到返回EAGAIN为止<br>. 当需要写出数据时，把数据write到fd中，直到数据全部写完，或者write返回EAGAIN<br>. 当EPOLLOUT事件到达时，继续把数据write到fd中，直到数据全部写完，或者write返回EAGAIN</p>
<p>使用ET模式，特定场景下会比LT更快，因为它可以便捷的处理EPOLLOUT事件，省去打开与关闭EPOLLOUT的<code>epoll_ctl(EPOLL_CTL_MOD)</code>调用。从而有可能让你的性能得到一定的提升。</p>
<p>例如你需要写出1M的数据，写出到socket 256k时，返回了EAGAIN, 然后：</p>
<ol>
<li>ET模式下，当再次epoll返回EPOLLOUT事件时，继续写出待写出的数据，当没有数据需要写出时，不处理直接略过即可。</li>
<li>LT模式则需要先打开EPOLLOUT，当没有数据需要写出时，再关闭EPOLLOUT（否则会一直返回EPOLLOUT事件）</li>
</ol>
<p>总体来说，ET处理EPOLLOUT方便高效些，LT不容易遗漏事件、不易产生bug如果server的响应通常较小，不会触发EPOLLOUT，那么适合使用LT，例如redis等。而nginx作为高性能的通用服务器，网络流量可以跑满达到1G，这种情况下很容易触发EPOLLOUT，则使用ET。</p>
<h2 id="和select-poll-相比"><a href="#和select-poll-相比" class="headerlink" title="和select/poll 相比"></a>和select/poll 相比</h2><p><a href="https://cloud.tencent.com/developer/article/1373483" target="_blank" rel="noopener"> 各种IO复用模型的比较-云社区 </a></p>
<p>epoll和kqueue （FreeBSD上和epoll类似的组件）是更先进的IO复用模型。比起select和poll来说：</p>
<ol>
<li>没有最大连接数的限制。1G内存，可以打开约10万左右的连接。而且仅仅使用一个文件描述符，就可以管理多个文件描述符。</li>
<li>内核拷贝只在初始化后发生一次。将用户关系的文件描述符的事件存放到内核的一个事件表中（底层采用的是mmap的方式），这样在用户空间和内核空间的copy只需一次。</li>
<li>复杂度O(1)。这种模型里面，采用了类似事件驱动的回调机制或者叫通知机制，在注册fd时加入特定的状态，一旦fd就绪就会主动通知内核。这样以来就避免了select的无脑遍历socket的方法，这种模式下仅仅是活跃的socket连接才会主动通知内核，所以直接将时间复杂度降为O(1)。</li>
</ol>
]]></content>
    
    <summary type="html">
    
      当处理多个IO时，阻塞往往不是可行的方案。本文介绍了select、poll、epoll的用法和注意点。
    
    </summary>
    
      <category term="Linux" scheme="chestnutheng.cn/categories/Linux/"/>
    
    
      <category term="Linux，后台" scheme="chestnutheng.cn/tags/Linux%EF%BC%8C%E5%90%8E%E5%8F%B0/"/>
    
  </entry>
  
  <entry>
    <title>[Linux]谈一谈并行Counting</title>
    <link href="chestnutheng.cn/2019/07/26/counting/"/>
    <id>chestnutheng.cn/2019/07/26/counting/</id>
    <published>2019-07-26T08:49:50.000Z</published>
    <updated>2019-07-26T10:26:43.000Z</updated>
    
    <content type="html"><![CDATA[<h1 id="简单的并行计数"><a href="#简单的并行计数" class="headerlink" title="简单的并行计数"></a>简单的并行计数</h1><p>在一个简单的多线程计数程序中，我们假设要每个线程去把sum的值多加100m，同时进行。代码如下：<br><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">if</span> 0</span></span><br><span class="line">  <span class="meta">#<span class="meta-keyword">define</span> ADD_P(x) __sync_fetch_and_add((x), 1)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line">  <span class="meta">#<span class="meta-keyword">define</span> ADD_P(x) (++(*x))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TC 8</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">thgo</span><span class="params">(<span class="keyword">void</span> *arg)</span></span>&#123;</span><br><span class="line">  <span class="keyword">long</span> i = <span class="number">1000</span>*<span class="number">1000</span>*<span class="number">100</span>;</span><br><span class="line">  <span class="keyword">while</span>(i-- &gt; <span class="number">0</span>)&#123;ADD_P((<span class="keyword">long</span> *)arg);&#125;;</span><br><span class="line">  <span class="keyword">pthread_t</span> me = pthread_self();</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"thread sum: %ld tid: %lu \n"</span>,  *(<span class="keyword">long</span> *)arg, (<span class="keyword">unsigned</span> <span class="keyword">long</span>)me);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span>&#123;</span><br><span class="line">  <span class="keyword">long</span> sum = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">pthread_t</span> ths[TC];</span><br><span class="line">  <span class="comment">// threads</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; TC; ++i)&#123;</span><br><span class="line">      pthread_create(&amp;ths[i], <span class="literal">NULL</span>, thgo, &amp;sum);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// main thread</span></span><br><span class="line">  thgo(&amp;sum);</span><br><span class="line">  <span class="comment">// join</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; TC; ++i)&#123;</span><br><span class="line">      pthread_join(ths[i], <span class="literal">NULL</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">"all final sum : %ld\n"</span>, sum);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>如果使用一般的计数，会出现严重的数据踩踏问题，导致结果只能取得一定近似的值：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">$ time ./t</span><br><span class="line">thread sum_added : 96683538 tid: 139864218973952 </span><br><span class="line">thread sum_added : 97597912 tid: 139864210581248 </span><br><span class="line">thread sum_added : 98631229 tid: 139864202188544 </span><br><span class="line">thread sum_added : 106625687 tid: 139864228308800 </span><br><span class="line">all final sum : 106628420</span><br><span class="line"></span><br><span class="line">real  0m1.079s</span><br><span class="line">user  0m4.240s</span><br><span class="line">sys	  0m0.000s</span><br></pre></td></tr></table></figure></p>
<p>如果切换原子原语，性能会下降大约八倍（4个线程，在一亿的计数时）。原因是一个变量被4个线程同时竞争，等待的时间大大加长。<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">thread sum_added : 297410188 tid: 140249129400064 </span><br><span class="line">thread sum_added : 323862913 tid: 140249121007360 </span><br><span class="line">thread sum_added : 325497470 tid: 140249137792768 </span><br><span class="line">thread sum_added : 399999202 tid: 140249147127616 </span><br><span class="line">all final sum : 400000000</span><br><span class="line"></span><br><span class="line">real 0m8.323s</span><br><span class="line">user 0m31.044s</span><br><span class="line">sys  0m0.000s</span><br></pre></td></tr></table></figure></p>
<p><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/count1.png" width="400"><br>上面是一个使用原子原语时，线程个数和花费时间的关系。随着线程个数的增多，耗时几乎是等比例增长。</p>
<h1 id="经常写计数，偶尔读计数"><a href="#经常写计数，偶尔读计数" class="headerlink" title="经常写计数，偶尔读计数"></a>经常写计数，偶尔读计数</h1><p>有一个程序每秒钟处理1亿左右的包，我们需要每五秒查看一下总包长。这是一个典型的经常写计数，偶尔读计数的场景。</p>
<h2 id="利用数组分开计数"><a href="#利用数组分开计数" class="headerlink" title="利用数组分开计数"></a>利用数组分开计数</h2><p>一个经典的方法是，取消上一节中进程们共享的变量，改为每个进程独有的变量，然后再做加和。在下面的代码中，用线程id作为key，线程自己的计数作为value建立了map，各个线程分别写入自己的计数器内，避免了冲突。伪代码如下：<br><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">counts = &#123;&#125;</span><br><span class="line">void write_count()&#123;</span><br><span class="line">   tid = pthread_self()</span><br><span class="line">   counts[tid] += <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">long read_count()&#123;</span><br><span class="line">    sum = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> tid, c <span class="keyword">in</span> counts&#123;</span><br><span class="line">        sum += c</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>这种方法有两个问题：</p>
<ol>
<li>读操作时，写操作依然在进行，导致结果不准确。读到的不同线程的计数，介于开始读到读结束的窗口之间。</li>
<li>读操作需要聚合所有线程，对读取端而言比较复杂</li>
</ol>
<h2 id="定时刷新，最终一致"><a href="#定时刷新，最终一致" class="headerlink" title="定时刷新，最终一致"></a>定时刷新，最终一致</h2><p>为了解决上述的两个问题，我们设计了最终一致的版本：每隔1ms把所有线程的计数加和，存入全局变量中。这样有两个优点：</p>
<ol>
<li>不一致窗口最多为1ms</li>
<li>读取的时候只要直接读变量就可以了，不用加和</li>
</ol>
<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line">counts = &#123;&#125;</span><br><span class="line">global_count = <span class="number">0</span></span><br><span class="line">void write_count()&#123;</span><br><span class="line">   tid = pthread_self()</span><br><span class="line">   counts[tid] += <span class="number">1</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">long read_count()&#123;</span><br><span class="line">    <span class="keyword">return</span> global_count</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 另起一个线程用来计数</span><br><span class="line">void eventual_count()&#123;</span><br><span class="line">    <span class="keyword">for</span> &#123;</span><br><span class="line">        sum = <span class="number">0</span></span><br><span class="line">        <span class="keyword">for</span> tid, c <span class="keyword">in</span> counts&#123;</span><br><span class="line">            sum += c</span><br><span class="line">        &#125;</span><br><span class="line">        global_count = sum</span><br><span class="line">        sleep <span class="number">1</span>ms</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Note: 线程数量增多时，<code>eventual_count</code>可能会越来越不准确。解决这个问题的方法是对线程分组，每组线程让一个<code>eventual_count</code>线程来处理计数，这样每个<code>eventual_count</code>线程都不会耗费太多的时间处理。必要的时候，可以采用树状的<code>eventual_count</code>线程，层级处理。</p>
<h2 id="线程私有的-thread"><a href="#线程私有的-thread" class="headerlink" title="线程私有的__thread"></a>线程私有的__thread</h2><p><code>__thread</code> gcc提供的关键字，修饰线程私有的变量。修饰后每个线程都有一份该变量实体，且值互不干扰。<br><code>counter</code> 是每个线程自己的计数器，long类型。<br><code>counterp</code> 是一个用于存放线程私有变量指针的数组。初始化时，把线程私有变量的地址写进数组。读取时，读取所有指针的值加和。<br><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="number">__</span>thread counter = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> *counterp[NR_THREADS] = &#123; <span class="literal">NULL</span> &#125;;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> finalcount = <span class="number">0</span>;</span><br><span class="line">DEFINE_SPINLOCK(final_mutex);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">inc_count</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	counter++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="title">read_count</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> t;</span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> sum;</span><br><span class="line"></span><br><span class="line">	spin_lock(&amp;final_mutex);</span><br><span class="line">	sum = finalcount;</span><br><span class="line">	for_each_thread(t)</span><br><span class="line">		<span class="keyword">if</span> (counterp[t] != <span class="literal">NULL</span>)</span><br><span class="line">			sum += *counterp[t];</span><br><span class="line">	spin_unlock(&amp;final_mutex);</span><br><span class="line">	<span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">count_register_thread</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> idx = smp_thread_id();</span><br><span class="line"></span><br><span class="line">	spin_lock(&amp;final_mutex);</span><br><span class="line">	counterp[idx] = &amp;counter;</span><br><span class="line">	spin_unlock(&amp;final_mutex);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="近似上限的计数"><a href="#近似上限的计数" class="headerlink" title="近似上限的计数"></a>近似上限的计数</h1><p>有一些场景需要我们限制计数器的上限，比如限制一个广告曝光的次数。各个线程处理广告的请求，每返回一个广告就给计数器加一，直到总上限到达，就不再返回广告。<br>比较理想的方案是把任务平均分给每个线程，当他们都达到上限时，认为总上限也已经到达。这样会有几个问题：</p>
<ol>
<li>每个线程曝光广告的速度不同，可能有点线程很快到达上限了，有的却没有曝光，攒了大量的指标。</li>
<li>这时候如果曝光满了的线程还要继续处理曝光，就要写别的线程的计数器。这会造成昂贵的跨线程通信。</li>
</ol>
<p>我们可以从上一节的最终一致的方法得到启发，把计数任务分割给各个线程。我们维护一个每个线程的计数器<code>counter</code>和每个线程的计数上限<code>countermax</code>，以及一个全局的计数器<code>globalcount</code>和全局的计数上限<code>globalcountmax</code>。<br>需要注意的是，我们额外引入了一个<code>globalreserve</code>。它在数值上是<code>countermax</code>的和，用来表示预分配给每个counter的名额，也算在已经用掉的指标里。</p>
<p>这个方法的思想在于，先预分配给每个线程一些指标。如果有一个线程指标都用掉了，那么就收集里面的计数，并给他分配新的指标。直到所有的指标都用尽为止。收集计数的过程，就是<code>globalize_count</code>。分配计数的过程，就是<code>balance_count</code>。流程如下图<br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/counters_add.png"><br><img src="https://chestnutheng-blog-1254282572.cos.ap-chengdu.myqcloud.com/counterflow.png"><br>这种设计就是一个并行快速路径的例子，这是一种重要的设计模式，适用于下面这种情况：在多数情况没有线程间通信和交互的开销，而偶尔进行的跨进程通信又使用了精心设计的（但是开销仍然很大）全局算法。<br>在阅读代码之后，还有几点需要思考：</p>
<ol>
<li>这种计数会有误差。这个方法给每个线程预分配了计数值，但是这些计数值未必有被真实使用。所以最大会有<code>globalreserve</code>大小的误差。</li>
<li>在每个线程刚开始计数时，<code>countermax</code>被设置为总上限除以线程数的值。此时<code>globalreserve</code>和总上限值相等，这意味着最差情况下，如果一直请求某一个特定线程，很快就到达上限了。</li>
<li>我们设置了<code>MAX_COUNTERMAX</code>来缓解第二点的问题。这样<code>globalreserve</code>不会迅速增长的过大。</li>
<li><code>countermax</code>的值直接决定了误差。当离上限还比较远时，可以给每线程变量countermax赋值一个比较大的数，这样对性能和扩展性比较有好处。当靠近上限时，可以给这些countermax赋值一个较小的数，这样可以降低超过<code>globalcountmax</code>的风险。</li>
<li>在balance操作的时候，我们把<code>counter</code> 设为 <code>countermax/2</code>，这样就可以保证，a)每次计数器把计数交还给总数时，至少有一半的计数被使用了。b)加减计数都能在快速路径中 c)第二点的问题得到缓解</li>
<li><code>MAX_COUNTERMAX</code> 导致不能进入快速路径的线程增加了，这会导致性能的降低。</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="number">__</span>thread counter = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="number">__</span>thread countermax = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> globalcountmax = <span class="number">10000</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> globalcount = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> globalreserve = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> *counterp[NR_THREADS] = &#123; <span class="literal">NULL</span> &#125;;</span><br><span class="line">DEFINE_SPINLOCK(gblcnt_mutex);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX_COUNTERMAX 100</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">globalize_count</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	globalcount += counter;</span><br><span class="line">	counter = <span class="number">0</span>;</span><br><span class="line">	globalreserve -= countermax;</span><br><span class="line">	countermax = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">balance_count</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	countermax = globalcountmax - globalcount - globalreserve;</span><br><span class="line">	countermax /= num_online_threads();</span><br><span class="line">	<span class="keyword">if</span> (countermax &gt; MAX_COUNTERMAX)</span><br><span class="line">		countermax = MAX_COUNTERMAX;</span><br><span class="line">	globalreserve += countermax;</span><br><span class="line">	counter = countermax / <span class="number">2</span>;</span><br><span class="line">	<span class="keyword">if</span> (counter &gt; globalcount)</span><br><span class="line">		counter = globalcount;</span><br><span class="line">	globalcount -= counter;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">add_count</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">long</span> delta)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="comment">// 减，而不是countermax + delta。防止整形溢出。</span></span><br><span class="line">	<span class="keyword">if</span> (countermax - counter &gt;= delta) &#123;</span><br><span class="line">		counter += delta;</span><br><span class="line">		<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">// 所有的全局变量访问都会上锁</span></span><br><span class="line">	spin_lock(&amp;gblcnt_mutex);</span><br><span class="line">	globalize_count();</span><br><span class="line">	<span class="keyword">if</span> (globalcountmax - globalcount - globalreserve &lt; delta) &#123;</span><br><span class="line">		spin_unlock(&amp;gblcnt_mutex);</span><br><span class="line">		<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	globalcount += delta;</span><br><span class="line">	balance_count();</span><br><span class="line">	spin_unlock(&amp;gblcnt_mutex);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="title">read_count</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> t;</span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span> sum;</span><br><span class="line"></span><br><span class="line">	spin_lock(&amp;gblcnt_mutex);</span><br><span class="line">	sum = globalcount;</span><br><span class="line">	for_each_thread(t)</span><br><span class="line">		<span class="keyword">if</span> (counterp[t] != <span class="literal">NULL</span>)</span><br><span class="line">			sum += *counterp[t];</span><br><span class="line">	spin_unlock(&amp;gblcnt_mutex);</span><br><span class="line">	<span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">count_register_thread</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> idx = smp_thread_id();</span><br><span class="line"></span><br><span class="line">	spin_lock(&amp;gblcnt_mutex);</span><br><span class="line">	counterp[idx] = &amp;counter;</span><br><span class="line">	spin_unlock(&amp;gblcnt_mutex);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      多线程计数依然是处理多线程问题中最基础的操作之一。本文讨论了简单的并行计数、最终一致的并行计数、有上限的并行计数等，视图从这个最简单的问题中学习并行设计的思维。
    
    </summary>
    
      <category term="后台" scheme="chestnutheng.cn/categories/%E5%90%8E%E5%8F%B0/"/>
    
    
      <category term="Linux，并行编程" scheme="chestnutheng.cn/tags/Linux%EF%BC%8C%E5%B9%B6%E8%A1%8C%E7%BC%96%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>[Linux]并行编程：进程、线程和同步</title>
    <link href="chestnutheng.cn/2019/07/25/pthread/"/>
    <id>chestnutheng.cn/2019/07/25/pthread/</id>
    <published>2019-07-25T07:45:04.000Z</published>
    <updated>2019-07-25T07:45:09.000Z</updated>
    
    <content type="html"><![CDATA[<p>如何进行并行编程？本文给了一些pthead库基本的多进程、多线程API，和详细的样例帮助理解这些API。<br>本教程基于<a href="http://www.apuebook.com/" target="_blank" rel="noopener">AUPE 2013</a>、 <a href="https://mirrors.edge.kernel.org/pub/linux/kernel/people/paulmck/perfbook/perfbook.html" target="_blank" rel="noopener">perfbook</a></p>
<h1 id="Shell中的并行"><a href="#Shell中的并行" class="headerlink" title="Shell中的并行"></a>Shell中的并行</h1><h2 id="后台执行-amp"><a href="#后台执行-amp" class="headerlink" title="后台执行 &amp;"></a>后台执行 &amp;</h2><p>通过&amp;符号指定实例在后台运行，然后统一等待结束。<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">compute_it 1 &gt; 1.out &amp;</span><br><span class="line">compute_it 2 &gt; 2.out &amp;</span><br><span class="line"><span class="built_in">wait</span></span><br><span class="line">cat 1.out 2.out</span><br></pre></td></tr></table></figure></p>
<h2 id="管道"><a href="#管道" class="headerlink" title="管道 |"></a>管道 |</h2><p>对于一个足够大的输入文件来说，grep模式匹配将与sed编辑和sort处理并行运行。<br><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">grep <span class="string">"<span class="variable">$pattern</span>"</span> | sed <span class="_">-e</span> <span class="string">"s/a/b/"</span> | sort</span><br></pre></td></tr></table></figure></p>
<h1 id="POSIX多进程"><a href="#POSIX多进程" class="headerlink" title="POSIX多进程"></a>POSIX多进程</h1><p>下面给出的程序中建立了一个进程，然后修改了x的值并打印，最后父进程等待子进程结束。</p>
<h2 id="fork"><a href="#fork" class="headerlink" title="fork"></a>fork</h2><p><code>int fork()</code>马上创建一个当前进程的子进程。子进程会复制(而不是共享)父进程的堆栈、数据空间、fd。</p>
<ol>
<li>如果是父进程，<code>fork</code>返回子进程的pid。如果是子进程，<code>fork</code>返回0。一般用这个区分不同的分支。</li>
<li>fork返回负数表示失败。失败的原因可能有：系统有了太多的进程；系统中用户进程数超过了CHILD_MAX。此时返回负数。</li>
<li>fork后如果不需要父进程的存储空间会立马调用exec。</li>
<li>使用<code>fork</code>一般有两个目的。父进程希望复制自己，或者想执行另外的程序（调用<code>exec</code>）</li>
<li>为了避免拷贝成本，出现了写时拷贝技术(Copy-On-Write, COW)，子进程创建后分享父进程的数据，并把内存区域设置为只读。当需要写数据时再为这块数据创建副本。</li>
</ol>
<h2 id="vfork"><a href="#vfork" class="headerlink" title="vfork"></a>vfork</h2><p><code>int vfork()</code>创建一个子进程，但分享(不复制)父进程的数据。当执行exec时父进程才退出休眠。专为了避免fork的拷贝成本设计。因为share父进程的数据有很大风险，所以man手册里明确说明vfork()之后，子进程只应该调用_exit()或者exec函数族。</p>
<h2 id="exit"><a href="#exit" class="headerlink" title="exit"></a>exit</h2><p><code>void exit(int)</code>退出当前进程。不像return会析构局部变量，弹栈，回到上级函数。如果在子进程的main中调用了return，main会返回两次，导致程序出错。exit不会有这个问题。</p>
<h2 id="wait"><a href="#wait" class="headerlink" title="wait"></a>wait</h2><p><code>pid_t wait(int &amp;status)</code> 阻塞等待任意一个子进程结束<br>a)如果子进程都在运行则阻塞<br>b)如果一个子进程已经终止，内核向父进程发出了SIGCHLD信号，则获得终止状态并立即返回<br>c)成功了返回pid，没有子进程，立即出错返回-1<br>d)子进程状态status可以用四个返回bool的宏 WIFEXITED(int)、WIFEXITSIGNALED(int)、WIFSTOPPED(int)、WIFCONTINUED(int)、来判断属于正常、异常、暂停、暂停后继续的状态。此外还有对应的WEIXTSTATUS(int)返回子进程exit函数的参数、WTERMSIG(int)返回信号编号、WCOREDUMP(int）返回是否生成coredump等。</p>
<p><code>pid_t waitpid(pid_t pid, int &amp;status, int option)</code>等待指定的pid的子进程结束。<br>a)当<code>pid=-1</code>，和<code>wait</code>等效。<br>b)当<code>option=WNOHANG</code>，此函数会立即返回不会阻塞。<br>c)成功了返回pid，没有子进程，立即出错返回-1</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;poll.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> x = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等待所有的子进程结束</span></span><br><span class="line"> <span class="keyword">static</span> <span class="number">__</span><span class="function">inline__ <span class="keyword">void</span> <span class="title">waitall</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"> </span>&#123;</span><br><span class="line">     <span class="keyword">int</span> pid;</span><br><span class="line">     <span class="keyword">int</span> status;</span><br><span class="line">     <span class="keyword">for</span> (;;) &#123;              </span><br><span class="line">         pid = wait(&amp;status);    <span class="comment">//sys/wait.h</span></span><br><span class="line">         <span class="keyword">if</span> (pid == <span class="number">-1</span>) &#123;</span><br><span class="line">             <span class="keyword">if</span> (errno == ECHILD)    <span class="comment">//errno.h 子进程不存在</span></span><br><span class="line">                 <span class="keyword">break</span>;      </span><br><span class="line">             perror(<span class="string">"wait"</span>);     </span><br><span class="line">             <span class="built_in">exit</span>(EXIT_FAILURE); </span><br><span class="line">         &#125;</span><br><span class="line">         poll(<span class="literal">NULL</span>, <span class="number">0</span>, <span class="number">1</span>);       <span class="comment">// 等待1ms</span></span><br><span class="line">     &#125;                   </span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> pid;</span><br><span class="line">    pid = fork();		<span class="comment">// &lt;unistd.h&gt;</span></span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123; <span class="comment">/* child */</span></span><br><span class="line">        x = <span class="number">1</span>;                  </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"Child process set x=1\n"</span>);  </span><br><span class="line">        <span class="built_in">exit</span>(EXIT_SUCCESS);         <span class="comment">//stdlib</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (pid &lt; <span class="number">0</span>) &#123; <span class="comment">/* parent, upon error */</span></span><br><span class="line">        perror(<span class="string">"fork"</span>);   <span class="comment">//stdio</span></span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);    <span class="comment">//stdlib</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* parent */</span></span><br><span class="line">    waitall(); </span><br><span class="line">    <span class="comment">//父进程一直等待到子进程退出后才调用printf（）。</span></span><br><span class="line">    <span class="comment">//通过不同进程并发地访问printf（）的I/O缓冲区并不简单，最好也不要这么做</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Parent process sees x=%d\n"</span>, x);    </span><br><span class="line">    <span class="keyword">return</span> EXIT_SUCCESS;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="POSIX多线程"><a href="#POSIX多线程" class="headerlink" title="POSIX多线程"></a>POSIX多线程</h1><h2 id="pthread-create"><a href="#pthread-create" class="headerlink" title="pthread_create"></a>pthread_create</h2><p><code>int pthread_create(pthread_t tidp, const pthread_attr_t *attr, void *(*start_rtn)(void *), void *arg)</code>创建一个线程去执行参数<code>start_rtn</code>中的函数。该函数参数为<code>arg</code>。<code>tidp</code>参数则会写入线程的线程ID。</p>
<ol>
<li>这三个指针都是<code>restrict</code>修饰的，代表保证他们不指向同一区域，使得编译器能够优化。</li>
<li><code>pthread_t</code>标识线程ID，是一个非负整数。为了移植性不能当<code>int</code>处理。<code>pthread_equal(tid1, tid2)</code>可以检测两个id是否相等。<code>pthread_self()</code>可以获取当前的线程ID。</li>
<li>成功返回0，失败返回 EINVAL、EAGAIN、EPERM。</li>
</ol>
<h2 id="pthread-exit"><a href="#pthread-exit" class="headerlink" title="pthread_exit"></a>pthread_exit</h2><p><code>void pthread_exit(void *status)</code>提供了一个线程退出的方法。<br>线程有三种退出方式：</p>
<ol>
<li>在线程函数中自然返回。返回值是线程的退出码。</li>
<li>被同进程的其他线程取消。此时线程的返回值是<code>PTHREAD_CANCELED</code>。</li>
<li>调用<code>pthread_exit</code>。返回值会通过参数<code>status</code>传出去。</li>
</ol>
<h2 id="pthread-join"><a href="#pthread-join" class="headerlink" title="pthread_join"></a>pthread_join</h2><p><code>int pthread_join(pthread_t thread, void **retval)</code> 用于在其他线程中等待指定的线程。</p>
<ol>
<li>该函数会一直卡住，等待ID为参数<code>thread</code>的线程退出。</li>
<li><code>retval</code>会接收等待的线程返回值。如果不关心返回值直接设为NULL。</li>
<li>成功时返回0，失败时返回EDEADLK、ESRCH、EINVAL。</li>
</ol>
<p><code>void pthread_cleanup_push(void (*routine)(void *), void *arg)</code>注册一个线程结束时调用的清理函数。参数routine为调用的函数指针，arg为该函数的参数。下面的情况会触发这个清理函数：<br>a）调用pthread_exit。<br>b）作为对取消线程请求(pthread_cancel)的响应。<br>c）以非0参数调用 <code>pthread_cleanup_pop(int)</code>。</p>
<p><code>pthread_cleanup_pop(int execute)</code>用于删除清理函数，当<code>execute</code>不为0时不会执行清理函数。需要注意此函数和push必须数量相等，不然会报错。</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> x = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mythread</span><span class="params">(<span class="keyword">void</span> *arg)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    x = <span class="number">1</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Child process set x=1\n"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> en; </span><br><span class="line">    <span class="keyword">pthread_t</span> tid;</span><br><span class="line">    <span class="keyword">void</span> *vp;</span><br><span class="line">    <span class="keyword">if</span> ((en = pthread_create(&amp;tid, <span class="literal">NULL</span>,mythread, <span class="literal">NULL</span>)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_join: %s\n"</span>, strerror(en));</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;   </span><br><span class="line">    <span class="comment">/* parent */</span></span><br><span class="line">    <span class="keyword">if</span> ((en = pthread_join(tid, &amp;vp)) != <span class="number">0</span>) &#123; </span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_join: %s\n"</span>, strerror(en));</span><br><span class="line">        <span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Parent process sees x=%d\n"</span>, x);</span><br><span class="line">    <span class="keyword">return</span> EXIT_SUCCESS;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="POSIX-锁"><a href="#POSIX-锁" class="headerlink" title="POSIX 锁"></a>POSIX 锁</h1><p>通常用互斥锁在避免资源间的竞争。当一个资源被锁住时，其他资源如果试图获得锁，他就会等待锁释放再执行。</p>
<h2 id="声明"><a href="#声明" class="headerlink" title="声明"></a>声明</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 声明</span></span><br><span class="line"><span class="keyword">pthread_mutex_t</span> mymutex;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 另一个静态声明+初始化方法，其中INITIALIZER是一个结构体 &#123; &#123; 0, 0, 0, 0, 0, 0, &#123; 0, 0 &#125; &#125; &#125;</span></span><br><span class="line"><span class="keyword">pthread_mutex_t</span> mtx = PTHREAD_MUTEX_INITIALIZER;</span><br></pre></td></tr></table></figure>
<h2 id="初始化和释放"><a href="#初始化和释放" class="headerlink" title="初始化和释放"></a>初始化和释放</h2><p><code>int pthread_mutex_init(pthread_mutex_t *restrict mutex, const pthread_mutexattr_t *restrict attr)</code> 使用前必须初始化。<code>mutex</code>是要初始化的锁。</p>
<ol>
<li><code>pthread_mutexattr_t</code>提供了锁的初始化选项。如果不需要设置为NULL。</li>
<li>成功返回0，失败返回错误码</li>
</ol>
<p><code>int pthread_mutex_destroy(pthread_mutex_t *mutex)</code>使用后锁必须被摧毁。</p>
<ol>
<li>成功返回0，失败返回错误码</li>
</ol>
<h2 id="加锁和释放锁"><a href="#加锁和释放锁" class="headerlink" title="加锁和释放锁"></a>加锁和释放锁</h2><p><code>int pthread_mutex_lock(pthread_mutex_t *mutex)</code> 占用锁，如果锁被占用，会一直等待<br><code>int pthread_mutex_unlock(pthread_mutex_t *mutex)</code> 释放锁</p>
<ol>
<li>成功返回0，失败返回错误码</li>
</ol>
<p><code>int pthread_mutex_trylock(pthread_mutex_t *mutex)</code> 试图占用锁<br>a) 如果锁没有被占用，会占用锁并返回0<br>b) 如果锁已经被占用，会返回<code>EBUSY</code></p>
<p><code>int pthread_mutex_timedlock(pthread_mutex_t *restrict mutex, const struct timespec *restrict abs_timeout)</code> 占用锁，如果锁被占用，会一直等待到<code>abs_timeout</code>这个时间点，返回超时</p>
<ol>
<li><code>abs_timeout</code>是绝对时间，是未来的一个时间戳</li>
<li>等待超时后返回<code>ETIMEDOUT</code></li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 声明、初始化</span></span><br><span class="line"><span class="keyword">pthread_mutex_t</span> mymutex;</span><br><span class="line">pthread_mutex_init(&amp;mymutex, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 锁</span></span><br><span class="line">pthread_mutex_lock(&amp;mymutex);</span><br><span class="line"><span class="comment">// ... do something</span></span><br><span class="line">pthread_mutex_unlock(&amp;mymutex);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 销毁</span></span><br><span class="line">pthread_mutex_destroy(&amp;mymutex);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 一个超时锁的例子</span></span><br><span class="line"><span class="keyword">struct</span> timespec tout;</span><br><span class="line"><span class="keyword">struct</span> tm *tmp;</span><br><span class="line"><span class="keyword">char</span> buf[<span class="number">64</span>];</span><br><span class="line"><span class="keyword">pthread_mutex_t</span> lock = PTHREAD_MUTEX_INITIALIZER;</span><br><span class="line">pthread_mutex_lock(&amp;lock);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"mutex is locked\n"</span>);</span><br><span class="line">clock_gettime(CLOCK_REALTIME, &amp;tout);</span><br><span class="line">tmp = localtime(&amp;tout.tv_sec);</span><br><span class="line">strftime(buf, <span class="keyword">sizeof</span>(buf), <span class="string">"%r"</span>, tmp);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"current time is %s\n"</span>, buf);</span><br><span class="line">tout.tv_sec += <span class="number">10</span>;    <span class="comment">/* 10 seconds from now */</span></span><br><span class="line"><span class="comment">/* caution: this could lead to deadlock */</span></span><br><span class="line">err = pthread_mutex_timedlock(&amp;lock, &amp;tout);</span><br><span class="line">clock_gettime(CLOCK_REALTIME, &amp;tout);</span><br><span class="line">tmp = localtime(&amp;tout.tv_sec);</span><br><span class="line">strftime(buf, <span class="keyword">sizeof</span>(buf), <span class="string">"%r"</span>, tmp);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">"the time is now %s\n"</span>, buf);</span><br></pre></td></tr></table></figure>
<p>下面的例子有两个线程。<br><code>lock_reader</code> 每隔1ms读一次全局变量x的值，用来监测x的变化<br><code>lock_writer</code> 每5ms把x的值加一<br>如果两个线程用了一把锁，reader会等待writer写完，再去读，x只被读到一次。<br>如果两个线程用了两把锁（等于不加锁），writer每次更改x都会被reader读到。</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;poll.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ACCESS_ONCE(x) (*(volatile typeof(x) *)&amp;(x))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> READ_ONCE(x) \</span></span><br><span class="line">            (&#123; typeof(x) <span class="number">___</span>x = ACCESS_ONCE(x); <span class="number">___</span>x; &#125;)</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WRITE_ONCE(x, val) (&#123; ACCESS_ONCE(x) = (val); &#125;)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">pthread_mutex_t</span> lock_a = PTHREAD_MUTEX_INITIALIZER;</span><br><span class="line"><span class="keyword">pthread_mutex_t</span> lock_b = PTHREAD_MUTEX_INITIALIZER;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> x = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">lock_reader</span><span class="params">(<span class="keyword">void</span> *arg)</span>				</span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> en;</span><br><span class="line">	<span class="keyword">int</span> i;</span><br><span class="line">	<span class="keyword">int</span> newx = <span class="number">-1</span>;</span><br><span class="line">	<span class="keyword">int</span> oldx = <span class="number">-1</span>;</span><br><span class="line">	<span class="keyword">pthread_mutex_t</span> *pmlp = (<span class="keyword">pthread_mutex_t</span> *)arg;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> ((en = pthread_mutex_lock(pmlp)) != <span class="number">0</span>) &#123;	</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"lock_reader:pthread_mutex_lock: %s\n"</span>,</span><br><span class="line">			strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;						</span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++)&#123;</span><br><span class="line">		newx = READ_ONCE(x);</span><br><span class="line">		<span class="keyword">if</span> (newx != oldx) &#123;</span><br><span class="line">			<span class="built_in">printf</span>(<span class="string">"lock_reader(): x = %d at %dms\n"</span>, newx, i);</span><br><span class="line">		&#125;</span><br><span class="line">		oldx = newx;</span><br><span class="line">		poll(<span class="literal">NULL</span>, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line">	&#125;</span><br><span class="line">        <span class="keyword">if</span> ((en = pthread_mutex_unlock(pmlp)) != <span class="number">0</span>) &#123;	</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"lock_reader:pthread_mutex_lock: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;						</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;							</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">lock_writer</span><span class="params">(<span class="keyword">void</span> *arg)</span>				</span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> en;</span><br><span class="line">	<span class="keyword">int</span> i;</span><br><span class="line">	<span class="keyword">pthread_mutex_t</span> *pmlp = (<span class="keyword">pthread_mutex_t</span> *)arg;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> ((en = pthread_mutex_lock(pmlp)) != <span class="number">0</span>) &#123;	</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"lock_writer:pthread_mutex_lock: %s\n"</span>,</span><br><span class="line">			strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;						</span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">		WRITE_ONCE(x, READ_ONCE(x) + <span class="number">1</span>);</span><br><span class="line">		poll(<span class="literal">NULL</span>, <span class="number">0</span>, <span class="number">5</span>);</span><br><span class="line">	&#125;						</span><br><span class="line">	<span class="keyword">if</span> ((en = pthread_mutex_unlock(pmlp)) != <span class="number">0</span>) &#123;	</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"lock_writer:pthread_mutex_lock: %s\n"</span>,</span><br><span class="line">			strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;						</span><br><span class="line">	<span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;							</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> en;</span><br><span class="line">	<span class="keyword">pthread_t</span> tid1;</span><br><span class="line">	<span class="keyword">pthread_t</span> tid2;</span><br><span class="line">	<span class="keyword">void</span> *vp;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"Creating two threads using same lock:\n"</span>);</span><br><span class="line">	en = pthread_create(&amp;tid1, <span class="literal">NULL</span>, lock_reader, &amp;lock_a);</span><br><span class="line">	<span class="keyword">if</span> (en != <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_create: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;							</span><br><span class="line">	en = pthread_create(&amp;tid2, <span class="literal">NULL</span>, lock_writer, &amp;lock_a);</span><br><span class="line">	<span class="keyword">if</span> (en != <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_create: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;							</span><br><span class="line">	<span class="keyword">if</span> ((en = pthread_join(tid1, &amp;vp)) != <span class="number">0</span>) &#123;	</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_join: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> ((en = pthread_join(tid2, &amp;vp)) != <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_join: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;						</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"Creating two threads w/different locks:\n"</span>);</span><br><span class="line">	x = <span class="number">0</span>;</span><br><span class="line">	en = pthread_create(&amp;tid1, <span class="literal">NULL</span>, lock_reader, &amp;lock_a);</span><br><span class="line">	<span class="keyword">if</span> (en != <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_create: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;</span><br><span class="line">	en = pthread_create(&amp;tid2, <span class="literal">NULL</span>, lock_writer, &amp;lock_b);</span><br><span class="line">	<span class="keyword">if</span> (en != <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_create: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> ((en = pthread_join(tid1, &amp;vp)) != <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_join: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> ((en = pthread_join(tid2, &amp;vp)) != <span class="number">0</span>) &#123;</span><br><span class="line">		<span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">"pthread_join: %s\n"</span>, strerror(en));</span><br><span class="line">		<span class="built_in">exit</span>(EXIT_FAILURE);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> EXIT_SUCCESS;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Creating two threads using same lock:</span></span><br><span class="line"><span class="comment">// lock_reader(): x = 0 at 0ms</span></span><br><span class="line"><span class="comment">// Creating two threads w/different locks:</span></span><br><span class="line"><span class="comment">// lock_reader(): x = 0 at 0ms</span></span><br><span class="line"><span class="comment">// lock_reader(): x = 1 at 1ms</span></span><br><span class="line"><span class="comment">// lock_reader(): x = 2 at 5ms</span></span><br><span class="line"><span class="comment">// lock_reader(): x = 3 at 10ms</span></span><br></pre></td></tr></table></figure>
<h1 id="POSIX-读写锁"><a href="#POSIX-读写锁" class="headerlink" title="POSIX 读写锁"></a>POSIX 读写锁</h1><p>和互斥锁类似，读写锁用来专门为读写操作加锁。读写锁有三种状态：</p>
<ol>
<li>锁未被占用。此时可以读，可以写。</li>
<li>锁被读锁占用（共享锁）。此时可以读，不可以写。</li>
<li>锁被写锁占用（互斥锁）。此时不可读不可写。</li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 初始化锁，参数同互斥锁。</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_init</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *<span class="keyword">restrict</span> rwlock, <span class="keyword">const</span> <span class="keyword">pthread_rwlockattr_t</span> *<span class="keyword">restrict</span> attr)</span></span></span><br><span class="line"><span class="function"><span class="comment">// 或者</span></span></span><br><span class="line"><span class="function">pthread_rwlock_t rwl </span>= PTHREAD_RWLOCK_INITIALIZER;</span><br><span class="line"><span class="comment">// 摧毁锁，参数同互斥锁。</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_destroy</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span> </span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function"><span class="comment">// 加读锁</span></span></span><br><span class="line"><span class="function"><span class="title">pthread_rwlock_rdlock</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="title">pthread_rwlock_tryrdlock</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="title">pthread_rwlock_timedrdlock</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="comment">// 加写锁</span></span></span><br><span class="line"><span class="function"><span class="title">pthread_rwlock_wrlock</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="title">pthread_rwlock_trywrlock</span><span class="params">()</span></span></span><br><span class="line"><span class="function"><span class="title">pthread_rwlock_timedwrlock</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"><span class="comment">// 解锁</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_rwlock_unlock</span><span class="params">(<span class="keyword">pthread_rwlock_t</span> *rwlock)</span></span></span><br></pre></td></tr></table></figure>
<h1 id="POSIX-条件变量"><a href="#POSIX-条件变量" class="headerlink" title="POSIX 条件变量"></a>POSIX 条件变量</h1><p> 条件变量用来让线程等待某个条件达成。我们在需要等待条件成功的地方加上while和<code>pthread_cond_wait</code>，以便于等待条件就绪后继续执行，否则卡在这里；我们在促使条件转为成功的地方加上<code>pthread_cond_signal</code>通知条件已经就绪。如果有多个线程在等待条件，可以使用<code>pthread_cond_broadcast</code>通知所有线程就绪；但是这种通知是顺序进行的（因为会搭配使用lock，lock会等待），那些醒来后发现条件又不满足的线程会继续进入等待。</p>
<ol>
<li>条件变量的访问由互斥量保护，使用时必须加锁</li>
<li><code>pthread_cond_wait</code>在调用时必须在while循环里。由于假醒/多核竞争等原因，内核的建议是我们配套使用while循环 <a href="http://blog.vladimirprus.com/2005/07/spurious-wakeups.html" target="_blank" rel="noopener">为什么？</a></li>
</ol>
<h2 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h2><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 条件变量使用前也需要初始化，使用完需要释放</span></span><br><span class="line"><span class="keyword">pthread_cond_t</span> cond = PTHREAD_COND_INITIALIZER;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_init</span><span class="params">(<span class="keyword">pthread_cond_t</span> *<span class="keyword">restrict</span> cond, <span class="keyword">const</span> <span class="keyword">pthread_condattr_t</span> *<span class="keyword">restrict</span> attr)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_destroy</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cond)</span></span>;</span><br></pre></td></tr></table></figure>
<h2 id="等待唤醒"><a href="#等待唤醒" class="headerlink" title="等待唤醒"></a>等待唤醒</h2><p><code>int pthread_cond_wait(pthread_cond_t *restrict cond, pthread_mutex_t *restrict mutex)</code> 在<code>cond</code>接收到唤醒之前，该函数会一直都待。<code>mutex</code>是用来保护该条件变量的锁。</p>
<ol>
<li>此函数必须用前加锁，用后解锁 <a href="https://stackoverflow.com/questions/4544234/calling-pthread-cond-signal-without-locking-mutex" target="_blank" rel="noopener"> 了解为什么？ </a></li>
<li>本函数首先将当前线程加入到唤醒队列，然后旋即解锁mutex，最后等待被唤醒。被唤醒后，又对mutex加锁（可能是看起来没有对用户的行为作任何的改变）。</li>
<li>2的操作保证了线程在陷入wait后至被加入唤醒队列这段时间内是原子的。如果不加锁，可能唤醒时，等待线程尚未被加入到唤醒队列，就会产生唤醒丢失。</li>
<li>这个锁保证了避免唤醒丢失外，还保证了唤醒前后临界区的其他变量不被操作。</li>
</ol>
<p><code>int pthread_cond_timedwait(pthread_cond_t *restrict cond, thread_mutex_t *restrict mutex,const struct timespec *restrict abstime);</code> </p>
<ol>
<li>在上面函数的基础上增加了绝对超时时间。</li>
<li>如果到达绝对时间还没被唤醒，该函数返回 ETIMEDOUT。</li>
</ol>
<h2 id="通知唤醒"><a href="#通知唤醒" class="headerlink" title="通知唤醒"></a>通知唤醒</h2><p><code>int pthread_cond_broadcast(pthread_cond_t *cond)</code> 会唤醒所有该等待<code>cond</code>的线程</p>
<ol>
<li>成功返回0，失败返回错误</li>
</ol>
<p><code>int pthread_cond_signal(pthread_cond_t *cond);</code> 会唤醒一个等待<code>cond</code>的线程</p>
<ol>
<li>成功返回0，失败返回错误</li>
</ol>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> msg &#123;</span><br><span class="line">	<span class="keyword">struct</span> msg *m_next;</span><br><span class="line">	<span class="comment">/* ... more stuff here ... */</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">struct</span> msg *workq;</span><br><span class="line"><span class="comment">// 信号量的语义是queue已经准备好消费</span></span><br><span class="line"><span class="keyword">pthread_cond_t</span> qready = PTHREAD_COND_INITIALIZER;</span><br><span class="line"><span class="keyword">pthread_mutex_t</span> qlock = PTHREAD_MUTEX_INITIALIZER;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">process_msg</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">struct</span> msg *mp;</span><br><span class="line">	<span class="keyword">for</span> (;;) &#123;</span><br><span class="line">		pthread_mutex_lock(&amp;qlock);</span><br><span class="line">		<span class="comment">// 这个while可以在queue为空时卡住自己</span></span><br><span class="line">		<span class="keyword">while</span> (workq == <span class="literal">NULL</span>)</span><br><span class="line">			pthread_cond_wait(&amp;qready, &amp;qlock);</span><br><span class="line">		mp = workq;</span><br><span class="line">		workq = mp-&gt;m_next;</span><br><span class="line">		pthread_mutex_unlock(&amp;qlock);</span><br><span class="line">		<span class="comment">/* now process the message mp */</span></span><br><span class="line">		read_map_val_add_process(mp);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">enqueue_msg</span><span class="params">(<span class="keyword">struct</span> msg *mp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	pthread_mutex_lock(&amp;qlock);</span><br><span class="line">	mp-&gt;m_next = workq;</span><br><span class="line">	workq = mp;</span><br><span class="line">	pthread_mutex_unlock(&amp;qlock);</span><br><span class="line">	pthread_cond_signal(&amp;qready);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="POSIX-屏障"><a href="#POSIX-屏障" class="headerlink" title="POSIX 屏障"></a>POSIX 屏障</h1><p>屏障保证了一组线程同时到达某一点。在其他所有的线程到达wait之前，先到的线程会休眠并等待，直到大家都到达wait，再继续后面的工作。</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// 初始化屏障，count为必须到达的线程数量。一旦数量满足，就继续运行</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_init</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *<span class="keyword">restrict</span> barrier,</span></span></span><br><span class="line"><span class="function"><span class="params">              <span class="keyword">const</span> <span class="keyword">pthread_barrierattr_t</span> *<span class="keyword">restrict</span> attr, <span class="keyword">unsigned</span> count)</span></span>;</span><br><span class="line"><span class="comment">// 摧毁屏障</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_destroy</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *barrier)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 到达并等待</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_barrier_wait</span><span class="params">(<span class="keyword">pthread_barrier_t</span> *barrier)</span></span>;</span><br></pre></td></tr></table></figure>
<p>下面的代码演示了8个线程共同完成100万个数排序的过程。<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"apue.h"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;limits.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NTHR   8				<span class="comment">/* number of threads */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NUMNUM 8000000L			<span class="comment">/* number of numbers to sort */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TNUM   (NUMNUM/NTHR)	<span class="comment">/* number to sort per thread */</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">long</span> nums[NUMNUM];</span><br><span class="line"><span class="keyword">long</span> snums[NUMNUM];</span><br><span class="line"></span><br><span class="line"><span class="keyword">pthread_barrier_t</span> b;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> SOLARIS</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> heapsort qsort</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">int</span> <span class="title">heapsort</span><span class="params">(<span class="keyword">void</span> *, <span class="keyword">size_t</span>, <span class="keyword">size_t</span>,</span></span></span><br><span class="line">                    int (*)(const void *, const void *));</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Compare two long integers (helper function for heapsort)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">complong(<span class="keyword">const</span> <span class="keyword">void</span> *arg1, <span class="keyword">const</span> <span class="keyword">void</span> *arg2)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">long</span> l1 = *(<span class="keyword">long</span> *)arg1;</span><br><span class="line">	<span class="keyword">long</span> l2 = *(<span class="keyword">long</span> *)arg2;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (l1 == l2)</span><br><span class="line">		<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">else</span> <span class="keyword">if</span> (l1 &lt; l2)</span><br><span class="line">		<span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		<span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Worker thread to sort a portion of the set of numbers.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> *</span><br><span class="line">thr_fn(<span class="keyword">void</span> *arg)</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">long</span>	idx = (<span class="keyword">long</span>)arg;</span><br><span class="line"></span><br><span class="line">	heapsort(&amp;nums[idx], TNUM, <span class="keyword">sizeof</span>(<span class="keyword">long</span>), complong);</span><br><span class="line">	pthread_barrier_wait(&amp;b);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * Go off and perform more work ...</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">return</span>((<span class="keyword">void</span> *)<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Merge the results of the individual sorted ranges.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span></span><br><span class="line">merge()</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">long</span>	idx[NTHR];</span><br><span class="line">	<span class="keyword">long</span>	i, minidx, sidx, num;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NTHR; i++)</span><br><span class="line">		idx[i] = i * TNUM;</span><br><span class="line">	<span class="keyword">for</span> (sidx = <span class="number">0</span>; sidx &lt; NUMNUM; sidx++) &#123;</span><br><span class="line">		num = LONG_MAX;</span><br><span class="line">		<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NTHR; i++) &#123;</span><br><span class="line">			<span class="keyword">if</span> ((idx[i] &lt; (i+<span class="number">1</span>)*TNUM) &amp;&amp; (nums[idx[i]] &lt; num)) &#123;</span><br><span class="line">				num = nums[idx[i]];</span><br><span class="line">				minidx = i;</span><br><span class="line">			&#125;</span><br><span class="line">		&#125;</span><br><span class="line">		snums[sidx] = nums[idx[minidx]];</span><br><span class="line">		idx[minidx]++;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span></span><br><span class="line">main()</span><br><span class="line">&#123;</span><br><span class="line">	<span class="keyword">unsigned</span> <span class="keyword">long</span>	i;</span><br><span class="line">	<span class="keyword">struct</span> timeval	start, end;</span><br><span class="line">	<span class="keyword">long</span> <span class="keyword">long</span>		startusec, endusec;</span><br><span class="line">	<span class="keyword">double</span>			elapsed;</span><br><span class="line">	<span class="keyword">int</span>				err;</span><br><span class="line">	<span class="keyword">pthread_t</span>		tid;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * Create the initial set of numbers to sort.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	srandom(<span class="number">1</span>);</span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NUMNUM; i++)</span><br><span class="line">		nums[i] = random();</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * Create 8 threads to sort the numbers.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	gettimeofday(&amp;start, <span class="literal">NULL</span>);</span><br><span class="line">	pthread_barrier_init(&amp;b, <span class="literal">NULL</span>, NTHR+<span class="number">1</span>);</span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NTHR; i++) &#123;</span><br><span class="line">		err = pthread_create(&amp;tid, <span class="literal">NULL</span>, thr_fn, (<span class="keyword">void</span> *)(i * TNUM));</span><br><span class="line">		<span class="keyword">if</span> (err != <span class="number">0</span>)</span><br><span class="line">			err_exit(err, <span class="string">"can't create thread"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	pthread_barrier_wait(&amp;b);</span><br><span class="line">	merge();</span><br><span class="line">	gettimeofday(&amp;end, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">	<span class="comment">/*</span></span><br><span class="line"><span class="comment">	 * Print the sorted list.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	startusec = start.tv_sec * <span class="number">1000000</span> + start.tv_usec;</span><br><span class="line">	endusec = end.tv_sec * <span class="number">1000000</span> + end.tv_usec;</span><br><span class="line">	elapsed = (<span class="keyword">double</span>)(endusec - startusec) / <span class="number">1000000.0</span>;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">"sort took %.4f seconds\n"</span>, elapsed);</span><br><span class="line">	<span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; NUMNUM; i++)</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">"%ld\n"</span>, snums[i]);</span><br><span class="line">	<span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
]]></content>
    
    <summary type="html">
    
      如何进行并行编程？本文给了一些pthead库基本的多进程（fork、wait）、多线程（create、join、mutex、rwlock、cond、barrier）API，和详细的样例帮助理解这些API。
    
    </summary>
    
      <category term="Linux" scheme="chestnutheng.cn/categories/Linux/"/>
    
    
      <category term="Linux" scheme="chestnutheng.cn/tags/Linux/"/>
    
  </entry>
  
  <entry>
    <title>[数据结构]深入理解红黑树</title>
    <link href="chestnutheng.cn/2019/03/20/rbtree/"/>
    <id>chestnutheng.cn/2019/03/20/rbtree/</id>
    <published>2019-03-20T15:00:25.000Z</published>
    <updated>2019-03-20T15:00:30.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="平衡树"><a href="#平衡树" class="headerlink" title="平衡树"></a>平衡树</h2><p>在谈红黑树之前，可以先看看红黑树的根源，234树。234树也是一种平衡树。<br>平衡树的原型都是二叉查找树，即左面的节点比他小，右边的节点比他大。但是在二叉查找树的过程中，很有可能变成树朝一边严重倾斜的情况。为了解决这个问题，设计了如下变种：</p>
<ol>
<li>avl：严格控制树的平衡，左右树的高度差不大于1。所以查找性能是logn。在插入和删除操作时，最差的情况是每一级的父节点都会旋转。时间复杂度都是O(logn)。</li>
<li>红黑树：红黑树的左右子树最高差时，一个子树是另一个子树的一倍。最坏查找性能是2logn。在插入操作时，最差旋转2次，删除操作最差旋转三次，可以减少avl的平衡操作，但是依然是O(logn)的复杂度（在找到节点的插入位置就要花费logn时间）。</li>
<li>splay：插入删除复杂度也是O(logn)，可以用数组实现，不需要额外维护树的节点信息。但在最坏情况下他会退化成一条链。而且只读操作也会影响树的结构，在多线程环境访问下比较复杂。</li>
<li>替罪羊树：在查找树不平衡的时候，找到最高的一个节点（满足左右子树不差0.7倍的平衡点），重构整个子树</li>
</ol>
<p>查找问题还可以用哈希表解决。哈希表是无序的，而且会耗费大块的内存。<br>一些常见的面试题：<a href="https://www.cnblogs.com/bethunebtj/articles/4853480.html" target="_blank" rel="noopener">常见面试题-cnblog</a><br><img src="https://pic4.zhimg.com/80/v2-3b0dd24fe1bc5e5940cc405233ce1e0e_hd.jpg">各种树的性能</p>
<h2 id="2-3-4树"><a href="#2-3-4树" class="headerlink" title="2-3-4树"></a>2-3-4树</h2><p><a href="https://blog.csdn.net/chen_zhang_yu/article/details/52415077" target="_blank" rel="noopener">2-3-4树-CSDN</a><br>234树是红黑树的等价变种。先来看看2-3树，这种树有两种节点，2节点和3节点。<br>2-节点：普通节点，有两个子连接<br>3-节点：有两个值A、B，三个连接（分别指向小于A，介于AB之间，大于B的儿子）<br>2-3树可以保证插入的时候所有叶子到根节点的距离是相同的。我们看看他如何插入：<br>(1) 如果值插入2节点，把他扩充成一个3节点。<br>(2) 如果插入插入3节点<br>A. 整个树只有一个3节点：把他扩展成4-节点，然后分解4-节点，然后将分解后的新树的父节点融入到2-节点的父节点中去。<br>B. 3-节点有一个2-节点的父节点，此时的操作是，3-节点扩充为4-节点，然后分解4-节点，然后将分解后的新树的父节点融入到2-节点的父节点中去。<br>C. 3-节点有一个3-节点的父节点，此时操作是：3-节点扩充为4-节点，然后分解4-节点，新树父节点向上融合，上面的3-节点继续扩充，融合，分解，新树继续向上融合，直到父节点为2-节点为止，如果向上到根节点都是3-节点，将根节点扩充为4-节点，然后分解为新树，至此，整个树增加一层，仍然保持平衡。<br>23树的流程比较复杂，而且涉及不同节点的转换，所以出现了红黑树来简化操作。我们把3节点的两个元素红色连接连起来。这时候红色连接出去的那个节点就成了红黑树的红节点，其余的都是黑节点。如果你将红黑树中所有的红色链接放平，那么它所有的叶子节点到根节点的距离都是相同的，所以是一个完美的黑色平衡。<br>所以，红黑树的另一种定义是满足下列条件的二叉查找树：<br>⑴ 红链接均为左链接。<br>⑵ 没有任何一个结点同时和两条红链接相连。(这样会出现4-节点)<br>⑶ 该树是完美黑色平衡的，即任意空链接到根结点的路径上的黑链接数量相同。</p>
<h2 id="红黑树"><a href="#红黑树" class="headerlink" title="红黑树"></a>红黑树</h2><p><a href="https://github.com/julycoding/The-Art-Of-Programming-By-July/blob/master/ebook/zh/03.01.md" target="_blank" rel="noopener">github红黑树(有比较好的图解)</a><br><a href="https://www.cnblogs.com/skywang12345/p/3245399.html" target="_blank" rel="noopener">cnblog红黑树流程详解</a><br>红黑树的五条性质：<br>1）每个结点要么是红的，要么是黑的。<br>2）根结点是黑的。<br>3）每个叶结点（叶结点即指树尾端NIL指针或NULL结点）是黑的。<br>4）如果一个结点是红的，那么它的俩个儿子都是黑的。<br>5）对于任一结点而言，其到叶结点树尾端NIL指针的每一条路径都包含相同数目的黑结点。</p>
<p>如果我们插入一个节点，并把他染红，则树的1235项原则都不会被破坏。如何保证原则4呢？调整的原则是把红色问题向上修正到根节点，最后把根节点染黑，来达到平衡。<br>我们先把插入的节点染红，然后进行修正操作：<br><strong>插入修复情况1</strong>：当前结点的父结点是红色且叔叔结点是红色。<br>将当前结点的父结点和叔叔结点涂黑，祖父结点涂红。在以祖父节点为新的当前结点再做一遍。<br>A. 为了解决本节点和父节点都是红色，把父节点染黑。<br>B. 但是父节点的在子树多了个黑色，所以要把叔叔也染黑来平衡。<br>C. 此时爷爷节点的子树也多了个黑色，所以把他染红。<br>D. 爷爷节点被染红了上面的节点曾爷爷有可能是红的，在做一次。<br><strong>插入修复情况2</strong>：当前结点的父结点是红色,叔叔结点是黑色，当前结点是其父结点的右子<br>对策：当前结点的父结点做为新的当前结点，以新当前结点为支点左旋。左旋后，本节点上移，把问题向上修正。可以转化到情况3<br><strong>插入修复情况3</strong>：当前结点的父结点是红色,叔叔结点是黑色，当前结点是其父结点的左子<br>解法：父结点变为黑色，祖父结点变为红色，在祖父结点为支点右旋<br>A. 为了解决本节点和父节点都为红，把父节点染黑<br>B. 为了解决父节点在的子树多了一个黑色，把叔叔染红并右旋解决</p>
]]></content>
    
    <summary type="html">
    
      在谈红黑树之前，可以先看看红黑树的根源，234树...
    
    </summary>
    
      <category term="数据结构" scheme="chestnutheng.cn/categories/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
    
      <category term="数据结构，平衡树" scheme="chestnutheng.cn/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%EF%BC%8C%E5%B9%B3%E8%A1%A1%E6%A0%91/"/>
    
  </entry>
  
  <entry>
    <title>[Web]Scrapy Scan</title>
    <link href="chestnutheng.cn/2019/03/20/scrapy/"/>
    <id>chestnutheng.cn/2019/03/20/scrapy/</id>
    <published>2019-03-20T15:00:25.000Z</published>
    <updated>2019-03-20T15:00:30.000Z</updated>
    
    <content type="html"><![CDATA[<p>scrapy是个很简单强大的python爬虫框架，不需要处理网络相关逻辑就可以轻松爬取。本文介绍了一些基本的内容。需要注意的是，本文档中所有的●表示非必须内容。<br>本教程基于官方文档：<a href="https://scrapy-chs.readthedocs.io/zh_CN/0.24/topics/settings.html" target="_blank" rel="noopener">官方文档</a></p>
<h1 id="安装scrapy"><a href="#安装scrapy" class="headerlink" title="安装scrapy"></a>安装scrapy</h1><h2 id="CentOS"><a href="#CentOS" class="headerlink" title="CentOS"></a>CentOS</h2><figure class="highlight sh"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">yum install python34-devel.x86_64</span><br><span class="line">pip3 install scrapy</span><br></pre></td></tr></table></figure>
<h1 id="创建项目"><a href="#创建项目" class="headerlink" title="创建项目"></a>创建项目</h1><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">//创建项目hello</span><br><span class="line">scrapy startproject hello</span><br><span class="line">//创建一个爬虫（在项目根目录运行，不要加http://），名字为baidu，域名为www.baidu.com</span><br><span class="line">scrapy genspider baidu &quot;www.baidu.com&quot;</span><br></pre></td></tr></table></figure>
<p>● 需要注意的是，如果要无视robots.txt文件，请在下面的<code>settings.py</code>中设置<code>ROBOTSTXT_OBEY = False</code><br>● 刚才建好的项目目录文件树如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">tree hello </span><br><span class="line">hello</span><br><span class="line">|-- hello</span><br><span class="line">|   |-- __init__.py</span><br><span class="line">|   |-- items.py            // 输出结构体定义</span><br><span class="line">|   |-- middlewares.py</span><br><span class="line">|   |-- pipelines.py</span><br><span class="line">|   |-- __pycache__</span><br><span class="line">|   |-- settings.py         // 设置，记得把 ROBOTSTXT_OBEY = False</span><br><span class="line">|   |-- spiders</span><br><span class="line">|       |-- __init__.py</span><br><span class="line">|       |-- baidu.py        // 刚创建的爬虫</span><br><span class="line">|       |-- __pycache__</span><br><span class="line">|-- scrapy.cfg</span><br></pre></td></tr></table></figure></p>
<h1 id="Shell：测试代码"><a href="#Shell：测试代码" class="headerlink" title="Shell：测试代码"></a>Shell：测试代码</h1><p>使用shell可以帮助实现爬虫的代码，查看网页的相关信息。结合浏览器中的审查元素查看会让你构造出需要选择的数据部分。<br><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 1. 在命令行中执行 scrapy shell 就可以进入shell。</span></span><br><span class="line"><span class="comment"># ● 下面改造了http头的USER_AGENT，一般可以防止请求因为USER_AGENT被拒绝</span></span><br><span class="line">scrapy shell -s USER_AGENT，=<span class="string">"Mozilla/5.0 (Windows NT 10.0; WOW64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/67.0.3396.99 Safari/537.36-480"</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 进入shell后可用的对象：request、response、sel（选择器）、settings、spider、crawler</span></span><br><span class="line"><span class="comment"># 可用的函数 帮助shelp() 请求fetch(url) fetch(req) 浏览器打开view(rsp)</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 接下来可以拉取网页，下面是一些操作的实例:</span></span><br><span class="line">fetch(<span class="string">'https://www.baidu.com'</span>)</span><br><span class="line"><span class="comment"># 浏览器中打开爬取的网页</span></span><br><span class="line">view(response)</span><br><span class="line"><span class="comment"># 查看req和rsp</span></span><br><span class="line">request.body</span><br><span class="line">response.headers</span><br><span class="line"><span class="comment"># 选择网页中的元素</span></span><br><span class="line">response.xpath(<span class="string">'//div'</span>)</span><br><span class="line">sel.xpath(<span class="string">'//title/text()'</span>).extract()</span><br><span class="line"><span class="comment"># ● get 改为post</span></span><br><span class="line">request = request.replace(method=<span class="string">"POST"</span>)</span><br></pre></td></tr></table></figure></p>
<h2 id="●-调试您的spider"><a href="#●-调试您的spider" class="headerlink" title="● 调试您的spider"></a>● 调试您的spider</h2><p>下面代码会在运行spider时，在这里进入shell调试。</p>
<figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> scrapy.shell <span class="keyword">import</span> inspect_response</span><br><span class="line">inspect_response(response, self)</span><br></pre></td></tr></table></figure>
<h1 id="选择器：找到爬取的内容"><a href="#选择器：找到爬取的内容" class="headerlink" title="选择器：找到爬取的内容"></a>选择器：找到爬取的内容</h1><p>Scrapy选择器构建于 lxml 库之上，这意味着它们在速度和解析准确性上非常相似。选择器从如下的库中获取：<br><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> scrapy.selector <span class="keyword">import</span> Selector</span><br><span class="line"><span class="keyword">from</span> scrapy.http <span class="keyword">import</span> HtmlResponse</span><br><span class="line"><span class="comment"># 可以从rsp中构造，也可以 text="&lt;h&gt;&lt;/h&gt;" 从字符串中构造</span></span><br><span class="line">response = HtmlResponse(url=<span class="string">'http://example.com'</span>, body=<span class="string">''</span>)</span><br><span class="line">Selector(response=response).xpath(<span class="string">'//span/text()'</span>).extract() <span class="comment"># [u'good']</span></span><br><span class="line"><span class="comment"># 幸运的是rsp提供了方法直接构造选择器：</span></span><br><span class="line">response.selector.xpath(<span class="string">'//span/text()'</span>).extract()</span><br><span class="line"><span class="comment"># 以及更短的快捷方式：</span></span><br><span class="line">response.xpath(<span class="string">'//title/text()'</span>).extract() </span><br><span class="line">response.css(<span class="string">'title::text'</span>).extract()</span><br></pre></td></tr></table></figure></p>
<p>● xpath和css是两种用来选择数据的工具，他们遵循不同的语法。但是他们都都返回一个selector对象。selector有几个成员函数很有用，他们是</p>
<ol>
<li>extract() 返回里面文字的值</li>
<li>re() 用正则过滤文字值，返回文字值</li>
<li>xpath() css() 返回的selector依然可以用这些工具，这意味着可以嵌套</li>
</ol>
<figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 下面是一个从的img标签的text中获取括号内文字的方法</span></span><br><span class="line">response.xpath(<span class="string">'//a[contains(@href, "image")]/text()'</span>).re(<span class="string">r'Name:\s*(.*)'</span>)</span><br><span class="line"><span class="comment"># 下面是用xpath的相对路径和选择器嵌套：</span></span><br><span class="line">divs = response.xpath(<span class="string">'//div'</span>)</span><br><span class="line"><span class="keyword">for</span> p <span class="keyword">in</span> divs.xpath(<span class="string">'.//p'</span>):    <span class="comment"># 父路径下的所有后代p</span></span><br><span class="line">    ...</span><br><span class="line"><span class="keyword">for</span> p <span class="keyword">in</span> divs.xpath(<span class="string">'p'</span>):    <span class="comment"># 父路径下的所有直接</span></span><br><span class="line">    ...</span><br></pre></td></tr></table></figure>
<h2 id="●-Xpath-语法参考"><a href="#●-Xpath-语法参考" class="headerlink" title="● Xpath 语法参考"></a>● Xpath 语法参考</h2><p><a href="http://www.w3school.com.cn/xpath/xpath_syntax.asp" target="_blank" rel="noopener">w3s : Xpath语法参考</a><br>Xpath是一种用于操作xml的选择器，可以快速操作想要的html标签。</p>
<table>
<thead>
<tr>
<th style="text-align:left">语法</th>
<th style="text-align:left">实例</th>
<th style="text-align:left">解释</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">a</td>
<td style="text-align:left">bookstore</td>
<td style="text-align:left">选取 bookstore 元素的所有子节点。</td>
</tr>
<tr>
<td style="text-align:left">/a</td>
<td style="text-align:left">/bookstore</td>
<td style="text-align:left">选取根元素 bookstore。</td>
</tr>
<tr>
<td style="text-align:left">a/b</td>
<td style="text-align:left">bookstore/book</td>
<td style="text-align:left">选取属于 bookstore 的子元素的所有 book 元素。</td>
</tr>
<tr>
<td style="text-align:left">//a</td>
<td style="text-align:left">//book</td>
<td style="text-align:left">选取所有 book 子元素，而不管它们在文档中的位置。</td>
</tr>
<tr>
<td style="text-align:left">a//b</td>
<td style="text-align:left">bookstore//book</td>
<td style="text-align:left">选择属于 bookstore 元素的后代的所有 book 元素，而不管它们位于 bookstore 之下的什么位置。</td>
</tr>
<tr>
<td style="text-align:left">@a</td>
<td style="text-align:left">//@lang</td>
<td style="text-align:left">选取名为 lang 的所有属性。</td>
</tr>
<tr>
<td style="text-align:left">*</td>
<td style="text-align:left">/bookstore/*</td>
<td style="text-align:left">选取 bookstore 元素的所有子元素。</td>
</tr>
<tr>
<td style="text-align:left"></td>
<td style="text-align:left">//*</td>
<td style="text-align:left">选取文档中的所有元素。</td>
</tr>
<tr>
<td style="text-align:left"></td>
<td style="text-align:left">//title[@*]</td>
<td style="text-align:left">选取所有带有属性的 title 元素。</td>
</tr>
</tbody>
</table>
<h2 id="●-CSS-语法参考"><a href="#●-CSS-语法参考" class="headerlink" title="● CSS 语法参考"></a>● CSS 语法参考</h2><p><a href="http://www.w3school.com.cn/cssref/css_selectors.asp" target="_blank" rel="noopener">w3s : CSS语法参考</a><br>如果要操作一个类或者id而不是标签，CSS是最快的方法。</p>
<table>
<thead>
<tr>
<th style="text-align:left">例子</th>
<th style="text-align:left">例子描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">.intro</td>
<td style="text-align:left">选择 class=”intro” 的所有元素</td>
</tr>
<tr>
<td style="text-align:left">#firstname</td>
<td style="text-align:left">选择 id=”firstname” 的所有元素</td>
</tr>
<tr>
<td style="text-align:left">*</td>
<td style="text-align:left">选择所有元素</td>
</tr>
<tr>
<td style="text-align:left">p</td>
<td style="text-align:left">选择所有 p 元素</td>
</tr>
<tr>
<td style="text-align:left">div,p</td>
<td style="text-align:left">选择所有 div元素和所有 p 元素</td>
</tr>
<tr>
<td style="text-align:left">div p</td>
<td style="text-align:left">选择 div 元素内部的所有 p 元素</td>
</tr>
<tr>
<td style="text-align:left">div&gt;p</td>
<td style="text-align:left">选择父元素为 <div> 元素的所有 <p> 元素</p></div></td>
</tr>
<tr>
<td style="text-align:left">div+p</td>
<td style="text-align:left">选择紧接在 <div> 元素之后的所有 <p> 元素</p></div></td>
</tr>
<tr>
<td style="text-align:left">[target]</td>
<td style="text-align:left">选择带有 target 属性所有元素</td>
</tr>
<tr>
<td style="text-align:left">[target=_blank]</td>
<td style="text-align:left">选择 target=”]_blank” 的所有元素。</td>
</tr>
<tr>
<td style="text-align:left">[title~=flower]</td>
<td style="text-align:left">选择 title 属性包含单词 “flower” 的所有元素</td>
</tr>
<tr>
<td style="text-align:left">[lang&#124;=en]</td>
<td style="text-align:left">选择 lang 属性值以 “en” 开头的所有元素</td>
</tr>
</tbody>
</table>
<h1 id="Item：存储你爬取结果的桶"><a href="#Item：存储你爬取结果的桶" class="headerlink" title="Item：存储你爬取结果的桶"></a>Item：存储你爬取结果的桶</h1><p>好了，现在开始写爬虫。不过在这之前需要先定义好你想要爬下来的数据格式是怎么样的，待会把他们装进去。你需要修改<code>hello/items.py</code>，编写一个简单的类。<br><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">HelloItem</span><span class="params">(scrapy.Item)</span>:</span></span><br><span class="line">    <span class="comment"># 用 scrapy.Field() 作为类型就可以了</span></span><br><span class="line">    id = scrapy.Field()</span><br><span class="line">    name = scrapy.Field()</span><br><span class="line">    description = scrapy.Field()</span><br></pre></td></tr></table></figure></p>
<p>● item和字典的API非常相似。可以使用get()、下标等读写item，甚至可以相互用对方初始化。</p>
<h1 id="Spider：爬虫逻辑"><a href="#Spider：爬虫逻辑" class="headerlink" title="Spider：爬虫逻辑"></a>Spider：爬虫逻辑</h1><p>spider是爬取流程的主体。打开<code>hello/spiders/baidu.py</code>，编辑成员函数和成员变量：</p>
<ol>
<li><code>name</code> 爬虫的名字</li>
<li><code>allowed_domains</code> 只允许爬取<code>allowed_domains</code>的内容</li>
<li><code>start_urls</code> 运行时没指定url，就从这里开始爬</li>
<li><code>start_requests()</code> 可以重写这个来处理第一个<code>request</code>。否则，将会用<code>make_requests_from_url</code>爬url列表里的内容。</li>
<li><code>make_requests_from_url(url)</code> 可以重写这个来处理请求的发起。默认调用parse来处理rsp。</li>
<li><code>parse(rsp)</code> 处理rsp。需要返回item或者req的迭代对象作为输出和下一批爬取的内容，或者都返回。<figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> scrapy</span><br><span class="line"><span class="keyword">from</span> hello.items <span class="keyword">import</span> HelloItem</span><br><span class="line"><span class="keyword">import</span> scrapy</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BaiduSpider</span><span class="params">(scrapy.Spider)</span>:</span></span><br><span class="line">    <span class="comment"># 创建爬虫时指定的名字</span></span><br><span class="line">    name = <span class="string">'baidu'</span></span><br><span class="line">    <span class="comment"># ● 设置中OffsiteMiddleware=True的时候，只允许爬取allowed_domains的内容</span></span><br><span class="line">    allowed_domains = [<span class="string">'baidu.com'</span>]</span><br><span class="line">    <span class="comment"># 爬取的目标链接</span></span><br><span class="line">    start_urls = [<span class="string">'http://www.baidu.com/'</span>]</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># 用这个函数处理url的rsp</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">parse</span><span class="params">(self, response)</span>:</span></span><br><span class="line">        sel = scrapy.Selector(response)</span><br><span class="line">        <span class="comment"># 选择所有的h3标题返回存储</span></span><br><span class="line">        <span class="keyword">for</span> h3 <span class="keyword">in</span> response.xpath(<span class="string">'//h3'</span>).extract():</span><br><span class="line">            <span class="keyword">yield</span> HelloItem(title=h3)</span><br><span class="line">        <span class="comment"># 返回链接继续爬取</span></span><br><span class="line">        <span class="keyword">for</span> url <span class="keyword">in</span> response.xpath(<span class="string">'//a/@href'</span>).extract():</span><br><span class="line">            <span class="keyword">yield</span> scrapy.Request(url, callback=self.parse)</span><br><span class="line">    </span><br><span class="line">    <span class="comment"># ● 一个重写start_requests的样例，构造了req，并用logged_in函数处理rsp</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">start_requests</span><span class="params">(self)</span>:</span></span><br><span class="line">        <span class="keyword">return</span> [scrapy.FormRequest(<span class="string">"http://www.example.com/login"</span>,</span><br><span class="line">                               formdata=&#123;<span class="string">'user'</span>: <span class="string">'john'</span>, <span class="string">'pass'</span>: <span class="string">'secret'</span>&#125;,</span><br><span class="line">                               callback=self.logged_in)]</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">logged_in</span><span class="params">(rsp)</span>:</span>...</span><br></pre></td></tr></table></figure>
</li>
</ol>
<p>写完后，在项目目录下运行scrapy crawl来运行spider：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">// 基础的运行</span><br><span class="line">scrapy crawl hello</span><br><span class="line">// 传递参数给myspider，并在构造函数中使用</span><br><span class="line">scrapy crawl myspider -a category=electronics</span><br><span class="line">class MySpider(Spider):</span><br><span class="line">    def __init__(self, category=None, *args, **kwargs):</span><br><span class="line">        super(MySpider, self).__init__(*args, **kwargs)</span><br><span class="line">        self.start_urls = [&apos;http://www.example.com/categories/%s&apos; % category]</span><br><span class="line">// 把爬到的item输出文件</span><br><span class="line">scrapy crawl dmoz -o items.json</span><br></pre></td></tr></table></figure></p>
<h1 id="PipeLine：处理item并输出"><a href="#PipeLine：处理item并输出" class="headerlink" title="PipeLine：处理item并输出"></a>PipeLine：处理item并输出</h1><p>Pipeline提供了处理返回的item的几个函数。编辑<code>hello/pipeline.py</code>修改Pipeline的成员函数：</p>
<ol>
<li>process_item(item, spider) 每个item获取后都会用这个处理。这个函数会返回一个处理好的item。</li>
<li>open_spider(spider) 开启spider时被调用</li>
<li>close_spider(spider) 关闭spider时被调用</li>
</ol>
<figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 下面是一个用id去重item的样例：</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DuplicatesPipeline</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self)</span>:</span></span><br><span class="line">        self.ids_seen = set()</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">process_item</span><span class="params">(self, item, spider)</span>:</span></span><br><span class="line">        <span class="keyword">if</span> item[<span class="string">'id'</span>] <span class="keyword">in</span> self.ids_seen:</span><br><span class="line">            <span class="keyword">raise</span> DropItem(<span class="string">"Duplicate item found: %s"</span> % item)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.ids_seen.add(item[<span class="string">'id'</span>])</span><br><span class="line">            <span class="keyword">return</span> item</span><br><span class="line"><span class="keyword">from</span> scrapy.exporters <span class="keyword">import</span> CsvItemExporter</span><br><span class="line"><span class="comment"># 一个用导出器导出的样例：</span></span><br><span class="line"><span class="keyword">from</span> scrapy.exporters <span class="keyword">import</span> CsvItemExporter</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ExportPipeline</span><span class="params">(object)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">open_spider</span><span class="params">(self, spider)</span>:</span></span><br><span class="line">        self.file = open(<span class="string">"test.csv"</span>, <span class="string">"wb"</span>)</span><br><span class="line">        self.exporter = CsvItemExporter(self.file,       </span><br><span class="line">        fields_to_export=[<span class="string">"id"</span>, <span class="string">"name"</span>, <span class="string">"desc"</span>])</span><br><span class="line">        self.exporter.start_exporting()</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">process_item</span><span class="params">(self, item, spider)</span>:</span></span><br><span class="line">        self.exporter.export_item(item)</span><br><span class="line">        <span class="keyword">return</span> item</span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">close_spider</span><span class="params">(self, spider)</span>:</span></span><br><span class="line">        self.exporter.finish_exporting()</span><br><span class="line">        self.file.close()</span><br></pre></td></tr></table></figure>
<p>最后，修改<code>settings.py</code>中的<code>ITEM_PIPELINES</code>保证你的Pipeline生效。key表示路径，值表示执行的顺序。<br><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ITEM_PIPELINES = &#123;</span><br><span class="line">    <span class="string">'mybot.pipelines.validate.DuplicatesPipeline'</span>: <span class="number">300</span>,</span><br><span class="line">    <span class="string">'mybot.pipelines.validate.ExportPipeline'</span>: <span class="number">800</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h2 id="●-Feeds-Export"><a href="#●-Feeds-Export" class="headerlink" title="● Feeds Export"></a>● Feeds Export</h2><p>序列化你的item结果有很多种方式：<br><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">FEED_EXPORTERS_BASE = &#123;</span><br><span class="line">    <span class="string">'json'</span>: <span class="string">'scrapy.contrib.exporter.JsonItemExporter'</span>,</span><br><span class="line">    <span class="string">'jsonlines'</span>: <span class="string">'scrapy.contrib.exporter.JsonLinesItemExporter'</span>,</span><br><span class="line">    <span class="string">'csv'</span>: <span class="string">'scrapy.contrib.exporter.CsvItemExporter'</span>,</span><br><span class="line">    <span class="string">'xml'</span>: <span class="string">'scrapy.contrib.exporter.XmlItemExporter'</span>,</span><br><span class="line">    <span class="string">'marshal'</span>: <span class="string">'scrapy.contrib.exporter.MarshalItemExporter'</span>,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<h1 id="高级特性"><a href="#高级特性" class="headerlink" title="高级特性"></a>高级特性</h1><h2 id="下载器中间件：修改req和rsp的钩子"><a href="#下载器中间件：修改req和rsp的钩子" class="headerlink" title="下载器中间件：修改req和rsp的钩子"></a>下载器中间件：修改req和rsp的钩子</h2><p><a href="https://scrapy-chs.readthedocs.io/zh_CN/0.24/topics/downloader-middleware.html#topics-downloader-middleware-ref" target="_blank" rel="noopener">官方文档：下载器中间件</a></p>
<h1 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h1><p>下面是些处理这些站点的建议(tips):</p>
<ol>
<li>使用user agent池，轮流选择之一来作为user agent。池中包含常见的浏览器的user agent(google一下一大堆)</li>
<li>禁止cookies(参考 <a href="https://scrapy-chs.readthedocs.io/zh_CN/0.24/topics/downloader-middleware.html#std:setting-COOKIES_ENABLED" target="_blank" rel="noopener">COOKIES_ENABLED</a>)，有些站点会使用cookies来发现爬虫的轨迹。</li>
<li>设置下载延迟(2或更高)。参考 <a href="https://scrapy-chs.readthedocs.io/zh_CN/0.24/topics/settings.html#std:setting-DOWNLOAD_DELAY" target="_blank" rel="noopener">DOWNLOAD_DELAY</a> 设置。</li>
<li>如果可行，使用 Google cache 来爬取数据，而不是直接访问站点。</li>
<li>使用IP池。例如免费的 Tor项目 或付费服务(ProxyMesh)。</li>
<li>使用高度分布式的下载器(downloader)来绕过禁止(ban)，您就只需要专注分析处理页面。这样的例子有: Crawlera</li>
</ol>
]]></content>
    
    <summary type="html">
    
      scrapy是个很简单强大的python爬虫框架，不需要处理网络相关逻辑就可以轻松爬取。本文介绍了一些基本的内容。
    
    </summary>
    
      <category term="Python" scheme="chestnutheng.cn/categories/Python/"/>
    
    
      <category term="scrapy" scheme="chestnutheng.cn/tags/scrapy/"/>
    
      <category term="爬虫" scheme="chestnutheng.cn/tags/%E7%88%AC%E8%99%AB/"/>
    
  </entry>
  
  <entry>
    <title>[deeplearning.ai]C1W1~C1W2</title>
    <link href="chestnutheng.cn/2018/08/14/C1W1-C1W2/"/>
    <id>chestnutheng.cn/2018/08/14/C1W1-C1W2/</id>
    <published>2018-08-14T13:09:13.000Z</published>
    <updated>2018-09-09T18:44:37.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="C1W1-什么是deep-learning"><a href="#C1W1-什么是deep-learning" class="headerlink" title="C1W1: 什么是deep learning"></a>C1W1: 什么是deep learning</h2><h3 id="单一神经元"><a href="#单一神经元" class="headerlink" title="单一神经元"></a>单一神经元</h3><p>deeplearning是模拟大脑的一种机器学习算法。以房价预测为例：</p>
<p><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/c1w1-1.png" style="width: 500px"></p>
<p>上图把房子面积作为输入X，房价作为输出Y，通过拟合得到了一个一次函数<br>$$<br>Y=aX+b<br>$$<br>这个函数的负值均视为0，即使用了ReLU函数作为神经元的激活函数做了处理。<br>$$<br>f(x) = \max(aX+b, 0)<br>$$</p>
<p>Note: ReLU函数：f(x)=\max(0, x)，近年来使用ReLU函数代替sigmoid函数为计算速度做了巨大的提升。</p>
<p>看看更多特征的情况：<br><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/c1w1-1.5.png" style="height: 250px"></p>
<h3 id="飞速发展"><a href="#飞速发展" class="headerlink" title="飞速发展"></a>飞速发展</h3><p><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/dlc1w1-3.png" style="height: 300px"></p>
<p>上图中可以看到传统算法和神经网络的效果的一个对比，在数据多的情况下神经网络有明显的优势。近年来以下的一些原因导致deep learning飞速发展成为主流</p>
<ol>
<li>计算速度飞速提升，使得训练较大的神经网络成为可能</li>
<li>数据变多（labeled data 变多）</li>
</ol>
<h3 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h3><p>一个典型的深度学习的流程，即是一个Idea-Code-Train 的循环</p>
<p><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/c1w1-2.png" style="height: 280px"></p>
<h2 id="C1W2-基本的神经网络"><a href="#C1W2-基本的神经网络" class="headerlink" title="C1W2: 基本的神经网络"></a>C1W2: 基本的神经网络</h2><h3 id="问题描述"><a href="#问题描述" class="headerlink" title="问题描述"></a>问题描述</h3><p><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/C1W2-1.png" style="height: 280px"></p>
<p>这里从一个简单的问题开始说起：识别一个64x64的图像是否为猫：</p>
<p>每个像素有RGB三个值组成，64*64个像素就是12228个值。所以X可以表示为一个12228维的向量。Y则是0或1（是或不是猫咪）。</p>
<p>这里需要很多(X, Y)组成的labeled data数据用来学习。每个样本用如下的数学方式表示：<br>$$<br>X\in R^{n_x}, Y\in{0,1}   \qquad 其中n_x为每个图片的维度(12288)<br>$$<br>训练集可以用很多样本表示：<br>$$<br>\textrm{m training examples:   } \{(X^{(1)}, Y^{(1)}), (X^{(2)}, Y^{(2)}), … ,(X^{(3)}, Y^{(3)})\}<br>$$<br>其中每个X都有n_x列，所以整个样本集可以表示为<br>$$<br>X\in R^{n_x\times m},Y \in R^{1 \times m}<br>$$</p>
<h3 id="逻辑回归"><a href="#逻辑回归" class="headerlink" title="逻辑回归"></a>逻辑回归</h3><h4 id="Sigmoid函数"><a href="#Sigmoid函数" class="headerlink" title="Sigmoid函数"></a>Sigmoid函数</h4><p>在开始正题之前，先看一个函数：<br>$$<br>\sigma(z) = \frac{1}{1+e^{-z}}<br>$$<br>如果Z为正无穷，这个函数将会是 1/(1+0) = 1</p>
<p>如果Z为负无穷，这个函数将会是1/bignum = 0</p>
<p>这个S型函数非常适用于取值0~1之间的x的映射。</p>
<p>同时，这个函数的导数也十分有趣：<br>$$<br>\begin{aligned}<br>f’(z) &amp;= (\frac{1}{1+e^{-z}})’= \frac{e^{-z}}{(1+e^{-z})^{2}} = \frac{1+e^{-z}-1}{(1+e^{-z})^{2}}<br>\<br>&amp;= \frac{1}{(1+e^{-z})}(1-\frac{1}{(1+e^{-z})})<br>\<br>&amp;= f(z)(1-f(z))<br>\<br>\end{aligned}<br>$$</p>
<h4 id="逻辑回归-1"><a href="#逻辑回归-1" class="headerlink" title="逻辑回归"></a>逻辑回归</h4><p>判断单一图片是否为猫的图片，可以表示为给定图片X，预估y为1的概率：<br>$$<br>\textrm{Given } x , \textrm{ want } \hat{y}=P(y=1|x) \qquad let \ x \in R^{n_x}, 0\leq\hat{y}\leq1<br>$$<br>设参数<br>$$<br>w \in R^{n_x},b \in R<br>$$<br>输出参数和X的乘积，然后用sigmoid做区间化：<br>$$<br>\hat{y} = \sigma(w^Tx+b)<br>$$<br>现在的问题就是从样本中估计出参数w和b的值。</p>
<h4 id="Cost-Function"><a href="#Cost-Function" class="headerlink" title="Cost Function"></a>Cost Function</h4><p>我们对样本做训练的目标，就是使得w和b对每一个样本的估计都损失最小。用数学的语言就是：<br>$$<br>\hat{y} = \sigma(w^Tx+b),\textrm{ where }\sigma(z) = \frac{1}{1+e^{-z}}<br>$$</p>
<p>$$<br>\textrm{ Given } \{(X^{(1)}, Y^{(1)}),  … ,(X^{(3)}, Y^{(3)})\}, \textrm{ want } \hat{y} \approx y<br>$$</p>
<p>$\hat{y}$和$y$之间的差距，就是我们的损失。我们当然可以用平方损失函数<br>$$<br>L(\hat{y}, y) = \frac{1}{2}(\hat{y}-y)^2<br>$$<br>不过这里却有更好的函数：<br>$$<br>L(\hat{y}, y) = -(y\log{\hat{y}} + (1-y)\log{(1-\hat{y})})<br>$$<br>当 y = 1时，$L(\hat{y}, y) = -\log{\hat{y}}$。如果要L小，就要$\hat{y}$尽可能大</p>
<p>当 y = 0 时，$L(\hat{y}, y)= -\log{(1-\hat{y})}$。如果要L小，就要$\hat{y}$尽可能小</p>
<p>这两条性质证明了这是个很好的损失函数。总的损失函数可以写成：</p>
<p>$$<br>J(w,b) = \frac{1}{m} \sum^{m}_{i=1} L(\hat{y^{(i)}}, y^{(i)}) = -\frac{1}{m} \sum^{m}_{i=1}(y^{(i)}\log{\hat{y^{(i)}}} + (1-y^{(i)})\log{(1-\hat{y^{(i)}})})<br>$$</p>
<h4 id="梯度下降"><a href="#梯度下降" class="headerlink" title="梯度下降"></a>梯度下降</h4><p>我们要找到上述函数J(w,b)的最小值。J(w,b)可能是下面的简化曲线：</p>
<p><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/C1W2-2.png" style="height: 150px"></p>
<p>假设我们先随机初始化w和b，在曲线上左上方的某一点。要让J的值变小，可以增加w的值。如果点在曲线的右上方，我们就需要减少w的值。事实上增加或减少w的值是有J对w的导数决定的。我们可以每次运行：<br>$$<br>w := w - \alpha \frac{dJ(w,b)}{dw} \qquad \alpha为学习速率，后面的是偏导数<br>$$<br>还有参数b：<br>$$<br>b := b - \alpha \frac{dJ(w,b)}{db} \qquad \alpha为学习速率，后面的是偏导数<br>$$<br>学习速率是用来决定每次参数迈的步子的大小。事实上J不会是一个凸函数，所以他拥有多个最小值，学习速率可以帮助参数不收敛到局部最小值。</p>
<h4 id="计算图"><a href="#计算图" class="headerlink" title="计算图"></a>计算图</h4><p>计算图可以让我们清晰地通过反向传播计算每个参数的导数。如下是一个代价函数求导的例子：</p>
<p><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/C1W2-3.png" style="height: 250px"></p>
<p>反向传播是一个从后向前的计算导数的过程，可以避免重复的计算。</p>
<p>先求根据上面的损失函数求出da：<br>$$<br>\frac{dL(a,y)}{da}=\frac{dL}{da} = -\frac{y}{a}+\frac{1-y}{1-a}<br>$$<br>然后求出dz：<br>$$<br>\frac{dL}{dz}=\frac{dL}{da} \cdot \frac{da}{dz} = (-\frac{y}{a}+\frac{1-y}{1-a})a(1-a)=(a-1)y+(1-y)a=a-y<br>$$<br>参数的导数更加容易：<br>$$<br>\frac{dL}{dw_1}=\frac{dL}{da} \cdot \frac{da}{dz} \cdot \frac{dz}{dw_1}=\frac{dL}{dz} \cdot \frac{dz}{dw_1}=\frac{dL}{dz} \cdot x_1<br>$$<br>链式求导可以很方便地运用在程序中，只要先记下前几步求导的值。同理，<br>$$<br>dw_2=dz \cdot x_2 , \quad db=dz<br>$$</p>
<h4 id="Vectorization"><a href="#Vectorization" class="headerlink" title="Vectorization"></a>Vectorization</h4><p>现在已经做完了所有求得参数的准备工作。一个标准的过程可以被表示为：</p>
<figure class="highlight matlab"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">J = <span class="number">0</span>; dw1 = <span class="number">0</span>; dw2 = <span class="number">0</span>; db = <span class="number">0</span></span><br><span class="line"># 遍历样本</span><br><span class="line"><span class="keyword">for</span> <span class="built_in">i</span> = <span class="number">1</span> to m:</span><br><span class="line">  z(<span class="built_in">i</span>) = w * x(<span class="built_in">i</span>) + b</span><br><span class="line">  a(<span class="built_in">i</span>) = theta(z(<span class="built_in">i</span>))</span><br><span class="line">  J += -(y(<span class="built_in">i</span>)<span class="built_in">log</span>(a(<span class="built_in">i</span>)) + (<span class="number">1</span>-y(<span class="built_in">i</span>))<span class="built_in">log</span>(<span class="number">1</span>-a(<span class="built_in">i</span>)))</span><br><span class="line">  dz(<span class="built_in">i</span>) = a(<span class="built_in">i</span>) - y(<span class="built_in">i</span>)</span><br><span class="line">  dw1 += x1(<span class="built_in">i</span>)*dz(<span class="built_in">i</span>)</span><br><span class="line">  dw2 += x2(<span class="built_in">i</span>)*dz(<span class="built_in">i</span>)</span><br><span class="line">  ...</span><br><span class="line">  db += dz(<span class="built_in">i</span>)</span><br><span class="line">J /= m</span><br><span class="line">dw1 /= m; dw2 /= m; db /= m</span><br></pre></td></tr></table></figure>
<p>在实际的计算中，分步去算参数的值非常不利于GPU进行加速。矩阵化运算可以提速400倍以上。下面是本次PA，也就是整个模型的全过程：</p>
<figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br></pre></td><td class="code"><pre><span class="line">train_set_x_orig, train_set_y, test_set_x_orig, test_set_y, classes = load_dataset()</span><br><span class="line">train_set_x_flatten = train_set_x_orig.reshape(train_set_x_orig.shape[<span class="number">0</span>], <span class="number">-1</span>).T</span><br><span class="line">test_set_x_flatten = test_set_x_orig.reshape(test_set_x_orig.shape[<span class="number">0</span>], <span class="number">-1</span>).T</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sigmoid</span><span class="params">(z)</span>:</span></span><br><span class="line">    s = <span class="number">1</span>/(<span class="number">1</span>+np.exp(-z))</span><br><span class="line">    <span class="keyword">return</span> s</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">init</span><span class="params">(dim)</span>:</span></span><br><span class="line">    w = np.zeros(dim).reshape(dim,<span class="number">1</span>)</span><br><span class="line">    b = <span class="number">0</span></span><br><span class="line">    <span class="keyword">return</span> w,b</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">propagate</span><span class="params">(w, b, X, Y)</span>:</span></span><br><span class="line">    m = X.shape[<span class="number">1</span>]</span><br><span class="line">    A = sigmoid(np.dot(w.T, X)+b)                   <span class="comment"># compute activation</span></span><br><span class="line">    cost = <span class="number">-1</span>/m*np.sum(Y*np.log(A) + (<span class="number">1</span>-Y)*np.log(<span class="number">1</span>-A))         <span class="comment"># compute cost</span></span><br><span class="line">    dw = <span class="number">1</span>/m*np.dot(X, (A-Y).T)</span><br><span class="line">    db = <span class="number">1</span>/m*np.sum(A-Y)</span><br><span class="line">    cost = np.squeeze(cost)</span><br><span class="line">    grads = &#123;<span class="string">"dw"</span>: dw, <span class="string">"db"</span>: db&#125;</span><br><span class="line">    <span class="keyword">return</span> grads, cost</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">optimize</span><span class="params">(w, b, X, Y, num_iterations, learning_rate, print_cost = False)</span>:</span></span><br><span class="line">    costs = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(num_iterations):</span><br><span class="line">        grads, cost = propagate(w, b, X, Y)</span><br><span class="line">        dw = grads[<span class="string">"dw"</span>]</span><br><span class="line">        db = grads[<span class="string">"db"</span>]</span><br><span class="line">        w = w - learning_rate*dw</span><br><span class="line">        b = b - learning_rate*db</span><br><span class="line">        <span class="keyword">if</span> i % <span class="number">100</span> == <span class="number">0</span>:</span><br><span class="line">            costs.append(cost)</span><br><span class="line">        <span class="keyword">if</span> print_cost <span class="keyword">and</span> i % <span class="number">100</span> == <span class="number">0</span>:</span><br><span class="line">            <span class="keyword">print</span> (<span class="string">"Cost after iteration %i: %f"</span> %(i, cost))</span><br><span class="line">    params = &#123;<span class="string">"w"</span>: w, <span class="string">"b"</span>: b&#125;</span><br><span class="line">    grads = &#123;<span class="string">"dw"</span>: dw, <span class="string">"db"</span>: db&#125;</span><br><span class="line">    <span class="keyword">return</span> params, grads, costs</span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">predict</span><span class="params">(w, b, X)</span>:</span></span><br><span class="line">    m = X.shape[<span class="number">1</span>]</span><br><span class="line">    Y_prediction = np.zeros((<span class="number">1</span>,m))</span><br><span class="line">    w = w.reshape(X.shape[<span class="number">0</span>], <span class="number">1</span>)</span><br><span class="line">    A = sigmoid(np.dot(w.T, X) + b)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(A.shape[<span class="number">1</span>]):</span><br><span class="line">        Y_prediction[<span class="number">0</span>][i] = <span class="number">1</span> <span class="keyword">if</span> A[<span class="number">0</span>][i] &gt; <span class="number">0.5</span> <span class="keyword">else</span> <span class="number">0</span></span><br><span class="line">    <span class="keyword">return</span> Y_prediction</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">model</span><span class="params">(X_train, Y_train, X_test, Y_test, num_iterations = <span class="number">2000</span>, learning_rate = <span class="number">0.5</span>, print_cost = False)</span>:</span></span><br><span class="line">    <span class="comment"># initialize parameters with zeros (≈ 1 line of code)</span></span><br><span class="line">    w, b = init(X_train.shape[<span class="number">0</span>])</span><br><span class="line">    <span class="comment"># Gradient descent (≈ 1 line of code)</span></span><br><span class="line">    parameters, grads, costs = optimize(w, b, X_train, Y_train, num_iterations, learning_rate, print_cost)</span><br><span class="line">    <span class="comment"># Retrieve parameters w and b from dictionary "parameters"</span></span><br><span class="line">    w = parameters[<span class="string">"w"</span>]</span><br><span class="line">    b = parameters[<span class="string">"b"</span>]</span><br><span class="line">    <span class="comment"># Predict test/train set examples (≈ 2 lines of code)</span></span><br><span class="line">    Y_prediction_test = predict(w, b, X_test)</span><br><span class="line">    Y_prediction_train = predict(w, b, X_train)</span><br><span class="line">    <span class="comment"># Print train/test Errors</span></span><br><span class="line">    print(<span class="string">"train accuracy: &#123;&#125; %"</span>.format(<span class="number">100</span> - np.mean(np.abs(Y_prediction_train - Y_train)) * <span class="number">100</span>))</span><br><span class="line">    print(<span class="string">"test accuracy: &#123;&#125; %"</span>.format(<span class="number">100</span> - np.mean(np.abs(Y_prediction_test - Y_test)) * <span class="number">100</span>))</span><br><span class="line">    d = &#123;<span class="string">"costs"</span>: costs,</span><br><span class="line">         <span class="string">"Y_prediction_test"</span>: Y_prediction_test, </span><br><span class="line">         <span class="string">"Y_prediction_train"</span> : Y_prediction_train, </span><br><span class="line">         <span class="string">"w"</span> : w, </span><br><span class="line">         <span class="string">"b"</span> : b,</span><br><span class="line">         <span class="string">"learning_rate"</span> : learning_rate,</span><br><span class="line">         <span class="string">"num_iterations"</span>: num_iterations&#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> d</span><br><span class="line"></span><br><span class="line">d = model(train_set_x, train_set_y, test_set_x, test_set_y, num_iterations = <span class="number">2000</span>, learning_rate = <span class="number">0.005</span>, print_cost = <span class="keyword">True</span>)</span><br></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      deeplearning.ai 课程1 Week1~Week2 课程笔记和推导、编码整理
    
    </summary>
    
      <category term="deeplearning.ai" scheme="chestnutheng.cn/categories/deeplearning-ai/"/>
    
    
      <category term="深度学习" scheme="chestnutheng.cn/tags/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0/"/>
    
      <category term="吴恩达" scheme="chestnutheng.cn/tags/%E5%90%B4%E6%81%A9%E8%BE%BE/"/>
    
  </entry>
  
  <entry>
    <title>[Linux]GDB调试技巧</title>
    <link href="chestnutheng.cn/2017/12/17/gdb/"/>
    <id>chestnutheng.cn/2017/12/17/gdb/</id>
    <published>2017-12-16T16:56:18.000Z</published>
    <updated>2017-12-16T16:56:24.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="命令行参数"><a href="#命令行参数" class="headerlink" title="命令行参数"></a>命令行参数</h2><p>gdb有下面几种运行方式：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 通过coredump文件，或者存在的进程id分析，不会拉起新进程</span></span><br><span class="line">gdb [options] [executable-file [core-file or process-id]]</span><br><span class="line"><span class="comment">// 2. 带参数运行程序</span></span><br><span class="line">gdb [options] --args executable-file [inferior-arguments ...]</span><br><span class="line"><span class="comment">// 3. redhat等含有用于调试python的工具</span></span><br><span class="line"><span class="comment">// gdb [options] [--python|-P] script-file [script-arguments ...]</span></span><br></pre></td></tr></table></figure>
<p>几个值得注意的参数：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 和上面的类似，使用参数指定的</span></span><br><span class="line">--args             Arguments after executable-file are passed to inferior</span><br><span class="line">--core=COREFILE    Analyze the core dump COREFILE.</span><br><span class="line">--pid=PID          Attach to running process PID.</span><br><span class="line">--exec=EXECFILE    Use EXECFILE as the executable.</span><br><span class="line"><span class="comment">// 远程调试</span></span><br><span class="line">-b BAUDRATE        Set serial port baud rate used <span class="keyword">for</span> remote debugging.</span><br><span class="line">-l TIMEOUT         Set timeout in seconds <span class="keyword">for</span> remote debugging.</span><br><span class="line"><span class="comment">// 运行某个文件中的gdb指令</span></span><br><span class="line">--command=FILE, -x Execute GDB commands from FILE.</span><br><span class="line"><span class="comment">// 运行某gdb指令，如gdb a.out -ex r开启文件并立即运行</span></span><br><span class="line">--eval-command=COMMAND, -ex   Execute a single GDB command.</span><br><span class="line">--directory=DIR    Search <span class="keyword">for</span> source files in DIR.</span><br><span class="line">--se=FILE          Use FILE as symbol file and executable file.</span><br></pre></td></tr></table></figure>
<h2 id="简单的例子：coredump分析"><a href="#简单的例子：coredump分析" class="headerlink" title="简单的例子：coredump分析"></a>简单的例子：coredump分析</h2><p>gdb的一大用处是通过coredump文件分析程序哪里发生了错误。首先要打开coredump生成开关：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 可以先运行 ulimit -a 查看所有限制，或者运行 ulimit -c 查看当前coredump设置</span></span><br><span class="line"><span class="comment">// 设置成无限可能会生成数GB的coredump文件</span></span><br><span class="line"><span class="comment">// 这条命令重启后无效</span></span><br><span class="line">ulimit -c unlimited</span><br></pre></td></tr></table></figure>
<p>coredump文件会默认生成在程序相同目录下。如果没有对应文件，可以查看<code>/etc/sysctl.conf</code>:</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 生成目录格式，%e 程序名 %p 进程id %s 信号 %t 时间 %e 命令名</span></span><br><span class="line">kernel.core_pattern =/data/coredump/core%e%p</span><br><span class="line"><span class="comment">// 1表示使用procid命名，0表示不使用</span></span><br><span class="line">kernel.core_uses_pid= <span class="number">0</span></span><br></pre></td></tr></table></figure>
<p>我们先编写一个简单的c程序<code>main.c</code>，它试图从非法地址读取数据：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = *((<span class="keyword">int</span> *)<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译，一定要加上<code>-g</code>选项：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gcc -o test_worker main.c -g</span><br></pre></td></tr></table></figure>
<p>运行，果然dump：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">./test_worker</span><br><span class="line">Segmentation fault (core dumped)</span><br></pre></td></tr></table></figure>
<p>然后使用第一节里的第一种方式启动， 携带coredump文件：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">gdb ./test_worker core.xxx.xxx</span><br></pre></td></tr></table></figure>
<p>进去后就能看到dump的地方以及原因。使用<code>bt</code>查看coredump时栈顶的信息：</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">Core was generated by `./t&apos;.</span><br><span class="line">Program terminated with signal 11, Segmentation fault.</span><br><span class="line">#0  0x0000000000400564 in main () at t.c:13</span><br><span class="line">13	    int a = *((int *)NULL);</span><br><span class="line">Missing separate debuginfos, use: debuginfo-install glibc-2.17-106.el7_2.4.x86_64</span><br><span class="line">(gdb) bt</span><br><span class="line">#0  0x0000000000400564 in main () at t.c:13</span><br></pre></td></tr></table></figure>
<h2 id="GDB的用法"><a href="#GDB的用法" class="headerlink" title="GDB的用法"></a>GDB的用法</h2><p><a href="https://sourceware.org/gdb/onlinedocs/gdb/index.html#SEC_Contents" target="_blank" rel="noopener"> GDB Online Docs </a></p>
<p>GDB启动时会读取二进制文件的符号表，然后进入调试命令行。这里可以运行各种命令查看程序的符号表、变量、内存值以及控制程序的运行。</p>
<h3 id="控制命令"><a href="#控制命令" class="headerlink" title="控制命令"></a>控制命令</h3><ol>
<li><strong>file</strong> 加载一个二进制文件，直接进入gdb时可以用这个加载。参数为文件名。</li>
<li><strong>run</strong> (或者r) 从头运行程序一直到断点。没有断点会一直运行结束，或者直到遇到异常。可以<strong>r &lt; a.in</strong> 重定向输入输出。</li>
<li><strong>continue</strong> (或者c) 运行程序一直到下一个断点。</li>
<li><strong>next/step</strong>  (或者n/s) 单步调试，next不会在行内进入函数体，step则会跳入函数体。 参数为跳多行。</li>
<li><strong>until</strong> (或者 u) 直接跳出当前循环。（但是还是会被断点卡住）。参数为跳到指定行。</li>
<li><strong>finish</strong> 直接运行到函数返回</li>
<li><strong>call</strong> 运行某函数，参数为函数和参数，如 <code>call foo(2, &quot;hello&quot;)</code></li>
<li><strong>回车键</strong> 重复上个指令</li>
</ol>
<h3 id="断点"><a href="#断点" class="headerlink" title="断点"></a>断点</h3><ol>
<li><strong>break n</strong> (或者b) 在第n行打断点  </li>
<li><strong>b main.c:n</strong> 指定文件打断点；  <strong>b main</strong> 指定函数入口处打断点；  <strong>b main:label</strong> 指定函数的标签处打断点</li>
<li><strong>b n if i == 5</strong> 满足条件打断点，对循环尤其有效。</li>
<li><strong>info b</strong> 查看断点号和信息，或者 <strong>i b</strong></li>
<li><strong>delete no</strong> (或者d) 删除对应编号的断点； <strong>d breakpoints</strong> 删除所有断点</li>
<li><strong>clear lineno</strong> 删除对应行的断点</li>
<li><strong>disable/enable no</strong>  屏蔽/使用对应编号的断点</li>
</ol>
<h3 id="查看代码和变量"><a href="#查看代码和变量" class="headerlink" title="查看代码和变量"></a>查看代码和变量</h3><ol>
<li><strong>list</strong>  (或者l) 列出10行源文件。每次从上次结束的地方开始列。</li>
<li><strong>list lineno</strong> 列出某行的前后源码；<strong>l main</strong> 列出某函数的源码</li>
<li><strong>print exp</strong> (或者p) 打印任意变量、表达式、函数、字符串、数组的值</li>
<li><strong>display exp</strong> 每次单步完打印该表达式</li>
<li><strong>watch exp</strong> 如果该表达式值改变了，打印并停止程序</li>
<li><strong>whatis</strong>  查询变量，函数的类型</li>
<li><strong>info</strong> (或者i) 查询信息 <strong>i locals</strong> 所有变量的值 <strong>i args</strong> 所有参数的值 <strong>i function</strong> 函数信息 <strong>i frame</strong> 栈信息<br><strong>i program</strong> 程序信息 <strong>i threads</strong> 线程信息</li>
</ol>
<h3 id="堆栈"><a href="#堆栈" class="headerlink" title="堆栈"></a>堆栈</h3><ol>
<li><p><strong>bt</strong> 查看栈信息； <strong>bt n</strong> 栈顶n层；<strong>bt -n</strong> 栈底n层</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">(gdb) bt</span><br><span class="line">#<span class="number">0</span>  foot () at t.c:<span class="number">7</span></span><br><span class="line">#1  0x0000000000400525 in main () at t.c:15</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>frame</strong> (或者f)查看帧信息。上面一个#号（一层）是一帧。<br><strong>frame n</strong> 查看第n帧 ；<strong>up n</strong> 上面n帧； <strong>down n</strong> 下面n帧；<strong>frame addr</strong> 查看某地址的帧</p>
</li>
<li><p><strong>info frame n/addr</strong> 帧详细信息<br>ip是下条命令的地址（pc），bp是栈底的地址，sp是栈顶的地址。<br>64位机的帧信息：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">(gdb) i f <span class="number">0</span></span><br><span class="line"> <span class="comment">// 帧地址</span></span><br><span class="line"> Stack frame at <span class="number">0x7fffffffe560</span>:</span><br><span class="line"> <span class="comment">// rip：帧PC      帧函数名       saved rip：caller帧PC</span></span><br><span class="line"> rip = <span class="number">0x4004fd</span> in foot (t.c:<span class="number">7</span>); saved rip <span class="number">0x400525</span></span><br><span class="line"> <span class="comment">// caller帧地址</span></span><br><span class="line"> called by frame at <span class="number">0x7fffffffe570</span></span><br><span class="line"> source language c.</span><br><span class="line"> <span class="comment">// 帧参数地址</span></span><br><span class="line"> Arglist at <span class="number">0x7fffffffe550</span>, args: </span><br><span class="line"> <span class="comment">// 帧局部变量地址，caller的栈顶地址</span></span><br><span class="line"> Locals at <span class="number">0x7fffffffe550</span>, Previous frame sp is <span class="number">0x7fffffffe560</span></span><br><span class="line"> <span class="comment">// 帧寄存器列表</span></span><br><span class="line"> Saved registers:</span><br><span class="line"> rbp at <span class="number">0x7fffffffe550</span>, rip at <span class="number">0x7fffffffe558</span></span><br></pre></td></tr></table></figure>
<p>32位机的帧信息：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">(gdb) i f</span><br><span class="line"> Stack level <span class="number">0</span>, frame at <span class="number">0xbffff630</span>:</span><br><span class="line"> <span class="comment">// eip：帧PC      帧函数名       saved eip：caller帧PC</span></span><br><span class="line"> eip = <span class="number">0x80483e4</span> in main (a.c:<span class="number">8</span>); saved eip = <span class="number">0xb7e31637</span></span><br><span class="line"> source language c.</span><br><span class="line"> <span class="comment">// 帧参数地址</span></span><br><span class="line"> Arglist at <span class="number">0xbffff628</span>, args: </span><br><span class="line"> <span class="comment">// 帧局部变量地址，caller的栈顶地址</span></span><br><span class="line"> Locals at <span class="number">0xbffff628</span>, Previous frame sp is <span class="number">0xbffff630</span></span><br><span class="line"> <span class="comment">// 帧寄存器列表</span></span><br><span class="line"> Saved registers:</span><br><span class="line"> ebp at <span class="number">0xbffff628</span>, eip at <span class="number">0xbffff62c</span></span><br></pre></td></tr></table></figure>
<p>​</p>
</li>
</ol>
<h3 id="内存"><a href="#内存" class="headerlink" title="内存"></a>内存</h3><p><a href="https://wizardforcel.gitbooks.io/100-gdb-tips/examine-memory.html" target="_blank" rel="noopener">100 GDB Tips</a></p>
<p>gdb中使用<code>x</code>命令来打印内存的值，格式为<code>x/nfu addr</code>。</p>
<p>含义为以<code>f</code>格式打印从<code>addr</code>开始的<code>n</code>个长度单元为<code>u</code>的内存值。参数具体含义如下：<br>a）n：输出单元的个数。<br>b）f：是输出格式。比如<code>x</code>是以16进制形式输出，<code>o</code>是以8进制形式输出,等等。<br>c）u：标明一个单元的长度。<code>b</code>是一个<code>bytes</code>，<code>h</code>是两个<code>bytes</code>（halfword），<code>w</code>是四个<code>bytes</code>（word），<code>g</code>是八个<code>bytes</code>（giant word）。</p>
<p>比如对一个字符串arr查看内存：（这个数组越界踩掉了总共10 char的内容，每个char在32位机上是32bit）</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">(gdb) p arr</span><br><span class="line">$<span class="number">4</span> = <span class="string">"\000\001\002\003"</span></span><br><span class="line"><span class="comment">// 显示11个byte，十六进制显示</span></span><br><span class="line">(gdb) x/<span class="number">11</span>xb arr</span><br><span class="line"><span class="number">0x7fffffffe540</span>:	<span class="number">0x00</span>	<span class="number">0x01</span>	<span class="number">0x02</span>	<span class="number">0x03</span>	<span class="number">0x04</span>	<span class="number">0x05</span>	<span class="number">0x06</span>	<span class="number">0x07</span></span><br><span class="line"><span class="number">0x7fffffffe548</span>:	<span class="number">0x08</span>	<span class="number">0x09</span>	<span class="number">0x40</span></span><br><span class="line"><span class="comment">// 显示3个4-byte，十六进制显示</span></span><br><span class="line">(gdb) x/<span class="number">3</span>xw arr</span><br><span class="line"><span class="number">0x7fffffffe540</span>:	<span class="number">0x03020100</span>	<span class="number">0x07060504</span>	<span class="number">0x00400908</span></span><br></pre></td></tr></table></figure>
<h3 id="信号"><a href="#信号" class="headerlink" title="信号"></a>信号</h3><p>这部分的详细内容在<a href="https://sourceware.org/gdb/onlinedocs/gdb/Signals.html" target="_blank" rel="noopener">GDB Online Docs-5.4 Signals</a>中。你可以自由地对程序进行发送信号（signal）、捕获信号（catch）或者处理信号（handle）。</p>
<h3 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h3><p>除了上文的查看线程信息之外，还可以查看详细的线程信息，参考<a href="https://sourceware.org/gdb/onlinedocs/gdb/Threads.html#Threads" target="_blank" rel="noopener">GDB Online Docs-4 Threads</a>，或者查看fork的情况，参考<a href="https://sourceware.org/gdb/onlinedocs/gdb/Forks.html#Forks" target="_blank" rel="noopener">GDB Online Docs-5Forks</a> 。</p>
]]></content>
    
    <summary type="html">
    
      本文从介绍一个coredump分析开始，全面介绍了GDB的各种指令。
    
    </summary>
    
      <category term="Linux" scheme="chestnutheng.cn/categories/Linux/"/>
    
    
      <category term="GDB" scheme="chestnutheng.cn/tags/GDB/"/>
    
      <category term="coredump" scheme="chestnutheng.cn/tags/coredump/"/>
    
  </entry>
  
  <entry>
    <title>[后台]分布式基础概念</title>
    <link href="chestnutheng.cn/2017/11/09/nosql-base/"/>
    <id>chestnutheng.cn/2017/11/09/nosql-base/</id>
    <published>2017-11-09T14:22:02.000Z</published>
    <updated>2017-11-21T16:10:12.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="CAP"><a href="#CAP" class="headerlink" title="CAP"></a>CAP</h2><p><a href="http://www.julianbrowne.com/article/brewers-cap-theorem" target="_blank" rel="noopener">Brewer’s CAP Theorem, 2009</a></p>
<p>CAP理论是分布式系统的基石，他指出了分布式系统的以下特性：</p>
<ul>
<li>Consistency  强一致性(返回的数据时刻一致)</li>
<li>Availability 高可用性(服务一直可用，响应时间正常)</li>
<li>Tolerance of network Partition 分区容错性(保证有机器宕机服务依然正常)</li>
</ul>
<p>CAP理论表明，一个分布式系统不可能同时满足一致性，可用性和分区容错性这三个需求，<br>最多只能同时满足两个。证明可以看上面的链接。</p>
<p>所以架构师往往要做出牺牲某一特性的选择：</p>
<p>CP：金融行业的数据库，价格昂贵，符合ACID<br>CA：传统关系型数据库，用于小型系统或单机系统<br>AP：key-value数据库，现代UGC服务基本都是这种架构，用最终一致性来换取高可用和分区容错性。<br>电商：牺牲少量的可用性和一致性，比较平衡，符合BASE</p>
<p><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/cap_thoery.png"></p>
<h2 id="ACID"><a href="#ACID" class="headerlink" title="ACID"></a>ACID</h2><p>老生常谈的数据库事务的特性：</p>
<p><strong>原子性(Atomicity)</strong>  事务中有失败，立即回滚到执行前。没有失败，全部成功</p>
<p><strong>一致性(Consistency)</strong> 事务执行后数据的约束没有被破坏</p>
<p><strong>隔离性(Isolation）</strong> 事务之间不交叉进行</p>
<p><strong>持久性(Durability）</strong> 事务完成，永久储存</p>
<h2 id="BASE"><a href="#BASE" class="headerlink" title="BASE"></a>BASE</h2><p><a href="http://www.infoq.com/cn/articles/ebay-scalability-best-practices" target="_blank" rel="noopener">可伸缩性最佳实践：来自eBay的经验</a></p>
<p>BASE理论是对CAP理论的延伸，核心思想是即使无法做到强一致性（CAP的Consistency），但应用可以采用适合的方式达到最终一致性，来保证系统的容量和可用性。</p>
<h3 id="Basically-Availble-（基本可用）"><a href="#Basically-Availble-（基本可用）" class="headerlink" title="Basically Availble （基本可用）"></a>Basically Availble （基本可用）</h3><p>基本可用是指系统只保证核心可用，在访问量突增时采用有损服务的策略，让部分用户使用降级的服务。<br>什么是基本可用的服务？以秒杀为例：<br>逻辑有损：秒杀时只执行重要逻辑，加载资源可以先使用预加载的或者小图等<br>业务有损：秒杀需要会员、抢秒杀资格<br>流程有损：比如秒杀时前段丢掉大部分请求，从后端少量请求中选取中奖的请求处理</p>
<h3 id="Soft-state-（软状态-柔性事务）"><a href="#Soft-state-（软状态-柔性事务）" class="headerlink" title="Soft-state （软状态/柔性事务）"></a>Soft-state （软状态/柔性事务）</h3><p>“Soft state” 可以理解为”无连接”的, 而 “Hard state” 是”面向连接”的。换句话说，软状态的数据库可能存在很多中间状态，不同节点到达最终统一的状态前会有延迟（如异步复制）。</p>
<h3 id="Eventual-Consistency-（最终一致性）"><a href="#Eventual-Consistency-（最终一致性）" class="headerlink" title="Eventual Consistency （最终一致性）"></a>Eventual Consistency （最终一致性）</h3><p>并非时刻保持一致，所有复制节点在某段时间后保持一致。<br>最终一致性是弱一致性的一种特例：<br><strong>Step 1.</strong> A首先write了一个值到存储系统<br><strong>Step 2.</strong> 存储系统保证在A,B,C后续读取之前没有其它写操作更新同样的值<br><strong>Step 3.</strong> 最终所有的读取操作都会读取到最A写入的最新值</p>
<p>从A写入到读取操作读取成功有一定的时间，即不一致性窗口。如果没有失败发生的话，“不一致性窗口”的大小依赖于以下的几个因素：交互延迟，系统的负载，以及备机slave的个数。最终一致性方面最出名的系统可以说是DNS系统，当更新一个域名的IP以后，根据配置策略以及缓存控制策略的不同，最终所有的客户都会看到最新的值。</p>
<p>在下一篇文章的中，会表明只要集群$V_r + V_w \leq N$，即此时读取和写入操作是不重叠的， 就能保证最终一致性。这个时候不一致性的窗口依赖于存储系统的异步实现方式，不一致性的窗口大小就等于从更新开始到所有的节点都异步更新完成之间的时间。<br>  ​</p>
<h2 id="Sharding-分片"><a href="#Sharding-分片" class="headerlink" title="Sharding 分片"></a>Sharding 分片</h2><p>当单库单表中的数据量特别大，查询就会非常慢。这个时候就要切分数据库存放在不同的Server上：</p>
<p>水平切分：行切分。比如按id的范围分表，或者hash分表</p>
<p>垂直切分：列切分。把一张表（库）中关系紧密的列（表）单独放入别的表（Server）中</p>
<h2 id="I-O五分钟法则"><a href="#I-O五分钟法则" class="headerlink" title="I/O五分钟法则"></a>I/O五分钟法则</h2><p><a href="http://queue.acm.org/detail.cfm?id=1413264" target="_blank" rel="noopener">The Five-minute Rule, 2008 </a></p>
<p>在一个内存页中维护一个内存页(64KB)的成本相当于在磁盘中维护五分钟的成本。</p>
<h2 id="不要删除数据"><a href="#不要删除数据" class="headerlink" title="不要删除数据"></a>不要删除数据</h2><p>在数据库中删除数据会破坏数据库的完整性。比如删除一个商品会导致关联的订单也要删除，引发连锁反应。打个下架标记即可，不要删除数据。</p>
]]></content>
    
    <summary type="html">
    
      分布式的基础概念，包括CAP，BASE，和一些基本的原则
    
    </summary>
    
      <category term="后台" scheme="chestnutheng.cn/categories/%E5%90%8E%E5%8F%B0/"/>
    
    
      <category term="[NOSQL, 后台]" scheme="chestnutheng.cn/tags/NOSQL-%E5%90%8E%E5%8F%B0/"/>
    
  </entry>
  
  <entry>
    <title>[C++]sizeof那些事儿</title>
    <link href="chestnutheng.cn/2017/08/16/size-of-things/"/>
    <id>chestnutheng.cn/2017/08/16/size-of-things/</id>
    <published>2017-08-16T15:33:20.000Z</published>
    <updated>2017-08-16T15:33:24.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="sizeof-是什么"><a href="#sizeof-是什么" class="headerlink" title="sizeof 是什么"></a>sizeof 是什么</h2><p>首先明确一点，sizeof是<strong>运算符</strong>，不是函数。运算符是一个对于编译器来说的概念，是由编译器处理的，在程序编译好之后所有的sizeof都已经被替换为实际的值。类似的还有decltype。所以像<br><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">sizeof</span>(++i);</span><br></pre></td></tr></table></figure></p>
<p>这类的语句是不会改变i的值的。在C99之后，sizeof增加了一些运行时特性，可以算出可变数组的大小，像这样：</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> num;</span><br><span class="line"><span class="built_in">scanf</span>(<span class="string">"%d"</span>, &amp;num);	 <span class="comment">//input 4</span></span><br><span class="line"><span class="keyword">int</span> arr[num];</span><br><span class="line"><span class="keyword">sizeof</span>(arr);		<span class="comment">//output 16</span></span><br></pre></td></tr></table></figure>
<h2 id="sizeof-怎么用"><a href="#sizeof-怎么用" class="headerlink" title="sizeof 怎么用"></a>sizeof 怎么用</h2><p><a href="http://zh.cppreference.com/w/c/language/sizeof" target="_blank" rel="noopener">sizeof</a> 支持下面的语法：</p>
<figure class="highlight"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">sizeof</span>(type) 	    (<span class="number">1</span>) 以字节数返回type类型对象表示的大小 </span><br><span class="line">sizeof expression 	(2) 以字节数返回expression的类型对象表示的大小</span><br></pre></td></tr></table></figure>
<p>其中，<code>sizeof(char)</code>、 <code>sizeof(signed char)</code>以及<code>sizeof(unsigned char)</code>始终返回1。</p>
<p><code>sizeof</code>不能用于函数类型、不完整类型（含void）或<a href="http://zh.cppreference.com/w/c/language/bit_field" target="_blank" rel="noopener">位域</a>左值。在一些编译器里，<code>sizeof(void)</code>会返回0，但这是没有定义的。</p>
<h2 id="普通的sizeof"><a href="#普通的sizeof" class="headerlink" title="普通的sizeof"></a>普通的sizeof</h2><p>几个上面用法的例子：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// type argument:</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sizeof(float)         = %u\n"</span>, <span class="keyword">sizeof</span>(<span class="keyword">float</span>));   <span class="comment">//4</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sizeof(void(*)(void)) = %u\n"</span>, <span class="keyword">sizeof</span>(<span class="keyword">void</span>(*)(<span class="keyword">void</span>)));   <span class="comment">//4</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sizeof(char[10])      = %u\n"</span>, <span class="keyword">sizeof</span>(<span class="keyword">char</span>[<span class="number">10</span>]));    <span class="comment">//10</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// expression argument:</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sizeof 'a'   = %u\n"</span>, <span class="keyword">sizeof</span> <span class="string">'a'</span>); <span class="comment">// 'a'的类型是int, 4</span></span><br><span class="line"><span class="comment">//  printf("sizeof main = %zu\n", sizeof main); // 错误：函数类型</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sizeof &amp;main = %u\n"</span>, <span class="keyword">sizeof</span> main()); <span class="comment">//类型为返回值int, 4</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sizeof \"hello\" = %u\n"</span>, <span class="keyword">sizeof</span> <span class="string">"hello"</span>); <span class="comment">// 类型为char[6], 6</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"sizeof(\"12345\" + 1)     = %u\n"</span>, <span class="keyword">sizeof</span>(<span class="string">"12345"</span> + <span class="number">1</span>)); <span class="comment">// 类型为指针, 4</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>需要注意的是，在函数传参等数组退化为指针的时候<code>sizeof</code>返回的当然是指针大小，切忌用它来计算数组大小。</p>
<h2 id="sizeof-和字节对齐"><a href="#sizeof-和字节对齐" class="headerlink" title="sizeof 和字节对齐"></a>sizeof 和字节对齐</h2><p>在sizeof用于非基本类型时，编译器会有字节对齐这样一个行为。它可以有效地用空间换时间，快速找到数据的地址。</p>
<p>字节对齐指的是每个变量的首地址和自身对齐值对齐，所以和变量顺序也有一定的关系。</p>
<p>字节对齐满足三个准则：</p>
<p>1) 结构体变量的首地址：能够被其最宽基本类型成员的大小所整除；</p>
<p>2) 结构体每个成员相对于结构体首地址的偏移量：都是该成员大小的整数倍，如有需要编译器会在成员之间加上填充字节。</p>
<p>3) 结构体末尾填充：结构体的总大小为结构体最宽<strong>基本类型</strong>成员大小的整数倍，如有需要编译器会在最末一个成员之后加上填充字节。</p>
<p>基本类型的宽度：char=1，short=2，int=4，double=8，指针=4</p>
<p>例如下面一个例子：</p>
<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span>&#123;</span><br><span class="line"> <span class="keyword">int</span> id;              <span class="comment">//0-7  由于下个元素要和4对齐，补齐了2字节</span></span><br><span class="line"> <span class="keyword">double</span> weight;       <span class="comment">//8-15</span></span><br><span class="line"> <span class="keyword">float</span> height;        <span class="comment">//16-23 由于结构体大小和8对齐，补齐了4字节</span></span><br><span class="line">&#125;Body_Info;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">struct</span>&#123;</span><br><span class="line"> <span class="keyword">char</span> name[<span class="number">2</span>];        <span class="comment">//0-3   由于下个元素要和4对齐，补齐了2字节</span></span><br><span class="line"> <span class="keyword">int</span>  id;             <span class="comment">//4-7   很齐</span></span><br><span class="line"> <span class="keyword">double</span> score;        <span class="comment">//8-15  很齐　　</span></span><br><span class="line"> <span class="keyword">short</span> grade;         <span class="comment">//16-23 由于下个元素要和8（BB中最长类型）对齐，补齐了6字节　　　</span></span><br><span class="line"> Body_Info b;         <span class="comment">//24-47 很齐</span></span><br><span class="line">&#125;Student_Info;</span><br><span class="line"><span class="comment">// 删除掉Student_Info的id或者name，大小都是不变的</span></span><br><span class="line"><span class="comment">// 删除掉Student_Info的grade或者把grade移动到name后，大小会成为40字节</span></span><br></pre></td></tr></table></figure>
<h2 id="sizeof的实现"><a href="#sizeof的实现" class="headerlink" title="sizeof的实现"></a>sizeof的实现</h2><p>因为是个编译器行为，就没有函数代码了，也不在标准库里。在<code>clang</code>的编译器代码中可以看到一些对<code>sizeof</code>的处理，可以把玩一下：</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line">TypeInfo ASTContext::getTypeInfoImpl(<span class="keyword">const</span> Type *T) <span class="keyword">const</span> &#123;</span><br><span class="line">  <span class="keyword">uint64_t</span> Width = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">unsigned</span> Align = <span class="number">8</span>;</span><br><span class="line">  <span class="keyword">bool</span> AlignIsRequired = <span class="literal">false</span>;</span><br><span class="line"><span class="keyword">switch</span> (T-&gt;getTypeClass())&#123;</span><br><span class="line"><span class="keyword">case</span> Type::Vector: &#123;</span><br><span class="line">    <span class="keyword">const</span> VectorType *VT = cast&lt;VectorType&gt;(T);</span><br><span class="line">    TypeInfo EltInfo = getTypeInfo(VT-&gt;getElementType());</span><br><span class="line">    Width = EltInfo.Width * VT-&gt;getNumElements();</span><br><span class="line">....</span><br><span class="line"><span class="keyword">case</span> BuiltinType::UInt:</span><br><span class="line"><span class="keyword">case</span> BuiltinType::Int:</span><br><span class="line">    Width = Target-&gt;getIntWidth();</span><br><span class="line">    Align = Target-&gt;getIntAlign();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line"><span class="keyword">case</span> BuiltinType::ULong:</span><br><span class="line"><span class="keyword">case</span> BuiltinType::Long:</span><br><span class="line">    Width = Target-&gt;getLongWidth();</span><br><span class="line">    Align = Target-&gt;getLongAlign();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">...</span><br><span class="line"><span class="keyword">case</span> Type::Pointer:</span><br><span class="line">    <span class="keyword">unsigned</span> AS = getTargetAddressSpace(cast&lt;PointerType&gt;(T)-&gt;getPointeeType());</span><br><span class="line">    Width = Target-&gt;getPointerWidth(AS);</span><br><span class="line">    Align = Target-&gt;getPointerAlign(AS);</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译结束后，<code>sizeof</code>就会根据<code>Width</code>和<code>Align</code>被替换为常量。</p>
]]></content>
    
    <summary type="html">
    
      sizeof是什么？怎么用？返回了什么？怎么实现？本文详细回答了这些问题。
    
    </summary>
    
      <category term="C++" scheme="chestnutheng.cn/categories/C/"/>
    
    
      <category term="c++" scheme="chestnutheng.cn/tags/c/"/>
    
      <category term="sizeof" scheme="chestnutheng.cn/tags/sizeof/"/>
    
  </entry>
  
  <entry>
    <title>[C++]C++ 100行实现线程池</title>
    <link href="chestnutheng.cn/2017/04/07/cpp-threadpool/"/>
    <id>chestnutheng.cn/2017/04/07/cpp-threadpool/</id>
    <published>2017-04-07T15:17:54.000Z</published>
    <updated>2017-04-07T15:17:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>一个100行左右的简单线程池。用到了std::mutex和std::thread等新特性。<br><a id="more"></a></p>
<h2 id="线程池模型"><a href="#线程池模型" class="headerlink" title="线程池模型"></a>线程池模型</h2><p>首先把每个函数抽象为一个任务(Task)，任务的过程就是调用这个Task的run函数。<br>然后把线程池中的线程封装为一个线程类（Thread），一直等待调度器分配任务（空闲状态），如果有任务分配立即进入忙状态。等任务执行结束再次变为空闲状态。<br>最后是一个调度器类（TreadPool），包含任务队列（随时添加新任务），和一个包含了Thread的vector（线程池中的线程）。如果任务队列非空，调度器每次从中取出一个任务，然后轮询线程池，搜寻空闲线程并把这个任务交给线程。<br>模型如下图所示：<br><img src="http://chestnutheng-blog-1254282572.file.myqcloud.com/tpool.png"></p>
<h2 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h2><p>下面的代码实现了上述模型。其中Task类通过睡眠一定的秒数模拟任务，可以看到T1先执行完毕（1秒完毕），T2和T3在之后同时完毕，说明调度非常成功。</p>
<figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//linux, g++ -std=c++14 -o t *.cpp -pthread</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;thread&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span> </span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> Task&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">int</span> no;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    Task(<span class="keyword">int</span> n)&#123;</span><br><span class="line">        no = n;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//可以继承这个类重写该方法执行任务</span></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        sleep(no); <span class="comment">//构造时决定执行几秒，模拟线程运行</span></span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; no &lt;&lt; <span class="string">"T\n"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> Thread&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">std</span>::thread <span class="number">_</span>thread;</span><br><span class="line">    <span class="keyword">bool</span> <span class="number">_</span>isfree;</span><br><span class="line">    Task *<span class="number">_</span>task;</span><br><span class="line">    <span class="built_in">std</span>::mutex <span class="number">_l</span>ocker;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//构造</span></span><br><span class="line">    Thread() : <span class="number">_</span>isfree(<span class="literal">true</span>), <span class="number">_</span>task(<span class="literal">nullptr</span>)&#123;</span><br><span class="line">        <span class="number">_</span>thread = <span class="built_in">std</span>::thread(&amp;Thread::run, <span class="keyword">this</span>);</span><br><span class="line">        <span class="number">_</span>thread.detach(); <span class="comment">//放到后台， join是等待线程结束</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//是否空闲</span></span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">isfree</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">_</span>isfree;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//添加任务</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">add_task</span><span class="params">(Task *task)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(<span class="number">_</span>isfree)&#123;</span><br><span class="line">            <span class="number">_l</span>ocker.lock();</span><br><span class="line">            <span class="number">_</span>task = task;</span><br><span class="line">            <span class="number">_</span>isfree = <span class="literal">false</span>;</span><br><span class="line">            <span class="number">_l</span>ocker.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//如果有任务则执行任务，否则自旋</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span>(<span class="number">_</span>task)&#123;</span><br><span class="line">                <span class="number">_l</span>ocker.lock();</span><br><span class="line">                <span class="number">_</span>isfree = <span class="literal">false</span>;</span><br><span class="line">                <span class="number">_</span>task-&gt;run();</span><br><span class="line">                <span class="number">_</span>isfree = <span class="literal">true</span>;</span><br><span class="line">                <span class="number">_</span>task = <span class="literal">nullptr</span>;</span><br><span class="line">                <span class="number">_l</span>ocker.unlock();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> ThreadPool&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">queue</span>&lt;Task *&gt; task_queue;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;Thread *&gt; <span class="number">_</span>pool;</span><br><span class="line">    <span class="built_in">std</span>::mutex <span class="number">_l</span>ocker;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//构造线程并后台执行，默认数量为10</span></span><br><span class="line">    ThreadPool(<span class="keyword">int</span> n = <span class="number">10</span>)&#123;</span><br><span class="line">        <span class="keyword">while</span>(n--)&#123;</span><br><span class="line">            Thread *t = <span class="keyword">new</span> Thread();</span><br><span class="line">            <span class="number">_</span>pool.push_back(t);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">std</span>::<span class="function">thread <span class="title">main_thread</span><span class="params">(&amp;ThreadPool::run, <span class="keyword">this</span>)</span></span>;</span><br><span class="line">        main_thread.detach();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//释放线程池</span></span><br><span class="line">    ~ThreadPool()&#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>;i &lt; <span class="number">_</span>pool.size(); ++i)&#123;</span><br><span class="line">            <span class="keyword">delete</span> <span class="number">_</span>pool[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//添加任务</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">add_task</span><span class="params">(Task *task)</span></span>&#123;</span><br><span class="line">        <span class="number">_l</span>ocker.lock();</span><br><span class="line">        task_queue.push(task);</span><br><span class="line">        <span class="number">_l</span>ocker.unlock();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//轮询</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">run</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">while</span>(<span class="literal">true</span>)&#123;</span><br><span class="line">            <span class="number">_l</span>ocker.lock();</span><br><span class="line">            <span class="keyword">if</span>(task_queue.empty())&#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 寻找空闲线程执行任务</span></span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">_</span>pool.size(); ++i)&#123;</span><br><span class="line">                <span class="keyword">if</span>(<span class="number">_</span>pool[i]-&gt;isfree())&#123;</span><br><span class="line">                    <span class="number">_</span>pool[i]-&gt;add_task(task_queue.front());</span><br><span class="line">                    task_queue.pop();</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="number">_l</span>ocker.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="function">ThreadPool <span class="title">tp</span><span class="params">(<span class="number">2</span>)</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="function">Task <span class="title">t1</span><span class="params">(<span class="number">1</span>)</span></span>;</span><br><span class="line">    <span class="function">Task <span class="title">t2</span><span class="params">(<span class="number">3</span>)</span></span>;</span><br><span class="line">    <span class="function">Task <span class="title">t3</span><span class="params">(<span class="number">2</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    tp.add_task(&amp;t1);</span><br><span class="line">    tp.add_task(&amp;t2);</span><br><span class="line">    tp.add_task(&amp;t3);</span><br><span class="line">    </span><br><span class="line">    sleep(<span class="number">4</span>);   <span class="comment">//等待调度器结束，不然会崩溃</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;一个100行左右的简单线程池。用到了std::mutex和std::thread等新特性。&lt;br&gt;
    
    </summary>
    
      <category term="C++" scheme="chestnutheng.cn/categories/C/"/>
    
    
      <category term="c++" scheme="chestnutheng.cn/tags/c/"/>
    
      <category term="c++11" scheme="chestnutheng.cn/tags/c-11/"/>
    
      <category term="线程池" scheme="chestnutheng.cn/tags/%E7%BA%BF%E7%A8%8B%E6%B1%A0/"/>
    
  </entry>
  
</feed>
